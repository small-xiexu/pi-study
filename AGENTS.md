# Pi Study Repository Instructions

项目规则验证标记：`PI_STUDY_AGENTS_V1`。

## 适用范围

- 本仓库只用于系统学习、实验和记录 Pi Coding Agent。
- 只修改当前任务明确涉及的文件；保留用户已有且与当前任务无关的改动。
- `docs/plans/pi-complete-learning-plan.md` 是唯一进度台账。计划项只有在取得直接验证证据后才能标记完成。

## 验证要求

- 修改前先读取目标文件和相关规则，确认当前状态与预期范围。
- 修改后执行与风险相匹配的最小验证，并检查实际差异。
- 没有运行验证时必须明确说明；命令成功不能自动证明结果正确、安全或可回滚。

## 凭据保护

- 不读取、输出、记录或提交 API Key、OAuth Token、完整认证文件及未脱敏 Session。
- 可以在必要时检查凭据文件是否存在及其权限，但不得展示凭据内容。
- 发现疑似凭据或个人敏感信息时停止扩散，并在继续前先完成脱敏。

## Git 操作

- 未经用户单独明确要求，不执行暂存、提交、推送、创建 Pull Request 或发布。
- 获得提交授权后，先检查完整工作区状态、实际差异、未跟踪文件和敏感信息，再确定提交范围。
- 不覆盖、回退或删除用户已有改动；可靠版本恢复依赖 Git，而不是 Session。

## 权限边界

- 本文件是交给 Model 的行为指令，不是权限系统或沙箱。
- 强制限制危险操作仍需 Tool 门禁、Extension、受限用户、容器或其他系统级隔离。
