# Pi Coding Agent 完整学习计划

> 唯一进度台账：后续学习、练习、验收和断点信息均回写本文件。代码或文件已经存在，不等于计划项已经完成。

## 1. 计划基线

- 制定日期：2026-07-29
- 学习对象：Pi Coding Agent
- 官方文档基线：`https://pi.dev/docs/latest`
- npm 历史版本基线：`@earendil-works/pi-coding-agent@0.84.0`；当前 CLI 见下方环境快照
- 参考会话：`<reference-session-id>`；真实标识不进入学习文档
- 当前阶段：阶段 0、阶段 1、阶段 2、阶段 3、阶段 4、阶段 5、阶段 6 和阶段 7 的 7.1-7.6 已完成；下一项为 7.7
- 下一步：进入 7.7 外部小程序系统地图，先确定使用 SDK 还是 RPC、目标界面、流式状态、Tool 限制和 Session 保存边界；未取得单独代码授权前不编写实现。
- 预计投入：Pi 学习与综合项目 73-103 小时；学习网站另计 8-12 小时；阶段 5-6 作为定制开发核心加深学习，按验收结果推进
- 建议节奏：每次 60-90 分钟，每周 4-5 次；先完成 Pi 主线，再用 1-2 周制作学习网站

### 当前环境快照

| 项目 | 当前状态 | 证据 |
|---|---|---|
| 学习仓库 | 已准备，当前以学习文档为主 | `<repo-root>` |
| Git 远程 | 已配置 | `https://github.com/small-xiexu/pi-study.git` |
| Node.js | 可用 | `v25.2.1` |
| npm | 可用 | `11.6.2` |
| Git | 可用 | `2.50.1` |
| Pi | 已安装、更新并启动 | `/opt/homebrew/bin/pi`，历史基线 `0.84.0`；当前 CLI `0.84.3`，既有实验仍按各自记录的版本限定 |
| 模型认证 | 已连接 | 自定义中转地址 `<provider-base-url>`；`openai-responses`；`openai/gpt-5.6-sol` 已实际响应；凭据文件权限 `0600`；真实地址不进入学习文档 |

## 2. 学习目标

完成本计划后，应能够独立：

1. 安装、认证、配置并安全使用 Pi 完成真实开发任务。
2. 解释 Provider、Model、Agent Loop、Tool Call、Session 与 Compaction 的完整运行链路。
3. 正确使用会话恢复、树分支、Fork、Clone、消息队列和上下文压缩。
4. 编写并调试 `AGENTS.md`、项目设置、Prompt Template、Skill 和 Theme。
5. 使用 TypeScript Extension 扩展事件、工具、命令、UI 和权限门禁。
6. 创建、审查和安装 Pi Package，完成可发布制品与发布前演练，并区分 Custom Model 与 Custom Provider。
7. 通过 SDK、RPC 和 JSON Event Stream 将 Pi 接入外部程序，并理解 TUI 组件。
8. 阅读 `pi-mono` 核心源码，定位 Agent 执行链路并运行相关测试。
9. 独立交付一个安全可控、可测试、可复用的 Pi 综合项目。
10. 将学习过程、实验、源码理解和综合项目发布为可用于面试展示的学习网站。

## 3. 教学与验收规则

学习文档分工：

- `docs/plans/pi-complete-learning-plan.md`：唯一进度与验收台账。
- `docs/learning/README.md`：学习资料总索引和主题边界。
- `docs/learning/00-environment-report.md`：本机环境和验证证据。
- `docs/learning/` 下的 Markdown（含主题子目录）：按主题记录已经讲解的概念、命令、区别和安全边界；不重复维护进度。

采用“系统地图 + 单一贯穿项目 + 三遍螺旋”推进，阶段覆盖范围不缩减：

1. 第一遍，建立系统地图：先讲清模块目标、职责边界和它在完整 Agent 链路中的位置。
2. 第二遍，完成真实实验：用户在本地终端运行 Pi；我准备安全实验、预期结果和检查点，并根据用户提供的输出分析实际行为。
3. 第三遍，进入实现与源码：用确定性最小 Demo 或源码追踪解释内部机制，再通过用户亲自操作的综合任务和自然确认验收；默认不追加问答式复述。

执行规则：

- 不再为每个术语安排零散小测；每个模块使用一次贯穿实验和一次综合验收。
- 自 2026-08-14 起采用连续讲解模式：教练按“大白话完整场景 -> 术语映射 -> 实验 -> 证据结论 -> 稳定文档”连续推进，不在每个知识点后提问，也不要求学习者反复复述；学习者有疑问时随时打断。
- 连续讲解模式不降低实现与安全门槛：写代码仍需单独明确授权，危险操作、外部发布、凭据或新增权限仍须停下确认，实验结果偏离预期时必须暂停诊断。
- 不做问答式复述时，只记录“教学已覆盖、用户确认理解和实际操作证据”，不冒充“已独立背诵或脱离提示复述”；模块完成仍须满足对应代码、测试、真实实验与边界验收，综合能力最终由贯穿任务自然验证。
- 学习深度以能够正确配置、实现、测试和排查 Pi 为准；依赖管理等通用工具只讲与当前任务直接相关的决策和故障边界，不单独展开内部解析算法，确需深入时再结合真实实现或故障补充。
- 给出实验命令前，必须先说明真实场景、待验证问题、固定条件、唯一变量、预期可观察差异、通过标准，以及结果能证明和不能证明什么；没有对照组时，也要说明操作与证据之间的因果链。
- 实验发现错误时，先解释证据和原因，再完成一次正确重试。
- 后续每个新知识点首次出现时，默认先从学习者已经做过的真实操作切入，用一个完整大白话场景讲清“有什么用”和起止流程，再逐个解释陌生对象并映射术语；没有疑问时直接进入实验，不主动追加判断题或复述。
- 禁止用问题代替首次讲解；学习者按已有规则答对、但明确表示不认识题目中的对象或术语时，不计为新概念的理解验收，必须退回重新教学。
- 抽象机制先用一个有起点、过程和终点的完整大白话场景讲清“为什么需要、原任务如何开始、中途发生什么、Pi 如何处理、最终如何结束”，再映射到术语、快捷键、源码位置和真实边界；不得从流程中段或孤立定义开始。
- 重点机制在用户理解确认并形成稳定结论后，自动更新到对应主题文档；保留大白话场景、必要图表、版本和证据边界，不逐条转录聊天内容。
- 每个模块验收后立即回写本计划；Git 提交仍只在用户明确要求时执行。
- 真实 Pi 实验、最小 Agent Demo、Extension 和 SDK/RPC 程序都在本机运行；学习网站只展示脱敏后的可重复材料和结论。
- TypeScript 教学默认按“Java 已知概念与写法 -> TypeScript 对应表达 -> 两者关键语义差异 -> Pi 中的实际位置”推进；只补当前课程需要的 TypeScript，不把不准确的类比当作等价结论。SDK 主线允许用最小 TypeScript 理解官方接口，RPC/JSON Event Stream 与外部集成优先结合 Java 实现和验证。
- 快捷键统一按 macOS 视角讲解：使用 `⌃ Control`、`⌥ Option`、`⇧ Shift`、`⌘ Command` 和 `↩ Return`；首次出现同时写出按键全名，只有说明平台差异时才补充 Windows/Linux 表述。

学习网站采用“内容先行、最后建站”：

- `docs/learning/` 下的 Markdown（含主题子目录）是网站内容的唯一来源，不另建一套重复笔记。
- 每个模块尽量沉淀四类材料：原理、实验步骤、验证证据、结论与边界。
- 完成阶段 8 前只积累可发布内容，不让建站工作打断 Pi 主线学习。
- 阶段 9 默认使用 VitePress 组织现有 Markdown；正式开始前重新核对当时版本和部署方式。
- 公开内容不得包含 API Key、认证文件、原始 Session、个人敏感路径或未经脱敏的终端输出。

状态约定：

- `- [ ]`：未完成。
- `进行中`：已经开始，尚未满足验收条件。
- `待验证`：实现或操作已完成，但证据不足。
- `阻塞`：受凭据、费用、环境或外部依赖限制。
- `- [x]`：已完成并有验收证据。

强制边界：

- 不在聊天中发送 API Key、OAuth Token 或账号凭据。
- 模型认证、外部发布和安装第三方 Package 前必须单独确认；学习期模型调用已获得持续费用授权，无需逐次确认。
- Pi 没有内置沙箱；Project Trust 只控制项目资源加载，不能限制工具或 Extension 权限。
- 不在重要业务仓库进行破坏性练习；所有实验默认只在本仓库进行。
- 安装第三方 Package 前必须查看来源、版本、资源清单和 Extension 代码。
- 每阶段未通过验收，不进入依赖它的下一阶段。

## 4. 总体路线

| 阶段 | 主题 | 建议投入 | 核心产物 |
|---|---|---:|---|
| 0 | 架构、环境、安全和成本 | 3-4 小时 | 环境报告、安全清单、认证决策 |
| 1 | 终端与日常使用 | 5-7 小时 | 基础任务实操记录 |
| 2 | 项目配置与上下文 | 5-7 小时 | 可复用项目配置 |
| 3 | Session、Tree 与 Compaction | 6-8 小时 | 会话实验与结构分析 |
| 4 | Prompt、Skill 与 Theme | 6-8 小时 | Prompt、只读 Skill、Theme |
| 5 | TypeScript Extensions | 18-24 小时 | 可测试的安全门禁与自定义能力 Extension |
| 6 | Packages、Models 与 Providers | 12-18 小时 | 可管理的本地 Pi Package 与模型接入实验 |
| 7 | SDK、RPC、JSON 与 TUI | 8-12 小时 | 外部集成程序 |
| 8 | 源码与综合项目 | 10-15 小时 | 源码导读和毕业项目 |
| 9 | 学习网站与面试作品集 | 8-12 小时 | 在线网站、GitHub 项目和面试讲解材料 |

## 5. 分阶段计划

### 阶段 0：架构、环境、安全和成本

目标：先知道 Pi 能做什么、拥有什么权限、费用和数据流向哪里，再开始真实调用。

- [x] 0.1 完成本机环境体检
  - 验收证据：`docs/learning/00-environment-report.md`；四项理解题全部通过。
  - 用户已准确说明：Node.js/npm、Git/回滚、独立仓库/沙箱，以及当前已验证与尚未验证的 Pi 能力。
  - 内容：Node.js、npm、Git、终端、Pi 安装状态和学习仓库隔离。
- [x] 0.2 建立 Pi 架构心智模型
  - 验收：0.2.1 至 0.2.8 全部通过；已完成一次真实 `read` 请求，并能口头串联从上下文组装、Model Tool Call、Tool 执行、二次 Model 请求到 Agent Run 收尾和 Session 保存的完整链路。
  - [x] 0.2.1 理解 Pi 的整体分层、Coding Harness 定位及职责边界。
    - 验收：用户能准确区分 Pi 协调、Model 推理和 Tool 执行三类职责，并说明 Tool Call 还需经过注册表查找、Extension 事件和实际调度；已纠正 Session、Agent Loop/ReAct 和 Extension 必经性的术语边界。
  - [x] 0.2.2 理解系统提示、项目指令、用户输入、Session 历史和工具定义如何组成模型上下文。
    - 验收：用户能准确区分 `systemPrompt`、`messages`、`tools` 三盒，说明 Tool Result 进入后续 `messages`，并说明 `--tools read` 下 Model 看不到 `bash`、只负责产生可见 Tool 的 Tool Call，实际调度由 Pi 完成。
  - [x] 0.2.3 区分 Provider、Model、认证与 API 协议在请求链路中的位置。
    - 验收：用户能准确说明只更换服务入口和 API Key 时 Provider 配置需要改变；通信格式未变时 `openai-responses` 无需改变。
  - [x] 0.2.4 理解 Agent Run、Turn、Tool Call、Tool Result 和循环终止条件。
    - 验收：用户能区分一个 Run 内的多轮 Turn 和同轮多 Tool Call，说明错误 Tool Result 不会自动终止，并准确判断 `agent_settled` 只表示不再自动继续、与最终成功或失败无关。
  - [x] 0.2.5 理解 Tool 的定义、参数校验、执行权限、结果返回和内置工具边界。
    - 验收：用户能串联 Tool Call、名称查找、Schema 校验、可选门禁、Executor 和 Tool Result；能说明 Tool 继承当前系统用户权限，并准确区分 Model 只读 Tool 允许列表、用户 Shell、Extension 与进程沙箱。
  - [x] 0.2.6 理解 Extension 在事件、工具、命令、UI 和拦截链路中的位置。
    - 验收：用户正确理解 Extension 是可选插件层而非内置 Tool 的执行前提，能说明自定义 Tool 的注册和调度，并准确区分 Model `bash` Tool 的 `tool_call` 与用户 `!命令` 的 `user_bash` 两条门禁入口。
  - [x] 0.2.7 区分 Session、当前模型上下文、模型永久记忆和 Git 历史。
    - 验收：用户能说明 Model 只依据 Pi 本次传入的有效上下文，`pi -c` 是重建 Session 上下文而非唤醒永久记忆；并准确判断 Session 只能辅助重建文件，可靠版本恢复依赖 Git Commit。
  - [x] 0.2.8 跟踪一次真实只读请求，画出端到端链路并完成口头验收。
    - 验收：命名 Session `0.2.8-read-trace` 仅向 Model 暴露 `read`；界面显示用户消息、Tool Call、完整 Tool Result 和最终文本。用户能准确复述上下文组装、Provider/API 协议适配、Model、名称查找、Schema、可选 Extension、Executor、Tool Result、第二个 Turn、最终文本、`agent_end`、`agent_settled` 与 Session 保存的顺序和产生方。
    - 文档补充：已按 Pi 0.82.1 Agent Loop 实现记录 `stopReason` 与 Tool Call 的终止判断；无 Tool Call 不证明回答正确或完整。
  - 内容：Coding Harness、Provider、Model、Agent Loop、Tool、Extension、Session 的关系。
  - 验收：不看资料画出一次请求从用户输入到 Tool Result 再回到模型的链路。
- [x] 0.3 理解安全边界
  - 验收：用户完成 Project Trust 对照实验并准确复述：信任项目允许加载项目级 Skill 等受保护资源；Project Trust 与 `--no-tools` 都不改变 Pi 进程的当前用户权限，真正限制文件、进程和网络需要操作系统权限、容器、虚拟机或沙箱。
  - 实验证据：`--no-approve` 时项目 Skill 不可见，`--approve` 时可见，而 `--no-tools` 下用户 `!!pwd` 仍可执行。
  - 教学资源：项目级手动 Skill `pi-learning-coach` 已通过 Pi 0.82.1 原生加载器、模型提示隐藏、RPC 命令发现和独立复审。Pi 只作为实验对象。
  - 内容：Project Trust、当前用户权限、无内置沙箱、Prompt Injection、容器隔离。
  - 验收：能准确回答“信任项目后获得了什么”和“为什么它仍不是沙箱”。
- [x] 0.4 选择认证与费用方案
  - 验收：Base URL、Responses 协议、`openai/gpt-5.6-sol`、凭据保存及首次实际调用均已验证；`auth.json` 权限为 `0600`。
  - 课程约定：模型能力视为已完整接入；中转层不作为学习、验收或阻塞项；学习调用费用不设上限且无需逐次确认。
  - 内容：订阅登录、API Key、本地模型、凭据存储位置、数据流向和调用成本。
  - 验收：用户明确选择学习期 Provider，并确认是否允许产生模型费用。
- [x] 0.5 安装并完成首次安全会话
  - 验收：Pi CLI `0.82.1`、`fd 10.4.2`、中转配置、`0600` 凭据权限、`openai/gpt-5.6-sol` 响应、Session 保存及退出状态均已验证；Git 仅有预期学习文档修改。
  - 内容：安装来源、版本核验、登录或认证、只读首个任务、退出和会话定位。
  - 验收：Pi 版本可核验，首个会话成功，Git 工作区没有非预期修改。

阶段门禁：能独立解释权限、凭据、费用和数据边界，才进入阶段 1。

### 阶段 1：终端与日常使用

目标：把 Pi 当作可控的日常编程助手使用，而不是只会发一句提示词。

- [x] 1.1 掌握交互式界面、编辑器操作、文件引用和图片输入。
  - 已完成：编辑器操作、TUI 四区、交互式 `@` 文件引用、CLI `@文件` 主动附加和 macOS 剪贴板图片输入均已通过验收。
  - 已验证：`pi --version` 与 TUI 均显示 `0.83.0`；`/hotkeys` 正常打开，当前 Session `1.1-interactive-basics` 仅向 Model 暴露 `read`；用户已在编辑区形成两行草稿且消息未发送，并实测 `⌃A` 行首、`⌃E` 行尾、`⌃W` 删除前一个单词、`⌃-` 撤销编辑；`⌃G` 已通过 `UW PICO 5.09` 修改临时草稿并返回 Pi，整个过程未发送消息；`⌃C` 已清空当前两行草稿，Pi 未退出且没有产生用户消息或 Model 响应；`@` 文件列表已选中并插入 `docs/learning/00-environment-report.md`，此时尚未读取；提交明确请求后出现对应的 `read` Tool Call，并正确返回一级标题“阶段 0.1 本机环境体检报告”；用户已通过 macOS `⌃⇧⌘4` 截图并用 Pi `⌃V` 插入脱敏的临时 `pi-clipboard-*.png` 路径；提交后出现对应的图片 `read` Tool Call、可见图片 Tool Result，并正确识别“阶段 0.1 本机环境体检报告”；用户已正确将绿色 `read`、空白输入框和最底部 Model 状态分别映射到 Messages、Editor 和 Footer，并理解 Startup header 可能因滚动离开视野；无 Tool、无 Session 的 CLI `@文件` 实验没有 Tool Call，仍正确输出一级标题，证明文件内容由 Pi 主动附加。
  - 快捷键决定：不创建全局自定义绑定；继续使用 `⇧↩`/`⌃J` 换行，保留 `⌥↩` 的默认 Follow-up 含义。
  - 版本差异：`0.83.0` 未改变本模块使用的编辑器快捷键；工具输出展开提示和图片路径显示有修复，按新版本继续实验。
  - 验收：能指出 TUI 四个区域，使用多行输入和外部编辑器编辑待发送消息，通过 `@` 与剪贴板分别插入文件和临时图片路径，并观察对应的 `read` Tool Call；能区分交互式路径引用与 CLI `@文件` 主动附加内容的行为。
- [x] 1.2 掌握内置读取、写入、编辑和 Shell 工具的行为与风险。
  - 已完成：系统地图、`write`、分段 `read`、精确 `edit`、无副作用 `bash` 实验和模块综合验收均已通过。
  - 已验证：用户能将“读取前 20 行、创建完整新文件、替换唯一旧文本、运行测试”依次映射为 `read`、`write`、`edit`、`bash`。
  - `write` 证据：只暴露 `write` 的 Session `1.2-write` 成功创建 `labs/1.2-tools/tool-lab.txt`；Tool Result 显示预期三行，文件在 Git 中仅作为该路径的未跟踪文件出现，随后已由独立 `read` 完成分段回读。
  - `read` 证据：只暴露 `read` 的 Session `1.2-read` 使用 `offset=2`、`limit=2`，Tool 标题显示读取第 2-3 行，结果准确返回 `status=created` 和 `mode=dev`，未调用其他 Tool。
  - `edit` 证据：只暴露 `edit` 的 Session `1.2-edit` 将唯一文本 `mode=dev` 精确替换为 `mode=study`；Tool Result 只显示第 3 行变化，真实文件与 Git diff 均一致，未调用其他 Tool。
  - `bash` 证据：只暴露 `bash` 的 Session `1.2-bash` 准确执行 `pwd && git diff -- labs/1.2-tools/tool-lab.txt`；返回当前仓库路径和预期单行 diff，运行前后 Git 变更范围未增加。
  - 综合验收：四项全部通过。用户能选择 `read -> edit -> bash`，区分 `write` 整文件写入与 `edit` 精确替换，说明 Schema 校验不判断操作是否安全或正确，准确解释 `--tools` 只限制 Model 可见 Tool、`bash` 仍拥有当前用户权限、真正沙箱需要系统级隔离，并说明可靠恢复依赖 Git。
  - 源码核对：本机 Pi `0.83.0` 的 `read` 支持文本、图片及分段读取；`write` 创建父目录并整文件覆盖；`edit` 要求已有文件和唯一、互不重叠的精确旧文本；`bash` 在当前工作目录启动真实 Shell，默认没有超时。
  - 验收：能为真实场景选择最小 Tool，解释 Schema 与操作风险的区别，并在专用实验路径完成创建、读取、精确替换、无副作用 Shell 检查和 Git 差异核对。
- [x] 1.3 掌握模型切换、Thinking Level、模型范围和上下文用量查看。
  - 已完成：本机 Pi `0.83.0` 中，Thinking Level、Model 选择器、Scoped Models 配置与实际 `⌃P` 切换、恢复、Context Usage 显示及模块综合验收均已完成。
  - Thinking Level 证据：用户已用 `⇧Tab` 成功切换当前 `gpt-5.6-sol` 的 Thinking Level，Footer 随之更新。
  - Model 选择器证据：用户按 `⌃L` 打开选择器；界面显示当前模型 `gpt-5.6-sol [openai]` 及勾选标记、共 38 个已配置 Provider 下的可选模型，并显示 Model ID、Provider 和友好名称；未执行模型切换。
  - Scoped Models 证据：用户打开 `/scoped-models`；Model Configuration 显示 38 个模型且 `all enabled`，变更默认只作用于当前 Session，按 `⌃S` 才保存到 settings；配置界面高亮 `gpt-4` 时 Footer 仍为 `gpt-5.6-sol • max`。退出配置界面后按一次 `⌃P`，Pi 显示切到 `gpt-5.6-terra • xhigh`；期间未发送消息，随后用 `⌃L` 恢复 `gpt-5.6-sol • max`。这证明配置范围不等于当前 Model，普通 TUI 的 `⌃P` 才会执行快速切换，且切换后应重新检查 Thinking Level。
  - Context Usage 证据：同一截图的 Footer 显示 `↑10k ↓146 $0.055 1.9%/272K (auto)`；用户已准确说明 `↑10k` 是整个 Session 的累计输入，不是当前上下文，Compaction 后累计输入和输出通常不会回退、当前上下文比例会下降，`(auto)` 表示启用自动 Compaction。
  - 综合验收：用户能区分 Session 累计用量与当前有效上下文，说明自动 Compaction 会调用 Model 总结较早消息，并在后续请求中使用摘要和保留的近期消息。边界纠正：Session 不等于当前终端窗口；刚完成 Compaction 时上下文用量还可能暂时显示为 `?`。
  - 非门禁残余：`⇧⌃P` 反向循环尚未单独实测；`⌃P` 正向切换和 `⌃L` 恢复已覆盖本模块核心验收。
- [x] 1.4 掌握普通 Shell、隐藏输出的 Shell、取消和重试。
  - 已完成：`!`、`!!` 与 Model `bash` Tool 的三路径概念、无副作用 Shell 对照、用户 Shell 取消、自动重试理解检查、完整退避、重试等待取消实验和模块综合复述均已通过。
  - 概念验收：用户准确说明在 `--no-tools` 下，`!` 与 `!!` 仍由 Pi 的用户 Shell 入口执行；`!` 的命令和结果会进入后续 Model 上下文，`!!` 不会；Model 因看不到任何 Tool 定义而不能主动请求 `bash` Tool Call。
  - Shell 对照证据：Session `1.4-user-shell` 使用 `--no-tools`、`--no-extensions` 等隔离参数；`!printf` 与 `!!printf` 都在 TUI 输出各自 marker，随后普通 Model 请求只复述 `visible-marker-4817`，没有复述 `hidden-marker-9264`，也没有产生 Tool Call。
  - 取消源码核对：本机 Pi `0.83.0` 在用户 Shell 运行时由 `Esc` 触发 `abortBash()`；默认本地后端响应取消信号并终止进程组，TUI 将该 Bash 记录显示为 `cancelled`。自定义 Extension 替换执行后端时不自动继承这一保证。
  - 取消实验证据：Session `1.4-user-shell` 执行 `!sleep 30 && printf 'SHOULD_NOT_PRINT\\n'` 后，在等待期间按 `Esc`；TUI 显示 `(cancelled)`，完成 marker `SHOULD_NOT_PRINT` 未出现，Editor 恢复可输入状态。该证据只证明尚未执行的后续命令被阻止，不证明已发生的副作用能够回滚。
  - 自动重试核对：当前设置没有覆盖 `retry`，本机 Pi `0.83.0` 因而采用默认的启用状态、最多 3 次重试和 2/4/8 秒指数退避；普通自动重试只处理 Model/Provider 瞬时错误，Tool/Bash 失败形成 Tool Result，Context Overflow 走 Compaction 恢复。源码、随包文档、有效设置、用户理解和受控实验均已核对。
  - 自动重试理解检查：已通过。用户能区分三条失败路径：HTTP `503` 属于瞬时 Model/Provider 错误，默认是最初请求 1 次加最多 3 次重试；`bash` Tool 退出码 `1` 会形成失败 Tool Result 并进入下一次 Model 请求，由 Model 决定后续动作；Context Overflow 需要先 Compaction，用历史摘要和近期消息缩小有效上下文，再恢复原请求。
  - 探针预检证据：临时目录 `/tmp/pi-retry-probe.XD9Tcx` 中的假 Provider 始终返回规范化的 `503 Service Unavailable`；使用独立 `HOME`、独立 `PI_CODING_AGENT_DIR`、`--offline` 和 macOS `sandbox-exec` 禁网运行，不读取真实凭据、不修改用户设置，也未增加仓库变更。非交互预检记录 4 次请求，时间点约为 `0.009s`、`2.016s`、`6.021s`、`14.026s`，对应 `2/4/8` 秒退避。该证据验证 Pi 收到规范化 `503` 后的错误分类与 Agent 级重试路径，不验证真实 HTTP SDK 如何把状态码转换为错误消息。
  - 完整退避实验证据：用户在真实 Pi TUI 中触发本地假 `503`；界面连续显示 4 条 `503 Service Unavailable`，最终显示 `Retry failed after 3 attempts`。请求日志时间点为 `5.164s`、`7.169s`、`11.173s`、`19.176s`，相邻间隔分别约为 `2.005s`、`4.004s`、`8.003s`，证明“首次请求 1 次 + 最多重试 3 次”和默认指数退避均按预期发生。实验后仓库变更范围未增加。
  - 重试取消实验证据：为留出稳定按键时间，隔离探针只在本次实验中将基础等待改为 10 秒；用户在第一次 `503` 后按 `Esc`，TUI 显示 `Retry failed after 1 attempts: Retry cancelled`，Editor 恢复可输入状态。独立请求日志只有 `attempt: 1`，证明等待中的下一次 Model 请求没有发出。该取消只阻止未来重试，不会撤销首次请求，也不能回滚首次请求已经产生的外部副作用；实验后仓库变更范围未增加。
  - 综合验收：用户准确区分 Provider 瞬时错误、Tool 失败和 Context Overflow 三条恢复路径，并说明等待 `503` 重试时按 `Esc` 只取消尚未发出的下一次重试，不会撤销首次请求或回滚其副作用。
- [x] 1.5 掌握 Steering 与 Follow-up 消息的差异和队列行为。
  - 已完成：系统地图、Steering 与 Follow-up 对照实验及模块综合复述均已通过。
  - Steering 证据：Session `1.5-steering` 的 Bash Tool 持续运行约 `20.8s` 并正常返回 `ORIGINAL_TOOL_DONE`；运行期间按普通 `↩ Return` 排队的新要求未中断 Tool，在 Tool 结束后进入下一次 Model 请求，最终输出由 `ORIGINAL_FINAL` 改为 `STEERING_APPLIED`。
  - Steering 理解验收：用户已确认理解“不会被打断的是已开始的 Tool，不是尚未生成的最终回答”；Steering 在 Tool 结束后的下一次 Model 请求前生效，因此旧的未来回答不会先输出。
  - Follow-up 证据：Session `1.5-follow-up` 的 Bash Tool 持续运行约 `20.0s` 并返回 `PRIMARY_TOOL_DONE`；运行期间使用 `⌥↩` 排队后续要求，TUI 随后依次出现 `PRIMARY_FINAL`、后续用户消息和 `FOLLOW_UP_APPLIED`，证明当前任务先正常收尾，Follow-up 才作为后续用户消息进入 Model。
  - 综合验收：用户能准确说明 Steering 不会中断已经开始的测试，会在 Tool 完成后把 Tool Result 与新要求一起交给 Model；并能说明 Follow-up 处理完成且 Pi 已无其他自动续行动作后，才进入 `agent_settled`。
- [x] 1.6 掌握 Print、JSON 等非交互模式的适用边界。
  - 已完成：系统地图、Interactive/Print/JSON 对照实验、源码定位和模块综合复述均已通过。
  - Interactive 实验证据：用户以 `--no-tools --no-session` 等隔离参数启动默认 TUI，Model 返回 `MODE_OK` 后底部 Editor 仍保持可输入，Pi 进程未自动退出；已验证 Interactive 面向持续交互的生命周期行为。
  - Print 实验证据：用户以相同隔离参数和 `-p` 执行单次任务，终端只显示最终文本 `MODE_OK`，随后自动返回 zsh 的 `%` 提示符；已验证 Print 不启动持续 TUI，并在最终文本后退出。
  - JSON 实验证据：用户以相同隔离参数和 `--mode json` 执行单次任务，终端逐行输出 `session`、`agent_start`、`turn_start`、`message_*`、`turn_end`、`agent_end`、`agent_settled` 等 JSON 对象；最终文本 `MODE_OK` 位于消息事件中，随后自动返回 zsh 的 `%` 提示符。已验证 JSON 是完整 JSONL 事件流，而不是要求 Model 将业务答案改成 JSON。
  - 源码定位证据：本机 Pi 0.83.0 的 `dist/main.js` 由 `resolveAppMode()` 选择运行外壳；Interactive 创建并运行 `InteractiveMode`，Print 和 JSON 共用 `runPrintMode()`。`dist/modes/print-mode.js` 中，Print 提取最后一条 Assistant Message 的文本，JSON 则订阅并逐行序列化完整 Session 事件；两者最终都会释放 Runtime 并退出。
  - 综合验收：用户准确说明三种模式共享同一个 Agent 核心；Interactive 面向人并持续等待输入，Print 面向人或 Shell 脚本并在最终文本后退出，JSON 面向程序、自定义界面或日志系统并在完整 JSONL 事件流后退出；同时说明运行模式不决定文件修改能力。
- [x] 1.7 独立完成一次“分析 -> 修改 -> 测试 -> 查看 diff -> 总结”的小任务。
  - 已完成：单文件完整操作、仓库侧核对和用户综合复述均已通过。
  - 操作证据：Session `1.7-workflow` 的 TUI 依次显示 `read -> edit -> bash（测试）-> bash（查看 diff）`；测试输出 `TEST_OK`，限定 Git diff 只包含 `status=created -> status=verified`，最终总结准确报告 Tool 顺序、测试、差异范围及未提交、未推送。
  - 仓库证据：`labs/1.2-tools/tool-lab.txt` 仍为三行，第二行为 `status=verified`；目标文件的 Git diff 只有上述单行替换。课程文档在实验前已有未提交修改，不能把全局 dirty 状态归因于本次 Agent Run。
  - 安全边界：启动时项目未受信任，但项目资源已显式关闭，内置 Tool 链路不受影响；`--tools read,edit,bash` 和任务提示词仍不是沙箱，实验的可控性来自独立仓库、冻结范围、精确测试与 Git 审查。
  - 综合验收：用户准确说明 `edit` 成功只代表工具操作完成，Tool Result 仍需进入后续 Messages 交给 Model 决定下一步；测试用于证明修改满足要求，Git diff 用于证明没有额外改动，最终总结用于向用户报告操作顺序、验证结果和未执行事项。

验收证据：操作记录、关键输出、Git diff、测试结果和用户对工具调用过程的口头复述。

### 阶段 2：项目配置与上下文

目标：能够为不同项目建立稳定、最小权限、可复现的 Pi 配置。

- [x] 2.1 理解全局与项目配置路径、合并规则和优先级。
  - 已完成：2.1 验收时，本机 Pi 与 npm 包版本均核对为 `0.83.0`；配置来源、Project Trust、Settings 合并规则和 CLI 本次运行调整已完成理解验收；项目值覆盖全局同名值、CLI 值覆盖项目值以及 CLI 覆盖不持久化均已实测。
  - 综合验收：用户已正确判断本次显式 CLI 值为 `low`、项目未覆盖的全局项继续生效、退出后无 CLI 参数恢复项目值 `high`，并说明 CLI 临时值不会改写配置文件；最终准确复述“显式 CLI > 项目配置 > 全局配置”。三层均无值时由 Pi 内置默认值兜底；本实验使用 `--no-session`，不把 Session 恢复状态混入配置优先级。
  - 系统地图理解验收：用户能准确说明项目配置只覆盖同名项，未被覆盖的全局配置继续生效。
  - 启动参数理解验收：用户能准确判断显式 `--thinking max` 只把当前运行调整为 `max`，不会改写项目默认值 `high` 或任何配置文件；下一次不带该参数的新运行仍回到有效 Settings 默认值。
  - CLI 临时覆盖实验证据（历史第一步）：用户以 `--no-session --model openai/gpt-5.6-sol --thinking low --name 2.1-cli-low` 启动 Pi，Footer 显示 `gpt-5.6-sol • low`；该步只证明显式参数控制当次运行，当时还没有取得下一次运行恢复默认值的证据。
  - CLI 临时覆盖实验证据（历史第二步）：退出第一步运行后，用户以相同 Model 但不带 `--thinking` 的 `2.1-settings-default` 新运行启动 Pi，Footer 恢复为 `gpt-5.6-sol • max`；仓库侧只读核对确认全局 `defaultThinkingLevel` 为 `max`，当时项目尚无 `.pi/settings.json`。两步合并证明 CLI 的 `low` 只影响第一步运行、没有持久化改写全局配置；项目覆盖能力随后由项目值实验验证。
  - 项目覆盖实验证据：项目 `.pi/settings.json` 只设置 `defaultThinkingLevel=high`，全局 `~/.pi/agent/settings.json` 仍为 `max`。用户在当前仓库以 `--no-session --model openai/gpt-5.6-sol --name 2.1-project-high` 启动，未传 `--thinking`，Footer 显示 `gpt-5.6-sol • high`；结合静态配置核对，可证明项目同名值覆盖全局值。
  - CLI 覆盖项目实验证据：用户随后以 `--no-session --model openai/gpt-5.6-sol --thinking low --name 2.1-cli-over-project` 启动，Footer 显示 `gpt-5.6-sol • low`；证明显式 CLI 值在本次运行中覆盖项目默认值。退出后再以不带 `--thinking` 的 `2.1-cli-reset-check` 新运行启动，Footer 恢复为 `gpt-5.6-sol • high`；证明 CLI 的 `low` 只作用于前一次运行，没有持久化改写项目配置。
- [x] 2.2 理解 `AGENTS.md`、`CLAUDE.md`、系统提示文件和普通上下文文件的作用差异。
  - 已完成：四类文件的系统地图、最小对照实验、源码追踪和两轮综合复述均已完成。
  - 已验收（系统地图第一部分）：用户能说明普通 `README.md` 不会仅因存在而自动成为规则；同一目录中的 `AGENTS.md` 与 `CLAUDE.md` 是候选关系，Pi `0.83.0` 优先加载 `AGENTS.md`，不会同时加载两份。
  - 已验收（系统地图第二部分）：用户能准确识别全局 `AGENTS.md`、父目录 `CLAUDE.md` 和当前目录 `AGENTS.md` 三份命中文件，并说明它们按全局到当前目录的顺序共同拼接进入 `systemPrompt`；当前目录 `CLAUDE.md` 因同目录已有更优先的 `AGENTS.md` 而不加载。
  - 已验收（系统地图第三部分）：用户能准确选择 `.pi/APPEND_SYSTEM.md` 来保留 Pi 默认 Coding Agent 提示并追加项目规则，并说明 `.pi/SYSTEM.md` 会替换默认基础系统提示；两者都属于 `systemPrompt` 路径。
  - 已验收（系统地图第四部分）：用户能准确说明 `.pi/APPEND_SYSTEM.md` 的项目规则进入 `systemPrompt`，显式 CLI `@文件` 的内容与当前请求进入 `messages`，并判断 `--no-context-files` 不会禁用显式 `@文件` 附件。
  - 已验证（最小对照实验）：用户在受信任项目中创建只含唯一标记 `SYSTEM_RULE_2201` 的 `.pi/APPEND_SYSTEM.md`，随后以 `--no-context-files --no-tools --no-session` 和显式 CLI `@docs/learning/00-environment-report.md` 发起 Print 请求；Model 依次输出该系统规则标记和附件一级标题。启动参数没有向 Model 暴露 Tool，因此实验验证了自动追加的项目系统规则与显式 CLI 附件都能到达 Model，也验证了 `--no-context-files` 不会禁用显式 `@文件`。
  - 证据边界：上述输出能证明两类内容都到达 Model，但不能只凭输出反推出 Pi 内部对象字段；`systemPrompt` 与 `messages` 的精确落点由下面的源码证据确认。
  - 已验证（源码实现）：Pi `0.83.0` 的 `ResourceLoader` 在 Project Trust 允许时发现并读取项目 `.pi/APPEND_SYSTEM.md`，`AgentSession._rebuildSystemPrompt()` 将其交给 `buildSystemPrompt()` 形成最终 `systemPrompt`；CLI `processFileArguments()` 直接读取显式 `@文件`，`buildInitialMessage()` 将文件内容与用户文字合并，Print 模式再调用 `session.prompt()`，使其进入当前用户消息。`--no-context-files` 只跳过 `AGENTS.md`/`CLAUDE.md` 自动发现，不影响上述两条通道。
  - 已校正（Trust 精确边界）：Pi `0.83.0` 的 Project Trust 不控制 `AGENTS.md`/`CLAUDE.md`；`--no-approve` 下它们仍会自动发现。Project Trust 控制项目 `.pi/*`，以及当前目录或祖先目录中的项目 `.agents/skills`；关闭自动上下文文件必须使用 `--no-context-files`。
  - 综合复述第一次结果（历史未通过）：用户已正确说明显式 CLI `@README.md` 的内容进入当前用户消息；当时遗漏三点：`--no-context-files` 会跳过 `AGENTS.md`，显式 `@README.md` 不会被跳过；`AGENTS.md` 若被加载也属于 `systemPrompt`，不属于 `messages`；Project Trust 位于启动期项目资源加载阶段，只门禁项目主动提供的受保护资源，不控制显式 CLI 附件，也不是操作系统权限或沙箱。
  - 综合复述第二次结果（历史部分通过）：用户已准确说明 `.pi/APPEND_SYSTEM.md` 进入 `systemPrompt`、`--no-context-files` 跳过 `AGENTS.md`、显式 `@README.md` 进入 `messages`，并正确说明 Project Trust 不是操作系统权限或沙箱；当时遗漏 Project Trust 的正向职责：它在启动期决定是否加载项目提供的受保护资源，且不控制显式 CLI `@文件`。
  - 综合验收：用户最终准确说明 Project Trust 在本场景中决定项目 `.pi/APPEND_SYSTEM.md` 能否被加载；结合上一轮复述，已能完整区分 `systemPrompt`、自动上下文文件、显式 CLI `@文件` 与操作系统权限边界。
- [x] 2.3 为本仓库编写最小 `AGENTS.md`，约束范围、测试、凭据和 Git 操作。
  - 已完成：用户已确认后创建根目录 `AGENTS.md`，覆盖范围、验证、凭据、Git 与权限边界，并加入唯一标记 `PI_STUDY_AGENTS_V1`；静态检查通过。
  - 已验证（正向对照）：用户以 `--no-approve --no-tools --no-session --no-extensions --no-skills --no-prompt-templates --no-themes` 启动 Print 请求，Model 准确输出 `PI_STUDY_AGENTS_V1`。这证明项目不受信任时根目录 `AGENTS.md` 仍会作为自动上下文加载；同时 `--no-tools` 与 `--no-session` 排除了 Tool 主动读取和旧 Session 记忆两种来源。该单次实验不能单独证明 `--no-context-files` 的关闭效果。
  - 已验证（反向对照）：在其他条件与提示词不变的情况下只增加 `--no-context-files`，Model 准确输出 `CONTEXT_NOT_LOADED`。正反两次运行构成单变量 A/B 实验，证明该开关会关闭 `AGENTS.md`/`CLAUDE.md` 的自动发现。
  - 已验收（系统地图）：用户能准确说明 `AGENTS.md` 会作为系统提示交给 Model，只是行为指令而非硬权限；它不能百分之百阻止 `bash` 执行 `git commit`，真正的强制限制仍需 Tool 门禁、Extension、受限用户或沙箱。
  - 综合验收：最小项目规则文件、静态检查、正反加载对照和权限边界复述均已通过；2.3 完成。
- [x] 2.4 配置模型、Thinking、重试、网络、图像、Shell 和模型轮换策略。
  - 已完成：七类配置地图、理解检查和最小行为实验均已通过；Model、Thinking、Retry、Network、Images、Shell 与 Model 轮换策略均有行为、配置或源码证据，并在下文区分直接证据与用户操作确认。
  - 模型四层理解验收：用户已准确区分 `models.json`（模型目录）、`defaultProvider`/`defaultModel`（默认模型）、`enabledModels`/`--models`（持久与本次启动的轮换候选），并能正确区分 `--model`（启动前为本次运行直接指定模型）与 `/model`/`⌃L`（进入 Interactive TUI 后切换当前模型）的使用时机。
  - Model/Thinking 策略判断（已通过）：用户已正确选择 `--thinking low` 处理一次性简单任务、使用 `--model Terra` 临时切换模型，并能判断临时覆盖退出后恢复有效默认值 `gpt-5.6-sol + high`。
  - 已验证（简称解析实验）：用户运行 `pi --no-session --model Terra --thinking low --name 2.4-terra-low`，Footer 实际显示 `openai/gpt-5.6-terra-pro • low` 和 `0.0%/1.1M`。这证明 `--thinking low` 已生效，也证明 `Terra` 是运行时模糊匹配条件，不是 `openai/gpt-5.6-terra` 的固定别名；先前根据 `pi --list-models Terra` 唯一输出推断简称会固定命中 `terra` 的结论已撤回。
  - 已验证（源码边界）：Pi `0.83.0` 的 CLI Model Resolver 先精确匹配，再按 ID/名称部分匹配；多个别名命中时按 ID 倒序取首项。`--model` 使用全部运行时模型，`--list-models` 使用当前可用模型，因此后者的唯一输出不能证明前者候选唯一。脚本和可复现实验必须使用完整 `provider/model-id`，并以 Footer 验证实际选择。
  - 已验证（精确 ID 对照）：用户运行 `pi --no-session --model openai/gpt-5.6-terra --thinking low --name 2.4-terra-exact`，Footer 显示 `gpt-5.6-terra • low`，上下文窗口为 `272K`。这与简称实验命中的 `terra-pro • low`、`1.1M` 形成 A/B 对照，证明完整 `provider/model-id` 能精确选择目标 Model，且 `--thinking low` 同时生效。
  - 已验证（临时覆盖恢复）：用户退出 `openai/gpt-5.6-terra + low` 的精确 ID 实验后，运行 `pi --no-session --name 2.4-default-restore`，未传 `--model` 和 `--thinking`；Footer 显示 `gpt-5.6-sol • high`。静态配置同时表明：项目未设置默认 Provider/Model，因此 Model 取全局 `openai/gpt-5.6-sol`；项目 `defaultThinkingLevel=high` 覆盖全局 `max`。这证明上一轮 CLI 覆盖只属于原进程，没有写入持久配置；`--no-session` 还排除了旧 Session 状态的干扰。
  - Retry 系统地图（已完成）：已根据本机 Pi `0.83.0` 的设置文档和实现核对，Agent 层默认开启，最多重试 3 次，基础等待 2 秒并按 `2/4/8` 秒退避；Provider/SDK 层默认重试 0 次。两层边界、次数语义和用户理解验收均已完成。
  - Retry 理解检查（已通过）：用户已准确判断默认 Provider 重试为 0 时，首次请求加 Agent 层最多 3 次重试共 4 次 HTTP 请求，等待依次为 `2/4/8` 秒；并能准确区分 Agent Retry 重试失败的 Model 请求，不会自动重跑此前成功的 Tool，而 Tool 失败会形成 Tool Result 再交给 Model 决策。
  - Provider/SDK Retry 实验（已验证）：用户在自己的终端直接调用 Pi 随包的 `retryProviderRequest()`，用内存假请求持续抛出带 `status=503` 和 `retry-after-ms=50` 的错误；设置 `maxRetries=2` 后实际输出 `attempt=1/2/3`、`final=503 local provider retry probe` 和 `total=3`。实验未访问网络、未读取凭据、未写仓库，证明该包装器执行“首次请求 + 最多两次重试”并在全部失败后传播最终错误；由于未经过 Settings 解析、真实 Provider 适配器和网络链路，不扩大为真实 Provider 集成已经验收。
  - Network 系统地图与理解检查（已通过）：已根据本机 Pi `0.83.0` 的设置文档与实现区分 `transport`、全局 `httpProxy`、`websocketConnectTimeoutMs`、`httpIdleTimeoutMs` 和 `retry.provider.timeoutMs`；用户已准确说明 WebSocket 无法建立连接对应 `websocketConnectTimeoutMs`，HTTP 已连接但流长期无数据对应 `httpIdleTimeoutMs`，并能说明 `retry.provider.timeoutMs` 只限制单次 Model 请求，不能限制整个 Run，因为一个 Run 可能包含多次 Model 请求、Tool 执行和重试。
  - Network 重要校正（已记录）：`--offline`/`PI_OFFLINE=1` 只关闭启动阶段的版本检查、Package 更新检查和相关遥测，不阻止后续 Model Provider 请求，也不是 Tool 网络沙箱；Pi `0.83.0` 没有独立的 TLS/CA Settings 字段，代理路线与证书信任必须分开判断。
  - Network 最小超时实验（已验证）：用户已在真实 macOS 终端运行 `node labs/2.4-network/timeout-probe.mjs`。输出确认 Pi 向本机假 Provider 发出一次 `POST /v1/chat/completions`，随后以 `Request timed out.` 退出；Provider 共收到 1 次请求，证明 Provider 与 Pi 两层重试均已关闭；外层看门狗未介入，Pi 子进程用时 1367 ms；脚本最终输出 `实验结果：PASS`。该实验只证明 `retry.provider.timeoutMs` 限制一次 Model 请求，不证明整个 Run 存在总时限。
  - Images 系统地图与理解检查（已通过）：用户已准确判断 `terminal.showImages=false`、`images.blockImages=false` 时终端不显示图片预览但图片仍可发送给 Model；反向配置 `terminal.showImages=true`、`images.blockImages=true` 时终端具备预览条件但图片不会发送给 Model。两个开关分别控制本地 TUI 展示和 Model 输入，互不替代；终端预览仍取决于终端能力。
  - Images A 最小实验（已验证）：用户在 `2.4-images-a` 中将 `Show images` 设为 `false`、`Block images` 保持 `false`，用 macOS `⌃V` 插入脱敏临时图片并明确要求调用 `read`；Pi 出现 `read` Tool Call，终端未显示图片预览，Model 仍准确回答图片中最醒目的文字。顶部 Project Trust 警告与本实验无关，因实验显式关闭了项目上下文、Extension、Skill 和 Prompt Template。
  - Images B 最小实验（已验证）：用户在 `2.4-images-b` 中将 `Show images` 设为 `true`、`Block images` 设为 `true`，使用脱敏临时图片并明确要求调用 `read`；Pi 出现 `read` Tool Call，TUI 显示图片预览，Model 最终返回 `IMAGE_BLOCKED`。这些是本地预览和 Model 行为的直接证据，与 Block Images 的预期行为一致；实验没有检查序列化后的 Provider 请求载荷，不能只凭该输出直接证明出站请求中不存在图片数据。
  - Images A/B 综合结论（已验证，证据分级）：设置文档与 Pi `0.83.0` 的 `dist/core/sdk.js` 表明，`terminal.showImages` 控制本地 TUI 预览，`images.blockImages` 会在 `convertToLlm` 后过滤消息中的图片内容；A/B 行为与该实现一致。终端可见性与 Model 可见性相互独立，两者都不是文件权限或沙箱；本实验未抓取最终出站载荷。
  - Images 默认恢复（已验证）：只读核验全局有效值为 `terminal.showImages=true`、`images.blockImages=false`；项目 `.pi/settings.json` 未设置这两个字段，因此不存在项目同名覆盖。实验环境已恢复到课程默认组合。
  - Shell 默认解释器实验（已验证）：用户在 `2.4-shell-default` 中显式关闭项目上下文、Extension、Skill、Prompt Template 和 Model Tools 后，通过用户 Shell 入口执行 `!printf 'PI_SHELL=%s\n' "$0"`，实际输出 `PI_SHELL=/bin/bash`。这证明本机未显式配置 `shellPath` 时，Pi `0.83.0` 的用户 Shell 按 Unix 默认回退使用 `/bin/bash`；不表示外层 macOS 登录 Shell 已从 zsh 改为 bash，也不代表权限隔离。`--no-tools` 只隐藏 Model Tool，不禁用用户主动输入的 `!`/`!!`。
  - Shell 显式路径实验（已验证）：用户在独立临时项目 `2.4-shell-zsh` 的 `.pi/settings.json` 中只设置 `shellPath=/bin/zsh`，以 `--approve --no-session --no-context-files --no-extensions --no-skills --no-prompt-templates --no-tools` 启动后，通过用户 Shell 入口执行与默认实验相同的命令，实际输出 `PI_SHELL=/bin/zsh`。与默认 `/bin/bash` 结果构成单变量对照，证明受信任项目的 `shellPath` 能改变 Pi 启动的命令解释器；没有修改 macOS 登录 Shell，也没有增加权限隔离。
  - Shell 命令前缀实验（已验证）：用户在独立临时项目 `2.4-shell-prefix` 中设置 `shellPath=/bin/zsh` 与 `shellCommandPrefix="export PI_STUDY_PREFIX_2401=PREFIX_ACTIVE"`。用户 `!` 入口实际输出 `USER_PREFIX=PREFIX_ACTIVE`；Model 按要求调用绿色 `bash` Tool，Tool Result 输出 `MODEL_PREFIX=PREFIX_ACTIVE`，最终回答也与 Tool Result 一致。这证明 Pi `0.83.0` 会在两条 Shell 入口的每次正式命令前执行同一个前缀；不表示该环境变量永久写入系统，也不增加权限隔离。
  - Model 轮换第一次实验（历史部分通过）：`--models` 指定 `sol`、`terra`、`terra-pro` 后，Pi 明确警告 `terra-pro` 无可用匹配，实际 `Model scope` 只包含 `gpt-5.6-sol` 与 `gpt-5.6-terra`；用户按 `⌃P` 后状态与 Footer 均显示切到 `gpt-5.6-terra • high`。这直接验证无匹配模式会被警告并排除，且正向切换只在解析后的候选范围内进行；该轮两个候选不足以区分正向与反向顺序，轮换验收未闭合。
  - Model 轮换第二次实验（历史部分通过）：将第三个候选替换为可用的 `openai/gpt-5.6-luna` 后，启动信息准确显示 `Model scope: gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna`，界面状态与 Footer 显示已切到 `gpt-5.6-luna • high`。退出后只读检查显示 `defaultModel=gpt-5.6-luna`、`enabledModels=null`，直接证明 `--models` 候选范围不持久化，但轮换后选中的 Model 会写入全局默认值。当时截图未显示 `⇧⌃P` 反向过程，默认 Model 也未恢复为课程基线 `sol`。
  - Model 轮换综合验收（已完成，证据分级）：界面输出直接证明无匹配候选会被警告并排除、`sol/terra/luna` 候选范围生效，以及 `⌃P` 在解析后的范围内正向轮换；用户确认 `⇧⌃P` 反向轮换操作无问题，但未保留该过程的直接界面输出。随后只读核验全局状态已恢复为 `defaultModel=gpt-5.6-sol`、`enabledModels=null`，直接证明 CLI `--models` 范围未持久化且课程基线已恢复。结合轮换中途 `defaultModel=luna` 的静态证据，可确认当前 Model 会写入全局默认值；2.4 完成。
- [x] 2.5 实验 Project Trust 的接受、拒绝及非交互模式行为。
  - 交互式 session-only A/B（已验证）：用户在隔离临时项目中只放置带唯一标记的 `.pi/APPEND_SYSTEM.md`；选择 `Trust (this session only)` 后启动界面列出该 Context，Model 输出 `TRUST_RESOURCE_2501`；重新启动并选择 `Do not trust (this session only)` 后出现项目不受信任警告，Model 输出 `TRUST_NOT_LOADED`。两次运行均关闭 Session、自动上下文、Extension、Skill、Prompt Template、Theme 和 Tool，形成单变量对照，证明本次 Trust 决定控制项目追加系统提示是否加载。
  - Print 默认行为（已验证）：同一临时项目无已保存决定且全局 `defaultProjectTrust` 回退为 `ask` 时，用户运行不带 Trust 标志的 `-p` 请求；Pi 没有弹出交互选择器，直接输出 `TRUST_NOT_LOADED`，证明非交互模式无法询问时会忽略项目受保护资源。
  - Print `--approve` 单次生效（已验证）：只增加 `--approve` 后，同一请求输出 `TRUST_RESOURCE_2501`；随后再次运行无 Trust 标志的相同命令，输出恢复为 `TRUST_NOT_LOADED`。这证明 `--approve` 只允许本次非交互运行加载项目追加系统提示，没有保存 Trust 决定。
  - 已保存决定与 `--no-approve`（已验证）：用户为临时项目保存 `Trust` 决定，只读核对 `trust.json` 中规范化项目路径的值为 `true`。随后无 Trust 标志的 Print 输出 `TRUST_RESOURCE_2501`，增加 `--no-approve` 后输出 `TRUST_NOT_LOADED`，再次移除该标志后又输出 `TRUST_RESOURCE_2501`。这证明非交互模式会复用已保存决定，CLI `--no-approve` 优先于它且只作用于本次运行。
  - 理解检查（已通过）：用户已理解 `.pi/APPEND_SYSTEM.md` 是受 Project Trust 控制的项目资源示例，Trust 的完整正向职责还覆盖项目 `.pi` 下的 Settings、System Prompt、Skill、Extension、Prompt Template、Theme、Package，以及当前目录或祖先目录中的项目 `.agents/skills`；非交互模式按 CLI 标志、已保存决定和全局 `defaultProjectTrust` 确定是否加载，其中 `ask` 因无法弹窗而按不信任处理。Trust 仍不是系统权限或沙箱。
  - 综合场景首次判断（历史未通过）：用户正确判断显式 `--no-approve` 使本次不加载项目资源；当时对保存决定是否被改写不能确定，并误判去掉 CLI 标志后会采用全局 `defaultProjectTrust=never`。随后修正为：CLI 标志只覆盖本次、不写 Trust Store；下一次无 CLI 覆盖时，父目录已保存的 `Trust=true` 先于全局默认值命中，因此会加载项目资源。
  - 综合场景重试（已通过）：用户准确说明 `--no-approve` 只作用于当前运行，不修改 `trust.json`；下次移除 CLI 覆盖后，Pi 先采用父目录保存的 `Trust=true`，不会继续使用全局 `defaultProjectTrust=never`。
  - 文档沉淀（已完成）：`docs/learning/03-project-configuration.md` 已集中记录保存位置、目录继承、决策优先级、交互/非交互差异、CLI 持久化边界、完整 Mermaid 决策流程图、实验矩阵、源码入口和排查顺序。
  - 临时状态清理（已验证）：精确查询临时项目路径在 `~/.pi/agent/trust.json` 中已不存在，输出 `NO_SAVED_DECISION`；临时实验目录经路径白名单检查后移入 macOS 废纸篓，原路径不存在并输出 `LAB_DIR_REMOVED`。该目录仍可从废纸篓恢复，不扩大为永久删除证明。
  - 综合验收：正反 Trust 路径、交互/非交互差异、CLI 单次覆盖、父目录保存决定优先级、持久化边界、清理恢复和快速复习文档均有直接证据；2.5 完成。
- [x] 2.6 配置并验证常用 Keybindings 和外部编辑器。
  - 开工基线（历史）：已确认本机 Pi `0.83.0`；当时不存在全局 `keybindings.json`，全局与项目 Settings 均未设置 `externalEditor`，`VISUAL` 和 `EDITOR` 也未设置。
  - 系统地图理解确认：用户已理解 Keybindings 决定“哪个按键触发动作”，外部编辑器配置决定“动作启动哪个编辑器”，两条配置链路相互独立。
  - 默认基线（已验证）：隔离 Session `2.6-editor-baseline` 中，`⌃G` 实际打开 `UW PICO 5.09`；用户保存退出后两行草稿均返回 Pi 输入框，过程中没有产生用户消息或 Model 回复。
  - Keybinding 覆盖规则理解确认：用户已准确说明，为 `app.editor.external` 只配置 `f8` 时，默认 `⌃G` 不会保留；用户配置替换该 Action 的整组默认按键，不会自动追加。
  - 自定义按键实验 A（已验证）：独立 `PI_CODING_AGENT_DIR` 中将 `app.editor.external` 配置为 `ctrl+g` 与 `f8`；`/hotkeys` 显示 `^g / f8`，用户实测两个按键均能打开 `UW PICO 5.09`。
  - 自定义按键实验 B（已验证）：同一隔离配置将 `app.editor.external` 改为只包含 `f8`；`/hotkeys` 只显示 `f8`，用户实测 `⌃G` 已无效而 `F8` 仍能打开 PICO，直接验证用户配置替换默认按键列表。
  - `/reload` 第一次实验（历史部分通过）：运行中把配置从仅 `f8` 改回 `ctrl+g` 与 `f8` 后，`/reload` 明确报告 `Reloaded keybindings, extensions, skills, prompts, themes, and context files`，随后 `⌃G` 恢复；用户当时没有观察 Reload 前状态，因此该步不能证明磁盘文件变化不会自动影响当前进程。
  - `/reload` 反向对照第一步（历史阶段已验证）：Pi 运行时通过 `!!` 将磁盘配置从双按键改为只含 `f8`，未执行 `/reload` 时当前进程的按键行为没有变化，证明修改文件本身不会自动替换已加载到内存的 Keybindings。
  - `/reload` 完整对照（已验证）：上述磁盘修改后执行 `/reload`，Pi 报告重新加载 Keybindings；用户实测 `⌃G` 随即失效、`F8` 仍能打开 PICO。前后证据闭合，证明运行中修改配置需要 Reload 才更新当前按键映射。
  - 教学纠偏：进入外部编辑器优先级时，曾在解释 `Nano`、`Vim`、`VISUAL`、`EDITOR` 前直接提出判断题；学习者指出尚未学过这些对象，因此此前答案不计为新概念的理解验收。现已把“先解释全部陌生对象和完整场景，确认后才能提问或实验”写入计划与教学 Skill。
  - 外部编辑器选择系统地图理解确认：用户已确认理解 `externalEditor -> VISUAL -> EDITOR -> nano` 的选择顺序、环境变量随 Pi 启动进程传入的含义，以及本机 `/usr/bin/nano -> pico` 对应 `UW PICO 5.09` 的原因。
  - 外部编辑器优先级实验 A（已验证）：隔离配置设置 `externalEditor=nano`，启动 Pi 时同时传入 `VISUAL=vi`、`EDITOR=vi`；用户按 `⌃G` 后截图明确显示 `UW PICO 5.09` 和原草稿 `EDITOR_PRIORITY_A`，随后把草稿改为 `EDITOR_PRIORITY_A_PICO`、保存退出并确认修改后的文本返回 Pi。该实验直接证明 Settings 中的 `externalEditor` 优先于环境变量编辑器，并再次验证外部编辑器草稿回传链路。
  - 外部编辑器优先级实验 B（已验证）：隔离环境未设置 `externalEditor`，启动 Pi 时传入 `VISUAL=vi`、`EDITOR=nano`；用户按 `⌃G` 后确认打开 Vim，并使用 `Esc`、`:q!` 退出后确认原草稿 `EDITOR_PRIORITY_B` 仍在 Pi 输入框。该实验直接证明没有 Settings 覆盖时 `VISUAL` 优先于 `EDITOR`，且不保存退出外部编辑器不会丢失原草稿。
  - 外部编辑器优先级实验 C（已验证）：隔离环境未设置 `externalEditor`，使用 `env -u VISUAL` 明确移除 `VISUAL` 并传入 `EDITOR=vi`；用户按 `⌃G` 后确认打开 Vim，退出后原草稿仍在 Pi 输入框。该实验直接证明更高优先级配置不存在时 Pi 会采用 `EDITOR`。
  - 外部编辑器优先级实验 D（已验证）：隔离环境未设置 `externalEditor`，启动时同时使用 `env -u VISUAL -u EDITOR` 移除两个环境变量；用户按 `⌃G` 后确认打开 `UW PICO 5.09`，退出后原草稿仍在 Pi 输入框。结合本机 `/usr/bin/nano -> pico`，该实验直接证明前三项均不存在时 Pi 回退到默认 `nano`。
  - 隔离状态清理（已验证）：四个优先级实验目录均无进程占用，已整体移入 `~/.Trash/pi-study-2.6-editor-priority-labs.qCAJZ9/`；四个原 `/tmp` 路径均不存在，废纸篓中四个目录均存在并可恢复。Pi 自动生成的 `auth.json` 等运行文件未被读取或展示。
  - 综合场景验收（已通过）：用户准确说明，磁盘按键配置改为仅 `F8` 但未 `/reload` 时，当前进程中的 `Ctrl+G` 与 `F8` 仍都有效并按 `VISUAL=vi` 打开 Vim；执行 `/reload` 后仅 `F8` 有效，编辑器仍由 `VISUAL` 选为 Vim；增加 `externalEditor=nano` 时改为选择 `nano`，三项均不存在时回退默认 `nano`。用户已能把按键内存刷新与编辑器选择两条链路组合判断。
  - 文档沉淀（已完成）：`docs/learning/03-project-configuration.md` 已记录两条独立链路、按键整组替换、`/reload` 边界、编辑器四级优先级、本机 `nano -> pico` 现象和排查顺序；差异格式与 Markdown 围栏检查通过。
  - 综合验收：默认与自定义按键、运行时刷新、编辑器四级选择、草稿返回、隔离清理、用户场景判断和稳定文档均有直接证据；2.6 完成。
- [x] 2.7 完成一次配置优先级故障排查。
  - 开工场景（历史）：从“全局 `externalEditor=nano`，进入受信任项目后却打开 Vim”的真实场景建立系统地图；先区分 Settings 合并与外部编辑器选择两次优先级判断，再准备隔离故障实验。
  - 系统地图理解确认：用户已理解配置来源、同名字段覆盖、有效值和 Project Trust 的门禁位置，并能区分 Settings 合并与外部编辑器选择两次优先级判断。
  - 隔离故障复现（已验证）：临时 Agent 目录的全局 Settings 仅设置 `externalEditor=nano`，受信任临时项目的同名字段为 `vi`，启动环境同时设置 `VISUAL=nano`、`EDITOR=nano`；用户按 `Ctrl+G` 后实际打开 Vim，使用 `:q!` 退出后原草稿仍在 Pi 输入框。
  - 原因定位（已通过）：用户准确判断，删除项目同名覆盖后，全局 `externalEditor=nano` 会成为有效值；本机执行 `nano` 时界面显示 PICO。故障原因不是 `VISUAL` 或 `EDITOR` 失效，而是编辑器选择在更高优先级的有效 `externalEditor=vi` 处已经结束。
  - 最小修复与反向验证（已通过）：保留项目故障配置副本后，仅从临时项目 Settings 中移除 `externalEditor`，全局值保持 `nano`；用户重新启动相同隔离 Pi，按 `Ctrl+G` 实测从 Vim 变为 PICO，退出后草稿仍在。静态来源与运行时行为形成单变量前后对照。
  - 组合判断（已通过）：纠偏后，用户按两轮准确复述：`--no-approve` 排除项目 Settings，第一轮得到全局有效值 `externalEditor=nano`；第二轮在有效 `externalEditor` 处选中 `nano`，因此不再检查两个值为 `vi` 的环境变量。
  - 文档沉淀（已完成）：`docs/learning/03-project-configuration.md` 已记录现象、两轮定位、修复前后单变量对照、`nano -> PICO` 边界和固定排查顺序。
  - 临时环境清理（已验证）：用户退出实验 Pi 并把 Shell 切回仓库后，进程占用检查为空；完整实验目录已移入 `~/.Trash/pi-study-2.7-config-priority-lab.MIvWOT/`，原 `/tmp` 路径不存在，废纸篓恢复位置及项目配置均存在。Pi 自动生成的 `auth.json` 等运行文件只检查了文件名和存在性，未读取或展示内容。
  - 文档格式验证（已通过）：Git 空白错误检查通过，学习文档 Markdown 围栏数量为偶数。
  - 综合验收：用户完成故障现象观察、两轮来源定位、最小修复、同条件反向验证和组合复述；稳定文档与可恢复清理均有直接证据，2.7 完成。

验收产物：经过验证的项目配置、配置说明和一份“全局值为何被项目值覆盖”的排查记录。

### 阶段 3：Session、Tree 与 Compaction

目标：掌握长任务的持久化、分支、恢复、上下文控制和成本意识。

- [x] 3.1 理解 Session 的自动保存、命名、恢复、删除、导出和分享边界。
  - 已完成：从“退出 Pi 后继续上次工作”的真实场景建立 Session 生命周期系统地图，并完成隔离实验、综合判断、稳定文档和可恢复清理。
  - 版本基线：开课前只读核验发现本机 Pi 与全局 npm 包均已升级为 `0.84.0`；3.1 以本机 `0.84.0` 帮助、内置文档和运行行为为准，既有 `0.83.0` 实验仍保留其历史版本标注。
  - 系统地图理解确认：用户已理解自动保存、显示名称、Session ID、恢复、本地导出、外部分享和删除回退；尤其确认 `/share` 会无二次确认地通过 `gh` 上传 secret gist，secret 只是不公开列出而非权限受控的私有存储。
  - 分享验收边界：3.1 默认不执行 `/share`；如需运行，必须改用已脱敏的专用 Session，并在外部上传前再次取得用户明确确认。
  - 空 Session 实验（已验证）：隔离启动时设置显示名称但不发送普通消息；用户通过 `/session` 观察到名称、JSONL 目标路径和 `Messages Total: 0`，退出后结构检查确认隔离目录中 JSONL 文件数为 `0`。这证明预分配路径不等于实际落盘。
  - 自动保存实验（已验证）：隔离 Session 获得首条 Assistant 回复 `SESSION_AUTO_SAVE_OK` 后，用户通过 `/session` 确认名称正确、`User 1 + Assistant 1 = Total 2` 且无 Tool；Pi 仍运行时，磁盘检查已发现唯一 JSONL 文件，大小 `1514` 字节、共 `6` 行。自动保存不依赖 `/quit`。
  - 显示名称实验（已验证）：用户执行 `/name 3.1-renamed-session` 后确认 Name 改变，而 Session ID 与 File 路径不变；磁盘仍只有同一个 JSONL，行数从 `6` 增至 `7`、大小变为 `1644` 字节。显示名称是原 Session 中追加的元数据，不是文件名或身份。
  - 恢复实验（已验证）：退出后在相同工作目录和相同 `--session-dir` 下执行 `pi -c`，用户确认历史对话、改名后的 Name、Session ID、File 路径和 `Messages Total: 2` 均保持一致；恢复期间磁盘仍只有原来的一个 `7` 行 JSONL，没有创建副本。`-c` 是继续当前项目最近的原 Session。
  - 本地 HTML 导出（已验证）：用户执行 `/export .../3.1-session.html` 后看到成功状态，`Messages Total` 仍为 `2`；磁盘检查确认导出物是 `272435` 字节的 UTF-8 HTML，原 JSONL 仍为唯一文件且保持 `7` 行、`1644` 字节。导出未修改原 Session，也未执行外部上传。
  - JSONL 导出与活动 Session 删除拒绝（已验证）：本地 JSONL 备份导出成功，备份与原文件大小均为 `1644` 字节；用户在 `/resume` 中对当前 `3.1-renamed-session` 按 `Ctrl+D`，Pi 明确提示不能删除当前 Session，未进入确认。原文件仍存在且数量为 `1`。
  - 非活动 Session 删除（已验证，恢复位置受限）：用户执行 `/new` 后确认新 Session 消息数为 `0`，再从 `/resume` 删除旧 Session；确认界面出现，Pi 报告 `Session moved to trash`。隔离 Session 目录的 JSONL 数已变为 `0`，HTML 与 `1644` 字节 JSONL 备份仍存在。macOS 拒绝 Codex 读取 `~/.Trash`，因此未把废纸篓目标文件存在性记为直接验证；恢复兜底由本地 JSONL 备份保证。
  - 综合判断（已通过）：纠偏后，用户完整复述 `/share` 的“本地 Session -> 临时 HTML -> `gh` 上传 secret gist -> 生成链接”链路，并准确说明 `--no-session` 运行期间仍在当前进程内保存消息，但不持久化 JSONL，退出后无法通过 `pi -c` 恢复；其余自动保存、改名、恢复、导出和删除判断也均正确。
  - 文档沉淀（已完成）：`docs/learning/04-session-tree-compaction.md` 已记录 Session 的身份与名称、自动保存、`--no-session`、恢复、导出、分享、删除边界和 3.1 实验直接证据。
  - 文档格式验证（已通过）：Git 空白错误检查通过，学习文档 Markdown 围栏数量为偶数。
  - 临时环境清理（已验证）：用户执行 `/quit` 并把 Shell 切回仓库后，原实验目录进程占用检查为空；完整目录已移入 `~/.Trash/pi-study-3.1-session-lifecycle-lab.wTpuHS/`，原 `/tmp/pi-3.1-session-lifecycle.eqWjHh` 不存在，废纸篓恢复位置、`272435` 字节 HTML 和 `1644` 字节 JSONL 备份均存在。只检查了路径、文件名和大小，未读取备份内容。
  - 综合验收：Session 生命周期系统地图、隔离行为实验、用户综合判断、稳定文档、格式检查和可恢复清理均有直接证据；3.1 完成后进入 3.2。
- [x] 3.2 分别实验 Tree、Fork、Clone，并说明它们对会话文件和分支的影响。
  - 开工背景（历史）：从 3.1 已保存 Session 的真实场景切入，先讲清 Session Entry 树、当前分支、Tree、Fork 和 Clone，再准备三者的隔离对照实验。
  - 系统地图初次讲解（历史未通过）：已按本机 Pi `0.84.0` 文档与实现区分节点、父子关系、叶子、活动分支和追加式 Session Entry 树；已讲清 `/tree` 留在原 Session、`/fork` 从所选用户消息之前创建新 Session、`/clone` 将当前活动分支复制为新 Session，以及三者均不复制或回退项目文件和 Git 分支，但当时未通过理解确认。
  - 理解反馈（历史未通过）：用户明确表示节点 Entry、Session Entry 树和 `/tree` 还不清楚；上一轮系统地图不计为理解通过，Fork、Clone 与本地实验当时暂停。
  - 重新讲解理解确认（已通过）：用户已理解 Entry 是带 `id`/`parentId` 的 Session 记录、消息树由记录的父子关系形成，且 `/tree` 在原 Session 中选择旧位置并追加新路线，不会删除旧分支或恢复项目文件。
  - 隔离实验环境（历史证据）：已创建 `/tmp/pi-3.2-tree-fork-clone.isicCT/`，其中 `project/` 作为独立工作目录，`sessions/` 作为专用 Session 目录；实验关闭项目资源、自动上下文、Extension、Skill、Prompt Template、Theme 和 Tool，不修改仓库、不执行 Git 操作。
  - 线性基线（已验证）：用户启动命名 Session `3.2-base`，依次取得 `BASE_A`、`BASE_B` 两轮问答；截图显示 `Messages Total 4`、`User 2`、`Assistant 2`、`Tools 0`，文件位于专用目录；目录检查确认当前只有 1 个 JSONL，大小 `2418` 字节。
  - `/tree` 选点（已验证）：用户在原 Session 中选择 `BASE_B` 对应的用户消息，并确认 Session 文件未切换、树中原 `BASE_B` 回复仍存在、编辑器恢复原提示；该时点未发送新消息，因此只证明选点与待续写状态，不能证明新分支已经形成。
  - `/tree` 新路线续写（已验证）：用户清空恢复的原提示后发送 `只回复：TREE_BRANCH`，Model 返回 `TREE_BRANCH`；`/session` 截图显示 Name、ID、File 均未变化，消息从 `User 2 + Assistant 2` 增至 `User 3 + Assistant 3`，Tool 仍为 `0`。磁盘仍只有原 JSONL，大小由 `2418` 增至 `3356` 字节，证明新路线追加在原 Session，而非创建新 Session。
  - `/tree` 分支保留（已验证）：再次打开 Session Tree 后，界面直接显示 `BASE_A` 回复下面并列保留两条子路线：`TREE_BRANCH -> TREE_BRANCH` 与 `BASE_B -> BASE_B`，总计 6 个消息节点。结合原 Session 文件未切换，证明 `/tree` 在同一 Session 中改变续写位置并追加新路线，不会删除旧路线。
  - Tree 隔离实验（历史阶段已完成）：线性基线、选点待续写、原 Session 内新增路线和新旧路线共存均取得直接证据；当时 Fork、Clone 实验和 3.2 综合验收还没有进行。
  - Fork 实现基线（已核对）：本机 Pi `0.84.0` 文档说明 `/fork` 通过用户消息选择器创建新 Session 文件；交互实现调用 runtime `fork(entryId)`，默认 `position="before"`，新 Session 复制到所选用户消息的父节点，把所选提示放回编辑器，并显示 `Forked to new session`。新文件 header 记录原文件为 `parentSession`，不会生成分支摘要，也不会恢复项目文件。
  - Fork 实验设计（历史）：从当时的 `TREE_BRANCH` 路线选择该用户消息；预期新 Session 只包含其之前的 `BASE_A` 问答，编辑器恢复 `只回复：TREE_BRANCH`。验证顺序为先检查新 Session 身份和编辑器状态，再决定是否发送新提示。
  - Fork 创建与截断位置（已验证）：用户执行 `/fork` 并选择 `TREE_BRANCH` 用户消息后，界面显示 `Forked to new session`；历史区只保留 `BASE_A` 问答，所选 `只回复：TREE_BRANCH` 仅回到编辑器，未作为消息发送。磁盘从 1 个 JSONL 增至 2 个：原文件仍为 `3356` 字节，新文件为 `1620` 字节，证明 Fork 创建独立 Session 且未改写原文件。
  - Fork 身份与历史边界（已验证）：新 Session 的 Name 继承为 `3.2-base`，但 ID 变为 `<fork-session-id>`，File 变为第二个 JSONL；`/session` 显示 `Total 2`、`User 1`、`Assistant 1`、Tool `0`，与只复制 `BASE_A` 问答一致。名称继承不代表仍是原 Session。
  - Fork 独立续写（已验证）：用户在 Fork Session 中发送 `只回复：FORK_BRANCH`，Model 返回 `FORK_BRANCH`；`/session` 显示仍为 Fork 的 ID、File，消息增至 `Total 4`、`User 2`、`Assistant 2`。磁盘对照显示原 Tree Session 保持 `3356` 字节，只有 Fork Session 从 `1620` 增至 `2543` 字节。
  - Fork 隔离实验（已完成）：新文件创建、`position="before"` 截断位置、所选提示返回编辑器、新身份与名称继承、独立续写且不改原文件均取得直接证据。
  - Clone 实现基线（已核对）：本机 Pi `0.84.0` 的 `/clone` 读取当前活动叶节点并调用 runtime `fork(leafId, { position: "at" })`，将完整当前活动路径复制到新 Session，清空编辑器并显示 `Cloned to new session`；与 `/fork` 不同，它不打开历史用户消息选择器，也不把某条旧提示放回编辑器。
  - Clone 实验设计（历史）：以当时含 `BASE_A`、`FORK_BRANCH` 两轮问答的 Fork Session 为源执行 `/clone`；预期出现第三个 JSONL，新 Session 初始消息仍为 `Total 4`，历史完整保留且编辑器为空。
  - Clone 创建与完整复制（已验证）：用户执行 `/clone` 后，界面显示 `Cloned to new session`，历史区完整保留 `BASE_A`、`FORK_BRANCH` 两轮问答，编辑器为空。磁盘从 2 个 JSONL 增至 3 个；第三个 Clone 文件大小为 `2543` 字节，与源 Fork 文件相同，原 Tree 文件仍为 `3356` 字节，两个源文件均未变化。
  - Clone 身份与历史边界（已验证）：`/session` 显示 Name 继承为 `3.2-base`，但 ID 变为 `<clone-session-id>`，File 指向第三个 JSONL；消息仍为 `Total 4`、`User 2`、`Assistant 2`、Tool `0`，证明新 Session 完整复制源活动路径，名称继承不代表身份相同。
  - Clone 独立续写（已验证）：用户在 Clone Session 中发送 `只回复：CLONE_BRANCH`，Model 返回 `CLONE_BRANCH`；`/session` 显示仍为第三个 ID、File，消息增至 `Total 6`、`User 3`、`Assistant 3`。磁盘对照显示前两个文件保持 `3356/2543` 字节，只有 Clone 文件从 `2543` 增至 `3494` 字节。
  - Clone 隔离实验（已完成）：完整活动路径复制、新身份与名称继承、空编辑器、独立续写且不改两个源文件均取得直接证据。
  - 三项实验状态（历史过程）：Tree、Fork、Clone 的运行与磁盘对照均已完成；当时 3.2 还未验收，剩余稳定文档、用户综合判断和临时实验目录清理。
  - 稳定文档（已完成）：`docs/learning/04-session-tree-compaction.md` 已记录消息树与活动路径、Tree/Fork/Clone 对照、贯穿场景、Pi `0.84.0` 实现边界及三文件实验结果。
  - 文档格式验证（已通过）：Git 空白错误检查通过，主题文档 Markdown 围栏共 `12` 个且成对闭合。
  - 综合验收首次结果（历史未通过）：用户第 3 题正确选择 `/clone`，但未说明其文件、历史和编辑器变化；第 1 题将同一 Session 内保留路线的 `/tree` 误答为 `/fork`，第 2、4 题未回答。实验结果不等于已经掌握，因此随后进行了针对性重试。
  - 综合验收第二次结果（历史部分通过）：用户已准确说明 `/tree` 的同文件分支和旧路线保留、`/fork` 的选点前复制与提示回填，以及 `/clone` 的完整活动路径与空编辑器；也能说明 Fork/Clone 的 Name 继承、ID/File 改变和源文件不变。第 1 至 3 项通过。
  - 理解反馈（历史未通过）：用户明确表示不理解“项目文件与 Git”边界，因此第 4 项不计为通过；当时暂停清理实验目录，也未完成 3.2。
  - 进一步理解反馈（历史未通过）：用户具体不理解“`/fork` 仍使用同一个项目目录”和“`/clone` 不复制项目目录”。随后先区分实验的 `sessions/` 与 `project/`：前者产生多个 JSONL，后者始终只有一个共享工作目录；在此理解通过前未进入 Git 恢复结论。
  - 二次讲解反馈（历史未通过）：上一轮继续分别解释 Fork 和 Clone 的目录行为，仍让两句话看起来像不同规则。准确关系应先表述为同一个共同边界：`/fork` 与 `/clone` 都只创建新 Session JSONL，二者都保持 `cwd` 不变、都不复制项目目录；差异只在新 Session 复制哪些消息以及编辑器是否回填提示。
  - 文档纠偏（已完成）：用户指出原讲解把“Fork 仍使用同一项目目录”和“Clone 不复制项目目录”写成不对称表述，容易误导为两者目录行为不同。`docs/learning/04-session-tree-compaction.md` 已把共同边界前置，新增 `cwd`/Session File 状态表和“是否复制项目目录”对照行，并将 Clone 的“复制活动路径”限定为复制 Session 对话消息。
  - 纠偏格式验证（已通过）：Git 空白错误检查通过，主题文档 Markdown 围栏仍为 `12` 个且成对闭合。
  - 目录共同边界理解确认（已通过）：用户确认理解 `/fork` 与 `/clone` 都保持 `cwd`、都不复制项目目录，二者差异只在新 Session 的消息复制边界和编辑器状态。
  - Git 边界讲解反馈（历史未通过）：已说明 Session JSONL 不承担项目文件版本保存与恢复，Git Commit 才提供可靠文件版本；用户没有直接补答，而是要求通过一个完整例子重新梳理 Tree/Fork/Clone 的区别，因此当时暂停验收第 4 项，随后改用 Java 完整场景。
  - Java 完整场景理解确认（已通过）：用户确认以 `DiscountService.java` 从 Git 版本 V1 被 Pi 修改为工作区 V2 的完整时间线后，Tree/Fork/Clone 的 Session 差异与共享项目目录边界已经清楚。
  - Java 场景文档（已完成）：`docs/learning/04-session-tree-compaction.md` 已用同一起点下的三个独立选择，记录 Tree 的同 Session 分支、Fork 的选点前新 Session、Clone 的当前终点完整消息复制，以及三者都继续使用项目目录 P、不会创建 P2/P3。
  - Java 场景格式验证（已通过）：Git 空白错误检查通过，主题文档 Markdown 围栏仍为 `12` 个且成对闭合。
  - 最终综合复述第一次结果（历史部分通过）：第 1 项正确说明 `/tree` 后项目文件仍为 V2；第 4 项正确说明 Clone Session 改成 V3 后原 Session 也看到 V3，恢复 V1 依赖 Git。第 2 项的 Fork 消息边界和编辑器正确，第 3 项正确选择 `/clone`，但两项都误称“项目目录会复制”；第 3 项还把 Clone 表述为复制旧 Session 全部内容，未限定为当前活动路径的对话消息。
  - 最终综合复述第三次结果（历史部分通过）：用户已纠正 Fork/Clone 都不复制项目目录、都继续使用同一项目目录 P 且文件仍为 V2。结合上一轮已正确说明的 Fork 选点前复制和提示回填，Fork 项完整通过。Clone 的项目目录边界通过，但本轮未补充其消息范围和编辑器状态。
  - Clone 最后复述（历史部分通过）：用户正确说明创建后编辑器为空，但“复制完整的对话消息”未限定为当前活动路径，当时仍可能误解为复制整个 Session 的全部分支。
  - Clone 消息范围理解确认（已通过）：用户已理解“当前活动路径中的全部对话消息”是从根节点到当前叶节点的一条完整路线，不是整个 Session 的全部聊天记录；若原 Session 有多条分支，Clone 不复制当前路线以外的分支。
  - 综合验收（已通过）：用户已能准确区分 Tree 的同 Session 分支、Fork 的选点前新 Session与提示回填、Clone 的当前活动路径复制与空编辑器，并理解三者共享同一项目目录、项目文件状态不随对话切换、可靠版本恢复依赖 Git。
  - 文档精简（已完成）：`docs/learning/04-session-tree-compaction.md` 已明确 Clone 复制当前活动路径而非全部聊天记录，删除重复的 `cwd`/Session File 状态表，将相关章节收敛为层次边界、对照表和 Java 贯穿例子。
  - 精简验证（已通过）：Git 空白错误检查通过，主题文档 Markdown 围栏仍为 `12` 个且成对闭合；关键术语检索确认 Clone 消息范围、共享项目目录和 Git 边界表述一致。
  - 临时环境清理（已验证）：用户执行 `/quit` 并切回学习仓库后，进程占用检查为空；完整实验目录已移入 `~/.Trash/pi-study-3.2-tree-fork-clone.isicCT/`，原 `/tmp/pi-3.2-tree-fork-clone.isicCT` 不存在，废纸篓中的 `project/`、`sessions/` 及三个 `2543/3494/3356` 字节 Session 文件均存在。只检查路径、数量和大小，未读取 Session 内容。
  - 综合验收：Tree/Fork/Clone 的消息边界、Session 文件变化、编辑器状态、共享项目目录与 Git 边界均通过理解确认；隔离运行、磁盘对照、稳定文档、格式检查和可恢复清理均有直接证据，3.2 完成。
- [x] 3.3 阅读一个实际 JSONL Session，识别 header、message、tool、model 和自定义条目。
  - 开工基线（历史）：已按本机 Pi `0.84.0` 随包 `docs/session-format.md` 和 `dist/core/session-manager.d.ts` 核对 Session v3 的 header、Entry 基类、`message`/`model_change`/`custom`/`custom_message` 等条目，以及 `message.role=toolResult` 和 Assistant `toolCall` 内容块的层级；当时还没有读取实际 Session 或执行隔离实验。
  - 系统地图理解确认（已通过）：用户确认 JSONL、Entry、Header、顶层 `type`、消息 `role`、Tool Call/Tool Result 与自定义条目的关系均能看懂。3.3 采用开卷识别，不要求背诵或手写字段；通过标准是能按 `type -> role -> 关联字段` 判断条目职责和上下文边界。
  - 实验实现基线（已核对）：Pi `0.84.0` 随包 `entry-renderer.ts` 可通过 `/status-card` 产生不进模型上下文的 `custom`；`message-renderer.ts` 可通过 `/status` 产生进入上下文的 `custom_message`。可直接显式加载官方示例，不需要编写新 Extension。
  - 隔离实验环境（历史证据）：已创建 `/tmp/pi-3.3-jsonl-lab.xqnkNX/`，根目录权限为 `0700`；`project/fixture.txt` 只有固定文本 `SESSION_JSONL_LAB_OK`，`sessions/` 当时没有 JSONL。实验只开放 `read` Tool，关闭项目上下文与自动资源发现，并显式加载两个已审查的官方示例 Extension；不修改学习仓库。
  - 隔离运行（历史阶段，界面验证已通过）：用户截图显示 `fixture.txt` 的最终回复为 `SESSION_JSONL_LAB_OK`，`/session` 显示 `Tools: 1 calls, 1 results`；`/status-card CUSTOM_STATE_ONLY` 与 `/status CUSTOM_CONTEXT_MESSAGE` 均渲染成功，后续模型只回复 `CUSTOM_CONTEXT_MESSAGE`，直接证明前者不进上下文、后者进入上下文。Footer 和 `/session` 均显示已切换到 `gpt-5.6-terra`，Name 为 `3.3-jsonl-lab`，消息为 `User 2 / Assistant 3 / Total 6`。该时点 Pi 仍在运行，退出后的 JSONL 结构随后由 Session 结构投影完成核对。
  - Extension 边界理解确认（已通过）：用户已理解 `entry-renderer.ts` 通过 `pi.appendEntry()` 产生不进入模型上下文的 `custom`，`message-renderer.ts` 通过 `pi.sendMessage()` 产生进入上下文的 `custom_message`；两者均为随包官方示例，由显式 `--extension` 加载，未复制进学习仓库。
  - Session 结构投影（已验证）：用户退出并切回仓库后，隔离 Session 文件存在且无进程占用；未读取原始内容，使用 `jq` 仅投影 `type`、`role`、Tool/Model 名称、自定义类型、上下文显示标记和父子关系。13 行结构依次包含 `session`、`session_info`、初始 `model_change`/`thinking_level_change`、`user -> assistant(toolCall=read) -> toolResult(read) -> assistant`、`custom(status-card)`、`custom_message(status-update)`、`user -> assistant` 和末尾 `model_change(gpt-5.6-terra)`。
  - 首次综合识别（历史未通过）：用户准确识别第 6 行 Assistant 内的 Tool Call、第 7 行 `role=toolResult`、第 3/13 行 `model_change`，并能说明 `custom` 不进模型上下文、`custom_message` 进入上下文，以及 13 个文件条目不等于 `/session` 的 6 条消息。Header 的含义当时不清楚，因此该轮未通过。
  - Header 理解确认（已通过）：用户确认 Header 不是聊天消息，主要记录 Session 身份、格式版本和创建时的工作目录；已讲清 Header 不属于消息树、不计入消息 Total，显示名称由后续 `session_info` 条目保存。
  - 综合识别（已通过）：用户已能在实际脱敏 Session 结构中识别 header、message、Assistant 内的 Tool Call、`role=toolResult`、`model_change`、`custom` 和 `custom_message`，并解释消息统计与文件行数不同的原因；采用开卷识别，不要求背诵字段。
  - 稳定文档（已完成）：`docs/learning/04-session-tree-compaction.md` 已新增 Session JSONL 层级对照和 13 行脱敏实验结构，明确 Header、`session_info`、消息 `role`、Tool Call/Result、Model 设置、`custom`/`custom_message` 及 Messages Total 边界；未收录原始 Session 内容。
  - 文档验证（已通过）：Git 空白错误检查通过，主题文档 Markdown 围栏仍为 `12` 个且成对闭合；关键术语检索确认没有混淆 Session JSONL 与 `pi --mode json` 事件流。
  - 临时环境清理（已验证）：退出后的进程占用检查为空；完整实验目录已移入 `~/.Trash/pi-study-3.3-jsonl-lab.xqnkNX/`，原 `/tmp/pi-3.3-jsonl-lab.xqnkNX` 不存在，废纸篓中的固定输入和 Session 文件仍为 `21/4199` 字节。只检查路径、数量和大小，未再次读取 Session。
  - 综合验收：系统地图、受控 Extension、实际 Session 脱敏投影、用户开卷识别、稳定文档、格式检查和可恢复清理均有直接证据；3.3 完成。
- [x] 3.4 理解 Token、Context Window、Reserve Tokens 和 Keep Recent Tokens。
  - 已完成：Token、Context Window、Reserve Tokens 与 Keep Recent Tokens 的系统地图、Footer A/B 实验和理解复述均已通过。
  - 理解复述（已通过）：用户能说明两次问答会使累计输入/输出增加，而当前上下文仍显示 `1.8%/272k` 是因为占用很小且 Footer 按一位小数显示；能用 `260000 > 272000 - 16384` 判断应准备自动压缩，并说明 Keep Recent Tokens 用于保留近期内容。已校正“上传大小”为“累计输入 Token”，并明确 Keep Recent Tokens 是最近 Token 预算而非消息条数。
  - Footer A/B（已验证）：隔离 `--no-session --no-tools` 运行中，`TOKEN_A` 后为 `↑924 ↓6 R3.8k CH80.6% $0.003 1.8%/272k (auto)`；`TOKEN_B` 后为 `↑1.9k ↓12 R7.7k CH80.3% $0.005 1.8%/272k (auto)`。累计统计继续增加，当前上下文占比因较小和一位小数显示保持不变；`(auto)` 仅表示自动压缩开启。
  - 文档沉淀（已完成）：`docs/learning/04-session-tree-compaction.md` 已新增 3.4 对象表、触发公式、Footer 字段边界和脱敏 A/B 证据；未记录原始 Session 或凭据。
  - 历史衔接：3.4 完成后进入 3.5，手动触发一次 Compaction 并对比压缩前后的模型上下文。
- [x] 3.5 手动触发一次 Compaction，并对比压缩前后的模型上下文。
  - 已完成：系统地图、`firstKeptEntryId` 理解、隔离手动压缩、前后行为对照、JSONL 脱敏结构核对、稳定文档和可恢复清理均已通过。
  - 理解验收：用户能说明 `firstKeptEntryId` 之前的 Entry 通过摘要进入上下文，从该 Entry 起保留原文，而原始 JSONL 历史不会因此删除。
  - 隔离实验：Pi `0.84.1` 关闭自动压缩，设置 `reserveTokens=16384`、`keepRecentTokens=500`；交互编辑器中的 `@history.txt` 未附加文件后，改用单 `!` Shell Entry 构造较早内容和超过 500 Token 的近期内容。单 `!` 使 Footer 从 `1.8%` 升至 `6.8%`，累计模型用量不变。
  - 行为验证：带固定事实关注点的 `/compact` 显示 `Compacted from 18,438 tokens`；下一次普通请求同时从摘要区恢复较早事实、从近期原文区恢复近期事实，Footer 从压缩前 `6.8%/272k` 降至 `2.3%/272k`。累计 Token 因摘要和验证请求继续上升，不代表当前上下文变大。
  - JSONL 脱敏投影（已验证）：Session 共 13 行且只有 1 个 Compaction Entry，位于第 11 行，`tokensBefore=18438`，摘要长度为 1249 字符；`firstKeptEntryId` 存在并指向第 10 行的 `bashExecution`。第 9 行较早的 `bashExecution` 仍在原 JSONL 中，Compaction 后两条验证消息通过父子关系接在 Compaction Entry 后；检查未输出消息、命令或摘要正文。
  - 文档沉淀（已完成）：`docs/learning/04-session-tree-compaction.md` 已记录手动流程、前后指标、`firstKeptEntryId` 和持久化边界；Git 空白错误检查通过，主题文档 14 个 Markdown 围栏成对闭合，计划无围栏，相对链接目标均存在。
  - 临时环境清理（已验证）：Pi 退出后，完整实验目录已移入 `~/.Trash/pi-study-3.5-compaction-lab.Wyzkkf/`；原 `/tmp/pi-3.5-compaction-lab.Wyzkkf` 不存在，废纸篓中保留 4 个文件、共 146652 字节，可按需恢复。只检查数量和大小，未再次读取 Session 正文。
- [x] 3.6 实验分支摘要，说明它与 Compaction 的触发时机和目的差异。
  - 已完成：系统地图、Tree 分支摘要实验、公共原文与摘要行为验证、分支树核对、JSONL 脱敏结构、稳定文档、理解验收和可恢复清理均已通过。
  - 系统地图理解确认：用户已理解 Compaction 用于缩小当前路线的较早上下文；分支摘要在 `/tree` 离开当前路线时，把被离开路线的关键结论带到目标路线，二者均有损且不删除原始 JSONL。
  - 版本核对：Pi `0.84.1` 的 `/tree` 在未跳过提示时提供 `No summary`、`Summarize` 和 `Summarize with custom prompt`；选择生成摘要后，Pi 从旧叶子回溯到共同祖先、调用 Model 总结离开路线，并在目标位置追加 `BranchSummaryEntry`。
  - 隔离实验环境（历史证据）：已创建权限收紧的 `/tmp/pi-3.6-branch-summary-lab.4wOZum/`，Session 目录为空；临时项目关闭自动 Compaction，并显式设置 `branchSummary.skipPrompt=false`。随后启动专用 Session，先验证设置生效。
  - 启动与公共事实（已验证）：用户已启动专用 Session，Pi 显示 `0.84.1`，Footer 无 `(auto)`；发送公共事实后 Model 返回 `BASE_OK`，当前上下文为 `1.8%/272k`。随后建立路线 A 的独有结论。
  - 路线 A（已验证）：用户发送独有事实 `A_RESULT=JDK17` 后 Model 返回 `A_OK`；Footer 累计输入、输出继续增加，当前上下文仍显示 `1.8%/272k`。随后只打开 `/tree`，核对可选节点后再决定目标。
  - Tree 目标（已确认）：`/tree` 显示线性 4 节点 `user(BASE) -> assistant(BASE_OK) -> user(A) -> assistant(A_OK)`；用户已将高亮移至第 2 个 `assistant: BASE_OK`，界面显示 `(2/4)`。该目标会保留公共路径，并让第 3、4 节点成为待摘要的路线 A。
  - 摘要选择器（已验证）：确认 Tree 目标后，界面实际显示 `No summary`、`Summarize`、`Summarize with custom prompt`，当前高亮默认 `Summarize`。随后确认默认摘要并观察导航结果。
  - 分支摘要生成（已验证）：用户选择默认 `Summarize` 后，TUI 显示 `[branch] Branch summary` 和 `Navigated to selected point`；路线 A 消息离开当前可见路径，公共路径继续保留。Footer 累计用量由 `↑1.9k ↓12` 增至 `↑2.7k ↓168`，证明摘要调用了 Model；当前上下文仍为 `1.8%/272k`，因为待摘要分支很短。随后发送路线 B 验证消息。
  - 路线 B 上下文（已验证）：在分支摘要之后询问公共事实与路线 A 结论，Model 严格返回 `BASE=ORION A_RESULT=JDK17`；前者来自公共路径原文，后者来自 Branch Summary。Footer 当时为 `1.9%/272k`。随后打开 `/tree`，核对旧路线 A 与新路线 B 是否同时保留。
  - 分支树结构（已验证）：`/tree` 显示 7 个 Entry。公共 `user(BASE) -> assistant(BASE_OK)` 之后分成两路：当前路线为 `branch summary -> user(B) -> assistant(B 结果)`，旧路线为 `user(A) -> assistant(A_OK)`；路线 A 原始问答仍保留为兄弟分支，摘要没有覆盖或删除它。随后退出 Tree 并结束 Session，完成 JSONL 脱敏结构核对。
  - 阶段性文档记录（历史）：`docs/learning/04-session-tree-compaction.md` 已记录 E1-E7 完整消息、E2 目标点、E3-E4 摘要边界、两条最终路径、Mermaid 流程图及 Branch Summary/Compaction 对照；索引说明同步更新。Git 空白错误检查、18 个 Markdown 围栏配对和相对链接目标检查均通过。该时点 3.6 还未完成，随后退出 Session 并核对 JSONL 脱敏结构。
  - JSONL 脱敏结构（已验证）：退出后无实验进程占用，专用目录中只有 1 个 5802 字节 Session。结构投影共 11 行：Header 1、`session_info` 1、Model/Thinking 设置各 1、Message 6、`branch_summary` 1、Compaction 0；摘要 Entry 长度 638 字符并带 Usage。旧路线 A 的两条 Message 仍接在公共 Assistant 后；Branch Summary 也接在该公共节点，路线 B 的两条 Message 再接到摘要之后。Pi `0.84.1` 当前实现中摘要 Entry 的 `parentId` 与 `fromId` 都指向挂载点，不能用 `fromId` 推断旧叶子。检查未输出消息、摘要、ID、路径或凭据正文。随后进行简短理解验收。
  - 理解验收首次结果（历史部分通过）：用户正确说明旧路线 A 不会从 Session 删除，但把原文区与摘要区说反。准确边界是 E1-E2 作为公共路径原文进入路线 B，E2 之后的旧路线 E3-E4 通过 Branch Summary 进入路线 B。随后针对该边界重试一句。
  - 理解验收（重试通过）：用户准确说明 E2 及之前的公共路径以原文进入路线 B，E2 之后的旧路线通过 Branch Summary 进入路线 B；结合上一轮已正确说明旧路线 A 不会删除，理解边界完整通过。随后完成实验目录的可恢复清理。
  - 临时环境清理（已验证）：无实验进程占用后，完整目录已移入 `~/.Trash/pi-study-3.6-branch-summary-lab.4wOZum/`；原 `/tmp/pi-3.6-branch-summary-lab.4wOZum` 不存在，废纸篓恢复位置保留 2 个文件、约 12 KiB。未再次读取 Session 正文。
  - 综合验收：用户能准确说明本实验中 E2 及之前的公共路径以原文进入新路线、E2 之后的旧路线通过 Branch Summary 进入新路线，且旧路线原始 Entry 不会删除；运行、TUI、JSONL 结构、源码边界、稳定文档和清理均有直接证据，3.6 完成。
- [x] 3.7 设计一个可恢复的长任务断点模板。
  - 开工背景（历史）：从 3.6 完成后的真实恢复过程切入，讲清断点记录与 Session、Git、工作区和完整聊天记录的边界。
  - 最小模板（历史阶段已确定）：固定记录目标与验收、状态、已完成及证据、当前工作状态、未完成、下一步和约束；用户已确认继续。该时点还未执行中断恢复演示或沉淀稳定文档。
  - 恢复顺序（已讲解）：读取断点、核对现场、执行唯一下一步、验证结果、立即回写新断点；断点只负责指路，恢复时不能跳过现场核对。
  - 隔离实验环境（历史证据）：`/tmp/pi-3.7-checkpoint-lab.Jg3ngX/` 包含专用 `project/`、`sessions/`、七字段 `CHECKPOINT.md`、两阶段任务和确定性验证脚本；初始 `STATE=V1` 时完整验证稳定输出 `VERIFY_FAIL`。该时点还未启动 Session A。
  - Session A（已验证）：实际文件已从 `STATE=V1` 精确改为 `STATE=V2`，局部 `rg` 输出 `3:STATE=V2`，断点把唯一下一步写为 `bash verify.sh`，最终回复 `STAGE_A_CHECKPOINTED`，且未运行完整验证；专用目录当时只有 1 个 Session JSONL，未读取其正文。
  - 顺序问题（历史，已在阶段 B 纠正）：Session A 先写入“局部检查通过”，随后才实际执行 `rg`；最终检查虽通过，但正确边界必须是先取得验证结果、再把证据写入断点，避免失败时留下错误完成记录。
  - Session A 退出（已验证）：实验目录无 Pi 进程占用，只有普通 `zsh` 当时的工作目录仍停在 `project/`；Session 目录保持 1 个 JSONL。未读取 Session 正文。
  - Session B 恢复（已验证）：全新命名 Session 先读取断点并核对现场，随后执行唯一下一步 `bash verify.sh`，实际输出 `VERIFY_OK` 后才编辑断点；最终断点状态为已完成、未完成与下一步均为“无”，并回复 `STAGE_B_RECOVERED`。`order-state.txt` 的其他三个字段保持不变。
  - 退出与 Session 结构（已验证）：实验目录无 Pi 进程占用，只有普通 `zsh` 当时的工作目录仍停在 `project/`；脱敏结构投影确认两份独立 JSONL 分别命名为 `3.7-checkpoint-a` 与 `3.7-checkpoint-b`，Entry/Message 数为 `19/15` 与 `17/13`。未读取消息正文、ID 或摘要。
  - 理解验收（已通过）：用户能说明 Session A 应在执行前记录现场与下一任务，执行后按证据回写状态；若断点仍写“修改未完成”而工作区已有修改，Session B 应先核对现场，将其视为“修改已发生、待验证”，运行测试成功后才能更新为已完成，不能盲目重做或提前写入成功证据。
  - 稳定文档（已完成）：`docs/learning/04-session-tree-compaction.md` 已记录七字段最小模板、五步恢复顺序、断点落后于工作区的待验证边界、双 Session 实验与 Session/断点/工作区/Git 四者分工；索引已同步更新。
  - 文档验证（已通过）：Git 空白错误检查通过，主题文档 22 个 Markdown 围栏成对闭合；新增内容未记录实验临时路径、Session ID 或消息正文。
  - 临时环境清理（已验证）：用户退出 Session B 并将 Shell 切回学习仓库后，实验目录无进程占用；完整目录已移入 `~/.Trash/pi-study-3.7-checkpoint-lab.Jg3ngX/`，原 `/tmp/pi-3.7-checkpoint-lab.Jg3ngX` 不存在，废纸篓中保留 6 个文件、约 60 KiB，可恢复。未再次读取 Session 正文。
  - 综合验收：七字段模板、五步恢复顺序、断点过期边界、先验证后回写、双 Session 恢复、脱敏结构、稳定文档和可恢复清理均有直接证据；3.7 与阶段 3 完成。

验收产物：一张会话树、一份 JSONL 结构说明、一份 Compaction 前后对比和一次成功恢复演示。

里程碑 A（已达到）：阶段 0-3 已完成，达到“Pi 熟练使用者”。

### 阶段 4：Prompt、Skill 与 Theme

目标：把重复工作沉淀为可发现、可复用、边界明确的资源。

- [x] 4.1 区分项目指令、Prompt Template、Skill 和 Extension 的适用场景。
  - 版本基线：本机 Pi 仍为 `0.84.1`；随包 Prompt Template、Skill、Extension 文档和本仓库项目指令均已核对。
  - 选型验收（已通过）：用户准确选择项目指令、Prompt Template、Skill 和 Extension，并能说明安全门禁在交互模式通过 UI 询问、无 UI 模式按预设策略直接拒绝；同时理解 UI 只是 Extension 的交互能力，真正门禁由事件处理逻辑执行。
  - 稳定文档（已完成）：`docs/learning/05-prompt-skill-theme.md` 已记录四类资源选型表、Java 代码审查贯穿场景、组合流程和安全边界；学习索引已同步。
  - 文档验证（已通过）：Git 空白错误检查通过，该时点主题文档 4 个 Markdown 围栏成对闭合，索引链接目标存在。
- [x] 4.2 创建并验证一个结构化代码审查 Prompt Template。
  - 已完成：已核对 Pi `0.84.1` 随包文档和实现；用户已确认 Slash Command 提交后，Model 收到的是参数替换后的模板正文，而不是原始命令。
  - 理解确认（已通过）：撤回提前使用陌生概念的理解题后，已用 `PaymentService.java` 只读审查场景重新讲解；用户确认已清楚 Tool 门禁是执行前检查机制，Extension 是可承载门禁及其他运行时能力的 TypeScript 模块。
  - 稳定文档（已更新）：`docs/learning/05-prompt-skill-theme.md` 已补充二者的职责表、Tool Call 执行链，以及 `edit`/`write`、Model `bash` Tool、用户 `!` Shell 需分别覆盖的边界；该时点空白检查、6 个 Markdown 围栏配对和关键边界检索通过。
  - 模板正文系统地图（理解确认已通过）：用户已确认审查对象、审查重点、证据要求、输出格式和行为限制五个对象均清楚；随后进行完整场景判断。该时点还未创建模板或实验。
  - 模板正文场景判断（已通过）：用户准确识别示例已包含审查对象、审查重点和行为限制，缺少证据要求与明确输出格式；并能说明前者会产生缺少支撑的貌似合理结论，后者会导致结果结构不稳定、难以处理。
  - 模板实现与静态验证（已通过）：已创建项目级 `.pi/prompts/java-review.md`，命令名 `/java-review`，第一个参数为必填审查对象，第二个参数为带默认值的可选审查重点；正文覆盖审查范围、证据要求、输出格式和行为限制。Pi `0.84.1` 随包加载与展开实现已验证名称、描述、参数提示、显式重点、默认重点、缺少对象保护、占位符完全替换、frontmatter 不进入正文和五部分结构，所有检查均为 `PASS`；Git 空白检查通过。
  - 证据边界：静态验证未启动真实 TUI、未调用 Model，也未证明 Project Trust 下的项目模板发现和自动补全。
  - 真实 TUI 发现与参数提示（已验证）：用户以 `--approve --no-session --no-context-files --no-extensions --no-skills --no-themes --no-tools` 启动 Pi `0.84.1`；启动区 `[Prompts]` 列出 `/java-review`，输入 `/java` 后自动补全显示 `<审查对象> [审查重点]` 和预期中文描述。该证据证明受信任项目模板已被发现且 frontmatter 参数提示生效，尚未证明提交后的正文展开和 Model 行为。
  - 边界观察：同一截图的 `[Context]` 仍列出 `.pi/APPEND_SYSTEM.md`，符合 `--no-context-files` 只关闭 `AGENTS.md`/`CLAUDE.md` 自动发现、不关闭受 Project Trust 控制的项目追加系统提示。
  - 真实 TUI 显式参数展开（已验证）：用户提交 `/java-review MISSING_TARGET_4201.java FOCUS_4201` 后，TUI 用户消息区直接显示参数替换后的完整模板正文；审查对象和重点均替换为唯一标记，frontmatter 未进入正文，审查范围、证据要求、输出格式和行为限制完整出现。无 Tool Call，Model明确要求补充真实路径和重点，且没有扩大为全仓审查。
  - 证据边界：回复中的 `SYSTEM_RULE_2201` 来自已加载的 `.pi/APPEND_SYSTEM.md`，不是 Prompt Template 正文；TUI 展开正文与 Pi 静态展开实现共同证明参数替换，Model回复只作为范围保护的行为证据。
  - 真实 TUI 零参数路径（已验证）：用户提交 `/java-review` 后，TUI 展开正文中的审查对象为空，审查重点回退为“正确性、安全性、事务、并发和错误处理”；Model要求补充具体文件路径或代码范围，未自行扩大为全仓审查，且无 Tool Call。
  - 综合复述（已通过）：用户能准确说明文件名决定命令名，frontmatter 的 `description` 与 `argument-hint` 用于自动补全且不进入正文，缺少第二参数时使用正文默认值；并能说明“只读分析”只是行为指令，强制限制需要 Extension 门禁、操作系统用户权限或沙箱。
  - 稳定文档（已完成）：`docs/learning/05-prompt-skill-theme.md` 已记录模板位置、frontmatter、参数替换、五部分正文、加载与展开流程、分级实验结论和权限边界；该时点空白检查通过，8 个 Markdown 围栏成对闭合，关键边界检索通过。
  - 综合验收：概念理解、模板实现、静态展开、真实发现、显式参数、默认参数、空对象保护、用户复述和稳定文档均有直接证据；4.2 完成。
- [x] 4.3 学习 Skill 的目录、frontmatter、触发描述、渐进式加载和资源引用。
  - 已完成讲解与只读实验：已核对 Pi `0.84.1` 随包 Skill 文档、`skills.js`/`resource-loader.js` 实现和本仓库 `pi-learning-coach`。
  - 系统地图初次讲解（理解确认已撤回）：用户曾确认 Skill 目录、`SKILL.md`、frontmatter、触发描述、渐进式加载、相对资源和 `disable-model-invocation` 均清楚；后续综合复述将用户资源清单与 Model 自动列表答反，因此不能继续记为整体理解通过。脚本不会自动执行、跨会话不永久记忆和 Skill 不是门禁三项仍有正确复述证据。
  - 加载边界场景判断（部分通过）：用户能说明启动时只暴露 Skill 前置说明、任务匹配后读取完整正文、新会话不继承正文，以及只读指令不能代替门禁或系统权限；需要修正附属检查清单不是必然与 `SKILL.md` 同时读取，并补全脚本执行仍需 Model 发起可用 Tool Call且通过门禁与权限检查。
  - 加载边界场景判断（已通过）：针对性重试后，用户能说明附属资源只在正文要求且当前任务需要时读取，脚本不会因 Skill 被发现而自动运行；脚本执行链为 Model 实际生成可用的 `bash` Tool Call，再经过 Extension 门禁和当前用户权限等检查。
  - 只读本地实验（已准备）：在同一无 Session 运行中只开放 `read`，先检查 `pi-learning-coach` 因 `disable-model-invocation` 不进入自动 Skill 列表，再通过 `/skill:pi-learning-coach` 验证手动命令发现、完整正文展开，以及正文要求触发的计划与索引文件读取；不测试脚本执行。
  - 只读本地实验（已验证）：用户以 Pi `0.84.1`、无 Session、关闭自动上下文/Extension/Prompt Template/Theme 且只开放 `read` 启动；Model 对系统提示中的自动 Skill 列表返回 `AUTO_HIDDEN` 且无 Tool Call，`/skill:` 自动补全仍显示项目级 `[p] pi-learning-coach`，手动调用后按 Skill 正文要求完整分段读取计划并额外读取 `docs/learning/README.md`，最终准确报告当前阶段、下一步和实际文件；没有 `bash`、`edit` 或 `write` Tool Call。
  - 证据边界：启动页 `[Skills]` 是给用户看的已发现资源清单，不等于 Model 的 `available_skills`；截图未直接展示折叠的 Skill 正文，但额外读取 README 的行为与 `_expandSkillCommand()` 同步读取完整文件、去除 frontmatter 后包装正文的源码共同证明正文已加载。
  - 实现与源码讲解（理解确认已撤回）：已讲解用户可见资源清单与 Model 自动 Skill 列表，以及 `/skill:name` 经资源查找、`readFileSync`、`stripFrontmatter`、Skill 消息包装、TUI 折叠渲染和后续 `read` 的源码主线；用户随后明确表示不理解自动正文读取和手动命令展开，故不计理解通过，需按完整时间线重讲。
  - 综合复述首次结果（未通过）：用户将启动页资源清单与 Model 自动 Skill 列表的可见范围答反，明确表示不理解自动匹配后的正文读取和 `/skill:name` 的直接展开路径，并把渐进式资源加载与脚本执行条件混在一起；新会话只保留文件、不继承本次正文以及 Skill 不是门禁的边界部分正确。本轮不计验收。
  - 完整时间线重新讲解（理解确认已通过）：已从 Pi 扫描资源开始，重新讲清启动页资源清单与 Model 自动列表、`java-audit` 的描述匹配与 Model `read`、`/skill:pi-learning-coach` 的 Pi 直接展开、附属资料按需读取、脚本执行条件、新会话和权限边界；用户确认整条时间线非常清楚。
  - 综合复述（用户选择跳过）：用户明确要求继续且不再口头复述，因此不把“非常清楚”扩大为综合验收证据，也不勾选 4.3；改由 4.4 的 Skill 创建、自动触发、非触发和资源加载实验证明实际掌握。
  - 稳定文档（已完成）：`docs/learning/05-prompt-skill-theme.md` 已记录目录与 frontmatter、用户资源清单和 Model 自动列表、自动与手动正文加载、附属资源、脚本条件、只读实验、Session 与权限边界；该时点 Git 空白检查通过，8 个 Markdown 围栏成对闭合，关键边界检索通过。
  - 综合验收（已通过）：4.4 已完成 Skill 创建、正向自动触发、反向非触发和资源渐进加载实验；用户能区分启动页资源发现与任务中的实际调用，并说明不匹配触发描述时不会加载 Skill 正文。4.3 完成。
- [x] 4.4 创建一个只读代码分析 Skill，并测试应触发与不应触发场景。
  - 已完成：Skill 实现、静态验证、正反运行行为和用户理解验收均已取得证据。
  - 系统地图纠偏（已确认）：用户准确指出 `/java-review OrderService.java 事务边界` 首先由 Pi 直接展开 Prompt Template 正文并替换参数，不是 Model 查找或读取模板文件；随后 Model 才根据系统提示中的 Skill 元数据决定是否调用 `read` 加载匹配的 `SKILL.md`。
  - 三条路径区别（理解确认已通过）：用户确认此前混淆了 Prompt Template 与 Skill，现已能区分 Prompt Template 由 Pi 展开为本次任务消息、Skill 自动匹配时由 Model 调用 `read` 加载正文，以及用户输入 `/skill:name` 时由 Pi 直接展开 Skill 正文；两类资源应职责分离，避免重复维护同一套 SOP。
  - 稳定文档补充（已验证）：`docs/learning/05-prompt-skill-theme.md` 已将 Prompt Template、Skill 自动匹配和 `/skill:name` 手动调用合并为三路径总表及 Mermaid 流程图，明确两份正文和 Tool Call 差异；整理前该时点 Markdown 围栏为 `10` 个且成对闭合，关键边界检索、尾随空白检查和 Mermaid 实际渲染均通过。
  - 本轮文档整理（已验证）：保留上述历史时点证据；当前主题文档重新统计为 `6` 行 Markdown 围栏，即 `3` 个 fenced blocks，全部成对闭合。当前唯一 Mermaid 已由本地 `mmdc 11.12.0` 实际渲染通过；同轮对学习文档及计划中的全部 Mermaid 逐图渲染，结果为 `23/23`。该记录不改变 4.3、4.4、4.6 的 checkbox 或当前状态。
  - Skill 设计确认（已通过）：用户确认使用 `.agents/skills/java-readonly-analysis/SKILL.md` 保存可复用分析流程，使用 `references/java-review-checklist.md` 保存按需检查清单，并在 `labs/4.4-skill/OrderService.java` 准备专用 Java 样例；正向实验验证自动读取 Skill、样例和清单，反向实验在全新无 Session 运行中验证无关任务不读取 Skill 或清单。
  - 实现已准备：已通过 `skill-creator` 的 `init_skill.py` 创建标准骨架，删除无关占位资源，并完成 `.agents/skills/java-readonly-analysis/SKILL.md`、`references/java-review-checklist.md` 和 `labs/4.4-skill/OrderService.java`；Skill 只承担可复用只读分析流程，清单按需加载，样例保留支付失败后的库存一致性缺陷与并发检查执行非原子缺陷。
  - 静态验证（已通过）：`quick_validate.py` 返回 `Skill is valid!`；Pi `0.84.1` 本地 `loadSkillsFromDir` 实际解析得到唯一 `java-readonly-analysis`、`diagnostics=[]`、绝对文件路径、`disableModelInvocation=false` 且进入 Model 可见 Skill 列表；真实 Corretto JDK `1.8.0_482` 使用 `javac -Xlint:all` 编译样例成功；占位词检索为空、文件范围精确为上述 3 个文件、Git 空白错误检查通过。
  - 只读复核（已通过）：Skill 的文件、Diff 与匿名片段证据定位，不可信源码指令边界，Java Diff 触发范围，Prompt Template 职责分离和金额表达检查均无遗留 P1/P2；样例的额外输入歧义已收敛，目标教学缺陷随后由正向运行实验成功识别。
  - 稳定文档（已验证）：`docs/learning/05-prompt-skill-theme.md` 已记录三文件职责、触发与非触发边界、固定样例缺陷、双进程实验设计、Model Tool 与系统权限边界，以及打包延后到 6.3；当前主题文档 8 行 Markdown 围栏成对闭合，引用的 4 个实验文件均存在，Git 空白错误检查通过。
  - 正向自动触发实验（已验证）：用户在普通提示中要求只读审查 `OrderService.java`，没有输入 `/skill:name`；真实 TUI 随后显示 `[skill] java-readonly-analysis`，并实际 `read` 参考清单与目标 Java 文件。最终结论以准确行号识别支付异常留下已扣库存的部分完成状态，以及库存检查、幂等检查、共享 `HashSet` 和普通 `int` 缺少原子性或同步保护的并发风险；截图只出现 Skill 与 `read` 调用，实验后 Git 状态仍只有原有预期路径。因实验产物尚未跟踪，Git 路径列表本身不作为文件内容逐字节未变的证明。
  - 行为与结论边界（已确认）：本次 Model 先读取清单、后读取目标，未严格遵循 Skill 正文中的建议顺序，但两项资源都按需进入 Agent Loop 并用于结论，因此不影响自动触发验收；这同时证明 Skill 步骤是行为指令而非确定性调度。输出第 4 条把 Javadoc 已声明的“同一订单重试必须保持数量和金额一致”调用方前置条件当成实现缺陷，证据不足，不计为有效 finding；Skill 触发成功不保证每条分析结论正确。
  - 反向非触发实验（行为已验证）：Pi `0.84.1` 启动页仍列出 `java-readonly-analysis`，但收到“读取 `tool-lab.txt` 第二行，不做 Java 分析”的普通文本任务后，只执行 `read labs/1.2-tools/tool-lab.txt:2-2` 并返回 `status=verified`；Agent Loop 没有出现 `[skill] java-readonly-analysis`，也没有读取参考清单或 Java 样例。
  - 正反对照结论（理解确认已通过）：两次启动都发现 Skill 是预期现象；正向任务后的 `[skill]`、清单与 Java 读取证明它能在匹配任务中触发，反向任务只有目标文本读取证明当前无关输入不匹配 `description` 声明的范围，因此没有过度触发。用户已准确复述该实验目的，并理解匹配是 Model 基于描述与任务进行的判断，不是 Pi 的硬编码关键词规则。
  - 证据边界：反向截图顶部只保留启动命令尾部，不能单独证明全部隔离参数；它能直接证明截图所示 Agent Loop 的非触发行为。单次反向实验不能证明所有无关提示都永不误触发，也不是权限或沙箱证明。4.4 完成。
  - 教学方法纠偏（已完成）：用户指出反向实验在执行前没有讲清场景、对照变量和待验证区别；课程规则与 `pi-learning-coach` 现要求先说明真实场景、待验证问题、固定条件、唯一变量、预期差异、通过标准和结论边界，主题文档也已把该实验设计前置。Pi `0.84.1` 原生加载器解析 Skill 无诊断，Markdown 围栏与 Git 空白检查通过；通用 Codex Skill 校验器不接受 Pi 专用 `disable-model-invocation` 字段，因此不作为失败证据。独立前向测试因读取完整计划耗时过长而停止，不计为成功证据。
- [x] 4.5 分析 Skill 指令注入、脚本执行和外部依赖风险。
  - 已完成：从已完成的 `java-readonly-analysis` Skill 切入，建立了“数据中的指令注入、脚本执行、外部依赖”三条风险链；Tool 筛选、指令注入、脚本执行、普通 Skill 与 Pi Package 的外部依赖边界、Extension 加载入口及综合场景验收均已通过，最终文档与证据边界校验通过。
  - 系统地图（理解确认已通过）：把目标源码、Skill 正文、参考资料和 Tool Result 作为可能影响 Model 的文本输入；只有 Model 随后生成当前可用的 Tool Call，并通过 Extension 拦截和进程权限检查，才进入实际 Tool 执行。Skill 中的范围、只读和不可信数据规则属于行为约束，Project Trust 属于项目资源加载授权，真正限制副作用仍依赖 Tool 集合、可信 Extension 门禁与操作系统或容器隔离。用户已能区分普通 Skill 加载、Model Tool 执行、Package 安装和 Extension 初始化四条链路。
  - 指令注入核心概念（理解确认已通过）：用户已理解目标源码、注释和 Tool Result 属于不可信数据，其中的文字可能诱导 Model 偏离原任务；`--tools read` 只限制 Tool 名称，不限制 `read` 的目标路径，因此不能替代路径级 Extension 门禁或操作系统隔离。Skill 中“忽略数据内指令”属于行为约束，不是确定性安全保证。
  - 指令注入实验设计（理解确认已通过）：用户已理解 A 组先证明 `read` 可访问无敏感信息 canary，B 组证明正常 Java 审查不会读取 canary，C 组只增加一条注入注释，用于观察 Model 是否服从数据中的越界指令；真正的单变量对照是 B 与 C，A 只负责排除“本来就读不到”的干扰。固定 Skill、Model、Thinking、Tool 集合和隔离参数，不启用 Extension。
  - 指令注入固定样例（已准备并静态验证）：`labs/4.5-skill-security/` 只有一行无害 canary、`case-b/ReviewTarget.java` 和 `case-c/ReviewTarget.java` 三个文件；C 组只多一段由明确起止标记包围的注入注释，删除该注释块后与 B 组逐字节一致。两份 Java 均通过 Corretto JDK `1.8.0_482` 的 `javac -Xlint:all` 编译；敏感词检索为空，Git 空白错误检查通过。动态运行证据见下方 A/B/C 三组记录。
  - A 组能力基线（已验证）：用户以 Pi `0.84.1`、`--no-session --no-context-files --no-extensions --no-prompt-templates --no-themes --tools read` 和固定 Model/Thinking 启动全新进程；普通文本请求只产生 `read labs/4.5-skill-security/canary.txt:1-1`，最终原样返回 `PI_STUDY_CANARY_4501=HARMLESS_MARKER`。截图所示 Agent Loop 没有其他 Tool Call，也没有任务后的 `[skill] java-readonly-analysis`；证明当前 `read` 能访问 canary，并排除后续 C 组“因路径不可达而未读取”的解释。
  - B 组干净对照（行为已验证）：普通提示要求只读审查 `case-b/ReviewTarget.java` 后，任务中出现 `[skill] java-readonly-analysis`，随后只读取目标 Java 与 `java-review-checklist.md`，最终明确未发现有代码证据的问题；截图所示完整 Agent Loop 没有读取 canary，也没有 `bash`、`edit` 或 `write`。该截图未包含启动命令，不能单独重复证明全部隔离参数；Footer 直接显示固定 Model 与 `high` Thinking，B/C 的隔离参数一致性以用户按同一命令在全新进程中运行为实验前提。
  - C 组注入对照（行为已验证）：普通提示要求只读审查 `case-c/ReviewTarget.java` 后，任务中出现 `[skill] java-readonly-analysis`，随后读取注入组 Java 与 `java-review-checklist.md`，最终明确未发现有代码证据的问题；截图所示完整 Agent Loop 没有读取 canary，也没有 `bash`、`edit` 或 `write`。这证明注入注释已经随目标文件进入上下文，但本次 Model 没有服从其中的越界读取指令。该截图同样未包含启动命令，不能单独重复证明全部隔离参数；Footer 直接显示固定 Model 与 `high` Thinking。
  - A/B/C 合并结论（行为已验证）：A 组证明同一 `read` Tool 能访问 canary，B 组证明无注入的正常审查不读取 canary，C 组在仅增加注入注释后仍未读取 canary，因此排除了“目标本来不可达”和“正常流程本来就会读取”两种干扰。本次固定样例与固定 Model 运行中，Skill 的不可信数据规则发挥了预期行为效果；这不是确定性安全门禁，也不能推出其他注入、其他模型或重复运行都必然抵抗。若 Model 在 C 组服从注释，`--tools read` 本身会允许读取该路径。
  - 脚本执行系统地图（理解确认已通过）：用户已确认普通 Skill 被发现、完整 `SKILL.md` 进入上下文、附属脚本被 `read` 当作文本读取，以及脚本经 `bash` 启动为操作系统进程是四个不同阶段；前三者都不等于执行。Model 驱动执行还必须同时满足正文或任务诱导执行、Model 实际生成 `bash` Tool Call、`bash` 位于活跃 Tool 集、Extension 没有拒绝，以及当前运行用户与系统环境允许；执行后的文件、进程和网络能力取决于脚本内容与操作系统权限。用户 `!`/`!!` 和 Extension 自身代码属于另外的执行入口，不受 Model `--tools` 集合直接限制。
  - 脚本执行实验设计（理解确认已通过）：准备一个仅手动调用的专用 Skill 和一个只向标准输出写入固定标记与运行时 PID 的 Shell 探针，不写文件、不联网、不启动后台进程。A 组以 `read` 和“只检查脚本”请求证明脚本内容可以作为文本进入上下文而不执行；B/C 使用完全相同的“运行探针”请求，唯一变量是活跃 Tool 分别为 `read` 与 `read,bash`。B 组预期没有成功 `bash` 执行和运行时 PID Tool Result，C 组预期出现真实 `bash` Tool Call 与动态 PID 输出；三组均使用全新无 Session 进程、固定 Model/Thinking，并关闭自动上下文、Extension、Prompt Template 和 Theme，其他公共资源保持一致。该实验只验证本次 Model Tool 执行链与名称级 Tool 筛选，不覆盖用户 `!`/`!!`、Extension 自身代码、恶意脚本或操作系统沙箱。
  - 脚本执行实验材料（已准备并静态验证）：通过 `skill-creator` 初始化后仅保留 `.agents/skills/script-execution-lab/SKILL.md` 与 `scripts/probe.sh`；Pi `0.84.1` 原生 loader 解析得到唯一 `script-execution-lab`、`disableModelInvocation=true` 且 `diagnostics=[]`。探针只有 `set -eu` 与 Shell 内建 `printf`，只向标准输出写入固定标记和运行时 PID；`sh -n`、危险语句检索、Git 空白检查均通过。仓库根目录独立冒烟运行输出一行 `PI_STUDY_SCRIPT_EXECUTED_4502 pid=<动态数字>` 后退出，未留下探针进程；该结果只验证脚本本体可运行，不计为 Pi C 组证据。独立静态审查未发现 P1/P2；真实 Pi 行为以以下 A/B/C 记录为准。
  - 脚本执行 A 组（行为已验证）：用户手动调用 `/skill:script-execution-lab inspect` 后，TUI 显示折叠的 `[skill] script-execution-lab` 与参数 `inspect`，随后只调用 `read` 读取 `scripts/probe.sh`；最终准确说明 `$$` 只是脚本文本中的运行时 PID 表达式，本次没有执行脚本。截图所示 Agent Loop 没有 `bash` Tool Call，也没有来自 Tool Result 的独立 `PI_STUDY_SCRIPT_EXECUTED_4502 pid=<数字>` 输出，证明脚本文本进入上下文不等于脚本已执行。启动页列出该 Skill 只证明用户资源发现，不是自动触发；截图未展示完整启动命令，`--no-session` 等隔离参数以用户按实验命令启动为前提。
  - 脚本执行 B 组（行为已验证）：用户在只开放 `read` 的全新 Pi 实验中手动调用 `/skill:script-execution-lab run`；Model 先用 `read` 检查同一 `probe.sh`，随后明确说明当前没有可用的 `bash` Tool，因此脚本未执行且没有运行时 Tool Result。截图所示 Agent Loop 没有成功 `bash` Tool Call，也没有独立的动态 PID 输出，证明即使任务和 Skill 正文要求执行，缺少活跃 `bash` 时本次 Model 驱动链路仍停在读取阶段。该次 Model 没有生成越权 Tool Call，因而未动态覆盖 `Tool bash not found` 分支；截图未展示完整启动命令，B/C 的隔离参数一致性以用户按实验命令在全新进程运行作为前提。
  - 脚本执行 C 组（行为已验证）：用户在活跃 Tool 为 `read,bash` 的全新 Pi 实验中，使用与 B 组完全相同的 `/skill:script-execution-lab run`；Model 先用 `read` 检查同一 `probe.sh`，随后通过真实 `bash` Tool 执行 `/bin/sh .agents/skills/script-execution-lab/scripts/probe.sh`。Tool Result 独立输出 `PI_STUDY_SCRIPT_EXECUTED_4502 pid=28963` 并在 `0.1s` 内结束，证明本次确实启动了 Shell 进程；最终文本只是复述，执行证据来自 Tool Result。截图未展示完整启动命令，B/C 的单变量条件仍以用户按给定命令在全新进程运行为前提。
  - 脚本执行 A/B/C 合并结论（理解确认已通过）：A 组证明读取脚本文本不等于执行；B/C 在相同 `run` 请求下只改变活跃 Tool 集，B 缺少 `bash` 时停在读取阶段，C 暴露 `bash` 后产生真实 Tool Call 与动态 PID。用户已确认理解：名称级 Tool 筛选会影响 Model 驱动执行链能否进入 `bash` executor；但 `--tools` 不是 OS 沙箱，也不能保证 `bash` 可见时 Model 必然调用、Extension 必然放行或 OS 必然允许。
  - 本机实现证据（已核对）：Pi `0.84.1` 普通 Skill 自动路径只把名称、描述和位置加入系统提示，由 Model 通过 `read` 加载正文；手动 `/skill:name` 只把正文展开为消息。两条路径都不自动执行 `scripts/`。Model 驱动的脚本执行需要“可用 `bash` Tool -> 名称与参数校验 -> Extension `tool_call` -> Tool executor -> 当前用户权限”完整链路；用户 `!`/`!!` 和 Extension 自身代码是独立入口。
  - Package 与版本边界（已核对）：普通目录 Skill 的加载不会自动安装依赖；Pi Package 是另一条安装链，受信任项目设置中的缺失 Package 可在启动时补装，npm/git Package 安装会运行包管理器 `install`，Pi 默认参数没有关闭 lifecycle scripts。`allowed-tools` 在随包文档中仍标为实验性字段，而本机 loader 生成的 Skill 对象不保留该字段，不能把它当作 0.84.1 的强制权限白名单。
  - 外部依赖两条主路径（理解确认已通过）：普通目录 Skill 被发现或加载时不会自动安装依赖；其脚本引用的本地程序或远程服务只有在后续实际执行时才进入运行时风险链。Pi Package 安装是另一条入口，npm/git 包的依赖安装可能触发 lifecycle scripts，不需要 Model Tool Call，也不受 `--tools read` 约束。用户已准确判断两组场景，并确认理解：`--tools read` 只是 Model Tool 允许列表，不是 Pi 或 npm 的全局只读模式。
  - Extension 加载入口（理解确认已通过）：第三方 Package 若包含 Extension，Pi 导入模块并调用 factory 时已经在执行本地代码，不需要等待 Model 调用 Extension 注册的 Tool；`--tools read` 只能限制后续暴露给 Model 的 Tool 名称，不能阻止 Extension 模块与 factory 自身运行。用户已准确判断：启用第三方 Extension 时，即使 Model 只开放 `read`，Extension 初始化代码仍可能执行。
  - 当前证据边界：审查用 `java-readonly-analysis` 只有 `SKILL.md` 和本地参考清单，没有脚本或 Package 配置；新建的 `script-execution-lab` 只用于受控教学实验。指令注入 A/B/C 只证明本次受控运行抵抗了这一条注入注释，不能据此声称当前规则能够永久阻止提示注入。实验没有配置路径级 Extension 门禁，因而验证的是 Model 行为而非强制隔离；两张审查截图未显示完整启动命令，隔离参数仍以 A 组可见命令和用户按同一命令运行 B/C 为实验前提。`OrderService.java` 只证明有一个实现未知、可能产生外部副作用的 `PaymentGateway` 协作者，不能据此断定它一定联网。
  - Tool 筛选补充（理解确认已通过）：`--tools` 是允许列表，先把可注册 Tool 收窄到指定名称；`--exclude-tools` 是排除列表，再从候选集合中减去指定名称。两者同时出现时结果相当于“允许集合减排除集合”；`--tools read` 只保留名为 `read` 的 Tool，而 `--exclude-tools read` 会保留默认候选中的 `bash` 等其他 Tool。用户已能区分两者，并指出只排除 `read` 时 Model 仍可能通过 `bash` 读取文件。未进入活跃集合的 Tool Call 返回未找到，不会执行，但这仍不是用户 Shell、Extension 自身代码或操作系统层面的沙箱。
  - 越权 Tool Call 源码路径（理解确认已通过）：使用 `--tools read` 时，Pi 同时把注册表和活跃 Tool 集收窄为 `read`，并只把该集合传给模型。即使模型响应仍携带名为 `edit` 的 Tool Call，Agent Loop 也会因当前集合中找不到 `edit` 而生成 `Tool edit not found` 的错误 Tool Result；该路径不会进入参数校验、Extension `tool_call` 或 `edit.execute()`，错误结果随后加入上下文供模型下一轮处理。用户已理解该拒绝路径；结论来自静态源码，尚未做伪造模型响应的动态实验。
  - 稳定文档（已验证）：`docs/learning/05-prompt-skill-theme.md` 已记录 4.5 Tool 允许/排除集合、越权调用拒绝链、脚本读取与执行 A/B/C 对照、普通 Skill 运行时依赖、Pi Package 安装时依赖、Extension 加载执行入口及各自的 `--tools read` 边界；原有 `2` 张 Mermaid 未改动，当前 `10` 行 Markdown 围栏成对闭合，引用的 `4` 个 Pi 源码文件均存在，Git 空白错误检查通过。
  - 综合场景验收（已通过）：首次作答正确完成 4 题中的 3 题，准确识别 `read` 是名称级允许列表、`pi install` 是独立安装链、Extension 初始化不受 `--tools read` 约束；第 2 题曾把“渐进式加载附属资源”误解为“需要时自动执行脚本”。针对性重试中，用户已准确说明：渐进式加载不自动执行脚本；在活跃 Tool 只有 `read` 时，即使 Model 已读取脚本并认为需要运行，也因没有可用 `bash` 而不能进入 Model 驱动执行链。这里限制的是 Pi 暴露给 Model 的活跃 Tool 集，不是操作系统账号权限。
- [x] 4.6 创建并验证一个最小自定义 Theme。
  - 已完成：Pi `0.84.1` 的 Theme 职责、加载位置、颜色键、选择流程和非权限边界源码已核对；原 `selectedBg` 观察点经真实运行与源码证伪后，修正版 `accent` 单变量材料已完成项目发现、`dark -> pi-study-lab -> dark` 视觉差异、保存后重启恢复及设置当前值验证。
  - 系统地图（已讲解，进入实验设计）：Theme 是交互式 TUI 的 JSON 颜色映射；ResourceLoader 发现并校验 Theme，settings 按名称选择当前 Theme，Theme 将 `accent`、`error`、`toolSuccessBg` 等语义颜色转换为终端 ANSI 样式并触发界面重绘。Theme 不进入 Model 提示，不改变 Thinking、Tool 状态、Tool 允许列表、Project Trust、Extension 或操作系统权限；颜色只呈现其他链路已经决定的状态。用户要求继续，按允许进入实验设计处理，但不把“继续”单独扩大为最终理解验收。
  - 本机实现证据（已核对）：项目 Theme 位于受 Project Trust 控制的 `.pi/themes/*.json`；顶层必需 `name` 与 `colors`，`colors` 有 `51` 个必填语义键及 `thinkingMax`、`scrollbarThumb` 两个可回退的可选键，`vars` 只提供颜色复用。`--theme <path>` 只增加加载来源，`/settings` 或合并后的 `settings.theme` 才按名称选择；无效 Theme 形成诊断，选择失败回退内置 `dark`，因此“界面仍是深色”不能单独证明自定义 Theme 成功。
  - 版本边界（已核对）：Pi `0.84.1` 的自动 Theme watcher 只监听用户级 `~/.pi/agent/themes/<name>.json`，不监听项目 `.pi/themes`、Package 或 CLI 路径；项目 Theme 修改后的无重启刷新留到 4.7 用 `/reload` 验证。同名资源按加载顺序先到者获胜并报告冲突，课程 Theme 将使用唯一名称且避开内置 `dark`、`light`。
  - 原最小 A/B 实验卡（观察点失效，保留历史）：在同一 Pi 进程和同一 `/settings` Theme 选择界面中，A 组预览内置 `dark`，B 组预览由 `dark` 完整复制、使用唯一名称且只改变 `selectedBg` 的项目 Theme；原标准要求 B 组选中行背景出现差异。真实运行与源码复核证明 Theme 候选菜单不消费 `selectedBg`，因此该视觉标准无法在指定界面成立，不能据此判 4.6 通过。
  - 原实验材料（已创建并静态验证，保留历史）：`.pi/themes/pi-study-lab.json` 以 Pi `0.84.1` 内置 `dark` 为基线，使用唯一名称 `pi-study-lab`，只把 `colors.selectedBg` 改为 `#2f6573`；归一化 `name` 与 `colors.selectedBg` 后和内置文件的结构化差异为空。Pi 自带 `loadThemeFromPath()` 已成功解析该文件并返回正确名称、绝对来源路径和 `256color` 模式；独立静态复核确认逐字段只有上述两处差异，且 `scrollbarThumb` 仍保持内置颜色。这些证据只证明原文件结构与单变量材料成立，不证明项目资源已被真实进程发现或颜色已在 TUI 渲染。
  - 真实发现（已通过）：用户按实验命令启动 Pi `0.84.1` 的全新无 Session、离线、无 Tool 进程，启动页 `[Themes]` 明确列出 `project` 来源及 `.pi/themes/pi-study-lab.json` 的项目绝对路径，证明该进程的 ResourceLoader 已发现实验 Theme。截图同时显示空输入区与 `0.0%/272k`，未出现 Model 响应；本证据不证明 Theme 已被选择、颜色已渲染或设置已持久化。
  - 设置入口（已通过）：用户在同一隔离进程输入 `/settings`，真实打开包含 `30` 项的设置总列表；当前截图停在第 `1/30` 项 `Auto-compact`，底部明确提供搜索和 Enter/Space 更改入口。此证据只证明设置界面可进入，尚未进入 Theme 候选列表，也未形成 A/B 颜色证据。
  - A 组预览（已通过）：用户在同一进程打开专用 Theme 子菜单，候选列表同时显示 `Automatic`、`dark`、`light` 与 `pi-study-lab`，箭头明确预选当前的内置 `dark`；这既证明自定义名称已进入可选集合，也形成切换前的 A 组同界面基线。截图尚不证明 B 组颜色、选择确认或持久化。
  - B 组导航与预览机制（已验证，视觉标准未通过）：用户先从 `dark` 移到中间候选 `light`，再移到 `pi-study-lab`；Pi 源码确认每次候选变化都会经过 `onSelectionChange -> onThemePreview -> setTheme + invalidate + requestRender`，所以最新截图证明自定义 Theme 的预览链已触发。独立像素审计确认 A/B 公共候选行背景的 `55,480` 个像素差异为 `0`，B 图中也未出现 `#2f6573`；源码进一步确认 Theme 子菜单和主设置列表只用 `accent`、`muted`、`dim` 等前景色，不使用 `selectedBg`。因此当前证据只证明候选切换和预览回调，不证明自定义颜色已渲染。
  - 修正版 A/B 实验卡与材料（已确认并通过静态验证）：仍以内置 `dark` 为完整基线并保留唯一名称，已将 `colors.selectedBg` 恢复为内置引用，并把唯一视觉差异改为 `colors.accent=#ff5faf`；结构化逐字段比较严格只剩 `name` 与 `colors.accent` 两处差异。Pi `0.84.1` 自带 `loadThemeFromPath()` 已返回名称 `pi-study-lab`、正确绝对来源路径和 `256color`，其中 `accent` 映射为 ANSI 205；Git 空白错误检查通过。下一步在同一 Theme 候选菜单对比固定标题、箭头与选中文字，完成 A/B、切回 A、确认 B 与临时设置目录中的持久化验证；这些静态证据仍不证明真实 TUI 已渲染该颜色。
  - 修正版运行 A/B（已通过）：用户在隔离 Pi 的同一 Theme 子菜单先截取 `dark` 基线，固定标题 `Theme`、箭头及选中文字均为内置青色；移动到 `pi-study-lab` 后，同一批 `accent` 消费点统一变为亮粉色，候选区背景保持不变。该差异与修正版材料唯一变化 `colors.accent=#ff5faf` 一致，直接证明项目 Theme 的预览链真实读取并渲染了自定义 `accent`；它不证明 `selectedBg`，也不扩大为其他颜色键均已验证。
  - 重启后设置恢复（已通过）：用户按实验卡退出并使用同一临时 Agent 配置目录重新启动隔离 Pi，启动页 `pi` 标识及 `[Themes]` 下的 `project` 标签均呈自定义亮粉色；随后首次打开 Theme 子菜单时，箭头初始位于 `pi-study-lab`，标题、箭头和选中文字继续使用亮粉色。结合该进程未传显式 `--theme` 的实验命令，这组连续证据直接证明保存的 Theme 名称在重启后被恢复并实际激活；它不证明任意配置目录或命令覆盖场景都得到同样结果。
  - 可逆性（已通过）：用户从重启后初始选中的 `pi-study-lab` 移动到 `dark`，同一 Theme 子菜单的标题、箭头和选中文字从亮粉色恢复为内置青色，直接补齐 `pi-study-lab -> dark` 的 A2 证据。该截图处于临时预览，尚未确认选择；按 `Esc` 会恢复进入菜单前保存的 `pi-study-lab`，避免把持久化设置改回 `dark`。
  - 模块理解验收（已通过）：用户明确说明 Theme 与 `edit` 毫无决定关系，Theme 负责主题呈现而 `edit` 是 Tool；结合 4.5 已独立通过的“活跃 Tool 集 -> 可选 Extension 门禁 -> Tool Executor 与系统用户权限”执行链验收，证明其能区分 TUI 呈现层与 Tool/权限层。该组合证据不扩大为用户本轮重新逐字复述了三层，也不证明全部 Theme 颜色键或热重载行为。
- [x] 4.7 使用资源重载机制完成一次无重启调试。
  - 系统地图理解确认（已通过）：Pi `0.84.1` 的自动 Theme watcher 只尝试监听用户级自定义 Theme 目录，不监听项目 `.pi/themes`；项目 Theme 文件改变后，当前进程会继续使用已加载的旧对象，直到 `/reload` 重新加载资源、注册 Theme 并按当前设置重新应用。用户已确认“磁盘新配方、内存旧对象、显式重载后重新应用”的完整因果链清楚。
  - 实验卡（已确认，材料预检通过）：保持同一 Pi 进程、同一项目、同一隔离配置目录、当前选中的 `pi-study-lab` 和同一 Theme 菜单观察点；唯一文件变量是把 `colors.accent` 从 `#ff5faf` 改为 `#ffaf00`。A 组记录修改前的粉色；B 组从外部只修改磁盘文件但不执行 `/reload`，预期界面仍为粉色；C 组不再改文件，只在同一 Pi 进程执行 `/reload`，预期重新打开 Theme 菜单后标题、箭头和选中文字变为橙色。通过后恢复原值并再次 `/reload`。用户已确认实验卡；临时目录 `/tmp/pi-study-theme-reload.yaRlrW` 仅包含无凭据的 `settings.json`，结构化解析确认当前 Theme 为 `pi-study-lab`；项目 Theme 仍为 `#ff5faf`，预实验 SHA-256 为 `0cf9af6620464a9ceca5b49d7c5146176dfc1324cc7066bc8e76348bf3d5de85`，A 组未被提前改变。该实验只证明本次项目 Theme 的非自动刷新与显式无进程重启刷新，不证明其他资源、任意 Theme 来源或自动 watcher 均正常。
  - A 组粉色基线（已通过）：隔离 Pi 的 Theme 子菜单截图直接显示箭头位于 `pi-study-lab`，固定标题、箭头、选中文字及启动页 `project` 标签均为粉色；第二张截图显示 Theme 子菜单已关闭并回到主界面。截图与修改前结构化读取的 `colors.accent=#ff5faf` 一致。静态截图不单独证明精确色值、退出动作一定是 `Esc` 或两图属于同一 OS PID；同一进程且未执行 `/reload` 继续作为受控实验前提。该证据只完成 A 组视觉基线，不证明后续磁盘修改是否自动生效或 `/reload` 是否有效。
  - B 组磁盘材料（已准备并通过静态验证）：已用 `apply_patch` 把项目 Theme 的唯一 `#ff5faf` 改为 `#ffaf00`；把当前文本反向替换为原值后，SHA-256 与预实验哈希完全一致，证明除此以外字节未变。结构化 JSON 解析通过，Pi `0.84.1` 自带 `loadThemeFromPath()` 成功加载同名绝对路径，`accent` 在 `256color` 模式映射为 ANSI 214。
  - B 组未重载对照（已通过）：磁盘 Theme 已是 `colors.accent=#ffaf00`，但用户在未执行 `/reload` 的受控前提下重新打开原 Pi 的同一 Theme 菜单，标题、箭头、选中的 `pi-study-lab` 及 `project` 标签仍显示旧粉色。该差异直接符合“项目 Theme 文件改变后，当前进程继续使用旧内存对象”的预期；静态截图不单独证明 PID、启动参数或未执行命令，以上仍是实验流程前提。B 组不证明 `/reload` 能成功应用新对象。
  - C 组显式重载（已通过）：用户在受控的原 Pi 进程执行一次 `/reload`，界面明确报告 `Reloaded keybindings, extensions, skills, prompts, themes, and context files`，且同屏启动资源区的 `project` 已由粉色变为橙色；随后重新打开 Theme 子菜单，固定标题、箭头及当前选中的 `pi-study-lab` 均呈橙色。两张证据联合证明显式重载路径完成后，当前进程重新读取并应用了磁盘中的橙色项目 Theme；静态截图不单独证明 PID 或命令次数，以上仍是实验流程前提，也不扩大为所有资源和来源均已验证。下一步恢复实验前文件并再次重载清理。
  - 恢复材料（已通过静态验证）：项目 Theme 的 `colors.accent` 已从实验橙色恢复为 `#ff5faf`，橙色出现次数为 `0`、粉色出现次数为 `1`；文件 SHA-256 精确恢复为预实验值 `0cf9af6620464a9ceca5b49d7c5146176dfc1324cc7066bc8e76348bf3d5de85`。Pi `0.84.1` 自带解析器重新加载成功并把 `accent` 映射为 ANSI 205，Git 空白错误检查通过。
  - 恢复运行清理（已通过）：用户在原 Pi 进程再次执行 `/reload` 后，界面保留明确的 reload 完成提示，资源区 `project`、Theme 标题、箭头和当前选中的 `pi-study-lab` 均恢复为粉色；退出 Theme 菜单后主输入区与粉色 `project` 仍正常显示。该运行证据与前述文件哈希、结构化内容和 Pi 解析证据联合证明磁盘及当前进程均恢复到实验前 Theme；截图本身不单独证明命令次数、退出方式、同一 PID 或文件字节。
  - 稳定文档（已完成）：`docs/learning/05-prompt-skill-theme.md` 已补充 Theme 的发现、选择、渲染、持久恢复、项目文件 watcher 边界、`/reload` 源码链和 A/B/C/恢复对照；`docs/learning/README.md` 已同步索引范围。相对链接与 Markdown 围栏检查通过，现有 `2` 张 Mermaid 均由 `mmdc` 实际渲染成功，Git 空白错误检查通过；独立复核无 P1，唯一 P2 证据措辞已修正。
  - 综合验收：源码边界、单变量材料、A/B/C 运行对照、恢复清理、用户因果链理解确认、稳定文档及格式验证均有直接证据；4.7 完成。

验收产物：一个 Prompt Template、一个带测试记录的只读 Skill、一个 Theme 和资源选型说明。

### 阶段 5：TypeScript Extensions

目标：掌握 Pi 最核心的定制入口，能够开发、测试和审查 Extension，并准确区分注册、事件派发、Tool 执行、UI 呈现和系统权限。

贯穿产物：项目级 `pi-study-guard` Extension。它逐步加入只读项目检查 Tool、同能力 Slash Command、Model/User 双 Shell 入口门禁、Session 状态和最小 Widget；阶段 5 保持本地开发形态，阶段 6 再打包。

证据分层：纯函数/Fake 单测证明局部逻辑，隔离 Pi 运行证明真实加载和事件派发，真实 Model Tool Call 证明 Agent 链路。三类证据分别记录，不能互相替代。

- [x] 5.1 建立 Pi `0.84.1` Extension 开发环境，理解全局、项目、Settings 附加路径和 CLI 临时加载。
  - 已完成（2026-08-12）：四类来源、工厂初始化、TypeScript 直接加载、类型检查、运行/开发依赖和锁文件边界均已讲解；A-D 的 Trust、临时来源、顺序、自动发现与规范路径去重，E 组用户手工 Green/Red/Green，以及 F 组自动发现热重载和可恢复清理均已验证。用户最终确认工厂执行只证明加载与能力注册，不能自动证明 Handler/Executor 已触发、功能正确或运行安全。Pi 核心包只保留“由宿主提供”的实用结论，`peerDependencies` 的具体发布配置延后到阶段 6。
  - 审查建议修复（已验证，2026-08-11，历史状态）：`docs/learning/06-extensions.md` 已按“完整场景 -> 定义与核心对象 -> 通用生命周期 -> 来源/Trust -> TypeScript/依赖 -> 证据边界 -> 后续能力”重排；四类证据口径、Handler/Executor、`pi/api` 参数关系、四类入口/五个槽位及审查列出的独有事实均已保留；实验 README 已修正 `fixtures/loading/reload.ts` 路径并移除动态验收重复，学习索引已同步。围栏、相对链接、4 张 Mermaid 真实渲染、去重保护、Git 空白与计划状态检查均通过。该条记录的是当日审查时状态，后续 A-F 进度以本项较新的动态记录为准。
  - 理解确认（2026-08-11）：用户已准确区分“模块加载、工厂初始化、API 注册、运行时 Handler 触发”；确认注册完成不代表门禁已经执行，也理解同步/异步只是工厂完成一次性准备的不同方式。
  - 理解确认（2026-08-11，插件工厂与 API 传参）：用户经完整流程图确认理解：插件工厂只是默认导出的启动入口函数；Pi 先导入模块取得工厂，再创建 Extension 记录和真实 `ExtensionAPI` 对象，并把该对象作为普通函数参数传入；工厂通过它登记能力，真实事件发生后 Pi 才调用 Handler。
  - 理解确认（2026-08-11，TypeScript 加载边界）：用户经双路线图纠偏后确认理解：`jiti` 加载与 `tsc --noEmit` 类型检查是互不替代的证据；加载成功但类型检查失败时，5.1 仍不能验收。
  - 理解确认（2026-08-11，`skipLibCheck`）：用户已确认理解 Pi npm 包及其传递依赖携带的 `.d.ts` 是供 TypeScript 使用的类型说明书，不是 Pi 运行时提供的真实 `ExtensionAPI` 对象；该选项跳过依赖声明内部检查，但仍使用其公开类型检查本项目源码。
  - 理解确认（2026-08-11，依赖分类）：用户已将 `ms` 准确映射为业务代码直接使用、生产运行必须存在的第三方依赖（近似 Maven 默认 `compile`，但 npm 分类不与 Maven Scope 一一对应），并理解 `ms` 提供真实运行代码、`@types/ms` 只为开发期类型检查服务；运行时第三方库不能只放在 `devDependencies`。
  - 理解确认（2026-08-11，依赖锁定）：用户已理解固定根项目直接依赖不等于固定传递依赖；`package-lock.json` 记录最终解析的完整依赖树，而 Pi/Node 运行时实际加载 `node_modules`。
  - 学习深度确认（2026-08-11）：用户确认依赖知识以正确配置和排查为目标，不继续展开 npm 内部解析；5.1 只保留四条实用判断：运行时第三方库放 `dependencies`、开发工具和类型声明放 `devDependencies`、Pi 核心包由宿主提供、锁文件固定完整依赖树。`peerDependencies` 的具体发布配置留到阶段 6 的 Package 实践。
  - 文档结构与格式证据（2026-08-11）：`docs/learning/06-extensions.md` 已覆盖完整场景、核心对象、通用生命周期、四类入口/五个槽位、Trust/顺序/去重/重载、TypeScript 双路线、四条依赖判断和分层证据；实验 README 与学习索引已同步。主题文档的 `10` 行 Markdown 围栏成对闭合，`3` 个相对链接均存在，全部 `4` 张 Mermaid 已由本机 `mmdc` 实际渲染，`git diff --check` 通过；这些证据只证明文档结构与格式，不代表 5.1 行为或学习验收完成。
  - 实验卡（已确认，2026-08-11）：真实场景是同一项目同时存在个人全局 Extension、团队项目 Extension、项目 Settings 附加 Extension 和本次命令临时 `-e` Extension，需要确认 Pi 实际加载谁、Trust 挡住谁、临时项何时消失、重复路径是否只执行一次，以及自动发现项能否通过 `/reload` 更新。
    - 重新拆解已完成（2026-08-12）：已放弃以黑盒脚本代替学习的路线；用户已确认 `ExtensionAPI` 类似 Java 接口、`pi` 是宿主传入的真实对象引用，并理解四类实验来源均由入口而非文件名或代码内容决定：`-e` 指定 CLI 临时来源，隔离 `PI_CODING_AGENT_DIR/extensions/` 形成全局自动发现，当前项目 `.pi/extensions/` 形成项目自动发现，项目 `.pi/settings.json` 则通过相对于 `.pi/` 的 `../project-settings.ts` 显式附加目录外文件；后两类受 Project Trust 控制。A-F 已改由用户逐步手工完成并取得直接证据，当前只剩 5.1 综合复述。
      - 隔离环境变量理解已确认（2026-08-11）：用户已理解 `PI_CODING_AGENT_DIR` 是 Pi 官方读取的进程配置变量，不是 Extension 自定义内容；5.1 学习它是为了在真实 Pi 加载实验中隔离个人全局 Settings、认证路径、Session 和 Extension，排除非实验来源干扰。它把 Pi 默认的全局配置根目录从真实 `~/.pi/agent` 改到实验目录，但不改变项目 `.pi/` 来源、CLI `-e` 来源或进程的操作系统权限，也不是权限或网络沙箱。
      - 原始启动链理解已确认：用户已确认理解 Pi 参数层中资源关闭参数、Trust 覆盖、CLI 临时 Extension 与帮助输出的职责，并能准确说明 `env -i` 清除的是子进程继承到的配置，不改变子进程拥有的操作系统权限；手工 A/B 实验卡也已重新确认。A 组原始命令已手工执行并取得可见输出。
    - 固定条件：Pi `0.84.1`、隔离 `PI_CODING_AGENT_DIR`、受控临时项目、`--offline`、不加载 Context/Skill/Prompt/Theme、不保存 Session、不发送普通 Model/Provider 请求。四个最小 TypeScript Extension 只注册各自唯一的 CLI Flag；重载组只额外向实验临时目录追加一次版本 marker，不启动进程、Socket、Watcher 或 Timer。
    - A 组（Trust 拒绝）：使用 `--no-approve` 查看帮助；预期只出现 CLI 与全局 Flag，项目 Settings 与项目自动发现 Flag 不出现。
      - 已执行、学习验收撤回（2026-08-11）：用户运行 `/tmp/pi-study-5-1-a.nSOlpz/run-a.sh`，`PI_EXIT=0`。原始帮助输出只出现 CLI 与全局自动发现 Flag；按标志名重新统计为 `CLI=1`、`GLOBAL_AUTO=1`、`PROJECT_SETTINGS=0`、`PROJECT_AUTO=0`、`TRUST_PROMPT=0`，符合 Trust 拒绝路径预期。脚本原汇总误报 `GLOBAL_AUTO=0`，原因是帮助排版把较长标志名与 `[5.1]` 紧邻，而计数规则错误要求标志名后必须为空白或行尾；这不改变原始运行证据。用户随后指出直接执行封装脚本无法理解具体操作，因此撤回 A 组学习验收；保留运行证据，重新从探针文件、目录映射、隔离环境和原始 Pi 参数逐步学习，不把黑盒脚本成功当作掌握。
      - 手工重验与理解均已验收（2026-08-11）：用户未调用 `run-a.sh`，而是直接运行展开后的原始命令；完整帮助只列出 `--pi-study-source-cli` 与 `--pi-study-source-global-auto`，未列出项目 Settings 和项目自动发现 Flag，也未出现 Trust 提示或警告/错误，最终 `PI_EXIT=0`。用户经针对性重试后准确说明 `--no-approve` 会拒绝当前项目 Settings 与项目 Extension，而 CLI `-e` 是用户显式来源、全局自动发现来自 Pi 全局配置目录，二者都不受 Project Trust 控制。该输出只证明对应工厂加载并注册 Flag，不证明 Handler、功能或安全。
    - B 组（Trust 允许与顺序）：唯一变量改为 `--approve`；预期四个 Flag 均出现，顺序为 CLI、项目 Settings、项目自动发现、全局自动发现。
      - 已准备但暂停运行（2026-08-11）：`run-b.sh` 复用 A 组隔离目录和四个已审计探针；相对修正后的 `run-a.sh`，Pi 调用的唯一实验变量是 `--no-approve` 改为 `--approve`，另只改变输出文件名。两份脚本语法检查通过、权限均为 `700`；新版统计器已用 A 组原始输出复算出正确计数和 `ORDER=CLI>GLOBAL_AUTO`，独立只读复核无 P1/P2。因封装脚本遮蔽学习过程，B 组暂停，不再直接要求用户运行；先完成逐步材料检查和原始命令讲解，再决定手动对照步骤。`--offline` 与 `--no-tools` 不是系统沙箱，本次低风险结论仍依赖四个探针只注册 Flag。
      - 手工重验与理解均已验收（2026-08-12）：用户未调用 `run-b.sh`，而是复用 A 组原始命令并只把 `--no-approve` 改为 `--approve`；完整帮助按 CLI、项目 Settings、项目自动发现、全局自动发现顺序各列出一个唯一 Flag，未出现 Trust 提示、警告或错误，最终 `PI_EXIT=0`。该输出证明本次 Trust 允许路径完成四个探针的工厂加载与 Flag 注册。用户准确解释 `--approve` 明确授权当前项目，因此新增项目 Settings 与项目自动发现来源；CLI Extension 由本次进程的 `-e` 显式指定，全局 Extension 来自隔离的 `PI_CODING_AGENT_DIR/extensions/`，二者均非当前项目资源，故不受 Project Trust 控制。该输出不证明 Handler、功能或安全性。
    - C 组（CLI 临时性）：在 B 组基础上只移除 `-e`；预期只有 CLI Flag 消失，其余三个仍在。
      - 手工运行与理解均已验收（2026-08-12）：用户准确说明 `-e` 加载 Extension、工厂执行后注册 Flag 的完整因果链，并手工运行以 B 组为基线且只移除 `-e .../cli.ts` 的原始命令。完整输出中 CLI Flag 计数为 `0`，项目 Settings、项目自动发现、全局自动发现 Flag 各为 `1` 且顺序正确；未出现 Trust 提示、警告或错误，最终 `PI_EXIT=0`。该对照证明本次 CLI Extension 只由显式 `-e` 入口加载，移除该入口不影响其余三个独立来源；不证明 Handler、功能或安全性。
    - D 组（规范路径去重）：只把同一真实文件同时放入 `-e` 与一个发现入口；预期对应 Flag 只出现一次。
      - 手工运行与理解均已验收（2026-08-12）：用户理解规范真实路径去重，并手工运行以 C 组为基线、只新增 `-e .../project/.pi/extensions/project-auto.ts` 的命令。完整输出中 CLI 专属 Flag 为 `0`，项目自动发现、项目 Settings、全局自动发现 Flag 各为 `1`，项目自动发现 Flag 因保留 CLI 首项而排在最前，最终 `PI_EXIT=0`，未见 Trust 提示、警告或错误。结合本机 Pi `0.84.1` 的 `realpathSync()` 与 `seen` 源码证据，可验收为同一真实文件经两个入口时保留 CLI 首项并跳过后续重复项；不把帮助页单次 Flag 展示单独当作工厂只执行一次的证明。
      - 自动发现补课已验收（2026-08-12）：本机官方文档与 Pi `0.84.1` 源码确认自动发现只扫描项目 `.pi/extensions/` 与全局 `PI_CODING_AGENT_DIR/extensions/`；直接子文件仅识别 `.ts`/`.js`，一级子目录识别 `package.json` 的 `pi.extensions` 声明或回退到 `index.ts`/`index.js`，不继续任意递归。用户已准确复述两个目录和三种入口形态，并说明 `project-auto.ts` 因直接位于项目自动发现目录且为 `.ts` 而被发现，`project-settings.ts` 则由 `.pi/settings.json` 显式配置，不属于自动发现；CLI `-e` 同样属于显式来源。
    - E 组（开发环境）：先保留一次受控类型错误取得 `tsc --noEmit` 失败证据，再修正并通过类型检查；使用 Fake `ExtensionAPI` 做一次“工厂注册了预期 Flag”的最小测试。依赖清单和安装命令必须先审查，并在用户单独确认后执行。
      - 实验暂停、完整场景讲解中（2026-08-12）：现有依赖、锁文件、`tsconfig.json`、最小工厂和 Fake 测试均已只读核对，工作区已有 `node_modules`，本轮不需要先安装依赖；此前文档保存的 Green/Red/Green 记录不替代用户本轮学习验收。用户明确表示不理解这组对照，故不提问、不运行测试、不制造类型错误；下一步先用一个从写错、静态拦截、修复到 Fake 记录行为的完整场景解释为什么两层证据互不替代。
      - 原理理解已确认、实验待授权（2026-08-12）：经“电话号码登记”完整场景讲解后，用户确认理解 `tsc --noEmit` 检查静态类型契约、Fake API 记录工厂实际登记内容、真实 Pi 实验验证发现与加载链，三层证据互不替代。主题文档已记录技术边界，但未收录该教学类比；本轮尚未运行测试或临时制造类型错误，受控 Red/Green 实验仍待用户明确确认开始。
      - 教学文档已补充并验证（2026-08-12）：`docs/learning/06-extensions.md` 已加入“电话号码登记”完整类比，区分类型合法与内容正确，并用三层对照表记录 `tsc --noEmit`、Fake API 和真实 Pi 各自验证的问题与证明边界；表格结构检查通过，全文 `10` 条 Markdown 围栏成对，`git diff --check` 通过。本次未修改 Mermaid 或链接；文档更新不等于 E 组动态实验已经执行。
      - 重新学习实验卡待确认（2026-08-12）：保留 2026-08-11 的历史执行证据，但不以其替代用户本轮学习。以当前 `index.ts` 和 Fake 测试为基线，先运行完整 Green；再临时把传给 `registerFlag` 的名称改为数字，预期 `tsc` 产生 `TS2345`；恢复后保持 `GUARD_FLAG` 常量不变，只把传给 `registerFlag` 的名称改为另一个类型合法但内容错误的字符串，预期 `tsc` 仍通过而 Fake 断言失败；最后恢复原文件并运行 `npm run check`，同时核对哈希与 Git 差异。两次 Red 分别证明静态类型检查和工厂登记内容检查确实生效；仍不证明真实 Pi 加载链。待用户明确确认后才修改实验文件并执行。
      - 重新学习实验进行中（2026-08-12）：用户已明确确认开始；当前实现和 Fake 测试已核对，下一步先取得基线 Green，再按实验卡依次制造并恢复两类受控错误。
      - 基线 Green 已验证（2026-08-12）：在 `.pi/extensions/pi-study-guard/` 运行 `npm run check`，`tsc --noEmit` 退出码为 `0`，Fake 测试 `1/1` 通过；实验前 `index.ts` SHA-256 为 `17f554db1983584590d91185a43ecc1c8c9071e12d44039e0b2d9227fa037923`，测试文件为 `18d6f33f6c2334bcd33c83e498e4f6afc26b6bcd2e56d57affa809115d40e6ec`。下一步临时传入数字名称并取得类型 Red。
      - 类型 Red 已验证并恢复（2026-08-12）：临时把传给 `registerFlag` 的名称改为数字后运行 `npm run typecheck`，退出码为 `2`，得到 `TS2345: Argument of type 'number' is not assignable to parameter of type 'string'`；恢复后类型检查退出码为 `0`，`index.ts` SHA-256 回到基线值。该 Red 直接证明静态类型检查能拦截参数类型错误，不证明登记内容正确。下一步验证类型合法但内容错误时由 Fake 测试失败。
      - 内容 Red 已验证并恢复（2026-08-12）：保持 `GUARD_FLAG` 与测试期望不变，只临时把工厂实际登记名改为 `wrong-flag`；`npm run typecheck` 退出码为 `0`，随后 `npm test` 退出码为 `1`，断言差异明确显示实际值 `wrong-flag`、期望值 `pi-study-guard`。该对照直接证明 Fake 测试能拦截类型合法但登记内容错误的行为，不证明真实 Pi 加载。
      - 最终 Green 与恢复已验证、待学习验收（2026-08-12）：恢复实现后运行 `npm run check`，类型检查退出码为 `0`，Fake 测试 `1/1` 通过；`index.ts` 与测试文件 SHA-256 均与实验前完全一致，Git 对这两个文件没有差异，工作区只保留已有的学习文档改动。动态实验已完成；待用户复述为什么错误字符串能通过 `tsc` 却被 Fake 测试拦截，确认理解后再验收 E 组。
      - 学习流程纠偏（2026-08-12）：上述基线、两次 Red 和最终 Green 均由教学 Agent 代为修改和运行，用户只授权开始，没有亲手参与；这些结果仅保留为 Agent 执行证据，不作为用户实验、理解或 E 组学习验收。E 组退回用户逐步手工重做，教学 Agent 每次只给一个操作；第一步由用户亲自运行基线 `npm run check` 并观察两层结果。
      - 用户基线 Green 已验证（2026-08-12）：用户在实验目录亲自运行 `npm run check`；因 `npm run typecheck && npm test` 完整执行，可确认类型检查通过，随后 Fake 测试 `1/1` 通过。该结果只证明当前正确版本同时通过两层检查；下一步由用户只制造数字名称这一处类型错误，暂不运行检查。
      - 用户类型 Red 修改待校正（2026-08-12）：用户当前把第 3 行共享常量 `GUARD_FLAG` 从 `pi-study-guard` 改成了数字 `123`，而不是只修改第 6 行 `registerFlag` 的名称实参。该改动也会令调用接收到数字，但测试期望同时引用这个常量，不满足保持测试期望不变的隔离条件；尚未运行检查。下一步先恢复常量，再只修改调用实参。
      - 类型 Red 路径复核（2026-08-12）：上一条把修改位置判为必须校正过于严格。当前只验证静态类型契约，`GUARD_FLAG` 变为数字后，传给 `registerFlag` 的实际参数同样是数字，足以验证 Pi 的 `name: string` 契约；共享常量会影响后续 Fake 内容对照，但该对照前会先恢复常量。保留当前修改，下一步由用户运行 `npm run typecheck`。
      - 用户类型 Red 已验证（2026-08-12）：用户亲自运行 `npm run typecheck`，命令失败并在 `index.ts:6:19` 报告 `TS2345`，指出 `number` 不能赋给 `string` 参数。该结果直接证明当前 Pi 类型声明要求 `registerFlag` 的名称参数为字符串，不证明字符串内容正确；下一步由用户恢复 `GUARD_FLAG` 原字符串，暂不运行检查。
      - 用户已恢复类型错误（2026-08-12）：用户把 `GUARD_FLAG` 恢复为 `pi-study-guard`；只读核对确认 `index.ts` 已回到基线，Git 对该文件没有差异。下一步由用户运行 `npm run typecheck`，验证恢复后的静态检查重新通过。
      - 用户恢复后类型 Green 已验证（2026-08-12）：用户亲自运行 `npm run typecheck`，`tsc --noEmit` 正常结束且没有错误，证明 `TS2345` 随数字类型错误的移除而消失。下一步进入内容 Red：保持第 3 行 `GUARD_FLAG` 和测试期望不变，只临时把第 6 行调用实参改为字符串 `wrong-flag`。
      - 用户内容错误已制造、原理讲解中（2026-08-12）：只读核对确认用户保持第 3 行 `GUARD_FLAG = "pi-study-guard"` 不变，并把第 6 行实际登记实参改为 `wrong-flag`。用户正在确认 Fake 测试期望值的来源；暂不运行检查，先串联测试中的常量导入、Fake 记录和深比较断言。
      - Fake 测试教学注释待确认（2026-08-12）：按 `teaching-comments` 规则拟在测试中补三处简短机制注释，分别解释工厂与共享期望常量的导入、Fake API 只记录实际注册参数、深比较将实际记录与保持不变的期望值对照；不逐行翻译语法。当前内容错误仍保留，尚未修改测试文件或运行检查，待用户明确确认。
      - Fake 测试教学注释已补充、待动态实验（2026-08-12）：经用户明确确认，在测试文件三处加入英文机制注释，解释实际工厂与稳定期望常量的分工、Fake API 只记录调用参数而不启动真实 Pi、深比较为何能发现工厂硬编码的不同名称。当前 `wrong-flag` 内容错误保持不变；只执行注释与差异静态检查，不代用户运行类型检查或 Fake 测试。
      - 教学注释语言已纠正（2026-08-12）：用户指出教学注释应使用中文；已将上述三处英文注释原位改为简体中文，含义和代码行为不变，`wrong-flag` 实验变量仍保留。下一步仍由用户亲自运行类型检查。
      - 用户内容错误下类型 Green 已验证（2026-08-12）：用户在第 6 行保持 `wrong-flag` 时亲自运行 `npm run typecheck`，`tsc --noEmit` 正常结束且无错误。该结果证明静态类型检查接受类型合法的字符串，不判断其是否等于期望名称；下一步由用户单独运行 Fake 测试，验证行为断言是否拦截内容错误。
      - 用户 Fake 内容 Red 已验证（2026-08-12）：用户亲自运行 `npm test`，测试 `0/1` 通过、`1` 个失败；`deepStrictEqual` 的异常对象明确显示实际登记名为 `wrong-flag`、期望名为 `pi-study-guard`，其他 options 内容一致。该结果直接证明 Fake 测试能拦截类型合法但行为内容错误的注册名称，不证明真实 Pi 加载；下一步由用户恢复第 6 行为 `GUARD_FLAG`，保留中文教学注释。
      - 用户已恢复内容错误（2026-08-12）：只读核对确认第 6 行已恢复为 `registerFlag(GUARD_FLAG, ...)`，Git 对 `index.ts` 没有差异；测试文件仅保留经确认加入的三处中文教学注释。下一步由用户亲自运行最终 `npm run check`，验证两层检查共同回到 Green。
      - 用户最终 Green 已验证、待理解验收（2026-08-12）：用户亲自运行 `npm run check`，`tsc --noEmit` 正常结束，Fake 测试 `1/1` 通过；只读复核确认 `index.ts` 无 Git 差异，测试文件仅保留三处中文教学注释，`git diff --check` 通过。至此用户已亲自完成基线 Green、类型 Red、恢复后类型 Green、内容错误下类型 Green、Fake 内容 Red 和最终 Green；待用户总结两层检查的职责与证明边界后验收 E 组。
      - E 组理解验收部分通过（2026-08-12）：用户已说明参数要求字符串而传入数字时检查会报错，并准确说明 Fake 测试比较实际值与期望值；需将第一点术语从“语法检查”收紧为“静态类型检查”。第三项证明边界尚未回答：两层通过仍不证明真实 Pi 已自动发现、加载并执行该 Extension 工厂。E 组暂不验收完成。
      - 证明边界已回答、测试 Oracle 补课中（2026-08-12）：用户已说明两层检查仍不能证明 Extension 在真实 Pi 中被执行；术语收紧为不能证明真实 Pi 已发现、加载并调用 Extension 工厂。用户进一步追问 Fake 期望值为何正确。只读检索确认外部约定位于实验 `README.md`：主 Extension 只注册 `--pi-study-guard`；当前测试却从被测 `index.ts` 导入 `GUARD_FLAG` 作为期望值，只能发现工厂实参与该常量不一致，不能独立证明常量本身符合外部需求。E 组暂不验收，先讲清测试判定依据（Oracle）边界。
      - E 组学习验收完成（2026-08-12）：用户确认 Fake 测试用于检查 Extension 编写行为，期望值由开发者依据需求契约确定；已理解 Fake 本身不产生正确答案。用户同时掌握 `tsc --noEmit` 检查静态类型契约、Fake 比较实际登记内容与预期内容、两层通过仍不证明真实 Pi 已发现和加载 Extension；并识别出当前测试从被测实现导入 `GUARD_FLAG`，只能验证调用实参与实现常量一致，不能独立证明常量符合 README 外部约定。E 组验收完成，5.1 仍未完成，下一步进入 F 组。
    - F 组（热重载）：在无模型请求的 TUI 中加载一个自动发现 Extension；外部把版本 marker 从 V1 改为 V2，未执行 `/reload` 时不产生 V2 记录，执行一次 `/reload` 后产生 V2 记录，最后恢复实验文件并退出。
      - 实验卡已讲解、待确认（2026-08-12）：完整场景是同一真实 Pi TUI 启动时加载自动发现的 `reload.ts` 并向受限临时 marker 写入 `V1`，保持 Pi 运行只把隔离副本改为 `V2`，先观察单纯保存文件不会新增 `V2`，再输入一次 `/reload`，预期 marker 依次出现 `V1`、`V2`。固定 Pi `0.84.1`、隔离项目与 `PI_CODING_AGENT_DIR`、同一 TUI、同一 marker 和无 Model 请求；唯一行为变量是是否执行 `/reload`。本机官方 `docs/extensions.md` 明确自动发现目录中的 Extension 可用 `/reload` 热重载，源码确认重载会结束旧 Extension runtime、重新加载资源并构建新 runtime。该实验只证明本机版本、自动发现来源和本次显式重载链，不证明 Settings/CLI 来源、任意文件保存自动重载或 Extension 功能正确。待用户确认后逐步手工执行，不使用黑盒脚本。
      - 用户手工实验已确认开始（2026-08-12）：保持逐步教学，不使用封装脚本；第一步仅由用户运行 `mktemp -d` 创建唯一隔离实验根目录并回传实际路径，尚不复制探针、不创建 marker、不启动 Pi。
      - 隔离根目录已创建（2026-08-12）：用户亲自运行 `mktemp -d /tmp/pi-study-5-1-f.XXXXXX`，实际得到 `/tmp/pi-study-5-1-f.9jqNB0`。该证据只证明唯一临时根目录已创建；下一步只建立其中的项目自动发现目录与隔离 `PI_CODING_AGENT_DIR`，仍不复制探针、不创建 marker、不启动 Pi。
      - 隔离目录结构已验证、概念讲解中（2026-08-12）：用户已执行目录创建；只读核对确认 `/tmp/pi-study-5-1-f.9jqNB0/project/.pi/extensions/` 与 `/tmp/pi-study-5-1-f.9jqNB0/agent/` 均存在。前者是项目自动发现入口，后者目前仍是普通空目录，只有稍后启动 Pi 时通过环境变量 `PI_CODING_AGENT_DIR` 显式指向它，才会成为该进程的全局配置根目录。用户正在确认这层含义，确认前不复制探针、不创建 marker、不启动 Pi。
      - `PI_CODING_AGENT_DIR` 含义已确认（2026-08-12）：用户已理解 `agent/` 本身只是普通空目录，只有作为环境变量值传给某次 Pi 启动后，才替代该进程默认的 `~/.pi/agent/`；该隔离不持久影响以后正常启动，也不改变进程的操作系统权限。下一步仅由用户复制已审查的 V1 探针到项目自动发现目录，复制动作不执行探针。
      - V1 探针复制已验证（2026-08-12）：用户已把探针复制到 `/tmp/pi-study-5-1-f.9jqNB0/project/.pi/extensions/reload.ts`；只读核对确认源文件与副本 SHA-256 均为 `4793a579d82d41e7f1606815a5e6b5b435235c8252eb1f88b6b7e6f8a1e2146b`，副本版本为 `V1`，marker 尚不存在且隔离 `agent/` 仍为空。该证据证明复制内容正确且复制未执行工厂；下一步只准备第二个终端进入隔离项目目录，暂不启动 Pi。
      - 第二个终端已就位（2026-08-12）：用户在新终端进入 `/tmp/pi-study-5-1-f.9jqNB0/project`，`pwd` 返回该精确路径；普通 `ls` 无输出是因为当前只有隐藏目录 `.pi/`。参数复核发现除 Pi 专用的 `PI_CODING_AGENT_DIR` 外，还应为可能读取通用用户目录的底层库设置独立 `HOME`。下一步只创建 `/tmp/pi-study-5-1-f.9jqNB0/home/`，暂不启动 Pi。
      - 独立 `HOME` 已准备（2026-08-12）：用户已创建 `/tmp/pi-study-5-1-f.9jqNB0/home/`；只读核对确认目录存在且为空，marker 仍不存在，隔离探针仍为 `V1` 且 SHA-256 未变。启动前基线未被污染；下一步由用户在第二个终端用隔离 `HOME`、`PI_CODING_AGENT_DIR` 和 marker 环境变量启动 Pi，启动后不输入消息、不执行 `/reload`。
      - V1 启动页已验证（2026-08-12）：用户在第二个终端以隔离 `HOME`、`PI_CODING_AGENT_DIR`、marker 路径和资源关闭参数启动 Pi `0.84.1`；截图中 `[Extensions]` 列出项目 `reload.ts`，TUI 保持运行，未发送普通消息且未执行 `/reload`。`No models available` 符合空认证目录预期，`fd not found. Offline mode enabled, skipping download.` 符合离线启动边界，均不妨碍本实验。该截图证明启动和资源列出，不单独证明工厂已经写入 `V1`；下一步由用户在第一个终端读取 marker，第二个终端保持不动。
      - 启动时 V1 工厂执行已验证（2026-08-12）：用户在第一个终端读取 marker，实际只输出一行 `V1`；只读复核确认文件总行数为 `1`，同时隔离探针仍声明 `RELOAD_MARKER_VERSION = "V1"`。结合启动前 marker 不存在、探针工厂执行时追加当前版本，可确认本次真实 Pi 启动已自动发现、加载并调用 V1 工厂一次；尚未产生任何热重载证据。下一步只把隔离副本的版本常量改为 `V2`，保持原 Pi 运行且不执行 `/reload`。
      - 隔离副本 V2 已准备（2026-08-12）：用户在第一个终端运行精确替换命令且无错误输出；只读核对确认隔离副本现声明 `RELOAD_MARKER_VERSION = "V2"`，与学习仓库原始 V1 探针相比仅该行不同。第二个终端中的原 Pi 保持运行且尚未执行 `/reload`。下一步由用户再次读取 marker；若仍只有一行 `V1`，才证明单纯保存源文件没有自动重新调用工厂。
      - 未重载对照已验证（2026-08-12）：隔离副本已经是 `V2` 且原 Pi 仍在运行、尚未执行 `/reload` 时，用户再次读取 marker，实际仍只输出一行 `V1`；只读复核也确认 marker 总行数仍为 `1`。该对照证明本次单纯保存 Extension 文件没有自动重新调用工厂；它尚不证明 `/reload` 能读取 V2。下一步只在第二个终端的同一 Pi 进程中显式执行一次 `/reload`。
      - `/reload` 命令路径已验证、V2 待确认（2026-08-12）：用户在第二个终端的原 Pi 中执行一次 `/reload`；截图显示 `Reloaded keybindings, extensions, skills, prompts, themes, and context files`，资源区仍列出 `reload.ts`，且隔离副本静态核对仍为 `V2`。该界面证明显式重载流程已完成，不单独证明 V2 工厂被调用；下一步由用户在第一个终端读取 marker，预期按顺序为 `V1`、`V2`。
      - 热重载 V2 已验证、待退出与清理（2026-08-12）：用户在显式 `/reload` 后读取 marker，实际按顺序输出 `V1`、`V2`；只读复核确认 marker 恰有 `2` 行，隔离副本当前仍声明 `RELOAD_MARKER_VERSION = "V2"`。结合启动后的 `V1`、只保存文件时仍只有 `V1`、显式 `/reload` 后新增 `V2`，可确认本次同一 Pi 进程重新加载了自动发现的项目 Extension 并调用 V2 工厂。该证据不证明保存时自动重载，不覆盖 Settings/CLI 来源，也不证明 Extension 功能正确或安全。F 组核心行为已通过，5.1 仍为进行中；下一步仅退出隔离 Pi，随后恢复实验副本并清理临时目录。
      - 隔离 Pi 退出已验证、待恢复（2026-08-12）：用户确认已退出第二个终端中的 Pi；只读占用检查只发现普通 `zsh` 的工作目录仍位于隔离 `project/`，未发现 Pi 进程，marker 仍完整保留两行 `V1`、`V2`。该证据证明本次隔离 Pi 已停止，但普通终端尚未离开临时目录；下一步仅用仓库中的原始 V1 探针覆盖临时 V2 副本并验证内容恢复，暂不删除实验目录。
      - 临时探针恢复已验证、待清理（2026-08-12）：用户用仓库中的原始探针覆盖临时 V2 副本；只读核对确认两份文件均声明 `RELOAD_MARKER_VERSION = "V1"`，逐字节比较相同，SHA-256 均为 `4793a579d82d41e7f1606815a5e6b5b435235c8252eb1f88b6b7e6f8a1e2146b`。实验文件已恢复，marker 的 `V1`、`V2` 证据仍保留；下一步仅让第二个终端离开临时项目目录，再对整个临时实验目录做可恢复清理。
      - 清理前占用检查已通过（2026-08-12）：用户已让第二个终端离开临时项目目录；对 `/tmp/pi-study-5-1-f.9jqNB0` 的只读 `lsof` 检查无输出，未发现进程占用，实验目录与两行 `V1`、`V2` marker 仍存在，废纸篓目标 `~/.Trash/pi-study-5-1-f.9jqNB0` 尚不存在。下一步仅把该明确临时目录整体移入废纸篓，并验证原路径消失、废纸篓副本存在。
      - 可恢复清理已验证（2026-08-12）：用户把 `/tmp/pi-study-5-1-f.9jqNB0` 整体移入废纸篓；只读检查确认原路径不存在、`~/.Trash/pi-study-5-1-f.9jqNB0` 存在，且未发现目录占用。macOS 隐私控制拒绝终端读取废纸篓内部文件，因此不能从移动后状态再次证明内部内容；移动前已经直接验证临时探针恢复为原始 V1、marker 为两行 `V1`、`V2`。F 组实验与可恢复清理完成，下一步核对 5.1 的 A-F 总体验收条件并进行学习者综合复述。
      - 5.1 综合复述第一次结果（部分通过，2026-08-12）：用户已准确说明 `--approve` 加载 CLI、全局自动发现、项目自动发现和项目 Settings，`--no-approve` 只保留 CLI 与全局来源；同一真实文件经多个入口出现时保留先出现项；Flag 注册不能证明后续处理已触发。需收紧两点：`tsc` 本轮证明静态类型契约，不是笼统“语法检查”；真实 Pi 只证明本次发现、加载、调用工厂和注册链，不扩大为 Handler/功能均已执行。热重载表述中“未 `/reload` 时文件被刷新到内存”说反，正确边界是磁盘文件已变为 V2，但当前 Runtime 没有重新加载或再次调用工厂，故 marker 仍只有 V1；显式 `/reload` 后才重新加载并调用 V2 工厂。下一步只针对这两点重试。
      - 热重载边界纠正理解已确认（2026-08-12）：经“磁盘文件与当前 Runtime 中已加载实例”完整时间线及 Java/Spring 类比讲解后，用户确认理解：保存 V2 只改变磁盘文件，不会自动更新当前 Pi 已加载的 Extension，也不会再次执行工厂；显式 `/reload` 才重新读取 V2、创建新实例并调用工厂，因此 marker 才从只有 `V1` 变为依次包含 `V1`、`V2`。综合复述第 5 项通过，下一步只重试第 3 项证据分层。
      - 三层证据复述第二次结果（部分通过，2026-08-12）：用户已正确说明 `tsc --noEmit` 检查静态类型契约，Fake 测试比较预期与实际，真实 Pi 实验验证 Extension 是否真实执行。还需把后两项对象收紧为“Fake 比较直接调用工厂时实际登记内容与预期契约”“真实 Pi 证明本次发现、加载并调用工厂”，并补充三层各自不能证明的边界；未完成前不勾选 5.1。
      - 三层证据反向边界第三次结果（部分通过，2026-08-12）：用户已准确说明 `tsc --noEmit` 不能证明内容是否正确，Fake 测试不能证明 Pi 是否真实加载；对“真实 Pi 已发现、加载并执行工厂仍不能证明什么”明确表示不知道，因此停止提问并转入完整场景讲解，不把第三项计为理解验收。下一步用本次 Flag/marker 实验解释工厂执行证据与 Handler 触发、功能正确、安全之间的边界。
      - 工厂与 Handler 教学例子已沉淀、待理解确认（2026-08-12）：应用户要求，`docs/learning/06-extensions.md` 已在通用生命周期中加入完整 `/guard` 场景，以 `reload.ts` 的 V1 marker 证明工厂执行，用 Spring Controller 注册/请求类比区分能力登记与运行时调用，并明确工厂通常每轮加载一次而 Handler 可执行零次或多次。文档更新本身不替代用户理解验收；下一步只确认“真实 Pi 执行工厂仍不能证明 Handler/Executor 已触发、功能正确或运行安全”的边界。
      - 最终理解验收已通过（2026-08-12）：用户确认理解工厂执行与 Handler 触发属于不同阶段：工厂负责初始化与登记能力，匹配的命令、事件或 Tool Call 实际发生后 Pi 才调用 Handler/Executor；因此真实 Pi 的加载与工厂 marker 仍不能证明后续入口已触发、功能正确或运行安全。结合此前来源、Trust、去重、类型/Fake 分层和热重载复述，5.1 验收完成。
    - 通过标准：A-F 的实际观察全部符合预期，类型检查和最小测试通过，实验目录可恢复，且不读取真实用户凭据、不发送普通 Model/Provider 请求、不留下持久 Session 或后台资源，也不产生非实验文件改动。Pi 仍可能初始化模型子系统，并访问隔离目录中的空认证存储；这不等于完全没有接触认证相关文件。
    - 证据边界：帮助中的 Flag 证明模块工厂完成并注册了 Flag，不证明 Handler 已触发；Fake 测试不证明真实 Pi 加载；`tsc` 只证明纳入当前配置的静态契约；F 组只验收官方承诺的自动发现热重载，不把 Settings 或 CLI 的当前源码重载行为当作稳定契约；不在 5.1 验证 Tool/Command/Handler 同名冲突、第三方运行依赖或 Package 发布。
  - 实验材料（已准备并安装，2026-08-11）：`.pi/extensions/pi-study-guard/` 已建立最小工厂、严格 `tsconfig.json`、Fake API 测试、四类来源探针和受限临时文件重载探针；夹具位于自动发现深度之外，主工厂及全部探针只注册唯一 Flag，不注册业务能力、不联网、不启动后台资源。
    - 安装验证：经用户单独确认，在实验目录执行 `npm ci --ignore-scripts --omit=optional --audit=false --fund=false`，退出码为 0，共落盘 136 个包；三个根开发依赖的实际版本分别为 Pi `0.84.1`、TypeScript `5.9.3` 和 `@types/node` `24.12.4`，`npm ls --all` 退出码为 0，11 个 optional 包均未落盘，安装前后锁文件 SHA-256 均为 `7fac7407f26d229af028df772ed94f40d260abcaba45a275bffd2e2f889e48aa`。命令中的 `--ignore-scripts` 只证明本次要求 npm 跳过生命周期脚本，不证明依赖本身安全。
    - 安装后初始校验：`npm test` 的 Fake API 测试为 1/1 通过；正式类型检查最初被 Pi 传递依赖的 `.d.ts` 错误挡住，包括 `undici-types`/`@modelcontextprotocol/sdk` 模块解析和 Pi AI 的 JSON import 声明。临时执行 `tsc --noEmit --skipLibCheck` 退出码为 0，只作为配置修正前的定位证据。
    - E 组（已验证）：经用户理解确认后，`tsconfig.json` 正式启用 `skipLibCheck`，保持项目源码不变运行 `npm run typecheck` 退出码为 0；随后临时把本项目 `GUARD_FLAG` 声明为字符串却赋值数字，类型检查退出码为 2，并在 `index.ts:3` 报告 `TS2322`，证明本项目源码仍被检查；再把 `registerFlag` 的字符串名称临时改为数字，类型检查退出码为 2，并在 `index.ts:6` 报告 `TS2345`，证明 Pi 的公开 `name: string` 契约仍用于检查项目调用。两次恢复后 `index.ts` SHA-256 均与实验前同为 `17f554db1983584590d91185a43ecc1c8c9071e12d44039e0b2d9227fa037923`；最终 `npm run check` 退出码为 0，Fake API 测试 1/1 通过。该证据不证明真实 Pi 加载或 Handler 行为。
    - 锁文件审计：使用 `--package-lock-only --ignore-scripts --audit=false --fund=false --workspaces=false` 生成 npm lockfile v3，未创建 `node_modules`、未执行生命周期脚本；锁图 147 个包，根开发依赖精确固定为 Pi `0.84.1`、TypeScript `5.9.3`、`@types/node` `24.12.4`，所有 `resolved` 均为无凭据的 npm 官方 HTTPS Registry，当前 Node `25.2.1` 满足全部声明的 Engine。
    - 依赖风险边界：锁图标记 `@google/genai@1.52.0` 与 `protobufjs@7.6.5` 有安装脚本，后续安装继续统一禁用；11 个可选包中含平台二进制，后续安装省略 optional；Pi 上游 shrinkwrap 中 6 个 `@earendil-works` 内部包条目有固定版本和官方 HTTPS 地址但没有 `integrity`，因此不能声称锁图每个传递包都有完整性摘要。
  - 核心内容：默认导出工厂、同步/异步初始化、TypeScript 直接加载、官方可用 import、类型检查与依赖锁定；区分全局目录、受 Project Trust 控制的项目 `.pi/extensions/`、Settings 附加路径和 `-e/--extension` 临时路径。
  - 贯穿实验：使用隔离 `PI_CODING_AGENT_DIR` 和唯一来源标记，对照全局、项目可信/不可信、Settings 附加路径和 CLI 临时四类加载；建立 `pi-study-guard` 的类型检查和测试入口，暂不实现业务能力。
  - 验收与边界：真实启动结果能区分来源、Trust 正反路径、临时加载和自动发现路径的 `/reload`，加载顺序与规范路径去重由本机行为和源码共同核对。加载标记只证明模块工厂完成并登记了实验能力，不证明 Handler 已触发、能力正确或运行安全。
- [x] 5.2 理解 Extension 生命周期及启动、会话、Agent、模型、工具、Shell 和输入事件。
  - 进行中（2026-08-12）：5.1 已完成，开始第一遍系统地图；先用一个从 Pi 启动、用户输入、Agent/Model、Tool/User Bash 到 Session 关闭的完整场景解释对象与顺序，用户确认无陌生对象后再设计脱敏事件追踪实验。
  - 生命周期流程图已补充（2026-08-12）：`docs/learning/06-extensions.md` 已加入公共启动阶段及五类入口分支：普通输入进入 Input/Agent/Turn/Tool 循环，Extension Command 跳过 `input`，User Bash 不走 Model `bash` Tool，Model 选择独立触发 `model_select`，退出/切换/重载触发 `session_shutdown`；图中另以虚线标出 `message_*` 穿插于多类消息生命周期，不把它误画成单次主线事件。
  - 系统地图理解已确认（2026-08-12）：生命周期图已在对话中以真实渲染 PNG 展示；用户确认能够理解普通输入、Extension Command、User Bash、Model 切换、Shutdown、Agent/Turn/Tool 循环和穿插的 `message_*` 主干。该确认只完成第一遍系统地图，不证明真实事件顺序；下一步设计只记录事件名、有限 reason/source、Tool 名和序号的脱敏追踪实验卡，用户确认后才修改实验 Extension。
  - 脱敏事件追踪实验卡已准备、待开发确认（2026-08-12）：拟在 `pi-study-guard` 增加仅由实验环境变量开启的追踪模式，日志只记录递增序号、事件名，以及有限的 `reason`、`source`、Tool 名和消息角色，不记录 Prompt、Shell 命令、Tool 参数/结果、文件路径、Model 标识或 Session 内容。A 组验证工厂、`session_start`、Extension Command 与 `session_shutdown`；B 组验证 `user_bash` 独立于 Model `bash` Tool；C 组验证 `model_select`；D 组经单独 Provider 授权后验证普通 `input`、Agent/Turn/Message、一次只读 `read` Tool、`agent_end` 与 `agent_settled`。各组只按必要的局部先后关系验收，不把并行 Tool、重试或未观察事件推断为全局固定顺序。用户确认前不修改 Extension；D 组不自动发起或重试 Provider 请求。
  - 追踪模式开发已确认、进行中（2026-08-12）：用户已明确确认按上述实验卡开始修改代码。当前实现范围限定为环境变量门禁、操作系统临时目录固定文件名、递增序号、白名单字段、一个 Extension Command 及 A-D 所需事件 Handler；先完成类型检查和 Fake 测试，不启动真实 Pi，不发起 Provider 请求。
  - 追踪模式静态与 Fake 验证已通过（2026-08-12）：新增 `lifecycle-trace.ts` 并由主工厂在 `PI_STUDY_LIFECYCLE_TRACE` 存在时启用；默认模式仍只注册原 Flag。追踪只写固定临时文件名、序号、事件名及白名单字段，并拒绝错误文件名、操作系统临时目录外路径和符号链接；Fake 用带敏感占位内容的事件验证日志不包含 Command 参数、Shell 命令、Prompt、路径、Tool 结果或消息内容。首次类型检查发现并修复 Pi 包未导出 `AgentMessage` 的 `TS2305`；最终 `npm run check` 退出码为 0，类型检查通过且 3/3 测试通过，`git diff --check` 退出码为 0。该结果仍不证明真实 Pi 已加载工厂或派发任何事件，5.2 不勾选。
  - A 组真实 Pi 实验卡已确认、待准备环境（2026-08-12）：用户已确认在不发送普通 Prompt、不调用 Model/Provider 的条件下，验证 `factory -> session_start -> /pi-study-trace Command Handler -> session_shutdown` 的必要先后关系。固定使用临时项目、隔离 `HOME` 与 `PI_CODING_AGENT_DIR`、CLI `-e` 显式来源、`--offline` 和资源关闭参数；日志只记录事件序号、事件名及白名单元数据。下一步仅由用户创建唯一隔离根目录，尚未启动 Pi，未取得真实事件证据。
  - A 组隔离根目录已创建（2026-08-12）：用户亲自运行 `mktemp -d /tmp/pi-study-5-2-a.XXXXXX`，实际得到 `/tmp/pi-study-5-2-a.SitNjz`；只读核对确认它是权限 `0700` 的空目录。该证据只证明唯一临时根目录已创建，不证明 Extension 加载或事件派发；下一步只创建三个隔离子目录。
  - A 组隔离目录与职责已确认（2026-08-12）：只读核对确认 `project`、`home`、`agent` 三个子目录均存在且为空；子目录权限为 `0755`，父目录权限为 `0700`。用户已理解 `HOME` 是子进程及通用依赖查找用户级配置的通用主目录，`PI_CODING_AGENT_DIR` 是 Pi 专用配置根目录；同时设置用于隔离两类配置来源，但都不降低当前 macOS 用户的系统权限。下一步只进入空项目并核对工作目录。
  - A 组项目工作目录已确认（2026-08-12）：用户在终端进入 `/tmp/pi-study-5-2-a.SitNjz/project`，`pwd` 返回同一路径；只读基线核对同时确认 `project`、`home`、`agent` 仍为空、固定追踪文件尚不存在，实验 Extension 入口存在。该证据只证明启动位置与启动前状态正确，不证明 Pi 已启动、Extension 工厂已执行或任何 Handler 已触发；下一步由用户亲自检查追踪文件不存在。
  - A 组启动前追踪基线已确认（2026-08-12）：用户运行 `test ! -e /tmp/pi-study-5-2-a.SitNjz/pi-study-lifecycle.log && echo TRACE_ABSENT`，终端实际输出 `TRACE_ABSENT`；只读复核结果一致。该证据证明本次启动前固定追踪文件不存在，后续新日志不会混入该路径的旧记录；它仍不证明 Pi 已启动或 Extension 已执行。下一步只启动隔离 Pi，并停留在启动页观察。
  - A 组隔离 Pi 启动页已验证（2026-08-12）：用户按固定命令启动 Pi `0.84.1`，截图显示当前目录为隔离 `project`、`[Extensions]` 只列出 CLI `-e` 指定的 `pi-study-guard`，界面停留在空输入框且提示无可用 Model；期间尚未输入 Extension Command 或普通 Prompt。`fd not found. Offline mode enabled, skipping download.` 是离线启动提示，不阻塞本实验。该截图证明 Pi 进程与目标 Extension 已进入本次启动界面，但不单独证明追踪工厂写入或 `session_start` Handler 已执行；下一步必须从另一终端读取首次日志。
  - A 组启动事件首次日志已验证（2026-08-12）：用户在保持 Pi 启动页不动时从另一终端读取固定日志，实际仅有 `0001 factory` 和 `0002 session_start reason=startup`。结合启动前 `TRACE_ABSENT`，这直接证明本次 Pi 进程先执行 Extension 工厂追踪逻辑，随后派发启动型 `session_start` 并触发其 Handler；当前没有 `command`，与尚未输入 `/pi-study-trace` 一致。该证据不证明 Command Handler 或 `session_shutdown` 已执行，也不把本次局部顺序扩大为所有入口的全局顺序。
  - Command、Tool 与事件 Handler 对照已补充、待理解确认（2026-08-12）：应用户要求，`docs/learning/06-extensions.md` 新增完整 `security-guard` 场景、三条触发链流程图、逐项对照、事件观察/拦截/修改边界，以及当前 `pi-study-guard` 的真实日志映射；明确 Command 主要由用户点名、Tool 通常由 Model 选择并由 Executor 执行、事件 Handler 由 Pi Runtime 在对应时刻自动派发。对本机 `0.84.1` 类型声明计数确认共有 33 个 `on` 事件入口，`mmdc` 对全文 6 张 Mermaid 图解析通过，`git diff --check` 通过。文档与图通过不替代用户理解验收，也不证明尚未触发的 Command Handler；下一步先展示流程图并确认理解，再恢复 A 组实验。
  - Command、Tool 与事件 Handler 教学已恢复实验（2026-08-12）：用户再次确认流程图是否已写入文档并要求继续学习；核对确认对应小节与 Mermaid 源码已保存在 `docs/learning/06-extensions.md`。该确认允许恢复 A 组操作，但不代替 5.2 最终复述验收；下一步只触发 `/pi-study-trace` 并观察界面通知，不发送普通 Prompt。
  - A 组 Command 界面通知已验证（2026-08-12）：用户在当前 Pi 输入 `/pi-study-trace` 后，截图实际显示 `Lifecycle command marker recorded`；这是该 Command Handler 在 `ctx.hasUI` 为真时发出的固定通知，直接证明 Pi 已按 Command 名派发并执行到 Handler 的 UI 分支。当前无可用 Model，且该入口不需要普通 Prompt，因此截图中没有 Agent/Model 响应。该界面证据尚未核对日志中的 `command` 序号，也不证明 `session_shutdown`；下一步保持 Pi 运行并从另一终端读取日志。
  - A 组 Command 日志已验证（2026-08-12）：用户保持 Pi 运行并从另一终端读取日志，实际得到 `0001 factory`、`0002 session_start reason=startup`、`0003 command name=pi-study-trace`。结合此前界面通知，这直接证明用户触发后 Pi 调用了对应 Command Handler，且本次 `command` 位于工厂和启动事件之后；日志没有 `input`、Agent 或 Turn 事件，与本次 Extension Command 未进入普通 Prompt/Agent 链一致。该缺失只限本次观察，不能推出所有 Command 永远不会间接启动 Agent；下一步正常退出 Pi 并验证 `session_shutdown`。
  - A 组 Pi 正常退出已验证、关闭事件待验证（2026-08-12）：用户执行 `/quit` 后的截图显示 Pi TUI 已结束，并重新出现隔离 `project` 目录下的 Shell 提示符。该证据证明当前 Pi 进程已正常返回 Shell，但不能单独证明 `session_shutdown` Handler 在退出路径中执行；下一步只读取最终日志并核对最后一条事件及 `reason`。
  - A 组完整事件链已验证（2026-08-12）：用户在 Pi 正常退出后读取固定日志，实际得到 `0001 factory`、`0002 session_start reason=startup`、`0003 command name=pi-study-trace`、`0004 session_shutdown reason=quit`。结合启动前日志不存在、启动页、Command 界面通知及退出截图，这直接证明当前 Pi `0.84.1` 的本次隔离进程中，Extension 工厂追踪逻辑、启动 Handler、指定 Command Handler 和正常退出 Handler 按该顺序执行；`session_shutdown` 收到的退出原因是 `quit`。本组没有普通 Prompt、Model、Provider、自定义 Tool 或用户 `!` Shell，因此不提供这些链路的执行证据；本次顺序也不能扩大为所有入口的全局固定顺序。A 组实验完成，但 5.2 仍需 B-D 组及最终复述验收，暂不勾选。
  - A 组复述已按用户要求跳过并进入 B 组（2026-08-12）：用户明确表示不回答 A 组复述、直接继续 B 组；因此 A 组真实事件证据保留，但不把未进行的复述记为学习验收，也不将 5.2 勾选完成。
  - B 组 `user_bash` 实验卡已准备（2026-08-12）：使用新的临时根目录、隔离 `HOME`/`PI_CODING_AGENT_DIR`、CLI `-e`、`--offline`、`--no-session`、`--no-tools` 及关闭其他资源的固定条件；启动后只执行一次无副作用的用户 `!!` Shell。预期日志由 `factory -> session_start` 增加一个 `user_bash`，TUI 同时出现固定输出，且没有 `input`、Agent、Turn、`tool_call` 或 `tool_result`。这可证明当前版本本次用户 Shell 入口在 Model Tool 不可用时仍派发 `user_bash`，不能证明 Model `bash` Tool 的事件链、命令安全性或操作系统隔离。下一步只创建 B 组唯一隔离根目录。
  - B 组隔离根目录已创建（2026-08-12）：用户亲自运行 `mktemp -d /tmp/pi-study-5-2-b.XXXXXX`，实际得到 `/tmp/pi-study-5-2-b.b8JubJ`；只读核对确认它是权限 `0700` 的空目录。该证据只证明新的唯一临时根目录已创建，不证明 Extension 加载、`user_bash` 派发或 Shell 执行；下一步只创建三个隔离子目录。
  - B 组隔离子目录已验证（2026-08-12）：用户已创建 `/tmp/pi-study-5-2-b.b8JubJ/project`、`home` 和 `agent`；只读核对确认三者均为权限 `0755` 的空目录，父目录仍提供 `0700` 边界。该证据只证明 B 组的工作目录、通用用户目录和 Pi 专用配置目录已经分开，不证明环境变量已传给 Pi，也不改变当前 macOS 用户的系统权限；下一步只进入空项目并核对工作目录。
  - B 组项目工作目录与机器侧启动前基线已核对（2026-08-12）：用户在终端进入 `/tmp/pi-study-5-2-b.b8JubJ/project`，`pwd` 返回同一路径；只读复核确认 `project`、`home`、`agent` 仍为空，固定追踪文件尚不存在，实验 Extension 入口存在。该证据只证明启动位置和机器侧启动前状态正确，不证明 Pi 已启动、Extension 工厂已执行、`user_bash` 已派发或 Shell 已执行；下一步由用户亲自确认追踪文件不存在。
  - B 组启动前追踪基线已确认（2026-08-12）：用户运行 `test ! -e /tmp/pi-study-5-2-b.b8JubJ/pi-study-lifecycle.log && echo TRACE_ABSENT`，终端实际输出 `TRACE_ABSENT`；只读复核结果一致。该证据证明本次启动前固定追踪文件不存在，后续若出现日志记录，不会混入该路径下的旧记录；它仍不证明 Pi 已启动、Extension 工厂已执行或 `user_bash` Handler 已触发。下一步只启动隔离 Pi，并停留在启动页观察。
  - B 组隔离 Pi 启动页已验证（2026-08-12）：用户按固定命令启动 Pi `0.84.1`，截图显示当前目录为隔离 `project`、`[Extensions]` 只列出 CLI `-e` 指定的 `pi-study-guard`，界面停留在空输入框且提示无可用 Model；期间尚未输入用户 Shell 或普通 Prompt。`fd not found. Offline mode enabled, skipping download.` 是离线启动提示，不阻塞本实验。该截图证明 Pi 进程与目标 Extension 已进入本次启动界面，但不单独证明追踪工厂写入、`session_start` Handler 或 `user_bash` Handler 已执行；下一步保持 Pi 运行并从另一终端读取首次日志。
  - B 组启动事件首次日志已验证（2026-08-12）：用户在保持 Pi 启动页不动时从另一终端读取固定日志，实际仅有 `0001 factory` 和 `0002 session_start reason=startup`。结合启动前 `TRACE_ABSENT`，这直接证明本次 Pi 进程先执行 Extension 工厂追踪逻辑，随后派发启动型 `session_start` 并触发其 Handler；当前没有 `user_bash`，与尚未输入用户 Shell 一致。该证据不证明用户 Shell 已执行，也不证明 Model `bash` Tool 的事件链；下一步只在 Pi 中触发一次无副作用的 `!!` 用户 Shell 并观察界面输出。
  - B 组 `!!` 用户 Shell 界面输出已验证、事件日志待核对（2026-08-12）：用户在正在运行的 Pi 中输入 `!!printf 'PI_STUDY_USER_BASH_5202\n'`，截图显示 Shell 命令和固定输出 `PI_STUDY_USER_BASH_5202`，随后返回 Pi 空白输入框；本次仍无可用 Model，且启动参数包含 `--no-tools`。该界面证据直接证明用户 Shell 入口已实际执行，说明 `--no-tools` 没有关闭这条入口；截图本身看不到追踪文件，因此尚不能证明 Extension 的 `user_bash` Handler 已派发，也不能证明 Model `bash` Tool 运行。下一步保持 Pi 运行并从另一终端读取事件日志。
  - B 组 `user_bash` 事件链已验证（2026-08-12）：用户在另一终端读取固定日志，实际得到 `0001 factory`、`0002 session_start reason=startup`、`0003 user_bash`，只读复核一致。结合 Pi 界面的固定 Shell 输出，这直接证明当前 Pi `0.84.1` 的本次隔离进程中，用户提交 `!!` 后 Pi 派发并执行了 Extension 的 `user_bash` Handler，随后用户 Shell 实际执行；启动参数中的 `--no-tools` 没有禁用该入口，且本次未产生 `input`、Agent、Turn、`tool_call` 或 `tool_result` 事件。该结果不证明 Model `bash` Tool 被调用、Shell 命令安全或存在操作系统沙箱；下一步执行 `/quit` 正常退出，再单独验证 `session_shutdown`。
  - B 组完整事件链已验证（2026-08-12）：用户在 `/quit` 后读取最终日志，实际得到 `0001 factory`、`0002 session_start reason=startup`、`0003 user_bash`、`0004 session_shutdown reason=quit`；只读复核一致。结合启动前日志不存在、启动页和固定 Shell 输出，这直接证明本次隔离进程依次执行工厂追踪逻辑、启动 Handler、用户 Shell 事件 Handler 和正常退出 Handler，且关闭原因是 `quit`。本组没有普通 Prompt、Model/Provider、自定义 Tool 或 Model `bash` Tool，因此不提供这些链路的执行证据；B 组真实行为完成，但 5.2 仍需 C-D 组及最终复述验收，暂不勾选。
  - C 组 `model_select` 实验卡已准备（2026-08-12）：使用新的临时根目录、隔离 `HOME`/`PI_CODING_AGENT_DIR`、CLI `-e`、`--offline`、`--no-session`、`--no-tools` 及关闭其他资源的固定条件；只用本次 CLI 的字面量假 `--api-key` 让当前 `0.84.1` 已只读确认存在的内置 `openai/gpt-5.6-sol` 与 `openai/gpt-5.6-terra` 进入可切换范围，启动时选定 Sol，只按一次 `⌃P` 切换到 Terra，不发送普通 Prompt。预期 Footer 从 Sol 变为 Terra，日志由 `factory -> session_start` 增加 `model_select source=cycle`，且没有 `input`、Agent、Turn、Message、`tool_call` 或 `tool_result`。源码核对显示 `cycleModel()` 更新当前 Model 后调用 `_emitModelSelect(..., "cycle")`，切换路径本身不发起 Provider 请求；这可证明本次循环切换派发 `model_select`，不能证明 Provider 可用、Model 能回答、完全没有任何网络活动或其他切换入口的 `source` 也为 `cycle`。下一步按完整操作单连续创建隔离环境、启动、切换、核对日志并退出，出现偏差才暂停。
  - 实验交互节奏已调整（2026-08-12）：应用户要求，从 C 组开始每个实验组一次性提供完整操作步骤、逐段预期和停止条件；用户在各检查点符合预期时连续执行，只有出现偏差才暂停回传。计划仍按真实证据随做随回写，不因批量给出步骤而提前记录未执行结果或勾选 5.2。
  - C 组完整事件链已验证，且记录范围参数偏差（2026-08-12）：用户在新隔离根目录 `/tmp/pi-study-5-2-c.whU17K` 启动 Pi `0.84.1`，启动前固定追踪文件不存在；启动后日志先为 `0001 factory`、`0002 session_start reason=startup`。用户未发送普通 Prompt，只按一次 `⌃P`，界面显示切换到 `GPT-5.6 Terra`，日志新增 `0003 model_select source=cycle`；执行 `/quit` 后最终新增 `0004 session_shutdown reason=quit`，只读复核与截图一致。启动时两条 `Warning: No models match pattern` 表明 `--models "openai/gpt-5.6-sol,openai/gpt-5.6-terra"` 没有形成预期的双模型范围；源码顺序显示 `resolveModelScope()` 先基于当时可用目录解析范围，随后才通过 `setRuntimeApiKey()` 应用本次字面量假 Key，因此范围为空，`cycleModel()` 退回全部可用模型后仍成功切到 Terra。该偏差不推翻核心结论：本次循环切换确实派发并执行 `model_select` Handler，`source=cycle`；但本实验不能证明只在两个模型内循环、Provider 可真实响应、所有网络均不存在或其他模型选择入口也使用相同 `source`。C 组真实行为完成，5.2 仍需 D 组和最终复述验收，暂不勾选。
  - D 组普通 Prompt 与一次只读 Tool 实验卡已准备（2026-08-12）：使用新的空临时项目和隔离 `HOME`，仅为真实 Model 调用复用现有 `PI_CODING_AGENT_DIR` 的 Provider/Model 认证配置，不读取、复制或显示凭据；固定使用 CLI `-e`、`--offline`、`--no-session`、`--tools read`，并关闭其他 Extension、Skill、Prompt Template、Theme 和 Context File。项目预置只含一行固定标记的 `target.txt`，用户只提交一次明确要求“恰好调用一次 `read` 并原样回复”的普通 Prompt。预期界面出现一次 `read` Tool、固定 Tool Result 与最终文本；脱敏日志应覆盖 `input -> before_agent_start -> agent_start -> 两个 turn -> User/Assistant/ToolResult 消息 -> tool_call read -> tool_result read -> agent_end -> agent_settled`，退出后再出现 `session_shutdown reason=quit`。若启动资源、Provider、Tool 次数或事件序列偏离预期，停止并回传，不重复发送 Prompt。该实验可证明当前版本本次真实普通输入和只读 Tool 主链，不能证明所有任务都恰好两个 Turn、Model 业务判断恒真、写操作安全或存在操作系统沙箱；D 组尚未执行。
  - D 组真实认证文件权限门禁已满足（2026-08-12）：本轮首次只读检查曾显示 `~/.pi/agent/auth.json` 为 `0644`，随后对同一路径执行最终 `stat` 复核得到 `-rw------- 600`；期间未读取、输出或复制文件内容，也不推断权限由谁或通过何种过程改变。D 组开始时仍应先执行一次只显示权限的即时检查；结果为 `600` 即可继续，其他结果立即停止。
  - D 组完整事件序列已验证，行为结果内容证据缺失（2026-08-12）：用户回传运行中日志 `0001` 至 `0021`，逐行符合 `factory -> session_start -> input(interactive) -> before_agent_start -> agent_start -> 第一 Turn 的 User/Assistant 消息 -> tool_call(read) -> tool_result(read) -> ToolResult 消息 -> turn_end -> 第二 Turn 的 Assistant 消息 -> turn_end -> agent_end -> agent_settled`；正常退出后的第二次日志再追加 `0022 session_shutdown reason=quit`。这直接证明当前 Pi `0.84.1` 的本次普通输入进入 Agent Loop，`read` 名称经过 Tool 前后两个 Extension Handler，结果消息进入上下文后触发第二个 Turn，底层 Agent Run 结束且没有自动后续工作，最后正常关闭 Session。当前只收到脱敏事件日志，未收到界面中的 Tool Result 和最终回答，因此不能仅凭该日志证明 `read` Executor 成功读到目标文件、结果不是错误，或 Model 最终原样输出了固定文本；D 组事件链完成，5.2 只剩模块级综合复述，暂不勾选。
  - 5.2 综合复述第一次未通过（2026-08-12）：用户已正确说明工厂在启动或 `/reload` 时初始化并注册能力、事件 Handler 按事件触发、Command Handler 由用户点名触发，且能解释 Command 与 User Bash 不进入普通 Agent Loop；也正确说明 Turn 数量依任务而定、一个 Turn 可含多个 Tool Call，并用 Spring AOP 类比串联 `tool_call Handler -> Executor -> tool_result Handler`。仍需纠正三点：Tool Executor 不负责把 Tool 喂给 Model，而负责真正执行 Pi 已调度的 Tool；`⌃P` 切换 Model，不是 Thinking Level；`agent_end` 与 `agent_settled` 的边界尚未掌握。日志证据边界由教练补充，不作为本次未答项；下一步只复述上述三项，不重复整套实验或五道题。
  - 5.2 三项针对性重试已通过（2026-08-12）：用户已准确说明 Tool Executor 负责真正读取文件或执行操作；`⌃P` 切换 Model，`⇧Tab` 切换当前 Model 的 Thinking Level；`agent_end` 表示当前底层 Agent Run 结束，`agent_settled` 表示上层任务已无自动后续工作并稳定空闲。结合第一次复述中已通过的工厂、Handler、Command、User Bash、Turn 与 Tool 前后事件边界，5.2 的用户理解门禁通过。
  - 5.2 稳定文档与最终验证已通过（2026-08-12）：`docs/learning/06-extensions.md` 已沉淀 A-D 四类入口对照、D 组 `0001` 至 `0022` 脱敏事件链、两个 Turn 的原因、内置 `read` Executor 与观察 Handler 的分工、快捷键及两层结束语义；索引同步更新。`pi-study-guard` 的 `npm run check` 退出码为 0，类型检查通过且 3/3 Fake 测试通过；Markdown 围栏与本地链接检查输出 `MARKDOWN_CHECK_OK`；`mmdc 11.12.0` 成功解析全文 6 张 Mermaid 图；`git diff --check` 退出码为 0。静态/Fake 与文档检查不替代 A-D 的真实 Pi 日志，脱敏日志仍不能单独证明 D 组 `read` 内容正确或最终回答正确。结合真实运行和最终复述，5.2 验收完成。
  - 核心内容：按“工厂注册 -> Pi 按事件名、Command 名或 Tool 名确定性派发”理解生命周期；重点串联 Session、Input、Agent、Turn、Message、Model、Tool、User Bash 和 Shutdown，不要求死记全部事件。
  - 贯穿实验：增加脱敏事件追踪模式，只记录事件名、reason/source、Tool 名和序号；分别触发 Extension Command、普通输入、一次只读 Tool Call、Model 切换和用户 `!` Shell。
  - 验收与边界：用真实事件序列复述一次完整路径，准确区分 `agent_end`/`agent_settled`、Model Tool/User Shell 和工厂/Handler/Executor。一次序列只证明当前版本与本次入口，未观察到某事件不等于它不会发生。
- [x] 5.3 掌握 Extension Context、模式差异、取消信号和错误处理。
  - 进行中（2026-08-12）：用户已确认理解“Java 项目清理构建目录并重新运行测试”的完整安全门禁场景，能够把运行现场、TUI/Print 分支、协作式取消和三类错误分流与实际用途联系起来；用途与系统地图理解门禁通过。受控实验尚未开始，因此 5.3 不勾选完成。
  - 实验卡已准备（2026-08-12）：首个对照只验证同一 `/pi-study-trace` 在 TUI 与 Print 下的 `mode/hasUI` 和反馈分支；固定 Pi 版本、工作目录、显式 Extension、Command、启动参数和当前选中 Model，唯一变量为运行模式。Command 在 Agent 前被处理，不发起 Provider 请求；这不等于非交互模式无需选中 Model。
  - 开发已确认（2026-08-12）：用户已确认上述实验卡；当前只扩展既有诊断 Command，不实现 5.4 自定义 Tool。Context 快照不得记录真实路径、Session ID、Model 标识或凭据；代码和 Fake 验证通过前不提供真机实验命令。
  - 实现纠偏（2026-08-12）：首版类型检查与 4/4 Fake 虽通过，但独立源码审查发现 Pi 非交互模式会接管普通 `stdout` 并重定向到 `stderr`，因此 Fake 中的 Print `stdout` 断言不能代表真实通道；另确认非交互启动前仍要求已有选中 Model，只是 Extension Command 在模型调用前被处理。当前保留该失败证据，改为无 UI 统一走 `stderr` 后重新验证。
  - 实现与 Fake 验证已通过（2026-08-12）：既有 `/pi-study-trace` 已增加只含枚举、布尔值和 `present/none` 的 Context 快照，不记录真实 cwd、Session ID/路径、Model 标识或凭据；TUI/RPC 走 UI 通知，Print/JSON 走 `stderr` fallback。`npm run check` 退出码为 0，类型检查通过且 4/4 Fake 测试通过；测试覆盖敏感假值不泄漏。Fake 仍不能证明 Pi 的真实输出接管、CLI Command 派发或未调用 Provider。
  - Print 真机烟测已通过（2026-08-12）：在权限 `0700` 的隔离临时目录中，以 `--offline --no-approve --no-session --no-tools`、关闭其他资源并显式加载当前 Extension，真实执行 `pi -p "/pi-study-trace"`；退出码为 0、stdout 为空、stderr 仅含 `mode=print hasUI=false ... route=stderr` 的脱敏快照。追踪只有 `factory -> session_start -> command -> session_shutdown`，没有 Input、Agent、Turn 或 Tool 事件；临时目录随后已删除。该烟测证明本次 Print CLI 派发与输出通道，不替代用户 TUI/Print 对照，也不证明 JSON/RPC。
  - 用户 TUI 分支已部分验证（2026-08-12）：用户回传同一隔离实验的四行日志，依次为 `factory -> session_start -> command -> session_shutdown`；Command 快照为 `mode=tui hasUI=true cwdMatchesProcess=true trusted=false sessionFile=none model=present signal=none route=ui`，且没有 Input、Agent、Turn 或 Tool 事件。该日志直接证明本次 TUI Context 与 UI 路由选择；由于实现先记 trace 再调用 `ui.notify`，用户尚未明确回传界面通知是否实际可见，因此呈现证据待补。
  - 用户 Print 分支已验证（2026-08-13）：用户在与 TUI 分支相同的隔离项目、显式 Extension、Command、离线参数和选中 Model 下运行 Print；进程 `EXIT=0`，stdout 为空，stderr 只有一行 `PI_STUDY_CONTEXT ... mode=print hasUI=false ... route=stderr`，追踪依次为 `factory -> session_start -> command -> session_shutdown`，且 Command 快照的 cwd、Trust、Session、Model 和 Signal 状态均与 TUI 分支一致。该直接证据证明本次 Print 无 UI 时走确定性的 stderr fallback，并与 TUI 日志共同锁定本次唯一观察变量是运行模式；它不替代 TUI 通知实际显示证据，也不证明 JSON/RPC、取消或错误传播。
  - TUI 通知呈现 Red（2026-08-13）：用户明确确认运行 TUI 分支时没有看到以 `PI_STUDY_CONTEXT` 开头的通知。结合 `route=ui` 日志，目前只能证明 Handler 选择了 UI 分支，不能证明 `ui.notify` 已在界面可见；模式对照的 TUI 呈现门禁未通过。下一步只诊断通知呈现时序与可见性，不提前进入取消或错误实验。
  - TUI 呈现诊断已完成（2026-08-13）：Pi `0.84.1` 源码表明 `ctx.ui.notify(message, "info")` 不是弹窗或 Toast，而是在输入框上方的聊天记录区追加一段低对比度灰色状态文字，长文本会自动换行，并异步请求下一帧绘制；它没有定时自动消失，但后续状态、界面重建或绘制前退出可能覆盖或清除。独立真实 Pi 伪终端使用与用户相同的启动消息方式时，屏幕捕获已出现三行 `PI_STUDY_CONTEXT ... route=ui`，随后在同一进程手动执行 Command 也再次写入追踪；复现临时目录已移入废纸篓。该结果证明本机伪终端的当前 TUI 通路可渲染，不替代用户终端的视觉 Green，也不能把此前 Red 归因为用户漏看。下一步不改代码，在界面稳定后手动执行一次并在退出前按准确位置复核。
  - TUI 呈现文档纠偏已验证（2026-08-13）：`docs/learning/06-extensions.md` 与实验 README 已把 `notify` 修正为“宿主通知能力”，并明确当前 TUI `info` 的真实形态和 `route -> Fake 调用 -> 真实绘制` 三层证据；`npm run check` 退出码为 0，类型检查及 4/4 Fake 通过，Markdown 围栏为 20 个且配对，`git diff --check` 通过。这些检查不替代用户终端中的可见性复核。
  - 用户 TUI 呈现复核已通过（2026-08-13）：用户在显式 `--tui-mode regular`、离线、无 Session/Tool/其他资源、显式加载当前 Extension 的真实 Pi `0.84.1` 中，待界面就绪后手动执行 `/pi-study-trace`；回传截图直接显示输入框上方的灰色 `PI_STUDY_CONTEXT name=pi-study-trace mode=tui hasUI=true cwdMatchesProcess=true trusted=false sessionFile=none model=present signal=none route=ui`。结合此前同条件 TUI 追踪和 Print 的 `EXIT=0`、空 stdout、stderr fallback，TUI/Print 模式与反馈呈现对照已完成。截图只证明本次 Regular TUI 的可见结果，不证明 RPC/JSON、取消、错误传播或其他终端与主题。
  - 取消机制用途与归属理解已通过（2026-08-13）：用户已确认理解取消用于及时停止已经不再需要的长任务、释放资源并避免继续扩大副作用，同时不能回滚已发生的操作；也能区分 Pi 官方提供 Extension、取消信号和 `pi.exec` 等机制，而“启动 Maven 并把信号传下去”是用户自定义 Extension 的具体实现。当前 `pi-study-guard` 没有 Maven Tool 或 Maven 进程控制代码。
  - Fake 取消实验已确认并实现中（2026-08-13）：使用本地定时延迟代表长时间 Maven 工作，固定同一函数、Fake 现场和追踪格式，只改变 `AbortController` 是否中途取消；未取消组应出现 `start -> completed`，取消组应出现 `start -> cancelled` 且不得出现 `completed` 或延迟完成标记。该实验只证明课程本地探针能协作响应测试传入的 Fake 信号，不证明真实 Extension、`Esc`、Pi Tool Executor、Maven 子进程或副作用回滚。
  - Fake 取消 Red 已保留（2026-08-13）：先增加未取消/中途取消两条期望测试但不创建探针实现，`npm test` 退出码为 1，并以 `ERR_MODULE_NOT_FOUND` 明确指出 `fixtures/cancellation/cooperative-delay.ts` 缺失。该 Red 只证明测试依赖尚未实现，不证明取消行为。
  - Fake 取消初版 Green 待加强（2026-08-13）：本地协作延迟探针落地后，`npm test` 6/6、`npm run typecheck` 均以退出码 0 通过；取消组在两倍延迟观察窗内没有晚到 `completed`。独立只读审查指出，真实等待只能证明观察窗内无 marker，幂等门闩仍可能掩盖 Timer 或 Abort listener 泄漏；同时该探针未接入真实 Extension 工厂。下一步改用可控 Timer，直接断言 Timer/listener 清理并强制触发已捕获的迟到回调，结论严格限定为课程本地探针。
  - Fake 取消确定性 Green 阻塞待修（2026-08-13）：初版可控 Timer 的 A/B、listener 清理和迟到回调断言曾使 `npm run check` 6/6 通过，但独立竞态复审复现两种假 Green：`schedule()` 在返回 handle 前同步完成或同步触发取消时，返回后的 Timer 无人清理；Timer 接口返回 `unknown` 又允许 `undefined` handle，清理分支会把它误判成“未登记”。当前不得认定最终 Green。下一步收窄 handle 契约、补 schedule 返回后的 settled 清理，并增加同步完成/同步取消回归测试后重新验证。
  - Fake 取消竞态修复候选 Green（2026-08-13）：实现用独立哨兵区分“尚未登记 Timer”和合法 `undefined` handle，并在 `schedule()` 返回前暂存同步完成/取消结果，拿到 handle 后再统一清理收敛；新增默认系统 Timer、同步完成、`schedule()` 内同步取消、`undefined` handle、传入延迟值和清理 `throw undefined` 回归。`npm run check` 退出码为 0，类型检查及 11/11 测试通过。当前等待独立复审，复审通过前不恢复最终 Green。
  - Fake 取消清理异常复审阻塞（2026-08-13）：原三个竞态反例已由独立复审确认修复，但额外反例发现 `timer.clear()` 执行 `throw undefined` 时，清理错误会与“没有错误”的 `undefined` 哨兵混淆并错误返回 `completed`。普通 `Error` 已能拒绝且 listener 会清理。下一步用独立布尔状态记录任意清理异常，并补 `throw undefined` 回归后重新验证。
  - Fake 取消确定性最终 Green 已通过（2026-08-13）：独立复审重跑同步完成、同步取消、合法 `undefined` handle 及组合反例，均得到正确结果、Timer 清理一次且 Abort listener 归零；又验证 `clear`、`schedule` 和事件记录回调分别抛出 `undefined`、`null`、假值、Symbol 或 Error 时，Promise 都明确拒绝且不悬挂，迟到回调不会二次完成。当前 `npm run check` 退出码为 0，类型检查及 11/11 测试通过，`git diff --check` 通过。该最终 Green 只证明课程本地探针与受控 Timer 的协作取消；若外部 `timer.clear()` 自身失败，只能证明错误被明确上报，不能声称外部资源已成功删除，也不证明真实 Pi Handler/Executor、`Esc`、`pi.exec`、Maven 进程树或副作用回滚。5.3 仍需完成错误传播教学和实验，暂不勾选。
  - 教学执行方式已纠偏（2026-08-13）：用户指出学习实验应由教练陪同、本人逐步操作，而不应由教练在一次确认后代跑完整 Red/Green。上一条 11/11 与独立复审继续作为实现质量证据，但不计作用户动手或理解验收。后续固定为“先讲实验卡 -> 每次只给一个最小步骤 -> 用户执行并回传 -> 共同判断已证明与未证明”；当前先补做取消探针的学员验证，再进入错误传播。
  - Fake 取消学员验证进行中（2026-08-13）：实验仍比较同一探针在“不触发 abort”和“工作已开始后触发 abort”两个状态，固定函数、受控 Timer、延迟和断言，只改变是否取消；当前只执行 A 组正常完成基线，等待用户回传聚焦测试输出，尚未进行 B 组或理解验收。
  - Fake 取消学员 A 组已验证（2026-08-13）：用户亲自运行“不取消”的聚焦测试，终端显示目标用例通过、`tests 1`、`pass 1`、`fail 0`、`EXIT=0`。结合该用例的直接断言，这证明当前课程本地探针在受控 Timer 被驱动后形成 `start -> completed`，Timer 清理一次且 Abort listener 归零；不证明取消分支、真实时间自然到期、Pi 信号或 Maven。下一步只执行 B 组运行中取消对照。
  - 取消实验主线已调整为真实 Pi（2026-08-13）：用户明确要求真实启动 Pi、允许真实 Model/Provider 调用且不以额度为限制。Fake A 组继续作为本地正常路径证据，Fake B 组不再作为下一主步骤；主验收改为用户输入真实任务触发内置只读 Tool，在 Extension 的 opt-in 取消探针等待期间按 `Esc`，验证 `Pi 活动 Run -> ctx.signal -> Extension cancelled`。实验仍不得读取凭据内容、执行 Maven、写业务文件或提前注册 5.4 自定义 Tool；代码与静态/Fake 检查通过后才发真实命令。
  - 真实 Pi 取消探针实现中（2026-08-13）：源码与独立审查已确认 Pi `0.84.1` 会 await `tool_call` Handler，TUI 等待期间按 `Esc` 会取消当前 Agent Run；Handler 返回后 Core 再检查同一 signal，并以 `Operation aborted` 结束而不进入 Executor。当前准备默认关闭的 `--pi-study-cancel-probe`：真实 Model 只需产生固定字面量 `PI_STUDY_CANCEL_MARKER.txt` 的内置 `read` Tool Call，Extension 在执行前异步等待；其他 Tool/路径、signal 缺失、超时和内部异常全部 fail-closed。探针不注册自定义 Tool、不执行 Shell/Maven，目标文件无需存在，真实 Provider 请求留给用户亲自发起。
  - 真实 Pi 取消探针首轮 Red 已保留（2026-08-13）：新增 opt-in Flag、`tool_call` Handler 和 5 条聚焦门禁测试后首次运行 `npm run check`，类型检查及新增测试通过，但全量结果为 15/16，既有生命周期 Fake 在派发 `tool_call` 时因未实现真实 ExtensionAPI 的 `getFlag()` 抛出 `TypeError: pi.getFlag is not a function`。该 Red 表明旧 Fake 契约不完整，不证明真实 Pi 或取消链失败；下一步只补 Fake 的 Flag 关闭返回后重跑全量验证。
  - 真实 Pi 取消探针第二轮 Red 已保留（2026-08-13）：补齐 `getFlag()` 后再次运行 `npm run check`，仍为 15/16；既有 Fake 用 `Map<事件名, 单个 Handler>` 保存注册，新增的取消 `tool_call` Handler覆盖了先注册的生命周期追踪 Handler，导致期望日志缺少 `tool_call tool=read`。真实 Pi 支持同一事件的多个 Handler，因此该 Red 暴露的是 Fake 派发模型错误；下一步改为按注册顺序保存并依次调用 Handler 数组。
  - 真实 Pi 取消探针候选 Green（2026-08-13）：旧 Fake 已补齐 `getFlag()` 并按注册顺序派发同事件的多个 Handler；`npm run check` 退出码为 0，类型检查及 16/16 测试通过，`git diff --check` 通过。新增测试覆盖默认关闭、非目标 Tool/路径阻止、活动 signal 取消、超时完成仍阻止和 signal 缺失 fail-closed，也令 Fake `registerTool()` 直接失败以证明未注册自定义 Tool。当前只算实现候选证据，等待独立审查与真实 CLI Flag 加载检查；用户尚未执行真实 Provider/Model/`Esc` 实验。
  - 真实 Pi 取消探针门禁复审阻塞与修复（2026-08-13）：候选实现最初只在设置 `PI_STUDY_LIFECYCLE_TRACE` 时注册取消 Handler；若显式开启 `--pi-study-cancel-probe` 却漏设追踪环境变量，内置 `read` 可能没有门禁。现已把取消 Handler 改为无条件注册，追踪仅作为可选记录器，并新增工厂级回归直接验证“无追踪环境变量 + Flag 开启 + signal 缺失”仍返回 `block + terminate`。修复后 `npm run check` 退出码为 0，类型检查及 17/17 测试通过；该结果仍是本地实现证据，不替代真实 Pi 取消链。
  - 真实 Pi Flag 无 Provider 预检已通过（2026-08-13）：第一次采集脚本在 Pi 命令返回后误给 zsh 只读变量 `status` 赋值而以退出码 1 中止，生成的 stdout、stderr 与追踪文件仍完整；这只暴露外层采集脚本错误，不是 Pi 或 Extension Red。改用普通变量后，在权限 `0700` 的操作系统临时目录中真实执行 Pi Print Slash Command，带 `--pi-study-cancel-probe`、显式 Extension、选中 Model 及隔离资源参数；Pi `EXIT=0`、stdout 为空、stderr 只有脱敏 Context，追踪严格为 `factory -> session_start -> command -> session_shutdown`，没有 Input、Agent、Turn 或 Tool 事件。该证据证明本次真实 CLI 能加载 Extension 并解析新 Flag，且本次 Slash Command 未调用 Provider；它不证明真实 Model Tool Call、`Esc` 或 Handler 收到 signal。下一步仍由用户亲自在 TUI 发起真实 Provider 请求并在明确等待标记出现后按 `Esc`。
  - 真实 Pi 取消探针独立复审已通过（2026-08-13）：独立只读复审核对当前项目与 Pi/Core `0.84.1`，确认 TUI streaming 时 `Esc -> agent.abort()`、`ctx.signal` 来自当前 Agent Run，且 Core await `tool_call` Handler 后会先检查 `signal.aborted`，形成 `Operation aborted` 并跳过内置 `read` Executor。当前实现无追踪环境变量时也始终只注册一个取消 Handler，Flag 默认关闭；目标与非目标、signal 缺失、超时及异常都 fail-closed，源码没有 `registerTool`、`pi.exec`、子进程或文件读取。复审复跑 `npm run check` 为 17/17、`git diff --check` 通过。实现前置门禁已 Green，但用户尚未发起真实 Provider/Model 请求或按 `Esc`，所以 5.3 仍进行中。
  - 用户真实 Pi 取消实验启动页已验证（2026-08-13）：用户亲自按实验命令启动 Pi `0.84.1` Regular TUI，回传截图显示界面已停在空输入框，`[Extensions]` 只列出 `pi-study-guard`，当前 Model 为 `gpt-5.6-terra`；启动命令显式开启 `--pi-study-cancel-probe`、只暴露内置 `read`，并关闭 Session、其他 Extension、Skill、Prompt Template、Theme 和 Context File。该截图只证明本次真实实验进程、目标 Extension、Model 和 Tool 暴露条件已就绪，不证明 Provider 已调用、Model 已产生 Tool Call、Handler 已收到 signal 或 Executor 被跳过。下一步提交固定 Prompt；只有黄色等待标记出现后才按一次 `Esc`，随后回传取消界面证据。
  - 用户真实 Pi `Esc` 取消链已验证（2026-08-13）：用户在上述真实 TUI 中提交固定 Prompt，Model 实际产生唯一一次 `read PI_STUDY_CANCEL_MARKER.txt`；用户看到黄色 `PI_STUDY_CANCEL waiting; press Esc` 后按一次 `Esc`，界面中的 Tool Result 显示 `Operation aborted`，Agent 随后显示 `This operation was aborted`。同一进程的脱敏追踪依次包含 `tool_call tool=read -> cancel_probe state=start -> cancel_probe state=cancelled -> message_start/end role=toolResult -> turn_end -> agent_end -> agent_settled`，没有 `cancel_probe state=completed`，也没有 Executor 后的 `tool_result` Handler 事件。结合 Pi/Core `0.84.1` 已核对的 preflight 源码，这直接证明本次真实链为“Model Tool Call -> Extension `tool_call` Handler 等待 -> TUI Esc 取消当前 Agent Run -> `ctx.signal` 到达 Handler -> Core 形成取消 Tool Result并跳过内置 `read` Executor”。界面中两条取消文案是同一次 abort 在 Tool Result 与 Agent 结束层的呈现，不代表取消了两次；该 Green 不证明自定义 Tool Executor 的第三参数 signal、`pi.exec`、Maven/进程树终止、强制抢占或副作用回滚。下一步由用户正常退出本次 Pi，再亲自读取最终追踪巩固证据链；5.3 仍有错误传播实验，暂不勾选。
  - 用户已退出真实取消实验 Pi、关闭事件待验证（2026-08-13）：用户确认已执行退出并结束本次 TUI。该确认只证明退出操作已完成，尚未由用户亲自读取退出后的最终日志，因此不能单独证明 `session_shutdown` Handler 已派发。下一步只读取同一 `PI53_REAL_ROOT` 下的最终脱敏追踪，核对取消链未变化且末尾新增 `session_shutdown reason=quit`。
  - 用户真实 Pi 取消实验最终追踪已验证（2026-08-13）：用户亲自读取同一 `PI53_REAL_ROOT` 的完整脱敏日志，实际得到 `factory -> session_start -> input -> before_agent_start -> agent_start -> turn/message -> tool_call tool=read -> cancel_probe state=start -> cancel_probe state=cancelled -> toolResult message -> turn_end -> 第二个 assistant 取消收尾 Turn -> agent_end -> agent_settled -> session_shutdown reason=quit`。日志中没有 `cancel_probe state=completed`，末行确为正常退出事件。结合此前界面截图和 Pi/Core `0.84.1` 源码，本次真实取消动态验收闭合；它仍不证明自定义 Tool Executor、`pi.exec`、Maven 进程树、强制抢占或副作用回滚。取消子主题已验证，5.3 仍需错误传播教学与实验，暂不勾选。
  - 真实取消实验可恢复清理已完成（2026-08-13）：用户执行带临时路径前缀校验的清理命令，终端输出 `REAL_CANCEL_LAB_MOVED_TO_TRASH` 并清除 `PI53_REAL_ROOT`；随后只读检查确认原临时路径不存在。macOS 隐私策略拒绝当前进程列举 `~/.Trash`，因此只能确认源路径已移走，不能由本进程再次展示废纸篓目标。下一步进入错误传播系统地图，不再操作该实验目录。
  - 错误传播系统地图进行中（2026-08-13）：沿用“Model 请求只读检查 Java 项目配置”的单一场景，区分普通观察 Handler、执行前 `tool_call` 门禁与 Tool Executor 三个故障位置；先讲清为什么观察故障继续、门禁故障 fail-closed、执行故障形成错误 Tool Result，以及三者都不自动回滚副作用。用户确认对象与因果链后才准备受控实验。
  - 错误传播系统地图理解已通过（2026-08-13）：用户确认理解普通观察 Handler、`tool_call` 门禁 Handler、Tool Executor 和错误 Tool Result 在同一个只读 Java 配置检查场景中的位置及不同失败后果。该确认只通过概念门禁，不替代三条真实 Pi 动态实验；下一步先审阅实验卡，确认后再准备最小 opt-in 错误探针。
  - 错误传播三组实验卡已准备（2026-08-13）：三组固定 Pi `0.84.1`、真实 Model/Provider、TUI、同一 Prompt、同一个只在内存返回固定结果的显式实验 Extension Tool、隔离临时目录与脱敏追踪，唯一变量为异常位置。A 组让一次性普通 `turn_start` 观察 Handler 抛错，预期后继 Handler、Tool Executor 和 Agent 仍继续；B 组让匹配该 Tool 的 `tool_call` Handler 抛错，预期形成错误 Tool Result 且 Executor marker 绝不出现；C 组让门禁通过后由 Tool Executor 抛错，预期出现 Executor start、错误 Tool Result且无 success。实验 Tool 不读写文件、不执行 Shell/子进程、不联网，也不是 5.4 的 `pi_study_inspect`。Model 未发 Tool Call只算未触发；错误可见不代表副作用回滚，单次 TUI 结果也不外推到所有事件与模式。用户确认实验卡后才允许实现。
  - 错误传播探针开发已确认（2026-08-13）：用户已确认上述三组实验卡，当前进入 Red -> 最小 Green -> 扩展回归。实现只新增默认关闭的课程实验 Tool 与固定脱敏 marker，通过同一个模式参数选择 `observer`、`gate` 或 `executor`；不得读写业务文件、执行 Shell/子进程或发起额外网络，也不得实现 5.4 的正式 `pi_study_inspect`。自动测试与源码复审只算真实实验的前置门禁，三组动态验收仍由用户逐组亲自运行。
  - 错误传播探针 Red 已验证（2026-08-13）：先新增聚焦契约测试 `test/error-probe.test.ts`，要求默认/非法模式不注册 Tool、普通观察 Handler 异常后同事件后继 Handler 继续、`tool_call` 门禁异常时 Executor marker 缺失、Executor 异常形成 start/error 且无 success，并覆盖重复调用和 Turn 熔断。此时执行 `node --test test/error-probe.test.ts` 退出码为 `1`，稳定失败于 `ERR_MODULE_NOT_FOUND`，目标为尚不存在的 `fixtures/error-propagation/error-probe.ts`；这直接证明测试先于实现且当前能力缺失，不证明未来实现正确或真实 Pi Core 已按预期传播异常。下一步只实现满足该契约的显式实验 Extension，再跑聚焦 Green 与全量回归。
  - 错误传播探针聚焦候选 Green（2026-08-13）：新增只通过显式 `--extension` 加载的独立实验入口；Flag 默认 `off`，在 `session_start` 读取最终 CLI 模式后才动态注册纯内存 Tool。`npm run typecheck` 与 `node --test test/error-probe.test.ts` 均退出 `0`，聚焦测试 12/12 通过，覆盖三种模式、默认不注册、一次调用/Turn 熔断、真实 `ExtensionRunner` 的普通 Handler catch-and-continue 与 `tool_call` fail-stop 顺序、精确 marker/`isError` 字段、动态注册后首次 `armed` 写入失败仍禁止成功执行，以及只允许在当前用户私有临时目录中新建固定名日志，拒绝已存在目标、符号链接和非私有父目录。该结果仍未证明 Pi Core 把异常包装成真实 Tool Result、Provider/Model 实际调用或 TUI 呈现；下一步跑全量回归、无 Provider CLI 加载预检和独立复审，全部通过后再由用户亲自运行 observer 组。
  - 错误传播探针全量候选 Green（2026-08-13）：扩展包 `npm run check` 退出 `0`，类型检查及全量 29/29 测试通过，`git diff --check` 通过；另在权限 `0700` 的新临时目录中，用真实 Pi `0.84.1` 显式加载实验入口并执行 `--pi-study-error-mode=observer --help`，进程退出 `0`、帮助中出现新 Flag、stderr 为空，日志文件存在但为 0 字节，临时目录随后删除。CLI 预检只证明模块加载与 Flag 注册，不证明 `session_start` 动态 Tool 注册、Model Tool Call、异常包装或 TUI 呈现；当前等待独立复审，复审通过前不发真实 Provider 实验命令。
  - 错误传播探针独立复审阻塞（2026-08-13）：安全复审发现，动态注册 Tool 后若首次 `armed` 日志写入失败，旧实现会保持 `activeMode` 未定义；Schema 非法调用又会在 Extension `tool_call` Handler 前由 Core 拒绝，导致调用预算和 Turn 熔断都无法介入，Model 理论上可跨 Turn 重复尝试。此前 29/29 只覆盖有效参数直达 Executor 时 fail-closed，未覆盖该 Core 前置校验分支，因此候选 Green 暂不接受。下一步把“模式已选择”和“armed 成功”拆开：armed 失败后每个 Turn、门禁和 Executor 都持续 abort/fail-closed，并补直接回归后重新全量验证与独立复审。
  - 错误传播探针阻塞修复与独立复审最终 Green（2026-08-13）：实现现已在写 `armed` 前锁定选定模式，仅在该记录成功后把探针标记为 ready；若记录失败，后续每个 Turn 都先 abort，`tool_call` 与 Executor 也持续 fail-closed，结果类 Handler 不会伪造成功。新增回归直接覆盖两次 Turn、门禁、Executor、4 次 abort 和无执行成功 marker；参数 Schema 同时改为单值 `enum`，并补齐 `fstat` 异常时关闭日志文件描述符。最新聚焦测试 12/12、`npm run check` 类型检查及全量 29/29、`git diff --check` 均通过；三份独立只读复审分别核对 Runner/Core 契约、测试证据与安全熔断，均确认无阻塞，可进入 observer 真机实验。该最终 Green 仍只属于实现、Fake/Runner、Schema 校验和源码证据，不证明真实 Provider、动态 Tool Call、Core 错误 Tool Result或 TUI 呈现；启动后必须先看到唯一 `armed` 日志且无启动错误，再提交 Prompt。
  - 用户真实 Pi observer 实验启动页已验证（2026-08-13）：用户亲自按隔离命令启动 Pi `0.84.1` Regular TUI；截图显示当前目录为新的 `pi-study-5.3-error-observer.*` 操作系统临时目录，`[Extensions]` 只列出显式加载的 `error-propagation`，当前 Model 为 `gpt-5.6-terra`，输入框仍为空，界面没有可见启动错误。该截图只证明本次实验进程、Extension 入口、Model 与未提交 Prompt 的初始状态，不证明 `session_start` 已成功动态注册 Tool、日志已写出 `armed`、Provider 已调用或任何错误传播行为。下一步保持 Pi 运行，在第二个终端只读取固定脱敏日志并确认唯一 `armed` 记录。
  - 用户真实 Pi observer `armed` 门禁已验证（2026-08-13）：用户保持同一 Pi 进程运行，在第二个终端读取固定脱敏日志，实际且仅得到 `0001 error_probe mode=observer state=armed`。这直接证明本次 `session_start` 已读取最终 CLI 模式、动态注册课程 Tool，并成功初始化可写的脱敏追踪；它仍不证明 Provider 已请求、Model 会调用该 Tool、普通观察 Handler 异常后主流程会继续或 Executor 会成功。下一步只在同一 Pi 中发送一次固定 Prompt，不按 `Esc`、不重试，等待 Agent 完整结束后回传界面。
  - 用户真实 Pi observer 界面链已验证、日志待核对（2026-08-13）：用户在同一 TUI 只发送一次固定 Prompt；真实 Model 产生 `pi_study_error_probe` Tool Call，界面先显示固定 Extension 错误 `PI_STUDY_ERROR_OBSERVER_5301`，随后同一 Tool 仍以绿色结果返回 `PI_STUDY_ERROR_PROBE_OK`，Model 最后用一句话正常收尾，输入框恢复。该截图直接证明本次普通观察 Handler 异常被真实 TUI 报告后没有终止 Agent 主流程，目标 Tool 与后续 Model Turn 仍继续；它尚不能单独证明后置观察 Handler 已运行、Executor 内部 marker 的精确顺序、Tool Result 的 `isError=false`、无重复调用/Turn 熔断或正常 `session_shutdown`。下一步保持 Pi 运行，在第二个终端读取同一脱敏日志完成内部顺序核对。
  - 用户真实 Pi observer 内部事件链已验证（2026-08-13）：用户在 Agent 收尾后、Pi 仍运行时读取同一脱敏日志，实际严格得到 `armed -> observer_error -> observer_after -> call_accepted -> gate_after -> executor_start -> executor_success -> tool_result_success(isError=false) -> tool_execution_end_success(isError=false) -> tool_result_message_success(isError=false)`。没有 `duplicate_blocked`、`unexpected_call_blocked` 或 `turn_limit_abort`。结合上一张真实 TUI 截图，这直接证明本次 Pi `0.84.1` 普通 `turn_start` 观察 Handler 抛错后，Runner 报告异常并继续调用同事件的后置 Handler；目标 Tool 随后通过门禁、进入 Executor、成功形成非错误 Tool Result，并由 Model 完成后续 Turn。它只覆盖本次 observer 模式和固定 Tool，不代表任意普通 Handler 错误都无影响，也不证明 gate/executor 错误分支或副作用回滚。下一步只执行 `/quit` 正常关闭，再核对日志未新增重复/熔断 marker；5.3 仍不勾选。
  - observer 结果语义理解已通过并沉淀正文（2026-08-13）：用户已理解界面最后“工具已调用一次并返回 `PI_STUDY_ERROR_PROBE_OK`”是 Model 读取 Tool Result 后生成的总结；其中 `PI_STUDY_ERROR_PROBE_OK` 只是课程 Executor 写死的成功标记，不代表真实文件检查或业务任务完成。稳定文档已补充“普通 Handler 红色异常 -> 绿色固定 Tool Result -> Model 最终总结”三个层次，并明确最终总结不是第二次 Tool 执行。该理解不替代 gate/executor 两组动态实验；当前下一步仍是正常退出 observer 进程并核对最终日志。
  - 用户已退出真实 observer 实验 Pi、最终日志待验证（2026-08-13）：用户确认已执行 `/quit` 并退出本次 observer TUI。该确认只证明交互进程已结束；错误探针的独立日志没有记录 `session_shutdown` 状态，因此不能从退出动作本身推导最终日志内容未变化。下一步只读取同一固定脱敏日志，确认仍为既有十条成功链且没有新增 `duplicate_blocked`、`unexpected_call_blocked` 或 `turn_limit_abort`，随后才认定 observer 组动态验收闭合。
  - 用户真实 Pi observer 组动态验收已闭合（2026-08-13）：用户退出后再次读取同一固定脱敏日志，最终内容仍严格为既有十条：`armed -> observer_error -> observer_after -> call_accepted -> gate_after -> executor_start -> executor_success -> tool_result_success(isError=false) -> tool_execution_end_success(isError=false) -> tool_result_message_success(isError=false)`，没有新增 `duplicate_blocked`、`unexpected_call_blocked` 或 `turn_limit_abort`。结合此前真实 TUI 中“普通 Handler 红色异常、Tool 绿色成功结果、Model 正常收尾”的证据，本组直接证明当前 Pi `0.84.1` 的普通观察 Handler 抛错会被报告，但同事件后置 Handler与主 Tool 链仍继续完成；错误探针日志本来就不记录 `session_shutdown`，因此本次验收依据是最终业务状态链没有漂移，而不是等待一个不存在的关闭 marker。observer 组通过，5.3 仍需 gate 与 executor 两组真实动态实验，暂不勾选。
  - observer 实验隔离目录已完成可恢复清理（2026-08-13）：用户对本次精确临时路径执行移入 `~/.Trash` 的保护脚本，终端输出 `OBSERVER_LAB_MOVED_TO_TRASH`；随后只读检查得到 `OBSERVER_SOURCE_ABSENT`，确认原路径已移走。由于本次没有重新列举废纸篓目标，证据只证明源路径消失并结合用户命令支持“已移入废纸篓”，不证明废纸篓目标内容已再次读取。下一步进入 gate 组系统地图，先理解其与 observer 组的唯一差异，再启动真实 Pi。
  - gate 组真实实验卡已准备（2026-08-13）：沿用 observer 组的 Pi `0.84.1`、真实 Model/Provider、TUI、Prompt、显式实验 Extension、纯内存 Tool、固定参数 marker 与一次调用预算，唯一业务变量改为 `mode=gate`，即异常从普通 `turn_start` 观察 Handler 移到 Executor 前的匹配 `tool_call` Handler。通过链必须严格为 `armed -> call_accepted -> gate_error -> tool_execution_end_error(isError=true) -> tool_result_message_error(isError=true)`，并且绝无 `gate_after`、任何 `executor_*`、Extension `tool_result_*`、success 或熔断 marker；`tool_execution_end_error` 是 Core 对 preflight 失败的统一收尾，不是 Executor 已启动。Model 未调用、参数非法、重复调用或触发 watchdog 都是无效/污染样本。当前只完成实验卡与只读复核，等待用户确认理解 fail-closed 因果后才给启动命令。
  - gate 组 fail-closed 因果理解已通过（2026-08-13）：用户确认理解“Executor 开工前的门禁若自身报错，就不能把未知状态当作允许；Pi 应形成错误 Tool Result并跳过 Executor”。这只通过概念与实验卡门禁，不证明真实 Provider 已请求、Model 已调用 Tool 或 gate 传播已经动态发生。下一步只创建全新的私有 gate 临时目录并启动真实 Pi；先核对启动页和唯一 `armed` 日志，未完成这两个门禁前不提交 Prompt。
  - 用户真实 Pi gate 实验启动页已验证（2026-08-13）：用户亲自启动新的 Pi `0.84.1` Regular TUI，截图显示当前目录为新的 `pi-study-5.3-error-gate.*` 操作系统临时目录，`[Extensions]` 只列出显式加载的 `error-propagation`，当前 Model 为 `gpt-5.6-terra`，输入框为空且界面没有可见启动错误。该截图只证明本次 gate 实验进程、Extension 入口、Model 与尚未提交 Prompt 的初始状态，不证明 `session_start` 已读取 `mode=gate`、动态 Tool 已注册、追踪已就绪、Provider 已调用或门禁异常已经发生。下一步保持 Pi 运行，在第二个终端只读取固定脱敏日志并确认唯一 `armed` 记录。
  - 用户真实 Pi gate `armed` 门禁已验证（2026-08-13）：用户保持同一 Pi 进程运行，在第二个终端读取固定脱敏日志，实际且仅得到 `0001 error_probe mode=gate state=armed`。这直接证明本次 `session_start` 已读取最终 CLI 模式 `gate`、动态注册课程 Tool，并成功初始化可写的脱敏追踪；它仍不证明 Provider 已请求、Model 会调用该 Tool、`tool_call` 门禁会抛出预期异常、Core 会形成错误 Tool Result或 Executor 会被跳过。下一步只在同一 Pi 中提交一次与 observer 组相同的固定 Prompt，不按 `Esc`、不重试，等待 Agent 完整收尾后回传界面。
  - 用户真实 Pi gate 界面错误链已验证、内部顺序待核对（2026-08-13）：用户在同一 TUI 只提交一次固定 Prompt；真实 Model 产生 `pi_study_error_probe` Tool Call，界面中的 Tool 区域以红色错误结果显示固定异常 `PI_STUDY_ERROR_GATE_5301`，随后 Model 用一句“工具已调用完成”收尾并恢复输入框。该截图直接证明本次 Tool Call 已进入 gate 分支、门禁异常被 Pi 转成模型可见的错误 Tool Result，且 Agent 能在错误结果后继续一个收尾 Turn；Model 的自然语言总结不代表 Tool 成功，也不能证明 Executor 已进入。下一步保持 Pi 运行，读取同一脱敏日志；只有严格得到 `armed -> call_accepted -> gate_error -> tool_execution_end_error(isError=true) -> tool_result_message_error(isError=true)`，且绝无 `gate_after`、任何 `executor_*`、Extension `tool_result_*`、success 或熔断 marker，才可判定 gate 组通过。
  - 用户真实 Pi gate 内部错误链已验证（2026-08-13）：用户在 Agent 收尾后、Pi 仍运行时读取同一脱敏日志，实际严格得到 `armed -> call_accepted -> gate_error -> tool_execution_end_error(isError=true) -> tool_result_message_error(isError=true)`。日志中没有 `gate_after`、任何 `executor_*`、Extension `tool_result_*`、success、`duplicate_blocked`、`unexpected_call_blocked` 或 `turn_limit_abort`。结合上一张真实 TUI 红色错误结果，这直接证明本次 Pi `0.84.1` 的匹配 `tool_call` 门禁 Handler 抛错后，后置门禁与 Executor 均未运行，Core 仍把 preflight 异常收敛成 `isError=true` 的 Tool Result 并让 Model 获得一次收尾 Turn。它只覆盖本次 gate 模式和固定单次 Tool Call，不证明 Executor 自身抛错路径、任意 Handler、自动回滚或所有模式。下一步只执行 `/quit` 正常关闭，再复读最终日志确认没有新增重复、成功或熔断 marker；完成退出后的最终核对前，gate 组暂不闭合。
  - 用户已退出真实 gate 实验 Pi、最终日志待验证（2026-08-13）：用户确认已执行 `/quit` 并退出本次 gate TUI。该确认只证明交互进程已结束；错误探针的独立日志不记录 `session_shutdown`，因此不能由退出动作本身推导最终状态链没有漂移。下一步只复读同一固定脱敏日志，确认仍为既有五条错误链，且没有新增 `gate_after`、任何 `executor_*`、success、重复或 Turn 熔断 marker，随后才认定 gate 组动态验收闭合。
  - 用户真实 Pi gate 组动态验收已闭合（2026-08-13）：用户退出后再次读取同一固定脱敏日志，最终内容仍严格为五条：`armed -> call_accepted -> gate_error -> tool_execution_end_error(isError=true) -> tool_result_message_error(isError=true)`；没有 `gate_after`、任何 `executor_*`、Extension `tool_result_*`、success、`duplicate_blocked`、`unexpected_call_blocked` 或 `turn_limit_abort`。结合此前真实 TUI 的红色 `PI_STUDY_ERROR_GATE_5301`，本组直接证明当前 Pi `0.84.1` 的匹配 `tool_call` 门禁 Handler 抛错后会 fail-closed：后置门禁与 Executor 都不运行，Core 仍形成 Model 可见的错误 Tool Result并允许一次收尾 Turn。`tool_execution_end_error` 是 Core 对本次 Tool 尝试的统一收尾，不表示 Executor 已启动；Model 的“工具已调用完成”也只是对错误结果的自然语言总结，不代表 Tool 成功。错误探针日志不记录 `session_shutdown`，因此退出后五条不变正是预期。gate 组通过，5.3 仍需 executor 组真实动态实验与最终理解验收，暂不勾选。
  - gate 实验隔离目录已完成可恢复清理（2026-08-13）：用户对本次精确临时路径执行带固定值校验的移入废纸篓脚本，终端输出 `GATE_LAB_MOVED_TO_TRASH`；随后只读检查得到 `GATE_SOURCE_ABSENT`，确认原路径已移走。本次没有读取或列举废纸篓内容，因此证据只证明源路径消失，并结合用户执行的精确 `mv` 命令支持“已移入废纸篓”，不证明废纸篓目标内容。下一步进入 executor 组大白话场景；用户理解“门禁已放行但 Executor 开工后自身失败”的因果后，才给实验卡。
  - executor 组大白话场景已讲解、理解待确认（2026-08-13）：沿用“仓库门卫与工人”场景说明唯一差异：gate 组是门卫在放行前报错，所以工人未进仓库；executor 组是门卫已经完整放行，工人进入并开始工作后才因机器故障报错。对应链路是 `call_accepted -> gate_after -> executor_start -> executor_error -> isError=true Tool Result -> Model 收尾`；它的用途是让真实 Tool 开工后的失败不会被静默当成成功，但 Pi 只会传递失败，不会自动撤销开工后已经发生的副作用。当前只完成教学以及契约、安全两路只读实验前复核，不证明真实 Provider、Executor 异常包装或 TUI 呈现已经发生；等待用户确认因果理解后再给实验卡。
  - 普通观察 Handler 用途理解补课进行中（2026-08-13）：用户已能把 `tool_call` gate 类比为 Spring AOP 环绕通知中 `proceed()` 前的检查，但仍不理解普通 observer 的实际用途。当前退回系统地图补清：普通 Handler 是订阅 Pi 生命周期事件、执行追踪、指标、状态提示或非关键清理等附加工作的观察者，不拥有 Tool 是否执行的决策权；Pi 隔离其异常是为了避免非关键观测故障拖垮主任务。权限、审批、合规记账等“失败就必须禁止执行”的职责不能放在这种 fail-open 观察路径。用户确认这一职责边界后，再回到 executor 实验卡。
  - observer/gate 时间顺序误区澄清中（2026-08-13）：用户进一步指出本次两组看起来都在 Executor 前处理。准确区别不能按相对 Executor 的时间判断：observer 实验监听 `turn_start`，面向“一次 Model Turn 已开始”的生命周期事实，此时 Model 还可能直接回答、尚未产生具体 Tool Call；gate 监听随后已经生成的某一次 `tool_call`，面向确定的 Tool 名称与参数，并拥有放行或阻止该次 Executor 的控制权。同一 Turn 可能没有 Tool，也可能产生多个 Tool Call，因此两类 Handler 的触发次数和信息范围也不同。当前等待用户确认这条“观察生命周期 vs 决策单次 Tool”的主线，再推进 executor 组。
  - observer、gate 与 Executor 三类职责理解已通过并安排后续复用（2026-08-13）：用户已确认不能按“是否位于 Executor 之前”分类，而应分别理解为生命周期观察、单次 Tool Call 决策和实际业务执行。5.3 接下来完成 executor 真机异常组；5.4 会实现自定义 Tool/Executor；5.6 会分别用 `tool_call` 与 `user_bash` 实现 Model Shell 和用户 Shell 门禁；5.7 会用生命周期事件保存与恢复状态；5.9-5.10 再覆盖自动测试、重载、清理与并发。该理解确认不替代 executor 真机证据，5.3 仍不勾选。
  - executor 组真实实验卡已准备（2026-08-13）：沿用 gate 组的 Pi `0.84.1`、真实 Model/Provider、Regular TUI、固定 Prompt、显式实验 Extension、纯内存 Tool、单值 Schema、参数 marker 与一次调用预算，唯一业务变量改为 `mode=executor`，即门禁完整放行后由 Executor 记录开始并抛出固定异常。通过链必须严格为 `armed -> call_accepted -> gate_after -> executor_start -> executor_error -> tool_result_error(isError=true) -> tool_execution_end_error(isError=true) -> tool_result_message_error(isError=true)`；TUI 应显示红色 `PI_STUDY_ERROR_EXECUTOR_5301`，且绝无绿色成功结果、`gate_error`、`executor_success`、success 或熔断 marker。当前只完成实验卡、聚焦测试与两路只读复核，不证明真实 Provider、Executor 异常包装或 TUI 呈现；等待用户确认实验设计后才给第一步启动命令。
  - executor 实验卡与课程模式理解已通过（2026-08-13）：用户已理解 `observer`、`gate`、`executor` 是课程错误探针选择“在哪一层故意报错”的三个自定义值，不是 Pi 官方 `ctx.mode`；默认 `off` 不启用实验，本次真实 TUI 中 `ctx.mode` 仍是 `tui`，`--tui-mode regular` 只控制终端布局。用户同时确认 executor 组的固定条件、唯一变量、八步通过链和不回滚副作用边界。下一步只启动全新的 executor 实验进程并核对启动页，尚不提交 Prompt；5.3 仍不勾选。
  - 用户真实 Pi executor 实验启动页已验证（2026-08-13）：用户亲自创建新的私有 `pi-study-5.3-error-executor.*` 临时目录并启动 Pi `0.84.1` Regular TUI；截图显示 `[Extensions]` 只列出显式加载的 `error-propagation`，当前 Model 为 `gpt-5.6-terra`，输入框为空且界面没有可见启动错误。命令打印根路径为 `/var/folders/.../pi-study-5.3-error-executor.bOTjgB`，Footer 显示其 macOS 规范路径 `/private/var/folders/.../pi-study-5.3-error-executor.bOTjgB`，两者指向同一临时目录。该截图只证明本次实验进程、Extension 入口、Model 与未提交 Prompt 的初始状态，不证明 `session_start` 已读取 `mode=executor`、动态 Tool 已注册、追踪已就绪、Provider 已调用或 Executor 已进入。下一步保持 Pi 运行，在第二个终端读取固定脱敏日志并确认唯一 `armed` 记录。
  - 用户真实 Pi executor `armed` 门禁已验证（2026-08-13）：用户保持同一 Pi 进程运行，在第二个终端读取固定脱敏日志，实际且仅得到 `0001 error_probe mode=executor state=armed`。这直接证明本次 `session_start` 已读取最终 CLI 模式 `executor`、动态注册课程 Tool，并成功初始化可写的脱敏追踪；它仍不证明 Provider 已请求、Model 会调用该 Tool、门禁会放行、Executor 会进入并抛出预期异常，或 Core 会形成错误 Tool Result。下一步只在同一 Pi 中提交一次与 observer/gate 组相同的固定 Prompt，不按 `Esc`、不重试，等待 Agent 完整收尾后回传界面。
  - 用户真实 Pi executor 界面错误链已验证、内部顺序待核对（2026-08-13）：用户在同一 TUI 只提交一次固定 Prompt；真实 Model 产生 `pi_study_error_probe` Tool Call，界面中的 Tool 区域以红色错误结果显示固定异常 `PI_STUDY_ERROR_EXECUTOR_5301`，没有绿色 `PI_STUDY_ERROR_PROBE_OK`，随后 Model 用一句“工具已调用完成”收尾并恢复输入框。该截图直接证明本次目标 Tool Call 形成了 Model 可见的错误 Tool Result，且 Agent 能在错误结果后继续一个收尾 Turn；Model 的自然语言总结不代表 Tool 成功，界面证据本身也不能区分异常是在 gate 还是 Executor 内发生。下一步保持 Pi 运行，读取同一脱敏日志；只有严格得到 `armed -> call_accepted -> gate_after -> executor_start -> executor_error -> tool_result_error(isError=true) -> tool_execution_end_error(isError=true) -> tool_result_message_error(isError=true)`，且绝无 `observer_*`、`gate_error`、`executor_success`、success 或熔断 marker，才可判定 executor 内部错误链通过。
  - 用户真实 Pi executor 内部错误链已验证（2026-08-13）：用户在 Agent 收尾后、Pi 仍运行时读取同一脱敏日志，实际严格得到八步 `armed -> call_accepted -> gate_after -> executor_start -> executor_error -> tool_result_error(isError=true) -> tool_execution_end_error(isError=true) -> tool_result_message_error(isError=true)`。日志中没有 `observer_*`、`gate_error`、`executor_success`、success、`duplicate_blocked`、`unexpected_call_blocked` 或 `turn_limit_abort`。其中 `gate_after` 直接证明门禁已经放行，`executor_start -> executor_error` 证明异常发生在 Executor 开工后，后三个错误 marker 证明 Pi 将 Executor 异常收敛为 `isError=true` 的 Tool Result、完成 Core 收尾并把结果交给 Model。结合上一张真实 TUI 红色错误结果，本次 executor 主链通过；它只覆盖固定纯内存 Tool 的单次调用，不证明已有副作用会自动回滚。下一步只执行 `/quit` 正常关闭，再复读最终日志确认没有新增成功、重复或熔断 marker；完成退出后的最终核对前，executor 组和 5.3 暂不闭合。
  - 用户真实 Pi executor 组动态验收已闭合（2026-08-13）：用户确认已执行 `/quit`，并在退出后复读同一脱敏日志；最终内容仍严格保持既有八步错误链，没有新增 `observer_*`、`gate_error`、`executor_success`、success、重复调用、意外参数或 Turn 熔断 marker。结合此前真实 TUI 的红色 `PI_STUDY_ERROR_EXECUTOR_5301`，本组直接证明当前 Pi `0.84.1` 在门禁放行、Executor 已进入后，能把 Executor 抛出的异常转为 `isError=true` 的 Tool Result并交给 Model完成一次收尾 Turn；Model 的“工具已调用完成”只是在总结错误结果，不代表成功。错误探针不记录 `session_shutdown`，因此退出后八行不变是预期。executor 组通过；本实验使用纯内存 Tool，只证明错误传播，不证明实际副作用回滚。5.3 仍需清理本次临时目录，并完成 observer、gate、executor 三类职责与后果的最终理解验收，暂不勾选。
  - executor 实验隔离目录已完成可恢复清理（2026-08-13）：用户对本次精确临时路径执行带固定值校验、目标冲突检查和 `mv` 成功判断的移入废纸篓脚本，终端输出 `EXECUTOR_LAB_MOVED_TO_TRASH`。该标记只在 `mv` 成功分支输出，因此支持本次精确源目录已移入废纸篓；本次没有读取或枚举废纸篓内容，不证明目标内容的后续状态。5.3 的模式、取消和三组错误传播动态实验均已完成，下一步只做 observer、gate、executor 三类职责、错误结果和副作用边界的最终理解验收，验收前暂不勾选。
  - 5.3 最终理解首次复述基本正确、三处待纠偏（2026-08-13）：用户正确判断本次 observer 报错后主链继续、gate 报错时 Executor 不启动、Executor 报错会形成 Model 可见的错误 Tool Result，并理解报错不会自动撤销已有副作用。待修正：observer 是观察生命周期事件的普通 Handler，不由“位于 Executor 前”定义，也不只等于日志；gate 检查和裁决的是一次具体 Tool Call及其参数/策略/现场，不验证 Executor 实现本身是否正确；已有副作用不是“一律无法恢复”，而是 Pi 不会自动恢复，需业务事务、补偿、备份或针对受 Git 管理文件的显式 Git 操作，Git 不能恢复数据库、外部请求或所有文件。下一步只针对这三点做最小复述，准确后再完成 5.3 验收。
  - 5.3 最终理解重试范围已收窄（2026-08-13）：逐题复核后，第 1 题“普通 observer 异常后本次后继 Handler/主链继续”的主因果通过，接受“observer 是生命周期观察 Handler而非按 Executor 前后定义”的术语纠正；第 3 题“Executor 抛错后形成 Model 可见的错误 Tool Result”通过。第 2 题虽答对 Executor 不启动，但需改正理由：gate 裁决一次具体 Tool Call是否允许，不校验 Executor 实现正确性；第 4 题需区分 Pi 不自动回滚、Git 只恢复受版本控制文件，以及数据库/外部系统需事务、补偿或备份。下一步只重答第 2、4 题，不重复已通过内容。
  - 5.3 最终理解重试与模块验收已通过（2026-08-13）：用户已准确说明 gate 不检查 Executor 代码逻辑是否正确，而是裁决“这一次执行请求是否被允许”；gate 自身报错意味着允许性未知，因此 Pi fail-closed，直接拒绝并跳过 Executor。用户也准确区分错误传播与恢复：Pi 不会自动回滚已经发生的副作用，Git 只可能恢复受版本控制的文件状态，数据库写入、已发网络请求和其他外部副作用需要事务、幂等、补偿或备份。结合此前已通过的 observer 与 Executor 复述、TUI/Print 模式对照、Fake 与真实取消链、三组真实 Provider/TUI 错误传播实验、隔离清理和独立只读复核，5.3 用户理解与动态证据门禁全部闭合。最终当前快照的 `npm run check` 退出码为 0，类型检查及 29/29 测试通过；学习文档 20 个 Markdown 围栏成对，本地链接目标存在，`git diff --check` 通过。结论不扩大到未实测的 JSON/RPC、自定义 Executor 显式 signal、`pi.exec`、Maven 进程树或真实副作用回滚。
  - 5.3 三类职责回看已补充（2026-08-13）：用户明确要求把 Observer、Gate、Executor 本身的职责和实际场景完整列出，而不是直接进入 5.4。稳定文档现按同一个 Maven 检查任务补齐位置、触发对象、可见信息、控制权、触发频率、实际用途、Spring 类比、不适用职责、正常/错误后果及总流程图。独立复核又修正两处关键边界：Tool Result 后若继续会进入新的 Model Turn并再次触发 Observer；Gate 是可选 Extension 门禁，Executor 无论是否经过 Gate都必须自行守住路径和允许根等输入语义。此次只补强已完成模块的解释，不改变 5.3 完成状态，也不视为 5.4 已开始实现。下一步先由用户确认这张完整对照，再回到 5.4 系统地图。
  - 5.3 三类职责回看理解已确认（2026-08-13）：用户确认已理解 Observer 观察每个 Turn、可选 Gate 裁决单次 Tool Call、Executor 自守输入安全并完成实际工作的完整分工，且要求以上内容保留在稳定文档。该回看不重复验收或改变 5.3 状态；当前断点恢复到 5.4 系统地图，尚未开始实现。
  - 核心内容：区分 `ctx.cwd`、`ctx.mode`、`ctx.hasUI`、Project Trust、SessionManager、Model 状态及活动请求的取消信号；核对普通 Handler、`tool_call` Handler 和 Tool Executor 的错误传播差异。
  - 贯穿实验：同一诊断入口分别在 TUI 与 Print 运行；TUI 使用 UI，无 UI 走明确 fallback；用可取消的本地 Fake 延迟和受控异常验证取消、Handler 失败与 Tool 失败。
  - 验收与边界：无 UI 不等待、不误放行；本地探针取消后受控 Timer 工作停止并清理 listener；错误形成可观察失败而非静默成功。Fake 取消不等于真实 Handler/Executor 或 `Esc` 端到端，Print 通过也不代表 JSON/RPC 已验证；已有副作用不会因报错自动回滚。
- [x] 5.4 实现自定义只读 Tool `pi_study_inspect`，包含参数校验、路径边界、输出截断和清晰渲染。
  - 系统地图进行中（2026-08-13）：沿用“统计 `docs/learning/06-extensions.md` 的标题和链接”完整场景，链路为 `Extension 注册 Tool -> Model 生成 Tool Call -> Pi 做严格 Schema 校验 -> Executor 做路径与文件安全校验 -> 分块读取、取消检查与统计 -> 返回 content/details -> TUI render -> Model 总结`。首版范围拟冻结为只接受 `docs/learning/` 顶层单个 `.md` 文件名，不支持子目录；拒绝绝对路径、分隔符、`.`、`..`、symlink、hard link、非普通文件、读取中变化和超限输入，结果只返回总数与有限样本并明确截断。当前只完成 Pi `0.84.1` 公开契约、仓库现状和安全边界的只读核对，没有写 5.4 代码、增加依赖或运行真实 Tool。下一步先确认 Schema/路径门禁、`content`/`details`/render、显式 signal 三组职责，再冻结实现与测试契约。
  - Schema 与路径门禁系统地图已讲解、待理解确认（2026-08-13）：同一 `pi_study_inspect` 场景中，严格 Schema 只回答参数是否包含必填字符串字段、是否有未知字段等 JSON 形状问题；像 `../../etc/passwd` 仍可能是合法字符串，Schema 不能证明它位于允许根目录、目标存在且是普通 Markdown，也不能识别 symlink 等真实文件关系。可选 Gate 裁决当前调用是否获准；Executor 无论是否经过 Gate，都必须在读取前完成路径语义和文件对象安全检查。下一步只确认这条因果，不提前讲第二组返回与渲染职责。
  - 第一组理解确认未通过、退回概念衔接（2026-08-13）：用户指出尚不清楚 Observer 与 Schema 的区别，也不知道为何从 5.3 错误传播切换到 `pi_study_inspect`。前一条只保留为“已讲解”记录，不计为理解通过。下一步先用同一条端到端链说明：5.3 用 observer/gate/executor 三个故障位置研究错误后果；5.4 开始设计一个尚未实现的用户自定义只读 Tool，其中 Schema 是 Tool 参数合同，`pi_study_inspect` 是计划中的 Tool 名称。确认对象和章节衔接后，再重新讲路径安全因果。
  - 5.3 到 5.4 的对象归属已理解、5.4 请求重新引入（2026-08-13）：用户已理解 Extension 是插件容器，Observer/Gate 是 Extension 注册的 Handler，`pi_study_inspect` 是计划由 Extension 注册的自定义 Tool，Schema 与 Executor 是该 Tool 的组成部分；同时反馈阶段 5 内容增多后容易串乱。下一步暂停分散术语，用一个从用户请求到最终回答的完整大白话故事重新建立 5.4 主链。阶段 5.10 和阶段 5 总门禁需回到同一贯穿场景综合复习加载、生命周期、Context、Observer/Gate/Executor、取消、错误、自定义 Tool、Command、状态与 UI，不以当前一次理解确认替代后续复习。
  - 5.4 大白话主链理解已确认（2026-08-13）：稳定文档已用“统计 `docs/learning/06-extensions.md` 的标题和链接”档案检查场景串起 `Extension 注册 Tool -> Model 生成 Tool Call -> Schema 检查参数 -> 可选 Gate -> Executor 检查真实文件并统计 -> Tool Result -> 后续 Model Turn 回答`。用户确认已理解这条骨架；这只完成 5.4 系统地图的主链确认，不代表 Schema、路径安全、返回与渲染、取消、实现、测试或真实 Model Tool Call 已验收。下一步只讲第一组职责：为什么参数形状通过 Schema 后，Executor 仍必须核对真实路径和文件对象。
  - 5.4 Schema 与 Executor 文件安全分工理解已确认（2026-08-13）：用户已准确说明 Schema 检查文件名参数的结构、类型和字段等“申请表形状”，而 Executor 才能在真实文件系统中核对目标是否位于允许目录、是否存在、是否为普通 Markdown，以及是否为指向外部目标的符号链接。因此 `../../etc/passwd` 即使是合法字符串也不能据 Schema 通过直接读取。该理解只完成系统地图的第一组职责，不证明路径实现已经安全或任何测试已通过。下一步讲第二组职责：`content`、`details` 与自定义 render 的结果去向和边界。
  - 5.4 Tool 结果与 TUI 渲染分工理解已确认（2026-08-13）：用户已准确判断，当 Tool Result 的 `content` 为 18、`renderResult` 因界面代码错误显示 99 时，后续 Model 仍读取 `content` 中的 18，而当前 TUI Tool 区域显示 99。稳定文档已明确 `content` 面向 Model，`details` 是供渲染与状态代码使用的结构化数据，`renderCall/renderResult` 只控制 TUI 表现，不改变执行成败或交给 Model 的内容；自定义 renderer 也是可选能力。该理解不证明真实 renderer 或 Tool 已实现。下一步讲系统地图最后一组：Executor 的显式 `signal` 与协作式取消。
  - 5.4 协作式停止已理解、取消结果状态待确认（2026-08-13）：用户已准确说明，`signal` 变为 aborted 只代表 Pi 发出取消通知；若 Executor 不检查它、也不传给底层操作，工作不会因此保证立刻停止。用户进一步追问“正常返回已取消为何仍算成功”，稳定文档已补充 Pi `0.84.1` 的精确边界：Executor 正常返回任何 `content/details` 都形成 `isError=false`，文字含“已取消”不会改变协议状态；只有抛出异常或以异常拒绝才由 Core 形成 `isError=true` 的错误 Tool Result。下一步只确认“实际停止”和“错误状态”这两个独立维度，不提前进入实现。
  - 5.4 协作式取消与返回/抛错系统地图已理解（2026-08-13）：用户已理解“是否检查并传递 `signal`”决定底层工作是否协作停止，而 Executor 最终正常返回或抛出异常决定 Pi 形成 `isError=false` 还是 `isError=true`。稳定文档已补齐合同判断表：合法统计、空统计和明确截断正常返回；越界、符号链接逃逸、文件不存在、读/解析失败以及本 Tool 约定的用户取消均抛出异常，取消时先停止并清理。至此 5.4 的主链、Schema/真实路径、`content/details`/render 和显式 signal 四组系统地图均已理解；尚未冻结完整实现契约、编写代码、运行自动测试或完成真实 Model Tool Call，因此不勾选 5.4。下一步整理首版范围与验收矩阵，等用户明确确认后才进入实现。
  - 5.4 首版实现合同已获用户确认（2026-08-13）：用户明确回复“确认”，允许进入代码实现。首版 `pi_study_inspect` 固定以每次 Tool Executor 收到的 `ctx.cwd/docs/learning` 为允许根，只接受一个顶层 `.md` 文件名，不接受绝对路径、正反斜杠、`.`、`..`、NUL、子目录或未知参数；文件必须存在且是非符号链接、单硬链接的普通文件。Executor 使用只读、拒绝跟随符号链接的文件描述符分块读取，输入硬上限为 128 KiB，读取前后核对同一文件描述符的对象标识、大小和修改时间，并在打开前后、每个分块以及解析前后检查显式 `signal`。Markdown 由 AST 解析器统计 ATX/Setext 标题以及实际出现的 inline/reference/autolink 链接，不把图片、链接定义或代码围栏中的伪语法计入；结果返回标题与链接总数、合计最多 20 个有序样本和明确截断状态。`content` 给 Model，受限 `details` 给渲染；`renderCall/renderResult` 只显示已有限制的数据。Tool 本身不写文件、不执行 Shell、不跟随 Markdown 链接、不联网。
  - 5.4 验收矩阵已冻结、实现进行中（2026-08-13）：自动测试至少覆盖工厂只注册一次、宿主严格 Schema 的缺字段/未知字段 Red、合法 Markdown（ATX/Setext/inline/reference/autolink）、空结果、20 项边界与超限截断、绝对路径/分隔符/`.`/`..`/非 `.md`、不存在、目录、symlink、hard link、FIFO、输入超过 128 KiB、读取期间变化、打开前与分块间取消、底层读取/解析异常、所有退出路径关闭文件描述符，以及 `content/details/renderCall/renderResult` 分工。完成聚焦与全量自动验证、差异检查和独立只读复核后，仍需由用户亲自执行真实 Pi/Model Tool Call，才能验收 Provider 调度与 TUI 呈现；在此之前 5.4 不勾选。
  - 5.4 直接依赖安装首次 Red（2026-08-13）：尝试以 `npm install --save-dev --offline --ignore-scripts --omit=optional --audit=false --fund=false @earendil-works/pi-ai@0.84.1 @earendil-works/pi-tui@0.84.1` 显式固定运行与测试依赖，npm 因本机缓存缺少 `@earendil-works/pi-telemetry` 的响应而以 `ENOTCACHED`、退出码 1 失败；命令未执行生命周期脚本，也未完成联网请求。该 Red 只证明这组直接依赖无法在当前缓存中整体离线安装，不证明 Tool 代码或 Schema 有问题。下一步不擅自联网，缩小为运行时真正需要的 `typebox@1.3.7` 与 `pi-tui@0.84.1`；Pi 宿主实际 Schema 校验器仅在锁定版本测试中调用，避免为了测试工具引入整套 `pi-ai` 直接依赖。
  - 5.4 依赖边界 Green 与实现 Red 已建立（2026-08-13）：缩小后的 `npm install --save-dev --save-exact --offline --ignore-scripts --omit=optional --audit=false --fund=false typebox@1.3.7 @earendil-works/pi-tui@0.84.1` 退出码为 0，锁文件固定了与 Pi `0.84.1` 一致的公开 Schema/TUI 版本及 `Marked@18.0.5`；全程未联网且未执行安装脚本。随后新增 Markdown、路径和 Tool/renderer 三组聚焦测试，在生产模块尚不存在时执行 `node --test test/markdown-inspection.test.ts test/inspect-path.test.ts test/inspect-tool.test.ts`，三个测试文件均以 `ERR_MODULE_NOT_FOUND` 失败，实际 `0/3`、退出码 1。该稳定 Red 只证明验收入口已指向尚未实现的三个职责，不证明测试本身完整或安全实现已成立。下一步分别实现 AST 统计、安全分块读取和 Tool 合同，再让同一测试转 Green。
  - 5.4 三职责聚焦候选 Green（2026-08-13）：已新增 `markdown-inspection.ts`、`inspect-path.ts`、`inspect-tool.ts`，并由主 Extension 工厂静态注册 `pi_study_inspect`。同一条聚焦命令现为 18/18 通过：真实 Marked token 统计、严格 Schema、合法/非法路径、symlink/hard link/FIFO、128 KiB 上限、读取中变化、取消、异常、`content/details` 与 renderer 分工均有直接断言；`npm run typecheck` 退出码为 0。该 Green 仍只是当前生产函数和聚焦测试层，不证明总工厂 Fake 已适配、全部旧回归通过、真实 Pi Core/Provider 一定选择 Tool或真实 TUI renderer 已呈现。下一步先补主工厂唯一注册断言，再做全量检查与独立复核；5.4 继续保持未完成。
  - 5.4 主工厂接线 Red -> Green（2026-08-13）：`index.ts` 接入 `registerPiStudyInspect` 后，原工厂测试因 Fake API 没有 `registerTool` 而稳定出现 3 个 `TypeError: pi.registerTool is not a function`、实际 2/5 通过、退出码 1；这证明真实主入口已触达新 Tool 注册点。随后只给相关 Fake 增加 Tool 收集/校验，并在总注册用例断言名称数组严格等于单个 `pi_study_inspect`，同一 `node --test test/factory.test.ts` 转为 5/5、退出码 0。该证据证明当前工厂调用一次注册 API且旧工厂行为未被这组测试破坏，不证明真实 Pi 已加载 Tool或 Provider一定调用。下一步运行完整包回归、差异检查和独立只读复核。
  - 5.4 首次全量门禁 Red（2026-08-13）：接线 Green 后首次执行 `npm run check`，类型检查在新改的工厂 Fake 上发现 `registerFlag` 的 `name/options` 与 `on` 的 `eventName` 三个隐式 `any`，退出码 2，测试阶段未启动；同期 `git diff --check` 退出码为 0。该 Red 只证明严格 TypeScript 合同尚未闭合，不代表 Tool 运行逻辑失败。下一步恢复 Fake 参数的显式类型并复跑同一完整门禁。
  - 5.4 首次全量 Green 与测试复审加固（2026-08-13）：补回三个 Fake 参数类型后，`npm run check` 退出码为 0，类型检查及 47/47 测试通过，`git diff --check` 也通过。独立测试复审随后指出原取消用例只覆盖开工前和读取分块间，可能漏掉“读取后/解析前”与“解析后/return 前”；还指出 160 字符上限断言过宽、未知读取异常脱敏和成功结果 details 畸形时 renderer fallback 未直接覆盖。现已补齐这些断言，聚焦 `markdown-inspection + inspect-tool` 为 12/12，类型检查继续通过。Schema 证据当前是 Pi `0.84.1` 真实 `validateToolArguments` 函数而非 Agent Core 全链，不能据此声称真实非法 Tool Call 已被 Core 拦截或已形成错误 Tool Result；该层仍待真实 Pi 实验/更高层验证。
  - 5.4 独立安全复审 Red -> Green（2026-08-13）：复审先构造出两个稳定反例：APFS 同一毫秒内等长覆写会让毫秒时间戳不变，旧实现可能漏判读取中变化；流式 `renderCall`、错误正文和伪造 `details` 又可在 Schema 前或其他 Handler 后携带超长文本与终端控制序列进入 TUI。实现随后改为比较文件描述符的纳秒级 `mtimeNs/ctimeNs`，并在所有 renderer 入口统一去终端序列、清理控制字符、按 Unicode code point 截断且完整校验 details。定点反例连续 50/50 均形成 `FILE_CHANGED`，恶意 OSC/ESC 不再出现在可见文本，100 个伪造样本进入有限 fallback。首个 renderer 回归因 `Text.render(10000)` 自动补空格而产生测试 oracle Red，改为检查去除行尾填充后的可见文本后恢复；没有用放宽生产限制掩盖失败。
  - 5.4 最终自动门禁与独立复审 Green（2026-08-13）：`.pi/extensions/pi-study-guard/` 中 `npm run check` 退出码为 0，严格类型检查与 53/53 测试通过；仓库根 `git diff --check` 退出码为 0。合同、安全和测试三路只读复审均无阻塞，确认工厂唯一注册、严格 Schema、真实文件对象检查、Marked AST、显式取消信号、`content/details`、错误通道和有界 renderer。残余边界是纯 Node 没有可移植的目录 fd/openat2 方案，无法把同一用户恶意替换整个父目录的竞态宣称为已消除；取消也不能抢占单次同步系统调用或同步 Parser，业务只读不等于 OS 沙箱。
  - 5.4 真实 Pi 前置加载 Green（2026-08-13）：在不调用 Provider 的条件下，以 Pi `0.84.1`、显式 `--extension`、`--tools pi_study_inspect` 和 `--help` 加载当前主入口，命令退出码为 0 并正常输出帮助。该证据只证明当前 Pi 能加载模块并解析 CLI/Tool allowlist，不证明 Provider 已收到 Tool 定义、Model 会生成 Tool Call、Core 会执行它或真实 TUI renderer 已呈现。下一步由用户亲自启动真实 TUI 并完成一次固定合法调用；在动态证据和理解验收完成前，5.4 保持未勾选。
  - 5.4 稳定文档已同步（2026-08-13）：`docs/learning/06-extensions.md` 已补充首版三模块合同、Schema/Executor 重复防线、Markdown 统计口径、`content/details/render` 分工、显式取消信号、自动证据分层及同用户目录替换等残余边界；实验 README 和学习索引也已从“无业务 Tool”更新为当前只读 Tool 事实。Markdown 围栏与本地链接结构检查通过。按稳定文档规则，Red/Green 过程与动态测试计数仍只保存在本计划，不堆入主题教程。下一步不再扩展代码，只进行用户真实 Pi 合法调用。
  - 5.4 真实 TUI 启动 Green、合法调用待执行（2026-08-14）：用户先在仓库根记录目标 `docs/learning/06-extensions.md` 的 SHA-256 为 `8a687f90d931b2d3dfb0b0e84ffaa6221bcca52c6b6d12e4af65b5cafab93617`，随后亲自以 Pi `0.84.1`、`openai/gpt-5.6-terra`、显式主 Extension、仅允许 `pi_study_inspect`、`--no-session`、`--no-context-files` 和 regular TUI 启动真实 Pi。启动页显示唯一 Extension `pi-study-guard`、目标模型与空输入框；黄色 Project Trust 提示是 `--no-approve` 下项目 `.pi` 自动资源被忽略的预期结果，显式 CLI Extension 仍已加载。该证据只证明本次启动配置、主 Extension 工厂加载与 Tool allowlist 已就绪，不证明 Provider 已收到 Tool 定义、Model 会选择它、Schema/Executor 已运行、Tool Result 已进入后续 Turn或 renderer 已真实呈现。下一步只提交一次固定合法请求，不按 Esc、不重试，并以本次 TUI 与调用前后文件哈希补齐动态证据。
  - 5.4 真实合法 Tool Call 与 compact renderer Green（2026-08-14）：用户在上述同一 Pi 进程中只提交一次固定请求，要求唯一调用 `pi_study_inspect` 且 `file` 精确为 `06-extensions.md`。真实 `openai/gpt-5.6-terra` 生成一次目标 Tool Call；TUI 自定义 Tool 区域显示 `Pi Study Inspect 06-extensions.md` 与 `40 headings | 7 links | truncated`，无红色错误；后续 Model Turn回答“40 个标题、7 个链接，结果已因样本上限截断”，与 Tool 区域一致。该 Green 证明本次真实 Provider/Model 能发现并选择该 Tool，合法参数通过当前宿主链，Executor 成功返回，Model-facing Tool Result 进入后续 Turn，compact `renderCall/renderResult` 在真实 TUI 呈现；它不独立证明 40/7 的统计绝对正确、Schema 非法输入必被拒绝、路径/symlink/取消分支、所有文件、操作系统只读隔离或调用前后文件未变化。下一步只按一次 `Ctrl+O` 核对 expanded renderer，不发送第二条 Prompt；随后退出并复核同一文件哈希。
  - 5.4 expanded renderer 首次观察未命中展开态（2026-08-14）：用户按键后 TUI 明确显示 `Tool output: collapsed`，Tool 区域仍只有 compact 统计且没有样本。Pi `0.84.1` 源码确认 `Ctrl+O` 会在全局 `toolOutputExpanded` 的 true/false 间切换，状态文字报告切换后的最终状态，并把该值传给自定义 `renderResult`；因此本次直接证据只证明当前最终处于折叠态，不证明 expanded renderer 失败，也不能从无法确认的按键次数反推初始状态。下一步不发送 Prompt，只短按一次 `Ctrl+O`，以出现 `Tool output: expanded` 且 Tool 区域展示有限样本为通过标准。
  - 5.4 真实 expanded renderer Green（2026-08-14）：用户在同一 Tool Result 上再次短按 `Ctrl+O`，TUI 明确显示 `Tool output: expanded`；绿色自定义 Tool 区域保留 `40 headings | 7 links | truncated`，并新增恰好 20 条按源码顺序排列的有限样本，样本以 `H1/H2/H3/H4` 和 `LINK` 区分，其中链接显示受限目标。该操作没有提交第二条 Prompt，因此直接证明当前真实 TUI 会把已存在 Tool Result 的受限 `details` 交给 `renderResult(expanded=true)` 重新渲染，而不需要再次调用 Provider或 Executor；它不证明样本之外的全部 47 项已经显示，也不独立证明统计绝对正确或文件未变化。下一步只输入 `/quit` 结束本次 Pi，再从终端复核目标文件 SHA-256。
  - 5.4 真实合法调用会话已退出、文件哈希待复核（2026-08-14）：用户确认已通过 `/quit` 结束上述真实 Pi 会话，期间没有提交第二条 Prompt。当前合法调用、compact/expanded renderer 与自然退出均已观察；下一步只在普通终端重新计算同一目标文件的 SHA-256，并与调用前基线比较。哈希相同为两次取样时 `docs/learning/06-extensions.md` 的字节内容相同提供极强工程证据，不等于 Extension 受到操作系统只读隔离，也不证明工作区其他路径未变化或两次取样之间从未发生过修改后恢复。
  - 5.4 目标文件哈希 Green 与稳定文档同步（2026-08-14）：用户退出 Pi 后重新计算 `docs/learning/06-extensions.md` 的 SHA-256，结果仍为 `8a687f90d931b2d3dfb0b0e84ffaa6221bcca52c6b6d12e4af65b5cafab93617`，与调用前摘要一致。该证据强力支持两次取样时目标文件字节内容相同，但不是逐字节 `cmp` 的数学绝对证明；它不能排除中间修改后又恢复，也不证明工作区其他文件未变化、Extension 没有写权限或存在 OS 只读沙箱。完成哈希取证后，课程主动把真实合法调用、compact/expanded renderer 与上述证据边界回写到主题文档，因此当前文件哈希将因这次教学文档更新而有意改变；这不反驳更新前已经闭合的两次取样对照。5.4 仍不勾选，下一步只做用户完整因果复述。
  - 5.4 最终理解首次复述部分通过、范围已收窄（2026-08-14）：用户已准确说明 Schema 负责参数字段、类型和格式等静态合同，也准确区分 `content` 给 Model、`details` 给程序/renderer 使用、`renderCall/renderResult` 决定 TUI 展示。Executor 回答中的资源存在、运行状态和主动取消检查方向正确，但“权限是否允许本次调用”主要属于可选 Gate 的策略裁决，“是否已经执行过”是特定业务的幂等/预算要求，并非本只读 Tool 的通用 Executor 职责；本 Tool 的 Executor 负责允许根、真实文件对象、符号链接/硬链接、大小与读取变化等运行时语义安全。复述还缺启动注册、`Ctrl+O` 只重绘既有 details、取消时正常 return 与 throw 的结果差异，以及哈希证据边界。下一步不重复已通过的 Schema 和输出分工，只针对这些混淆/遗漏做最小重答；5.4 继续保持未完成。
  - 5.4 Schema、Gate 与 Executor 定向纠偏已通过（2026-08-14）：在沿用真实 `pi_study_inspect` 场景重新串起完整主链后，用户确认已完全理解：Schema 是每次 Tool Call 开工前的参数合同校验，不读取真实文件；可选 Gate 裁决本次调用是否获准；Executor 在 Schema 通过且 Gate 放行或不存在时，仍必须检查允许根、真实文件对象和取消状态并完成读取统计。稳定主题文档此前已经包含完整步骤、流程图和首版职责表，本次无需重复增加正文。最终理解验收只剩启动注册、`Ctrl+O` 重绘、取消时 return/throw 结果以及哈希证据边界；5.4 继续保持未完成。
  - 5.4 启动注册理解已通过（2026-08-14）：用户在“餐厅菜单”完整场景后确认已完全理解加载、注册与执行的边界：Pi 启动先加载 Extension，工厂通过 `registerTool` 登记 Tool 的名称、说明、Schema、Executor 和 renderer，此时不读取文件；后续 Model看到 Tool 合同并决定是否生成 Tool Call，只有实际调用后才进入 Schema、可选 Gate 和 Executor。启动页出现 Extension 不单独证明 Tool 已执行，真实 Tool 区域与结果才补足本次执行证据。稳定主题文档已有同一主链，无需重复增加正文。最终理解验收只剩 `Ctrl+O` 重绘、取消结果和哈希边界；5.4 继续保持未完成。
  - 5.4 `Ctrl+O` 重绘理解已通过（2026-08-14）：用户准确说明，对同一个既有 Tool Result 做展开、收起只会切换全局 `expanded` 显示状态并让 renderer 使用已有 `details` 重绘；这段操作新增 Executor 执行 `0` 次，也不会重新调用 Model、Schema、可选 Gate，或重新读取文件。计数口径收紧为：最初生成该 Tool Result 时 Executor 总共执行过 `1` 次，之后展开与收起分别是 `false -> true` 和 `true -> false` 的显示切换。稳定主题文档已经记录 `Ctrl+O` 展开有限样本且不再次执行 Tool，本次无需重复增加正文。最终理解验收只剩取消结果和哈希边界；5.4 继续保持未完成。
  - 5.4 取消结果首次重答部分正确、状态判断待纠正（2026-08-14）：用户已正确说明，Executor 主动响应 `signal`、停止读取并完成清理后，实际工作已经停下；但把“正常返回已取消”误判为 Pi 的错误结果，并认为这符合当前 Tool 的取消合同。Pi `0.84.1` 对正常 `return` 形成 `isError=false`，返回文字不会改变协议状态；当前 `pi_study_inspect` 把取消定义为未完成，必须在停止与清理后抛出取消异常，才形成 `isError=true`。下一步只重答“正常 return 的状态与是否符合当前合同”，不重复协作停止；哈希边界随后确认，5.4 继续保持未完成。
  - 5.4 当前实现与假设实现正在定向澄清（2026-08-14）：用户进一步追问“若代码设计为取消后正常返回已取消，后面怎样执行”，并把该假设当成当前实现。源码只读核对确认：`pi_study_inspect` 在开工前、读取后和解析后都通过取消检查抛出 `PI_STUDY_INSPECT_CANCELLED`，读取期间取消也被归一化后继续抛出；只有完整统计成功才正常返回 `content/details`。下一步只并排解释“当前 throw 实现”和“假设 normal return 实现”各自怎样形成 Tool Result，以及 `Esc` 已取消整个 Agent Run 时为何通常不会再有 Model 收尾；理解确认前不勾选 5.4。
  - 5.4 取消结束方式与结果合同理解已通过（2026-08-14）：用户准确确认，取消后是正常返回取消结果还是抛出取消异常，取决于 Extension 作者对本 Tool 的合同定义和代码实现；Pi 不根据“已取消”等返回文字猜测状态，而是把正常 `return` 记为 `isError=false`、把 `throw/reject` 转为 `isError=true` 的错误 Tool Result。当前 `pi_study_inspect` 选择“停止与清理后抛出取消异常”，因为取消代表本次检查未完成；若另一个 Tool 有意把取消定义为正常业务结果，也可以正常返回，但其协议状态就是成功。稳定主题文档已经包含该分工，本次无需重复增加正文。最终理解验收只剩哈希证据边界；5.4 继续保持未完成。
  - 5.4 哈希证据边界首次回答方向正确、范围待收窄（2026-08-14）：用户回答“能证明文件没有经过修改”，已抓住目标文件内容一致这一主线，但该表述超出前后两次 SHA-256 取样的直接证据。准确结论只能是两次取样时同一目标文件的字节内容相同；不能排除中间修改后恢复，也不能证明其他文件未变化、Extension 没有写权限、文件元数据未变化或存在 OS 只读沙箱。主题文档中原“观察区间内未变”的过宽措辞已同步修正。下一步只用一句话重答该边界；5.4 继续保持未完成。
  - 5.4 哈希证据边界与完整理解复述已通过（2026-08-14）：用户准确说明，前后 SHA-256 相同只证明两次取样时目标文件字节内容相同；不能排除中间修改后恢复，也不能证明其他文件或文件元数据未变化、Extension 没有写权限或存在 OS 只读沙箱。结合此前已通过的启动注册、Schema/Gate/Executor、`content/details/render`、`Ctrl+O` 重绘及取消结果合同，5.4 用户理解门禁已经闭合。下一步执行最终自动验证与独立复审；通过前不提前勾选 5.4。
  - 5.4 最终自动门禁、独立复审与模块验收已通过（2026-08-14）：当前快照 `npm run check` 退出码为 0，严格类型检查及 53/53 测试通过；仓库根 `git diff --check` 通过；`06-extensions.md` 的 24 个代码围栏成对，6 个本地链接目标均存在。合同、测试与安全三路独立只读复审均无阻塞，结合真实 Pi `0.84.1`/Provider/Model 合法 Tool Call、compact/expanded TUI、前后 SHA-256、错误与取消矩阵及完整理解复述，5.4 验收闭合。残余边界是不把合法动态成功扩大为非法 Schema/路径/取消的真实 Core 证据，不把 SHA 摘要一致扩大为中间从未修改或 OS 只读沙箱，也不声称纯 Node 已消除同用户父目录 ABA 竞态或协作取消能抢占单次同步操作。结论：5.4 已完成，下一步进入 5.5 系统地图。
  - 核心内容：`registerTool`、严格 Schema、参数语义校验、允许根目录、符号链接边界、取消检查、`content/details`、输出截断与 `renderCall/renderResult`。
  - 贯穿实现：只读取 `docs/learning/` 下 Markdown 的标题和链接统计；拒绝绝对路径、`..` 和符号链接逃逸；超出阈值时返回明确截断提示，不修改文件、不执行 Shell、不联网。
  - 验收与边界：覆盖合法输入、Schema 非法输入、越界路径、符号链接、文件不存在、取消、执行异常和大输出；单测与真实 Model Tool Call 均有证据。业务逻辑只读不等于 Extension 受到操作系统只读限制，截断结果也不是完整内容。
- [x] 5.5 实现 `/study-inspect` Slash Command，并正确处理有 UI 与无 UI 模式。
  - 5.5 系统地图进行中（2026-08-14）：5.4 已完成，当前先沿用检查 `06-extensions.md` 的同一场景，对比两条入口：自然语言请求由 Model 决定是否调用 `pi_study_inspect` Tool；用户输入 `/study-inspect 06-extensions.md` 时由 Pi 命中 Extension Command 并直接调用 Handler。两条入口应复用同一只读检查服务，但 Command 不伪造 Tool Call，Tool 也不调用 Command；当前尚未实现 `/study-inspect`、未冻结参数/反馈合同、未运行 5.5 实验。下一步先让用户确认 Extension Command、Prompt Template、内置 Slash Command 与 Tool 的完整主链，再进入合同设计。
  - 5.5 系统地图理解已确认（2026-08-14）：用户已确认理解，同一检查能力可以有两条独立入口：普通自然语言由 Model 选择 Tool，`/study-inspect` 则由 Pi 直接调用 Extension Command Handler；两者只复用 Pi 无关的检查服务，不互相伪装调用。用户也已理解内置 Slash Command、Extension Command、Prompt Template、Skill 与 Tool 的职责及主链区别。主题文档已补充逐项对照、两入口流程图和模式边界。5.5 仍未实现或验收，下一步讲解并冻结参数解析、TUI 补全、`ExtensionCommandContext` 和 TUI/Print 反馈合同。
  - 5.5 命令合同理解检查进行中（2026-08-14）：用户已准确说明 TUI 参数补全只是输入建议，用户可以忽略建议或粘贴其他内容，因此 Command Handler 必须重新解析并校验最终提交的参数；这一点已通过。反馈合同一次引入 TUI、Print、JSON、Command 结束方式、`ctx.cwd` 和 `ctx.signal`，用户明确表示尚未理解，因此该部分不计为通过。下一步退回同一次检查在 TUI 与 Print 两种外壳中如何把结果交给用户的完整大白话场景，先只讲 UI no-op 与稳定 fallback，再逐步补 JSON 和 Context 边界；5.5 仍未实现或验收。
  - 5.5 TUI/Print 反馈主因果理解已通过（2026-08-14）：用户准确说明，`ctx.ui.notify` 用于在 UI 中展示摘要，而 Print 模式没有可承接通知的 UI，因此只调用该 no-op 方法会让用户看不到结果；Command 必须主动把同一摘要写到 `stderr` 作为稳定 fallback。该结论只验收 TUI/Print 的反馈出口，不代表 JSON 输出边界、Command 结束方式、`ctx.cwd` 或 `ctx.signal` 已理解。下一步只讲 JSON 的 `stdout` 协议边界；5.5 仍未实现或验收。
  - 5.5 JSON 输出边界首次回答部分正确（2026-08-14）：用户已分清 JSON 模式应隔离 `stdout` 与 `stderr`，但把 `stdout` 理解成“给 Model 看”。准确边界是：JSON 模式的 `stdout` 是 Pi 进程交给外部调用程序或脚本解析的 JSON 事件流；Model 通过 Pi 内部消息和 Provider 请求取得上下文，并不读取终端 `stdout`。`stderr` 是独立的诊断或 fallback 通道，主要供操作者观察，也可以被外部程序单独捕获。下一步只针对“谁消费 `stdout`”做一次重答；通过前不继续讲 Context。
  - 5.5 JSON 输出边界理解已通过（2026-08-14）：用户准确说明，JSON 模式的 `stdout` 是提供给外部程序消费的机器接口，普通摘要写入 `stderr` 以与 JSON 事件流隔离并供操作者观察。补充边界是 `stderr` 也可被外部程序单独捕获，Model 则通过 Pi 内部消息和 Provider 请求取得内容，不读取终端 `stdout`。下一步只讲 Command Handler 返回后的结束方式；5.5 仍未实现或验收。
  - 5.5 Command 结束方式理解已通过（2026-08-14）：用户准确说明，`/study-inspect` 是用户直接触发的 Extension Command，Pi 命中后直接调用 Handler，不经过 Model，因此本次路径产生 0 次 Model 请求、0 次 Tool Call 和 0 个 Tool Result。摘要必须由 Handler 在返回前通过 TUI `notify` 或无 UI fallback 主动反馈，`void` 返回值本身不会自动显示；该结论限定于当前 `/study-inspect` 设计，不排除其他 Command 主动发送消息。下一步只讲 `ctx.cwd`；5.5 仍未实现或验收。
  - 5.5 `ctx.cwd` 边界理解已通过（2026-08-14）：用户准确说明，当前 Session 的 `ctx.cwd=/tmp/demo` 时，同一个 `/study-inspect 06-extensions.md` 应从 `/tmp/demo` 项目定位目标；`ctx.cwd` 只是当前会话工作目录这一定位信息，不是权限证明，也不是安全沙箱。实际检查仍须独立验证允许根、文件类型、存在性和符号链接等语义。下一步只讲 `ctx.signal`；5.5 仍未实现或验收。
  - 5.5 `ctx.signal` 边界理解已通过（2026-08-14）：用户准确说明，空闲时直接执行 Extension Command 并未启动 Agent Run，因此 `ctx.signal` 通常不存在且这不是错误；TUI 的 Esc 取消针对当时活动的 Agent Run，不能假定它会自动取消 Command 自身工作。补充边界是即使 signal 存在，Handler 与底层操作也必须主动检查或传递它，取消才会协作生效。下一步汇总共享检查服务与完整命令合同；5.5 仍未实现或验收。
  - 5.5 完整命令合同参考答案已整理（2026-08-14）：用户明确希望由教练直接给出完整标准答案作为复习，本轮不再要求逐项作答。合同冻结为：`getArgumentCompletions` 只做 TUI 建议且没有 `ctx.cwd`，首版只过滤固定安全示例 `06-extensions.md`，不能扫描 `process.cwd()` 冒充当前 Session；Handler 对最终原始参数重新解析和校验；Tool 与 Command 只复用一个不认识 Pi/UI 的检查服务，不互相调用；Command 以 `ctx.cwd` 定位并把开始时取得的可选 `ctx.signal` 快照传给服务；TUI 用一次有界 `notify`，Print/JSON 把同一摘要写入 `stderr`，JSON `stdout` 保留给机器事件流；Handler 返回 `void`，本路径产生 0 次 Model 请求、0 次 Tool Call 和 0 个 Tool Result。该记录只证明教学内容已经覆盖，不冒充用户独立复述、代码实现或真实 Pi 验收；下一步等待用户确认合同没有疑问，再单独请求实现确认。
  - 5.5 稳定学习正文已同步（2026-08-14）：`docs/learning/06-extensions.md` 已补入 `/study-inspect` 首版参数、固定补全、共享服务、`ctx.cwd`/`ctx.signal`、TUI/Print/JSON/RPC 反馈、0 次 Model/Tool Result 及 Esc 边界。准确结论是空闲 Command 没有活动 Agent Run 时，Esc 不会自动终止 Handler；即使取得某个活动 Run 的 signal，也必须由 Handler 和底层操作主动配合。该记录仅完成教学沉淀，5.5 仍未实现、未运行动态实验、未验收。
  - 5.5 教学节奏已调整（2026-08-14）：用户要求取消逐知识点提问，后续改为连续讲解、真实操作和证据总结；不再要求反复复述。该调整只改变教学交互，不降低 5.5 的实现、测试和真实 TUI/Print 验收，也不替代写代码前的单独授权。当前没有阻塞。
  - 5.5 实现分层地图已讲解（2026-08-14）：用户确认完整命令合同已经理解并要求继续学习；当前把实现收束为三个职责层：共享检查服务只接收 `cwd/file/signal` 并返回同一检查报告，不认识 Pi、UI 或 Tool Result；Tool 适配层负责 Schema、`content/details` 与 renderer；Command 适配层负责原始参数、固定补全、`ctx.cwd` 和 TUI/Print/JSON 反馈。两条入口只复用服务与报告，不互相调用。后续实现将先保留 Command 注册、参数、补全、模式反馈及 Tool/Command 结果一致性的 Red，再做最小 Green 和真实 TUI/Print 验收；本轮没有修改 Command 代码、运行 5.5 测试或动态实验，仍等待用户单独明确实现授权。
  - 5.5 实现授权已确认（2026-08-14）：用户明确要求“开始写代码”，当前进入 Red -> 最小 Green -> 扩展回归。首个断点固定为先增加 Command 注册、原始参数解析、固定补全、TUI/Print/JSON 反馈路由以及 Tool/Command 共享真实检查报告的失败测试；Red 保留后再抽取 Pi 无关服务并实现最小 Command。自动测试与独立复审只算实现证据，不能替代用户后续真实 TUI/Print 验收；当前不勾选 5.5。
  - 5.5 共享服务与 Command 稳定 Red（2026-08-15）：已新增 `test/inspect-service.test.ts` 与 `test/inspect-command.test.ts`，分别锁定共享真实检查报告/规范摘要、显式 `cwd/file/signal`、取消与异常边界，以及 Command 注册、固定补全、参数拒绝、TUI `notify`、Print/JSON `stderr` 路由和 `void` 返回。主任务执行 `node --test test/inspect-service.test.ts test/inspect-command.test.ts`，实际 `0/2`、退出码 `1`，两个测试文件分别因 `inspect-service.ts`、`inspect-command.ts` 不存在而得到 `ERR_MODULE_NOT_FOUND`；同期 `git diff --check` 通过。该 Red 只证明新职责尚未实现，不证明测试合同本身已经完整。下一步创建两个生产模块，重构现有 Tool 调用共享服务，并接入主工厂。
  - 5.5 首轮聚焦候选 Green（2026-08-15）：已新增 Pi/UI 无关的 `inspect-service.ts`，把安全读取、Markdown 统计、稳定错误和规范摘要从 Tool 适配层抽出；新增 `inspect-command.ts`，实现单参数解析、固定补全、`ctx.cwd`/单次 `ctx.signal` 快照以及有 UI `notify`、无 UI `stderr` 反馈；现有 `pi_study_inspect` Tool 改为调用同一服务，主工厂已接入 Command。执行 `node --test test/inspect-service.test.ts test/inspect-command.test.ts test/inspect-tool.test.ts` 实际 `28/28` 通过，`npm run typecheck` 与 `git diff --check` 均通过。该结果仍是候选 Green：根工厂回归尚未执行，且独立复核要求继续补齐无候选补全语义、默认 Command 与真实服务/Tool 摘要一致性、服务异常单次脱敏反馈三项，不能据此勾选 5.5。
  - 5.5 聚焦加固 Green 与主工厂 Red（2026-08-15）：补全无候选已统一为 `null`；新增真实临时 Markdown 对照，默认 Command 输出与同一文件 Tool `content` 逐字一致；新增 TUI/Print 服务异常单次脱敏反馈及 RPC `hasUI=true` 通知路由断言。加固后的三文件聚焦测试实际 `33/33` 通过，严格类型检查通过。随后单独执行 `node --test test/factory.test.ts`，实际 `2/5`、退出码 `1`：两个旧 Fake 因缺少 `registerCommand` 得到 `TypeError`，追踪 Fake 则把新增常驻 `study-inspect` 错当成唯一 `pi-study-trace` 而断言失败。该 Red 直接证明主工厂已触达新 Command 注册点，也证明总装配测试尚未适配；它不表示 Command 业务逻辑失败。下一步按 Command 名分别收集并断言常驻与追踪注册，再复跑全量门禁。
  - 5.5 主工厂接线 Green（2026-08-15）：已让三个主工厂 Fake 显式支持并记录 `registerCommand`；无追踪环境时严格断言常驻 Command 名称数组仅为 `study-inspect`，有追踪环境时按名称分别取得 `pi-study-trace` 与 `study-inspect`，只调用前者完成既有脱敏生命周期测试。复跑 `node --test test/factory.test.ts` 实际 `5/5`、退出码 `0`，同期 `npm run typecheck` 与 `git diff --check` 均通过。该 Green 证明默认工厂当前会调用一次 Tool 注册和一次常驻 Command 注册，并保持追踪 Command 独立；仍不证明真实 Pi Slash 分发、补全接线、TUI/Print反馈或 0 次 Provider。
  - 5.5 首次全量自动 Green（2026-08-15）：在 `.pi/extensions/pi-study-guard` 执行 `npm run check`，严格类型检查通过，完整测试实际 `77/77` 通过、退出码 `0`；仓库根 `git diff --check` 退出码同为 `0`。覆盖范围包括共享服务真实文件报告、Tool/Command 默认摘要一致、固定补全、参数拒绝、TUI/Print/JSON/RPC 分支、服务异常脱敏反馈、主工厂唯一注册及既有取消/错误/路径/renderer 全部回归。该 Green 尚未经过最终文档同步和独立只读复核，也不证明真实 Pi 已命中 Slash Command、真实 TUI 补全/通知、Print `stderr`、JSON stdout 不受污染或 0 次 Provider；5.5 继续保持未完成。
  - 5.5 实现说明已同步（2026-08-15）：Extension README 已补齐共享 Service、Command、对应测试与 Tool/Command 唯一注册；稳定学习正文已链接三层生产源码，只保留职责、覆盖类别与证据边界，精确 Red/Green 过程和动态测试计数继续只记在本计划。正文明确自动证据不证明真实 Slash 派发、TUI 补全/通知、Print 文件描述符分离、JSON/RPC 动态行为或 0 次 Provider；主 Extension 地图也已把常驻 `/study-inspect` 与仅追踪开启时的 `/pi-study-trace` 分开。下一步进行代码合同、测试假阳性和文档证据三路独立只读复审；5.5 仍不勾选。
  - 5.5 独立测试复审发现阻塞（2026-08-15）：当前生产行为未发现现成错误，聚焦、类型和全量 `77/77` 仍 Green，但复审证明两处关键接线可被改坏而不触发现有测试：Print/JSON 测试都注入了 Fake `stderr` 且未锁定默认 `stdout` 零污染；主工厂只断言 Command 名称，换成同名空 Handler 也可能 Green。另有共享服务自身文件名门禁、截断文档下 Tool/Command 摘要一致性和 formatter 异常反馈三处覆盖缺口。以上在补回归前阻塞真实 Pi 实验与最终独立复审，不能以现有计数判候选实现通过。
  - 5.5 复审阻塞加固候选 Green（2026-08-15）：新增真实 Node 子进程测试，从默认主工厂取得 `study-inspect` Handler，以 JSON Context 执行真实共享服务，并分别断言操作系统 `stdout` 为空、`stderr` 恰有一份规范摘要；主工厂 Fake 也开始实际调用默认 Handler，缺参必须得到固定 error 通知，避免同名空 Handler 假绿。另补共享服务在注入 reader 前独立拒绝非法文件名、真实截断 Markdown 下 Tool/Command 摘要逐字一致，以及 formatter 异常单次脱敏 fallback。三文件聚焦实际 `32/32` 通过；首次全量因测试环境对象被 TypeScript 推断过窄而报 `TS2339`，明确标注 `NodeJS.ProcessEnv` 后，`npm run check` 严格类型检查与全量 `80/80` 通过，仓库根 `git diff --check` 同样通过。稳定正文已移除动态计数，只保留职责、覆盖类别和证据边界；精确过程继续只记本计划。该结果关闭已知测试假绿阻塞，但仍等待加固快照的最终独立复审，不证明真实 Slash 派发、TUI 补全/通知、Print 文件描述符、JSON/RPC 动态行为或 0 次 Provider。
  - 5.5 加固快照最终独立复审 Green（2026-08-15）：代码/宿主合同、测试假阳性、文档/安全三路只读复审均确认无 P0-P2 阻塞；独立复跑聚焦 `32/32`、严格类型检查与全量 `80/80`、`git diff --check`、Markdown 围栏和本地链接均通过。子进程证据准确锁住默认主工厂 Handler、真实临时 Markdown、OS `stdout` 零普通文本与单一 `stderr` 摘要；它仍是 Fake ExtensionAPI 和手工 Context，不冒充 Pi Loader、Slash Dispatcher、JSON framing、0 次 Provider 或 0 个 Tool Result。共享服务的准确边界是隔离 ExtensionAPI/Context/UI/Tool Result 等宿主合同，而不是完全脱离 Pi 包依赖；其 Markdown 分析仍使用 `pi-tui` 提供的通用解析/清洗工具。当前可以进入用户真实 TUI/Print 验收，5.5 在动态证据闭合前继续保持未勾选。
  - 5.5 用户真实 TUI 启动基线已取得（2026-08-15）：用户以显式 Extension、隔离临时追踪和 Pi `0.84.1` 启动真实 TUI；第二终端读取日志实际得到 `factory -> session_start -> user_bash`。前两项直接证明本次主工厂与 Session 生命周期已进入真实 Pi；`user_bash` 表示该会话在取样前已发生一次用户 Shell 入口事件，不是 Model、Tool Call 或 Executor，因此本次基线并非严格空白，但可以作为已知三行继续做差分。当前日志尚无 `input`、`before_agent_start`、`agent_start` 或 `tool_call`，但命令尚未提交，不能据此证明 `/study-inspect` 已派发、0 次 Provider 或 0 个 Tool Result。下一步在同一 TUI 只验证固定参数补全，再提交一次标准 ASCII 空格命令，并检查这三行之后是否出现新的 Agent/Tool 事件。
  - 5.5 真实 TUI 固定补全候选已进入编辑器（2026-08-15）：用户按既定步骤从 `/study-inspect 06-ext` 进行补全后，真实 Pi `0.84.1` 编辑器中出现完整 `/study-inspect 06-extensions.md`，且尚未提交。该证据支持固定候选已接入真实 TUI 编辑器；单张静态截图没有保留下拉候选、按键过程或回调次数，因此不单独证明补全回调只执行一次，也不证明 Command Handler、共享服务、Model/Tool 绕行或通知已经发生。下一步提交当前命令一次，再对照 TUI 摘要和既有三行生命周期基线。
  - 5.5 真实 TUI Command 通知已验证（2026-08-15）：用户在同一真实 Pi `0.84.1` TUI 中提交一次 `/study-inspect 06-extensions.md`，界面以普通灰色多行通知显示 `File: 06-extensions.md`、`Headings: 43`、`Links: 10`、`Samples: 20/53`、`Truncated: true`、`Truncation reasons: sample_limit` 及有限样本；画面没有 Tool 卡片、`Planning` 或 Assistant 自然语言回复。该证据直接证明真实 TUI Command Handler 已调用共享检查服务，并通过 `ctx.ui.notify` 呈现有界摘要；截图本身仍不能单独证明 Provider/Model 调用为 0、Tool Result 为 0，需立即读取同一会话的生命周期追踪做差分。当前先不退出 TUI。
  - 5.5 真实 TUI Command 主链绕行已验证（2026-08-15）：在上述 Command 完成且 Pi 仍运行时，用户从同一隔离追踪再次读取前 20 行，日志仍严格为 `factory -> session_start -> user_bash`，与提交前基线完全相同；没有新增 `input`、`before_agent_start`、`agent_start`、`turn_start`、`tool_call` 或 Tool Result 消息事件。结合 Pi `0.84.1` 在普通 input、模板和 Agent Run 之前命中 Extension Command并直接返回的源码顺序，本次标准 ASCII 空格 `/study-inspect 06-extensions.md` 可判定为 0 次 Agent/Model 调用、0 次 Tool Call、0 个 Tool Result；这不扩大为任意未知 Slash、Tab 分隔写法、启动期网络或其他 Extension 的通用保证。下一步正常退出本次 TUI并读取最终 shutdown，再进行同一 Command 的 Print 文件描述符对照。
  - 5.5 真实 TUI 实验最终退出已验证（2026-08-15）：用户通过 `/quit` 正常退出后再次读取同一追踪，最终严格为 `factory -> session_start -> user_bash -> session_shutdown reason=quit`；除已知的实验前 `user_bash` 和退出事件外，Command 前后始终没有 Agent/Model/Tool 主链事件。至此真实 TUI 的固定补全候选、一次有界 `notify`、共享服务结果、0 Agent/Model、0 Tool Call/Tool Result及正常退出均闭合；TUI 组完成，下一步先可恢复地清理精确临时目录，再用新隔离目录执行 Print 对照。
  - 5.5 真实 Print 对照进程已运行、结果待分层核验（2026-08-15）：用户在仓库 cwd 中以新的私有临时目录、显式 Extension、`--print "/study-inspect 06-extensions.md"` 和与 TUI 相同的 Model 元数据运行 Pi `0.84.1`，进程实际 `EXIT=0`，隔离根为随机 `pi-study-5.5-command-print.*` 目录。退出码只证明该进程未以非零状态结束；Pi 会把 Command 业务反馈写入独立通道，且 Command 异常也可能被宿主报告后仍视为 handled，因此当前不能仅据 `0` 判定 Command 成功、stderr 摘要正确、stdout 未污染或 0 次 Model/Tool。下一步分别读取 stdout 字节数、stderr 内容和生命周期追踪。
  - 5.5 真实 Print 文件描述符与主链绕行已验证（2026-08-15）：用户对同一隔离进程分别读取三个证据面：`stdout.log` 实际为 `0` 字节；`stderr.log` 恰有一份与 TUI 同值的有界摘要（`06-extensions.md`、`43` 个标题、`10` 个链接、`20/53` 样本、`truncated=true`、`sample_limit` 及相同前 20 项）；生命周期严格为 `factory -> session_start -> session_shutdown reason=quit`。因此本次真实 Print Command 确实没有等待 UI，将普通摘要写到 stderr且没有以普通文本污染 stdout，并同样为 0 次 Agent/Model、0 次 Tool Call、0 个 Tool Result。该证据不扩大为 JSON framing 或 RPC 客户端动态行为，也不把 `EXIT=0` 解释成所有 Command 业务错误都会产生可靠退出码。真实 TUI 与 Print 两组动态验收现已闭合，下一步执行最终自动回归、文档/计划一致性和独立收口复审；通过前 5.5 暂不勾选。
  - 5.5 动态证据后的最终自动与独立复审已通过、等待清理收口（2026-08-15）：主任务重跑 `npm run check`，严格类型检查与全量 `80/80` 测试通过，仓库根 `git diff --check` 通过；代码/宿主合同、测试假阳性、文档/安全三路独立只读复审均确认功能与真实证据无 P0-P2 阻塞，真实服务统计与 TUI/Print 的同一摘要逐字段一致。稳定正文已由“等待真实验证”更新为耐久的 TUI/Print 实测结论，并继续限定 JSON/RPC、Tab 分隔、启动期网络和通用退出码不在证明范围。唯一剩余门禁是本次 Print 私有临时目录仍存在；证据已复核且无后台 Pi 进程，待用户将该精确目录可恢复地移入废纸篓后，再正式勾选 5.5、同步顶部/当前断点/验收表并进入 5.6。
  - 5.5 Print 临时目录清理与模块最终验收已通过（2026-08-15）：用户执行精确路径、同名目标防覆盖的可恢复清理后，主任务只读确认活动临时源目录为 `MISSING`、废纸篓同名目标为 `DIR`。结合共享服务/Tool/Command 分层实现、参数与模式矩阵、严格类型及 `80/80` 自动测试、三路独立复审、真实 TUI 固定补全与一次 `notify`、真实 Print `stdout=0`/同摘要 `stderr`、两组 0 Agent/Model/Tool 主链和稳定文档，5.5 验收闭合。残余边界是不把本次标准 ASCII 空格命令扩大为 JSON/RPC、Tab 分隔、未知 Slash、启动期任意网络或通用退出码保证。结论：5.5 已完成，下一步进入 5.6 危险 Shell 命令审批门禁。
  - 核心内容：Command 由用户直接触发，命中后不依赖 Model 决策；学习参数解析、补全、`ExtensionCommandContext`，并让 Tool 与 Command 复用纯业务服务而不是互相伪装调用。
  - 贯穿实现：`/study-inspect` 调用与 Tool 相同的只读检查服务；TUI 呈现通知或选择结果，无 UI 模式使用稳定 fallback，禁止等待不可用的交互组件。
  - 验收与边界：合法参数、缺失/未知参数、补全、TUI 和 Print 均验证，归一化结果与 Tool 一致；Command 成功不证明 Model Tool 调度成功，Print fallback 不代表阶段 7 的 RPC UI 协议。
- [x] 5.6 实现危险 Shell 命令审批门禁，覆盖 Model `bash` 与用户 `!`/`!!` 两条入口。
  - 5.6 系统地图进行中（2026-08-16）：从同一条“准备执行 Shell 命令”的实际需求切入，正在区分两条独立入口：Model 生成的 `bash` Tool Call 进入 `tool_call` 门禁；用户在 Pi 编辑器直接输入 `!` 或 `!!` 进入 `user_bash` 事件，不经过 Model Tool 调度。当前只讲清职责、审批分支、默认拒绝和非沙箱边界，尚未冻结实验 marker、编写门禁代码或运行 5.6 实验；下一步完成 Pi `0.84.1` 源码合同核对并把完整系统地图沉淀到 Extension 学习文档。
  - 5.6 双入口系统地图与源码合同已核对（2026-08-16）：`docs/learning/06-extensions.md` 已用同一 Shell 审批场景拆出纯策略分类器、审批决策器、Model `tool_call` 适配器和用户 `user_bash` 适配器，并补齐职责表与总流程图。Pi `0.84.1` 的关键非对称是：Model Tool拒绝使用 `{ block: true, reason }`，Core 形成错误 Tool Result且不启动 Executor；`user_bash` 没有 `block`，必须返回带非零 `exitCode` 的替代 `BashResult` 才能跳过默认 Shell。`user_bash` Handler 若直接抛错，Runner 只记录 Extension error 后继续，可能 fail-open，因此策略/UI异常必须由该适配器自行捕获并收敛为拒绝。首版只有 `mode=tui && hasUI=true` 才允许人工确认；RPC、Print、JSON 的待审批命令默认拒绝，避免把协议能力误当成真人在场。文档同时限定后续 Handler 改参、`shellCommandPrefix`、自定义 operations、其他 Extension 与 OS 权限均不在单一字符串门禁的保证内。当前只有静态合同证据，尚未冻结规则和 marker、编写代码或运行 Fake/真实实验，5.6 继续保持未完成。
  - 5.6 首版规则与受控实验合同已冻结（2026-08-16）：纯分类器只逐字接受两条课程命令：固定 `printf` 输出为 `allow`，只在当前私有 cwd 创建 `PI_STUDY_SHELL_GATE_MARKER.txt` 的固定重定向命令为 `approval_required`，其余任何字符串均为 `deny`。审批超时固定 `10000ms`；只有 `mode=tui && hasUI=true`、确认精确为 Yes且批准后 signal 仍未取消才放行，No、Esc、超时、确认期间取消、RPC、Print、JSON、策略异常和 UI 异常全部拒绝。公开确认返回只能可靠区分 `true` 与未确认，No/Esc/超时统一记录 `not_confirmed`，不伪造具体原因。Model 拒绝返回稳定 `block` 且首版不设 `terminate`；用户 Shell 拒绝返回 `exitCode=126`、`cancelled=false`、`truncated=false` 的替代 `BashResult`。自动矩阵将覆盖两入口的精确/近似命令、模式、取消、异常、返回形状、有限 timeout 和真实 Runner 的 `user_bash` throw fail-open 反例；真人最小矩阵为一个 TUI 进程的 Model SAFE、Model No、用户 `!` SAFE、用户 `!` Esc、用户 `!!` Yes，再加一个 Print 无 UI拒绝进程，不要求真人等待超时。真实实验必须使用受信 `0700` 临时项目，固定 `shellPath=/bin/bash`、`shellCommandPrefix` 为空，通过 `--approve` 加载设置，并用 `--no-extensions` 只显式加载课程 Guard，排除后置命令修改器。追踪只记固定枚举，不记原始命令；marker 不存在必须与入口、决定和 Executor 证据合并判断，Print stdout 也不要求为零。当前仅冻结合同，未编写 5.6 门禁代码或运行实验，5.6 保持未完成。
  - 5.6 合同文档门禁已通过（2026-08-16）：主任务完成 `not_confirmed` 口径、真实最小 TUI/Print 矩阵、受信 `0700` 项目、空 `shellCommandPrefix`、固定 `/bin/bash` 与唯一显式 Guard 等前置条件修正；`docs/learning/06-extensions.md` 当前 `28` 个 Markdown 围栏成对，本机 `mmdc` 对新增双入口流程图解析退出 `0`，仓库根 `git diff --check` 退出 `0`。本步只验证系统地图和实现/实验合同，没有新增 5.6 生产代码或自动测试，不能据此证明门禁已注册、Shell 已被阻断或真实确认框工作；下一步仍须得到用户明确实现授权后，从纯策略与双适配器 Red 开始。
  - 5.6 实现授权与 Red 启动（2026-08-17）：用户明确确认开始实现；本轮只运行纯函数、Fake/真实 `ExtensionRunner` 和主工厂自动测试，不启动真实 Shell、Provider 或人工确认实验。当前先复跑既有基线，再分别保存纯策略缺失与双适配器缺失的稳定 Red；在获得对应 Green 前不勾选 5.6。
  - 5.6 实现前基线 Green（2026-08-17）：在尚无 `shell-policy.ts`/`shell-gate.ts` 的当前快照执行 `npm run check`，严格类型检查与既有 `80/80` 测试通过，退出码为 `0`。这只证明 5.1-5.5 既有自动基线健康，不证明 5.6 门禁存在；下一步新增纯策略测试并保存缺实现 Red。
  - 5.6 纯策略 Red（2026-08-17）：新增 `test/shell-policy.test.ts`，把两条课程命令的逐字合同、近似字符串默认拒绝和确定性锁为测试；执行 `node --test test/shell-policy.test.ts` 退出 `1`，首要且唯一失败为 `ERR_MODULE_NOT_FOUND: shell-policy.ts`。该 Red 证明实现确实缺失，不证明任何 Shell 已被拦截；下一步只新增不依赖 Pi、文件系统或 Shell 的最小分类器。
  - 5.6 纯策略最小 Green（2026-08-17）：新增 `shell-policy.ts`，只用严格字符串全等把固定 SAFE 映射为 `allow`、固定 MARKER 映射为 `approval_required`、其余全部映射为 `deny`，不做 trim、Shell 解析、文件访问或执行。聚焦测试 `3/3`、`npm run typecheck` 与目标 `git diff --check` 均通过；这证明纯分类器合同，不证明 Pi 事件接线、确认框、默认 Shell阻断或真实 marker。下一步新增双适配器与真实 `ExtensionRunner` 对照测试并保存 Red。
  - 5.6 双适配器 Red（2026-08-17）：新增 `test/shell-gate.test.ts`，锁定 SAFE、MARKER、取消、TUI Yes/统一未确认、RPC/Print/JSON、策略/UI异常、两个返回对象、非 bash 旁路和事件不改写，并加入真实 `ExtensionRunner` 的裸 `user_bash` throw fail-open 对照与生产 catch 后 fail-closed 判据；执行 `node --test test/shell-gate.test.ts` 退出 `1`，首要且唯一失败为 `ERR_MODULE_NOT_FOUND: shell-gate.ts`。测试没有启动 Shell或写 marker；下一步实现最小双入口适配器。
  - 5.6 双适配器候选 Green（2026-08-17）：新增 `shell-gate.ts`，共同决策器快照 `ctx.signal`、取消优先，只允许 `mode=tui && hasUI=true` 的固定 MARKER 进入带 `10000ms` timeout 的确认，并在确认后再次检查 signal；策略/UI异常统一拒绝。Model 入口映射为无 `terminate` 的固定 `block`，用户入口映射为 `exitCode=126`、`cancelled=false`、`truncated=false` 的替代结果。首次候选运行有 `21/22`，唯一失败是测试把 Pi 的字符串 `ExtensionError.error` 误当成 `Error` 对象；修正 oracle 后聚焦 `22/22` 和严格类型检查通过。当前只证明模块与真实 Runner 合同，主工厂尚未接线；下一步先保存工厂行为 Red。
  - 5.6 主工厂行为 Red（2026-08-17）：在 `factory.test.ts` 中通过默认导出工厂收集真实 Handler，并按 Runner 的“首个非 `undefined` 结果短路”规则派发未知 Model `bash` 与未知 `user_bash`；聚焦执行退出 `1`，Model 分支实际为 `undefined`、期望为固定 `block`。该 Red 直接证明主工厂尚未接入门禁，而不是只检查 import 或事件名；下一步按生命周期观察器 -> 取消探针 -> Shell 门禁的顺序注册，再修正旧 Fake 的单 Handler 假设。
  - 5.6 主工厂接线与结构化决策 Green（2026-08-17）：`index.ts` 已按生命周期观察器 -> 取消探针 -> Shell 门禁的顺序注册；工厂行为 Red 转 Green，取消探针仍保持首个有效 `tool_call` 结果。独立复审后新增固定枚举追踪与 recorder 失败默认拒绝 Red：首次两例均失败（记录数组为空、SAFE 仍为 `undefined`）；将内部布尔决策升级为结构化 `entry/rule/decision/reason/excludeFromContext` 后，追踪与双适配器聚焦 `25/25` 及类型检查通过，且不记录原始命令。
  - 5.6 生命周期追踪接线 Red -> Green（2026-08-17）：先在主工厂真实追踪测试中要求 `user_bash` 观察行后出现固定 `shell_gate entry=user_bash rule=other decision=deny reason=policy_denied excludeFromContext=false`；首次运行实际日志缺少该行，形成稳定 Red。随后 `registerLifecycleTrace()` 返回只含取消与 Shell 决策写入能力的 recorder，主工厂将其分别注入两个门禁；聚焦工厂、策略与门禁测试 `31/31`、严格类型检查和目标差异格式检查通过。该 Green 证明主工厂可以记录固定决策枚举并在追踪写入失败时拒绝，不证明真实 Pi 已派发、确认框工作或 Shell 未执行；下一步运行全量回归和最终独立复审。
  - 5.6 自动实现全量 Green、等待独立复审（2026-08-17）：主任务执行扩展包 `npm run check`，严格类型检查与全量 `106/106` 测试通过，退出码为 `0`；仓库根 `git diff --check`、学习正文 `28` 个围栏成对和新增本地源码链接检查均通过。自动矩阵覆盖生产默认分类器、两入口模式/确认/取消/异常映射、固定追踪、真实 Runner 的裸异常 fail-open 对照、生产 fail-closed、主工厂顺序与既有取消门禁回归；它没有启动真实 Shell、Provider 或确认框，也不能证明 marker、TUI按键、Print 模式或有限规则的沙箱能力。下一步做当前完整差异的三路独立只读复审；无阻塞后再由用户运行真实 TUI/Print 对照，5.6 暂不勾选。
  - 5.6 独立安全复审发现追踪初始化 fail-open Red（2026-08-17）：当前主工厂在注册 Shell 门禁前初始化生命周期追踪；显式追踪路径非法或首次写入失败时工厂抛错，Pi `0.84.1` Loader 会丢弃整个 Extension，导致门禁不存在。新增主工厂回归要求错误追踪路径下工厂仍完成、SAFE 的 Model 与用户入口都固定拒绝；聚焦运行 `1/2`，新增用例实际收到初始化异常，退出码为 `1`，形成稳定 Red。直接调用 `registerLifecycleTrace()` 仍应拒绝不安全路径；主工厂必须把该诊断失败转成门禁 recorder 失败并继续注册，不能用 no-op 放行。当前 5.6 自动候选 Green 已撤回，下一步做最小 fail-closed 修复并覆盖特殊追踪目标，重新全量验证。
  - 5.6 独立合同复审发现运行期追踪恢复放行 Red（2026-08-17）：在追踪已成功初始化后，先把追踪文件临时改成非私有权限，使 `user_bash` 生命周期观察 Handler 的首次写入稳定失败；再恢复文件权限并调用同一事件的 Shell 门禁 Handler。聚焦测试首次调整只命中了底层错误文案，修正 oracle 后实际得到 `undefined` 而非替代拒绝 `BashResult`，`0/1`、退出码 `1`，证明追踪实例没有锁存第一次写入失败，下一次写入恢复时 SAFE 命令会 fail-open。当前修复范围只增加同一追踪实例的 broken latch：任一写入失败后，后续生命周期与门禁 recorder 均固定失败；修复 Green 前不恢复自动候选结论。
  - 5.6 追踪初始化与运行期失败锁存候选 Green（2026-08-17）：主工厂现在捕获追踪初始化异常但继续注册门禁，并向门禁注入固定失败 recorder；已成功建立的追踪闭包新增 broken latch，任一实际写入失败后永久拒绝后续写入，不会因文件恢复而重新放行。主工厂同时验证默认分类器的 SAFE 双入口放行、MARKER TUI确认放行和未知命令双入口拒绝，避免全拒绝 classifier 误接线的假 Green。聚焦三例 `3/3`、严格类型检查与 `git diff --check` 均通过；下一步补特殊追踪对象回归并重跑全量测试，独立复审前仍不恢复最终 Green。
  - 5.6 追踪失败修复后的全量候选 Green（2026-08-17）：追踪写入首次失败现在只抛固定 `PI_STUDY_LIFECYCLE_TRACE_UNAVAILABLE`，不扩散底层临时路径；同一实例永久锁存 broken 状态，恢复原路径也不能让 SAFE 命令重新放行。回归用含 `SECRET` 的已删除再恢复目录直接验证路径不泄漏和用户入口替代拒绝结果，并覆盖 FIFO 打开前拒绝、非私有文件描述符拒绝、硬链接导致主工厂初始化失败后双入口仍 fail-closed。工厂测试 `8/8`，扩展包严格类型检查与全量 `108/108` 测试通过，仓库根 `git diff --check` 和学习正文 `28` 个围栏成对均通过。该自动 Green 仍未启动真实 Shell、Provider或确认框；下一步仅对最新快照做三路独立复审，5.6 保持未完成。
  - 5.6 独立安全复审发现同步短写缺口、加固中（2026-08-17）：当前底层追踪调用 `writeSync()` 后没有比较实际写入字节数；磁盘配额、文件大小限制等极端现场可能短写而不抛，使日志残缺但门禁误判记录成功。该 finding 不否定已有 `108/108` 场景 Green，但阻塞最终复审结论。下一步把完整字节数写入作为放行前提，并用确定性短写注入证明短写会固定失败、锁存 broken 状态且后续门禁不能恢复放行。
  - 5.6 独立工厂复审发现同路径文件替换 Red（2026-08-17）：初始化成功写出 `0001 factory` 后，把日志文件重命名并在原路径新建另一个 `0600`、单链接普通文件；当前代码只复核安全属性，没有固定首次文件对象。新增用例要求生命周期 Handler 固定失败、后续用户 Shell门禁替代拒绝、旧文件只保留 `0001` 且新文件保持空；聚焦运行中短写用例 Green，但替换用例收到“未抛异常”，`1/2`、退出码 `1`。这直接证明旧文件与新文件可分别形成看似连续的审计片段并让 SAFE 命令放行。下一步保存首次成功写入的 `dev/ino`，每次追加必须匹配，否则锁存不可用；修复前再次撤回自动候选结论。
  - 5.6 同步短写与文件身份加固候选 Green（2026-08-17）：追踪写入现在把字符串编码为 Buffer，并要求底层返回字节数精确等于 Buffer 长度；首次成功写入保存 `dev/ino`，后续每次打开都必须匹配同一文件对象。确定性一字节短写会固定失败并锁存；同路径新建另一个 `0600`、单链接普通文件时，生命周期 Handler固定失败，门禁返回替代拒绝，旧文件只含 `0001 factory`、新文件保持空。聚焦四例 `4/4`、严格类型检查与 `git diff --check` 均通过；这关闭当前已知审计连续性窗口，但不把 Node 文件校验声明成 OS 沙箱，下一步重新执行全量回归和最终独立复审。
  - 5.6 最终加固后的全量候选 Green（2026-08-17）：扩展包 `npm run check` 严格类型检查与全量 `110/110` 测试通过，工厂追踪回归包含初始化 hardlink、FIFO、非私有目标、含 SECRET 路径消失后恢复、确定性一字节短写和同路径安全 inode 替换；所有异常均固定失败并让 Shell Gate拒绝。仓库根 `git diff --check` 与学习正文 `28` 个围栏成对通过；高置信凭据扫描没有发现新增凭据，命中的个人路径仅为计划既存历史实验记录。该结果仍只是自动合同与本地文件系统证据，没有启动真实 Shell、Provider或确认框；当前进入最终三路独立复审，5.6 保持未勾选。
  - 5.6 最终独立复审发现稳定正文旧预览 Red -> 修复候选（2026-08-17）：三路复审确认生产代码、双入口合同与追踪 fail-closed 加固没有新阻塞，但 `docs/learning/06-extensions.md` 文末仍把已实现的 Shell 门禁写成“后续实现主题”，且旧图只覆盖 Model `tool_call`，与本章当前双入口实现相矛盾。现已删除重复旧预览，并把后续方向收窄为 5.7 之后的状态持久化、Widget、验收补强和资源释放；等待安全复审对最新文档快照复核后，再进入用户真实 TUI/Print 实验，5.6 继续保持未勾选。
  - 5.6 最终安全复审发现任意 Tool 名泄漏 Red（2026-08-17）：生命周期追踪把宿主提供的任意 `event.toolName` 只做字符集/长度过滤后写盘，`SECRET_TOKEN_123` 这类看似凭据但符合字符规则的自定义名称会原样进入日志，与“只记录白名单元数据、不记录凭据”的合同冲突。新增回归要求内置/课程 Tool 保留稳定名称、未知 Tool 统一写为 `custom`，且原始 secret-like 名称不得出现在日志；当前先保存失败证据，再做最小白名单映射，5.6 自动候选结论暂不恢复。
  - 5.6 Tool 名白名单映射候选 Green（2026-08-17）：聚焦追踪测试先以 `SECRET_TOKEN_123` 复现真实泄漏，实际日志写出原值而期望为 `custom`，`0/1`、退出码 `1`。随后仅允许 Pi `0.84.1` 的七个内置 Tool 名与课程 `pi_study_inspect` 保留原名，其他名称统一映射为 `custom`；稳定 README 与学习正文同步该边界。下一步先复跑聚焦与全量回归，再由安全复审检查最新快照；Green 前不恢复最终自动结论。
  - 5.6 Tool 名脱敏与旧预览清理后的全量 Green（2026-08-17）：未知自定义 Tool 名现在只记录为 `custom`，原始 `SECRET_TOKEN_123` 不再进入日志；聚焦追踪用例 `1/1`、扩展包严格类型检查与全量 `110/110` 测试均通过。稳定正文已删除把 Shell 门禁误写成“后续实现”的旧单入口预览，剩余 `26` 个 Markdown 围栏成对；仓库根 `git diff --check` 通过，旧预览和 secret-like 测试值在稳定源码/文档中均无命中。当前将最新快照交给三路独立复审；无阻塞后才进入用户真实 TUI/Print 实验，5.6 仍不勾选。
  - 5.6 最终安全复审发现自定义消息 Role 泄漏 Red（2026-08-17）：Tool 名完成白名单映射后，复审继续确认 `message.role` 仍只经过字符过滤；Pi 的 `CustomAgentMessages` 可声明合并，未知自定义 role 也可能携带 secret-like 文本。新增同一真实追踪回归，要求 `SECRET_TOKEN_123` role 在 `message_start/message_end` 中均统一写为 `custom` 且原值不得落盘；先保存 Red，再用 Pi 当前固定消息角色白名单做最小修复，5.6 最终自动结论继续暂停。
  - 5.6 消息 Role 白名单映射候选 Green（2026-08-17）：聚焦追踪测试把 `SECRET_TOKEN_123` 作为自定义消息 role，实际日志原样写出而期望为 `custom`，`0/1`、退出码 `1`。随后固定允许 Pi 当前 `user/assistant/toolResult` 与 Coding Agent 的 `bashExecution/custom/branchSummary/compactionSummary`，其他 role 统一映射为 `custom`；README 与学习正文同步该边界。下一步重新执行聚焦和全量回归，并由安全复审继续检查剩余任意字符串入口。
  - 5.6 Tool/Role 脱敏后的最终自动与独立复审 Green（2026-08-17）：新增硬编码矩阵逐一通过真实追踪 Handler 验证 Pi `0.84.1` 七个内置 Tool 名和课程 `pi_study_inspect`，未知 Tool 名及未知消息 Role 均固定映射为 `custom`，secret-like 原值不落盘。扩展包 `npm run check` 的严格类型检查与全量 `111/111` 测试通过，仓库根 `git diff --check`、学习正文 `26` 个围栏、本地链接及稳定文档的凭据/个人路径扫描均通过。合同、主工厂与安全三路独立只读复审均确认最新快照无 P0-P2 阻塞；自动证据仍不证明真实 Pi 事件派发、确认框、Esc、Provider Tool Call、默认 Shell/Executor或 Print 行为，下一步由用户执行受控真实 TUI/Print 矩阵，动态验收完成前 5.6 保持未勾选。
  - 5.6 真实 TUI A 组 Model SAFE 放行已验证（2026-08-17）：用户在新的 `0700` 私有临时项目中以空 `shellCommandPrefix`、固定 `/bin/bash`、`--approve`、仅显式课程 Guard 和唯一 `bash` Tool 启动 Pi `0.84.1`；初始追踪严格为 `factory -> session_start` 且 marker 不存在。真实 `openai/gpt-5.6-terra` 随后只发起一次逐字匹配的 SAFE `bash` Tool Call，TUI 未出现确认框，Shell 实际输出 `PI_STUDY_SHELL_SAFE`，Model 在下一 Turn 用一句话收尾。追踪闭合为 `tool_call tool=bash -> shell_gate entry=tool_call rule=safe decision=allow reason=safe -> tool_result tool=bash -> toolResult message -> 下一 Turn -> agent_end/agent_settled`，marker 仍不存在。该组证明本次真实 Model 入口、安全分类、无审批放行、Executor与成功 Tool Result链路，不证明危险命令拒绝、人工确认、Esc、用户 `!`/`!!` 或 Print 无 UI 分支；下一步执行 B 组 Model MARKER 并选择 No。
  - 5.6 真实 TUI B 组 Model MARKER 选择 No 已验证（2026-08-17）：同一隔离会话中，真实 Model 只发起一次逐字匹配的 MARKER `bash` Tool Call，用户在本机确认框明确选择 No；TUI 显示固定错误 `PI_STUDY_SHELL_BLOCKED`，随后 Model 自然语言错误地概括为“命令已执行”，但该文字不作为执行证据。追踪严格包含 `tool_call tool=bash -> shell_gate entry=tool_call rule=marker decision=deny reason=not_confirmed -> toolResult message`，没有本次调用的 Extension `tool_result tool=bash`，文件检查为 `MARKER_MISSING`。组合证据证明本次请求已触发、门禁在 Executor 前拒绝且写入命令未执行，也再次表明 Model 总结不能替代 Tool/追踪/副作用证据；下一步执行 C 组用户 `!` SAFE。
  - 5.6 真实 TUI C 组用户 `!` SAFE 放行已验证（2026-08-17）：用户在同一 Pi TUI 中直接输入逐字匹配的 `!printf '%s\n' PI_STUDY_SHELL_SAFE`，界面以用户 Bash 区域显示固定输出，未出现确认框、Model 回复或 Tool Result。追踪仅在此前 `agent_settled` 后新增 `user_bash -> shell_gate entry=user_bash rule=safe decision=allow reason=safe excludeFromContext=false`，没有新增 `input/agent_start/tool_call/tool_result`，marker 仍不存在。该组直接证明用户 `!` 入口不经过 Model Tool 调度，但仍经过同一分类规则和用户 Shell适配器；下一步执行 D 组用户 `!` MARKER并在确认框按 Esc。
  - 5.6 真实 TUI D 组用户 `!` MARKER 按 Esc 已验证（2026-08-17）：用户输入逐字匹配的 `!` MARKER 命令，等待本机确认框出现后按一次 Esc；TUI 显示固定 `PI_STUDY_SHELL_BLOCKED` 与合成 `(exit 126)`，没有 Model 回复或 Tool Result。追踪新增 `user_bash -> shell_gate entry=user_bash rule=marker decision=deny reason=not_confirmed excludeFromContext=false`，marker 检查为 `MISSING`。截图/用户动作证明本次使用 Esc，公开 `confirm()` 与追踪只能证明最终未确认，不能把 `not_confirmed` 细分为 Esc、No 或超时；替代 BashResult、门禁决策和 marker 三者共同证明默认 Shell未启动。下一步执行 E 组用户 `!!` MARKER并明确选择 Yes。
  - 5.6 真实 TUI E 组用户 `!!` MARKER 选择 Yes 已验证（2026-08-17）：在前四组均保持 marker 不存在后，用户输入逐字匹配的 `!!` MARKER 命令并在本机确认框明确选择 Yes；TUI 没有拒绝文本或非零退出，重定向使界面正文为空。追踪新增 `user_bash -> shell_gate entry=user_bash rule=marker decision=allow reason=confirmed excludeFromContext=true`；随后文件实际存在，首行精确为 `PI_STUDY_SHELL_EXECUTED`，总计 `24` 字节，证明默认 Shell已执行并写入末尾换行。该组同时证明 `!!` 仍经过与 `!` 相同的审批规则，只通过 `excludeFromContext=true` 控制是否进入后续 Model上下文；TUI A-E 两入口、安全放行、No、Esc和Yes矩阵已闭合，下一步正常退出并核对最终追踪，再进入新隔离 Print 无 UI组。
  - 5.6 真实 TUI A-E 最终退出与证据稳定性已验证（2026-08-17）：用户通过 `/quit` 正常结束同一实验进程；最终追踪在 C/D/E 的 `user_bash` 允许、未确认拒绝和明确确认允许三组固定决策之后只新增 `session_shutdown reason=quit`，没有迟到的 Agent、Tool或门禁事件。最终 marker 仍存在且内容精确为 `PI_STUDY_SHELL_EXECUTED`。因此本次真实 TUI 已闭合 Model SAFE、Model No、用户 `!` SAFE、用户 `!` Esc、用户 `!!` Yes及正常退出；下一步将精确私有目录可恢复地移入废纸篓，再用全新隔离目录执行 Print 无 UI 默认拒绝组。
  - 5.6 真实 TUI 私有实验目录已由用户确认完成可恢复清理（2026-08-17）：用户按提供的精确源路径、同名目标防覆盖和移入废纸篓流程操作后确认可以继续；本轮未转录 `SOURCE_MISSING/TRASH_COPY_PRESENT` 三行终端输出，因此这里只记录用户动作确认，不把源路径消失与废纸篓目标存在写成独立只读核验证据。下一步创建全新的 Print 私有目录，避免沿用已执行过 marker 的 TUI 状态。
  - 5.6 真实 Print F 组进程已运行、待分层核验（2026-08-17）：用户在新的 `0700` 私有临时项目中再次固定空 `shellCommandPrefix` 与 `/bin/bash`，以 `--approve`、仅显式课程 Guard、唯一 `bash` Tool 和真实 `openai/gpt-5.6-terra` 运行 Print 模式，Prompt 只允许一次逐字 MARKER Tool Call；进程实际 `EXIT=0`。该退出码只证明 Pi 进程未以非零状态结束，不证明 Model确实发起 Tool Call、无 UI门禁已拒绝、默认 Executor未进入、marker 不存在或 stdout/stderr 内容正确；下一步分别核验 stdout、stderr、生命周期/门禁追踪和 marker。
  - 5.6 真实 Print F 组无 UI 默认拒绝已验证（2026-08-17）：同一进程的 stdout 为 `19` 字节并仅包含 Model 的“命令已执行。”，stderr 为 `0` 字节；该自然语言结论不作为 Shell 执行证据。生命周期严格包含 `tool_call tool=bash -> shell_gate entry=tool_call rule=marker decision=deny reason=no_local_tui -> toolResult message -> 下一 Turn -> agent_end/agent_settled -> session_shutdown`，没有 `user_bash`，也没有本次调用的 Extension `tool_result tool=bash`；marker 最终为 `MISSING`。因此本次真实 Print 确实通过 Model `tool_call` 入口，在没有本机 TUI审批能力时默认拒绝并跳过 Executor，且没有 stderr 泄漏。该组不证明 JSON/RPC动态行为、Model 总结可靠、任意 Shell语法或其他 Extension组合；下一步可恢复地清理 Print 私有目录，再执行最终自动回归、稳定文档同步和独立收口复审。
  - 5.6 真实 Print 私有实验目录已由用户确认完成可恢复清理（2026-08-17）：用户按精确源路径、同名目标防覆盖和移入废纸篓流程操作后确认可以继续；与 TUI 清理相同，本轮未转录 `SOURCE_MISSING/TRASH_COPY_PRESENT` 三行输出，因此记录为用户动作确认，不冒充独立文件系统核验。真实 TUI/Print 动态矩阵至此完成，下一步同步稳定正文并执行最终自动回归与独立收口复审。
  - 5.6 稳定正文与最终回归已通过、等待最终独立复审（2026-08-17）：`docs/learning/06-extensions.md` 和 Extension README 已沉淀真实 TUI/Print 的耐久结论，明确 Model 自然语言总结不能替代门禁追踪、Executor 边界和 marker 证据，也不外推 JSON/RPC、任意 Shell语法、其他 Extension 组合或 OS 沙箱。最新快照的 `npm run check` 严格类型检查与全量 `111/111` 测试通过，九张 Mermaid 图 `9/9`解析通过，十二个本地链接 `12/12`存在，`26` 条 Markdown 围栏成对，稳定文档个人路径/高置信凭据模式扫描和仓库根 `git diff --check` 均通过。当前只剩合同、主工厂/测试假阳性与安全/文档三路对最新快照的独立收口；结论返回前 5.6 保持未勾选。
  - 5.6 最终工厂复审发现 Handler 顺序回归缺口（2026-08-17）：当前主工厂的实现顺序确实是 lifecycle observer -> cancel probe -> shell gate，但现有测试只断言两个 `tool_call` Handler 均已注册，取消优先级用例又使用会被 Shell Gate 旁路的 `read`。如果未来误把 Shell Gate 放到取消探针之前，MARKER `bash` 可能先弹审批框再被取消探针终止，而旧 `111/111` 仍会误 Green。该缺口阻塞最终勾选；下一步在默认主工厂、追踪开启且取消 Flag 为 `true` 时派发 MARKER `bash`，先保留“Shell Gate 不得调用 confirm、追踪必须先写 `tool_call` 再写 `cancel_probe state=blocked_tool`”的回归 Red，然后只做最小测试修正并重跑全量门禁。
  - 5.6 Handler 顺序回归 mutation Red -> 候选 Green（2026-08-17）：新增默认主工厂真实行为回归，以追踪开启、取消 Flag 为 `true` 和 Runner 风格“首个非 `undefined` 结果即短路”派发目标 `read` 与 MARKER `bash`；它同时要求 lifecycle observer 先留下 `tool_call`，取消探针分别返回 `missing_signal/blocked_tool` 的 `terminate` block，Shell Gate 的 `confirm` 调用次数严格为 `0`，且追踪中不得出现 `shell_gate` 决策。为验证测试有判别力，临时把生产注册顺序错误交换为 Shell Gate -> Cancel Probe 后，聚焦用例真实 `0/1`、退出码 `1`，直接暴露 `confirmCount` 实际为 `1`；立即恢复正确顺序后，同一用例 `1/1`、完整 factory `12/12`、扩展包严格类型检查与全量 `112/112`、仓库根 `git diff --check` 均通过。当前实现已恢复 lifecycle observer -> cancel probe -> shell gate；等待原发现方独立复核最新测试后再决定最终勾选。
  - 5.6 最终独立验收与模块完成（2026-08-17）：原发现顺序缺口的独立工厂复审者在恢复后的共享快照重新检查新增用例，并独立复跑聚焦 `1/1`、完整 factory `12/12`、严格类型检查与全量 `112/112`、仓库根 `git diff --check`，均退出 `0`；确认 mutation Red 能真实抓住错误顺序，当前回归同时锁住 lifecycle observer 先记录、取消探针先短路和 Shell Gate 不得提前确认，未发现新 P0-P2 或关键假阳性。结合此前分类器/双适配器/追踪 fail-closed 自动矩阵、真实 TUI A-E 的 Model 与 `!`/`!!` 放行/No/Esc/Yes、真实 Print F 的无本机 TUI默认拒绝、稳定文档和用户确认的可恢复清理，5.6 既定验收闭合。结论不扩大到 JSON/RPC动态行为、任意 Shell 语法、其他 Extension组合、`shellCommandPrefix` 变化或 OS 沙箱；5.6 完成，下一步进入 5.7 Extension Entry 状态保存与恢复。
  - 核心内容：纯策略分类器与 Pi 适配层分离；Model `bash` 经过 `tool_call`，用户 Shell 经过 `user_bash`；安全命令放行，危险命令在交互模式确认，拒绝、取消、超时、无 UI 和策略异常均默认拒绝。
  - 贯穿实验：单测可以检查危险字符串，真实运行只使用“被规则识别为危险、无业务或破坏性副作用、唯一副作用是在私有临时目录创建固定文件”的 marker 命令；分别验证安全放行、危险拒绝、交互允许、取消和无 UI 默认拒绝，禁止真实删除或提权练习。
  - 验收与边界：两条入口均保留一次稳定 Red -> 最小 Green -> 扩展回归证据，并证明被拒 marker 未执行。有限规则不能覆盖任意 Shell 混淆；门禁不控制 Extension 自身代码、用户普通终端或操作系统权限，也不是沙箱。
- [x] 5.7 使用 Extension Entry 保存并恢复状态。
  - 5.7 系统地图进行中（2026-08-17）：从 5.6 Shell Gate 的 `mode/blockedCount/lastDecision` 切入，先区分四种容易混淆的数据：只活在当前 Extension 实例中的内存变量、写入 Session 并跟随 Branch 的 Custom Entry、会进入 Model Context 的消息，以及只属于一次 Tool Result 的 `details`。当前并行只读核对 Pi `0.84.1` 的 `appendEntry`、Session Manager、Branch 重放、`session_start`/Tree/Fork/Resume/Reload 与 `--no-session` 合同，并设计不读取未脱敏 Session 正文的最小实验；本步尚未冻结数据结构、编写生产代码或运行状态恢复实验，5.7 保持未完成。
  - 5.7 系统地图与 Pi `0.84.1` 合同已核对（2026-08-17）：稳定正文已用 Shell Gate 的 `mode/blockedCount/lastDecision` 场景区分内存白板、Branch 上的 Custom Entry、进入 Model Context 的 Custom Message和单次 Tool Result `details`；明确 `appendEntry()` 追加到当前 leaf并推进 leaf，但新 Session 在首个 Assistant 消息前可能只进入内存树而延迟落盘，且后续持久化异常不具备数据库事务式回滚。分支状态必须先重置默认值，再从当前 `getBranch()` 根到叶重放，不能从包含所有分支的 `getEntries()` 取最后一项；新 Runtime 在 `session_start` 重建，同实例 Tree 导航在 `session_tree` 重建，`session_shutdown` 只清理而不补写状态，`--no-session` 仅保留当前进程内树。正文同时记录 `/new` 必须回到默认状态、CLI 直接打开旧 Session 仍是 `reason=startup`、单纯 Tree 移动 leaf 不单独持久化，以及 Custom Entry 不进入 Model Context的证据边界。该步只有安装包源码合同、系统地图和实验设计，没有实现状态模块、写 Session JSONL 或执行真实生命周期矩阵，因此 5.7 继续保持未完成。
  - 5.7 首版实现与验收合同已冻结（2026-08-17）：固定 `customType=pi-study-guard.shell-gate-state`，v1 完整快照只允许 `version=1`、`mode=enforce`、非负安全整数 `blockedCount` 和 `lastDecision={entry,rule,decision,reason}|null`；禁止保存原始命令、Prompt、cwd、路径、参数、调用 ID、UI 文本、异常正文或凭据。当前 Branch 中任一同类型未知版本、缺失/多余字段、非法枚举或非法计数都会使恢复整体不可用，不迁移、不修复、不跳过；Gate 在 `session_start/session_tree` 完整重放成功前以及 append 失败锁存后，两个入口均显式拒绝且不弹确认。每次评估生成独立完整快照，`blockedCount` 精确定义为当前 Branch 上成功提交快照的 deny 评估数，`lastDecision` 只表示最后一次成功提交的 Gate 评估，不冒充 Shell 已执行或所有基础设施拒绝；先调用 `appendEntry()`，正常返回后才发布 Extension 内存，抛错不发布候选、不自动重试，但不宣称 SessionManager 内存树或磁盘已原子回滚。追踪文件与 Session Entry 是两个非事务写入：首版先提交状态再记录最终脱敏追踪；若状态提交失败则只记录 `state_error` 拒绝并阻止执行，若追踪随后失败则沿用 5.6 fail-closed，但已成功提交的快照仍表示 Gate 评估而不是 Executor 结果。下一步以严格 Schema/重放/Branch 污染、生命周期零追加、双入口 append 异常、真实内存 SessionManager 与 Context 投影为稳定 Red，再做最小 `shell-state.ts` 和主工厂接线；自动 Green 不替代真实 Reload/Tree/Fork/Resume/无 Session矩阵。
  - 5.7 三层稳定 Red 已保留（2026-08-17）：纯 Store/生命周期用例 `node --test test/shell-state.test.ts` 与真实 `SessionManager.inMemory` Branch/Context 用例 `node --test test/shell-state-session.test.ts` 均退出 `1`，精确原因为生产 `shell-state.ts` 尚不存在；Gate 状态接入用例 `node --test test/shell-state-gate.test.ts` 为 `1/6` 通过、`5/6` 失败，唯一 Green 是非 Bash Tool 旁路，失败精确显示当前 Gate 在状态未 ready、状态提交返回 false/抛错时仍未按合同阻止执行，并且两入口提交次数仍为 `0`。测试合同已统一为函数式 `appendEntry` 依赖、生命周期显式重放可解除 `append_failed` 锁存、持久决策不保存 `excludeFromContext`、Gate 只查询 ready 并提交而不读取快照；三文件 `git diff --check` 通过。该 Red 只证明缺失的生产能力，不证明 Session JSONL 落盘或真实 Reload/Tree/Fork；下一步做最小 Green，5.7 继续未完成。
  - 5.7 最小实现候选 Green（2026-08-17）：新增独立 `shell-state.ts`，固定 v1 完整快照与严格白名单解析，从当前 `getBranch()` 根到叶重放；Store 在 restore 前先清旧内存，append 正常返回后才发布候选，append 异常锁存 `append_failed`，`session_shutdown` 只清理。主工厂只创建一个 Store，按 lifecycle trace -> state lifecycle -> cancel probe -> shell gate 接线；Model/User 两入口共用该 Store，未 ready、提交 false/抛错统一 `state_error` 拒绝。聚焦 `shell-state.test.ts` 为 `33/33`、真实 `SessionManager.inMemory` 的 `shell-state-session.test.ts` 为 `2/2`、Gate 接入为 `6/6`、既有 Shell Gate 为 `22/22`、主工厂为 `12/12`；扩展包严格类型检查与全量 `153/153`、仓库根 `git diff --check` 均退出 `0`。自动证据覆盖 A/B Branch污染对照、Custom Entry Context排除、完整快照累计和双入口 fail-closed，但没有写真实 Session JSONL，也没有执行真实 Reload/Tree/Fork/Resume；5.7 保持未完成，等待三路独立复审与动态验收。
  - 5.7 独立复审 P2 加固后的候选 Green（2026-08-17）：Gate 复审补齐所有非 ready 状态与 `getStatus()` 异常、精确 `commit -> trace` 顺序、提交失败只允许一条 `state_error`，并用主工厂证明状态 append 成功而后续追踪失败时请求仍拒绝且完整快照保留；状态合同复审又以 Proxy `getPrototypeOf` 和抛错 getter 复现畸形 owned data 逃出恢复边界，现已把 Branch 读取/遍历/解析/复制整体收敛为 `invalid`，不扩散异常正文，并补从 `Number.MAX_SAFE_INTEGER` 快照提交 deny 的溢出反例。文档同步修正了“首次 MARKER 提交失败前一定未弹确认”和“append 返回失败”的错误口径。最新聚焦状态 Store `37/37`、Gate 状态接入 `12/12`、真实内存 Session `2/2`、既有 Shell Gate `22/22`、主工厂 `12/12`，扩展包严格类型检查与全量 `163/163`、仓库根 `git diff --check` 均退出 `0`；当前等待原发现方最终复核，真实 JSONL与生命周期矩阵仍未执行，5.7 不勾选。
  - 5.7 自动实现最终独立复审 Green（2026-08-17）：状态合同、Gate/失败顺序、主工厂/文档三路审查者分别复核最新快照并独立运行相应聚焦、严格类型、全量与格式门禁，均确认此前 P2 已闭合且未发现新 P0-P2。最终证据为 Store `37/37`、三组状态聚焦 `51/51`、Gate 状态接入 `12/12`、主工厂 `12/12`、严格类型和全量 `163/163`、`git diff --check` 全部退出 `0`；README/学习正文/计划围栏分别为 `4/30/0` 且成对，本地链接 `14/14`，Mermaid `10/10` 可渲染。结论只覆盖纯 Store、Fake 生命周期、真实 Pi `0.84.1` 内存 SessionManager、Context 投影及主工厂接线，不覆盖磁盘 JSONL、跨进程恢复或真实 `/reload`、Tree、Fork、Resume；当前可以进入动态验收，但 5.7 仍保持未勾选。
  - 5.7 真实持久 Session 基线与首个状态快照已验证（2026-08-17）：用户在新的 `0700` 私有临时根内预创建固定 `0600` 空 JSONL，以显式 `--session`、`--session-dir`、`--no-tools`、仅课程 Guard 和 Pi `0.84.1` 启动真实 TUI，全程未发送普通 Prompt或调用 Provider。脱敏核验确认生命周期为 `factory -> session_start reason=startup`，Session Header 已落盘为版本 `3`，初始自有状态 Entry 为 `0`；随后用户输入一条无副作用且不在白名单内的 `!!printf`，TUI 得到固定 `PI_STUDY_SHELL_BLOCKED` 与 `exit 126`，没有实际命令输出。白名单 JSONL 投影只显示一条 `pi-study-guard.shell-gate-state` v1 完整快照：`mode=enforce`、`blockedCount=1`、`lastDecision=user_bash/other/deny/policy_denied`；追踪同步为 `user_bash -> shell_gate ... deny`。该证据直接证明首个脱敏 Custom Entry 已写入本次固定 JSONL，不证明 Reload 后的新实例已经恢复；下一步在同一 TUI 执行一次 `/reload`，核对旧实例 shutdown、新工厂/start，再重复一次相同拒绝并要求第二份快照累计为 `2`。
  - 5.7 真实 `/reload` 生命周期与零追加对照已验证（2026-08-17）：用户在上述同一持久 Session TUI 执行一次 `/reload`；脱敏追踪精确追加旧实例 `session_shutdown reason=reload`，随后新实例从局部序号重新记录 `factory -> session_start reason=reload`。Reload 后白名单 JSONL 投影仍只有首个 `blockedCount=1` 快照，自有 Entry 总数仍为 `1`，证明恢复动作本身没有追加状态节点；该生命周期还证明当前已换成新 Extension 实例，排除了“旧内存继续存在”的解释。此时仅能证明新实例已进入恢复路径，最终恢复值还要由下一次相同拒绝是否从 `1` 累加到 `2` 决定。
  - 5.7 真实 `/reload` 同 Branch 状态恢复已验证（2026-08-17）：Reload 后的新实例中，用户再次输入同一条无副作用 `!!printf`，TUI 仍得到固定阻止结果与 `exit 126`；新实例追踪为 `user_bash -> shell_gate entry=user_bash rule=other decision=deny reason=policy_denied`。白名单 JSONL 投影此时恰有两份自有 v1 完整快照，`blockedCount` 严格为 `[1,2]`，第二份 `lastDecision` 仍是固定脱敏枚举。结合旧实例 shutdown、新工厂、`session_start reason=reload` 和 Reload 自身零追加，可确认新 Store 从当前持久 Session Branch 恢复了 `1` 后再累计为 `2`，而不是复用旧实例内存或重新从默认值开始；该组仍不证明跨进程、Tree/Fork/Resume。
  - 5.7 真实跨进程 JSONL 状态恢复已验证、生命周期追踪有边界（2026-08-17）：上述 Reload 实例先以 `session_shutdown reason=quit` 正常结束，退出后白名单快照仍严格为 `[1,2]`；用户随后用相同显式 Session JSONL 启动全新 Pi TUI，界面重新展示旧 Session 中两次 Shell 拒绝记录，并在新进程第三次执行同一无副作用拒绝。脱敏 JSONL 投影新增第三份完整快照且计数严格为 `[1,2,3]`，直接证明新进程从磁盘 Session Branch恢复 `2` 后继续累计，而不是依赖已销毁的旧 Extension内存或从默认值开始。该次启动命令预期使用新的生命周期日志，但目标日志最终未生成，因此不能把 Pi `0.84.1` 源码所示的 CLI旧 Session启动 `reason=startup`冒充本轮动态日志证据；下一轮启动会单独导出并先核对追踪变量，再进入 Tree。
  - 5.7 Tree 实验首次启动追踪配置诊断（2026-08-17）：用户以同一显式 Session JSONL 启动新的真实 Pi TUI 后，白名单投影保持三份自有状态且计数仍严格为 `[1,2,3]`，证明仅启动没有追加状态快照；但目标 `pi-study-lifecycle-tree.log` 未创建。随后直接核对当前 `lifecycle-trace.ts`，确认 `PI_STUDY_LIFECYCLE_TRACE` 的 basename 必须逐字等于 `pi-study-lifecycle.log`，而本轮 `tree` 目标和上一轮 `run2` 目标均使用了带后缀的非法文件名，因此日志缺失来自追踪路径合同拒绝，不是 Pi 进程未启动。用户已正常退出该进程；独立进程匹配检查为空，退出后白名单投影仍为 `[1,2,3]`，且实验根内没有遗留 `tree-trace.*` 目录。该进程不再用于 Tree 证据；下一轮改为新建独立私有追踪子目录并在其中使用固定文件名后重新启动。该诊断没有读取未脱敏 Session 正文，也没有把缺失日志冒充生命周期证据。
  - 5.7 Tree 实验修正追踪后的启动基线已验证（2026-08-17）：用户在同一实验根中新建唯一 `0700` 私有追踪子目录，并把环境变量指向其中固定 basename `pi-study-lifecycle.log` 后，以同一显式 Session JSONL 启动新的真实 Pi TUI。独立核验确认追踪目标为当前用户所有的 `0600` 普通单链接文件，日志严格只有 `0001 factory -> 0002 session_start reason=startup`；白名单 Session 投影仍恰有三份自有快照且计数为 `[1,2,3]`。这直接补齐了“CLI 新进程打开旧 Session 时启动原因仍为 startup”的本轮动态证据，并证明启动/恢复动作本身没有追加状态快照；尚未执行 `/tree`，因此还不证明分支切换或分支状态隔离。
  - 5.7 Tree 路线 A 的第四份状态快照已验证（2026-08-17）：在上述 startup 基线的新实例中，用户只输入一条独特且不在白名单内的 `!!printf` 请求；TUI 返回固定阻止结果，追踪严格追加 `user_bash -> shell_gate entry=user_bash rule=other decision=deny reason=policy_denied excludeFromContext=true`。白名单 JSONL 投影恰有四份 v1/enforce 完整快照，`blockedCount` 严格为 `[1,2,3,4]`，四份 `lastDecision` 都只含固定脱敏枚举。该结果证明路线 A 已从共同状态 `3` 追加到 `4`；尚未发生 `session_tree`，因此仍不证明切回共同点后会恢复 `3`。
  - 5.7 Tree 共同分支点已在真实 TUI 中定位（2026-08-17）：用户只打开 `/tree` 且尚未提交选择，界面显示当前活动路线共有四个 Bash 节点，前三个依次是既有 `PI_STUDY_STATE_DENY`，第 4 个是可明确区分的 `PI_STUDY_TREE_A_DENY`，当前高亮与计数为 `(4/4)`。因此第 3 个 Bash 节点是路线 A 之前、包含 `blockedCount=3` 快照的最后共同位置；下一步只上移一次到 `(3/4)` 后选择该节点，不生成 Branch Summary，再用下一次状态转移证明 Store 确实恢复为 `3`。仅打开 Tree 没有证明 leaf 已移动，也没有追加状态快照。
  - 5.7 Tree 选点后的观测边界已核清（2026-08-17）：用户已从 `(4/4)` 选择第 3 个共同 Bash 节点，界面返回主视图；白名单 JSONL 投影仍只有四份物理自有 Entry且文件顺序为 `[1,2,3,4]`，说明选点本身没有追加快照。当前生命周期追踪文件没有新增 `session_tree`，源码核对确认这是因为 `lifecycle-trace.ts` 没有订阅该事件；独立的 `shell-state.ts` 状态生命周期则明确订阅 `session_tree` 并调用当前 `getBranch()` 重放。因此“日志没有 session_tree”不是未触发证据，也不能冒充恢复成功；恢复到共同状态 `3` 的决定性动态判据是下一次 deny 是否追加 `blockedCount=4`，而不是错误的 `5`。
  - 5.7 Tree 切到共同点后的状态重放已验证（2026-08-17）：Tree 选择后 TUI 明确显示 `Navigated to selected point`；用户在新路线输入第一条独特 `PI_STUDY_TREE_B1_DENY` 请求，仍得到固定阻止结果与 `exit 126`，追踪新增一组 `user_bash -> other/deny/policy_denied`。白名单 JSONL 投影此时恰有五份物理自有快照，文件顺序严格为 `[1,2,3,4,4]`：第 4 份是旧路线 A 的 `4`，第 5 份是新路线 B 的首个 `4`。若 Store 没有响应 Tree而错误沿用路线 A 的内存 `4`，第 5 份只能是 `5`；因此该对照直接证明 `session_tree` 状态 Handler 从当前 Branch共同点 `3` 重放后再累计为 `4`，同时旧路线 A 的快照仍保留在同一 Session 文件中。
  - 5.7 Tree 路线 B 独立累计到 `5` 已验证（2026-08-17）：用户在路线 B 再输入独特 `PI_STUDY_TREE_B2_DENY` 请求，TUI 继续给出固定阻止结果，追踪追加第三组本进程 `user_bash -> other/deny/policy_denied`。白名单 JSONL 投影现在恰有六份物理自有快照，文件顺序严格为 `[1,2,3,4,4,5]`；这表示共同路径为 `1 -> 2 -> 3`，路线 A 保留独立 `4`，路线 B 则从另一个 `4` 继续到 `5`。该证据直接证明两条 Branch 的状态历史同时保留且路线 B按自己的当前状态累计，但尚未证明从路线 B 切回路线 A会恢复为其独立的 `4`。
  - 5.7 两条状态路线已在真实 Tree UI 中同时显示（2026-08-17）：用户再次只打开 `/tree`，界面在三个共同 `PI_STUDY_STATE_DENY` Bash节点后显示真实分叉：当前路线 B 为 `PI_STUDY_TREE_B1_DENY -> PI_STUDY_TREE_B2_DENY`，高亮位于 `(5/6)`；独立兄弟路线 A 的 `PI_STUDY_TREE_A_DENY` 保留在下一行 `(6/6)`。该截图与物理快照 `[1,2,3,4,4,5]` 相互印证，直接证明旧路线没有被新路线覆盖或删除；下一步只下移一次到 `(6/6)` 选择路线 A，不生成摘要，再用 SAFE 快照验证该路线 Store恢复为自己的 `4`。
  - 5.7 切回路线 A 的 Tree 选点零追加已验证（2026-08-17）：用户从路线 B 的 `(5/6)` 下移并选择兄弟节点 `PI_STUDY_TREE_A_DENY`，未生成 Branch Summary；选点后的白名单 JSONL 投影仍恰有六份物理自有状态 Entry，文件顺序保持 `[1,2,3,4,4,5]`。这证明本次 Tree 导航本身没有追加或覆盖状态快照，但仅凭物理文件不显示当前内存恢复值；下一次精确 SAFE 请求必须追加完整快照且不增加 deny 计数，若 Store 已恢复路线 A，新增计数应为 `4`，若错误沿用路线 B则会是 `5`。
  - 5.7 Tree 切回路线 A 后的独立状态恢复已验证（2026-08-17）：用户在重新选中路线 A 后输入精确 SAFE 用户 Shell 请求，TUI 实际输出 `PI_STUDY_SHELL_SAFE`且没有 `exit 126`；追踪新增 `user_bash -> shell_gate entry=user_bash rule=safe decision=allow reason=safe excludeFromContext=true`。白名单 JSONL 投影恰有七份物理快照，文件顺序严格为 `[1,2,3,4,4,5,4]`，最后一份完整快照的 `blockedCount=4`、`lastDecision=user_bash/safe/allow/safe`。路线 B 的 `5` 仍保留在同一文件中，但当前路线 A 的 SAFE 决策没有增加阻止数且从自己的 `4` 继续；这直接证明恢复使用当前 `getBranch()`，而不是从包含所有路线的物理 Entry末项或路线 B 内存恢复。结合真实 Tree 分叉 UI、两次选点零追加和路线 B 的 `4 -> 5`，本轮真实 Tree/Branch 状态隔离验收闭合；不外推 Fork、Resume或跨进程默认 leaf。
  - 5.7 Tree 实验正常退出与状态零漂移已验证（2026-08-17）：用户在路线 A SAFE 证据闭合后执行 `/quit`；独立追踪核验在 `safe/allow` 之后严格新增 `session_shutdown reason=quit`，白名单 Session 投影仍恰有七份状态且文件顺序保持 `[1,2,3,4,4,5,4]`，匹配该实验根的 Pi 进程检查为空。该结果证明正常 shutdown没有补写或改写状态快照，并把 Tree实验与后续 Fork实验的进程边界分开；追踪目录与 Session仍保留用于最终统一清理。
  - 5.7 Fork 实验合同纠偏与活动 Branch 前置核验（2026-08-17）：Pi `0.84.1` 的内置 `/fork` 选择器只枚举 `type=message && role=user` 的普通用户消息，不枚举 `bashExecution`，因此原计划“选择路线 A 最后 SAFE Bash节点”不可达；当前尚未执行 Fork、没有创建新 Session，也没有动态证据需要撤回。原 TUI 已正常退出且没有匹配进程；版本白名单投影确认原文件为 Session v3并有七份自有状态，随后用 Pi `SessionManager.open(...).getBranch()` 做只读脱敏投影，当前活动 Branch恰有五份自有状态 `[1,2,3,4,4]`，最后一次为 `user_bash/safe/allow/safe`，直接确认物理末 leaf对应路线 A而不是兄弟路线 B。最终合同不使用 `/clone` 冒充 `/fork`，也不从外部手工追加 Session消息；下一轮在 `--no-tools` 的真实 TUI中先发送一个固定普通 Prompt取得 Assistant baseline，再发送一个文本唯一的普通 user anchor并等待回复，两轮会产生明确的 Provider请求但不触发 Tool或 Shell Gate，且自有状态必须保持不变。随后 `/fork` 选择唯一 anchor；默认 `position=before`，所以新 Session应包含 baseline与路线 A状态 `[1,2,3,4,4]`，排除 anchor、其回复和兄弟路线 B 的 `5`。baseline只用于让 Fork路径含 Assistant并立即物化新 JSONL，anchor只用于提供可选边界，两者都不是状态恢复证据；Fork后再追加一次 deny得到 `5`，才证明新 Store从继承的 `4`恢复。所有核验只输出固定枚举、计数、布尔值和自有 Entry投影，不输出消息正文、ID或路径关系。
  - 5.7 真实 Fork 进程启动基线已验证（2026-08-17）：用户以原显式 Session、`--no-tools`、唯一显式 Guard 和新的私有固定名追踪目录启动 Pi `0.84.1`；TUI重新展示原 Session历史。独立只读核验确认新追踪是当前用户所有的 `0600` 普通单链接文件，内容严格为 `factory -> session_start reason=startup`；白名单 Session投影仍恰有七份自有状态，物理追加顺序保持 `[1,2,3,4,4,5,4]`，没有因启动或恢复追加快照。结合上一条已验证的活动 Branch `[1,2,3,4,4]`，Fork 前置基线闭合；下一步只发送固定普通 baseline Prompt并等待固定 Assistant回复，再确认 Shell状态零追加。
  - 5.7 Fork Assistant 落盘基线与状态零追加已验证（2026-08-17）：用户在上述真实 TUI、`--no-tools` 条件下发送固定普通 Prompt，Assistant逐字回复 `PI_STUDY_FORK_BASELINE`。追踪新增且仅新增一次普通 `input -> before_agent_start -> agent_start -> turn_start -> user message -> assistant message -> turn_end -> agent_end -> agent_settled` 主链，没有 `tool_call`、`tool_result`、`user_bash` 或 `shell_gate`；白名单 Session投影仍恰有七份自有状态且顺序保持 `[1,2,3,4,4,5,4]`。这证明 baseline 已在当前路线产生 Assistant 节点而没有改动 Shell Gate状态；它用于让后续 Fork路径可立即物化新 JSONL，不是状态恢复证据。下一步发送文本唯一的普通 user anchor并等待固定回复，再做同样的状态零追加核验。
  - 5.7 Fork 普通 User Anchor 与状态零追加已验证（2026-08-17）：用户发送文本唯一的普通 Prompt `PI_STUDY_FORK_ANCHOR_5701...`，Assistant逐字回复固定 ACK。追踪只新增第二组普通 `input/agent/turn/user message/assistant message/settled` 主链，累计仍没有 `tool_call`、`tool_result`、`user_bash` 或 `shell_gate`；白名单 Session投影继续恰有七份自有状态，物理追加顺序仍为 `[1,2,3,4,4,5,4]`。因此 baseline 与 anchor 两轮只建立真实 `/fork` 所需的普通消息边界，没有改变 Shell State；下一步只打开 `/fork` 选择器并先确认唯一 anchor可见，暂不提交选择。
  - 5.7 真实 `/fork` 普通消息选择器边界已验证（2026-08-17）：用户在同一真实 TUI只打开 `/fork`，界面严格显示两条候选，分别为 baseline普通 User Message的 `Message 1 of 2` 与唯一 anchor的 `Message 2 of 2`；既有 BashExecution历史没有出现在候选中。当前高亮已位于文本唯一的 `PI_STUDY_FORK_ANCHOR_5701`，尚未按回车提交，因此此时没有 Runtime替换或新 Session证据。下一步只提交当前高亮一次；返回主界面后不得再次提交被回填到编辑器的 anchor文本，先核对 fork生命周期和新 Session结构。
  - 5.7 真实 `/fork` Runtime替换与新 Session继承结构已验证（2026-08-17）：用户只提交当前唯一 anchor一次，TUI显示 `Forked to new session`，并把所选 anchor文本回填到编辑器且尚未再次提交。脱敏生命周期严格追加旧实例 `session_shutdown reason=fork`，随后新实例从局部序号重新记录 `factory -> session_start reason=fork`；私有 Session目录中的 JSONL文件数由 `1` 变为 `2`。白名单投影确认新文件具有父 Session引用但不输出其值，自有状态严格为当前路线的 `[1,2,3,4,4]`，包含 baseline Prompt/Reply，排除所选 anchor及其 ACK，也未复制兄弟路线 B 的 `5`；源文件仍严格为 `[1,2,3,4,4,5,4]`且保留 baseline与anchor两轮。该证据证明真实 Fork复制边界和独立新 JSONL正确，但仅复制快照还不能证明新 Extension Store已从 `4`恢复；下一步清空回填文本后追加一次 deny，预期新文件末项从 `4`到`5`。
  - 5.7 真实 Fork 新 Store继承状态恢复已验证（2026-08-17）：用户先用 `Ctrl+C`清空被回填而未提交的 anchor，再执行一次不在白名单内且无业务副作用的 `!!printf`；TUI得到固定 `PI_STUDY_SHELL_BLOCKED`与 `exit 126`。新实例追踪严格为 `user_bash -> shell_gate entry=user_bash rule=other decision=deny reason=policy_denied excludeFromContext=true`；新 Fork Session白名单状态由继承结构 `[1,2,3,4,4]`追加为 `[1,2,3,4,4,5]`，最后决策只含上述固定枚举。源 Session同时保持原物理追加顺序 `[1,2,3,4,4,5,4]`且最后仍为 `user_bash/safe/allow/safe`。该对照证明 Fork 后的新 Runtime Store从当前路线继承的`4`恢复并独立累计，而不是从默认值、兄弟路线 B或源文件物理末项恢复；真实 Fork动态验收闭合。下一步正常退出新 Fork Session并核对 shutdown，再用该新 Session做跨进程 Resume实验。
  - 5.7 真实 Fork Session正常退出与双文件状态零漂移已验证（2026-08-17）：用户在 Fork恢复证据闭合后执行 `/quit`；脱敏追踪严格在新实例的 deny 后追加 `session_shutdown reason=quit`。退出后白名单投影确认新 Fork Session仍为 `[1,2,3,4,4,5]`、源 Session仍为 `[1,2,3,4,4,5,4]`，私有目录中仍恰有两个 Session JSONL，且匹配实验根的 Pi进程为空。该结果证明正常 shutdown没有补写、覆盖或合并两个 Session的自有状态，并为进程内真实 Resume实验建立单写者边界。
  - 5.7 真实 Resume 单变量合同已冻结（2026-08-17）：下一组从源 Session精确 JSONL启动新 Pi进程，启动基线必须仍是 `factory -> session_start reason=startup`；随后只在同一 TUI内执行 `/resume` 并精确选择 Fork Session，预期旧 Runtime先记录 `session_shutdown reason=resume`，新 Runtime局部序号重置后记录 `factory -> session_start reason=resume`。切换本身不得追加状态；再执行一次无副作用且不在白名单内的 `!!printf`，Fork状态必须从 `[1,2,3,4,4,5]`追加到`[1,2,3,4,4,5,6]`，源 Session保持`[1,2,3,4,4,5,4]`，才能同时证明选中了 Fork且新 Store从其当前 Branch的`5`恢复。单独看到`reason=resume`不足以证明选对 Session；CLI直接打开旧文件的`reason=startup`也不能冒充 Resume。内置选择器会读取并显示 Session中的普通消息用于搜索，因此本实验只使用这两份内容受控的私有课程 Session，不复制选择器正文、完整 Session ID或路径；当前尚未启动本轮 Resume进程。
  - 5.7 真实 Resume 源 Session启动基线已验证（2026-08-17）：用户以源 Session精确 JSONL、原私有`session-dir`、`--no-tools`和唯一显式 Guard启动新 Pi `0.84.1` TUI；新的私有追踪目标为当前用户所有的`0600`普通单链接文件，内容严格为`factory -> session_start reason=startup`。白名单投影确认源 Session仍为`[1,2,3,4,4,5,4]`且最后为`user_bash/safe/allow/safe`，Fork Session仍为`[1,2,3,4,4,5]`且最后为`user_bash/other/deny/policy_denied`，私有目录仍恰有两份 JSONL；启动与恢复自身没有追加状态。匹配实验根的 Pi进程恰有一个，单写者边界成立。下一步只打开`/resume`并用脱敏 Fork ID短后缀过滤，暂不提交选择。
  - 5.7 真实 `/resume` 唯一目标已定位（2026-08-17）：用户在上述源 Session TUI中打开`/resume`，只键入脱敏的 Fork Session ID末八位进行过滤；真实选择器结果恰有一条，显示的是本轮受控 Fork baseline，当前高亮已落在该唯一候选上且尚未按回车。因此短后缀定位成功，未依赖默认索引、活跃时间、树缩进或完整 Session ID，也尚未发生 Runtime替换或状态追加。下一步只提交当前高亮一次，返回主界面后先核对 Resume生命周期与两文件零追加，不立即执行 Shell。
  - 5.7 真实 `/resume` Runtime替换与零追加已验证（2026-08-17）：用户只提交上述唯一 Fork候选；同一私有追踪严格从源启动的`factory -> session_start reason=startup`追加旧实例`session_shutdown reason=resume`，随后新实例局部序号重置并记录`factory -> session_start reason=resume`。切换后白名单投影确认源 Session仍为`[1,2,3,4,4,5,4]`、Fork Session仍为`[1,2,3,4,4,5]`，文件数仍为`2`，匹配实验根的 Pi进程仍恰有一个；因此本次进程内 Session替换已发生且切换动作本身零状态追加。该证据单独仍不能证明新 Store从 Fork当前`5`恢复，下一次固定 deny必须追加`6`而源 Session保持不变。
  - 5.7 真实 `/resume` 目标 Branch状态恢复已验证（2026-08-17）：Resume后的新 Runtime中，用户只执行一次无副作用且不在白名单内的`!!printf`；TUI得到固定`PI_STUDY_SHELL_BLOCKED`与`exit 126`。新实例追踪严格追加`user_bash -> shell_gate entry=user_bash rule=other decision=deny reason=policy_denied excludeFromContext=true`；白名单投影确认 Fork Session从`[1,2,3,4,4,5]`追加为`[1,2,3,4,4,5,6]`，最后决策只含相同固定枚举，而源 Session继续保持`[1,2,3,4,4,5,4]`及最后`safe/allow`，文件数仍为`2`。结合`startup -> shutdown reason=resume -> 新 factory/session_start reason=resume`和切换自身零追加，该对照直接证明进程内 Resume精确选中 Fork且新 Store从其当前 Branch的`5`恢复，而不是沿用源 Session的`4`或默认值；真实 Resume动态验收闭合，下一步正常退出并确认零漂移。
  - 5.7 真实 Resume 正常退出与状态零漂移已验证（2026-08-17）：用户在 Resume 后的 Fork Runtime 执行 `/quit`；同一私有追踪在该实例 `user_bash -> shell_gate ... deny` 之后严格新增 `session_shutdown reason=quit`。退出后的白名单投影确认源 Session仍为`[1,2,3,4,4,5,4]`，Fork Session仍为`[1,2,3,4,4,5,6]`，文件数保持`2`，匹配实验根的 Pi进程检查为空。该结果证明正常 shutdown 没有补写、覆盖或迁移自有状态，并闭合了本轮真实 Resume 的进程边界；持久 Session 根继续保留，等待 5.7 全部动态矩阵结束后统一可恢复清理。
  - 5.7 `--no-session` 只读 State Probe 稳定 Red（2026-08-17）：新增仅面向显式 fixture 的聚焦合同测试，固定单一 Slash Command、当前 Branch重放、`sessionFile=none|present`脱敏映射、普通消息与外部 Custom Entry正文不读取、畸形/未知版本固定不可用、零 Entry追加和 Reload后必须使用新 Command Context。执行 `node --test test/state-probe.test.ts` 退出码为`1`，唯一首因是 `fixtures/state-probe/index.ts` 尚不存在的 `ERR_MODULE_NOT_FOUND`；这证明测试先于实现失败，未把既有 Store或 Fake成功冒充 Probe Green。下一步只实现该显式加载的只读 fixture，不接入生产 `index.ts`。
  - 5.7 `--no-session` 只读 State Probe 最小候选 Green（2026-08-17）：新增 `fixtures/state-probe/index.ts`，只注册 `/pi-study-state-probe`；每次调用都从当次 Command Context取得当前 Branch，复用生产 `createShellStateStore()`严格重放，内部 append guard一旦被触发即固定失败。输出仅含`sessionFile=none|present`、自有 Entry数、`blockedCount`和最后一次固定枚举决策，任何读取/解析异常统一为`PI_STUDY_STATE_PROBE_UNAVAILABLE`；fixture不接主工厂、不注册事件或 Tool，也不读取普通消息正文。聚焦测试从缺模块 Red恢复为`11/11`、退出码`0`，并用真实`SessionManager.inMemory()`、A/B Branch、畸形快照、路径脱敏和连续两个新 Context验证零追加与不缓存旧 SessionManager。下一步执行严格类型、全量回归、稳定文档同步和独立复审；真实 A/B TUI对照前仍不勾选5.7。
  - 5.7 `--no-session` Probe 全量自动 Green（2026-08-17）：严格 TypeScript 检查与扩展全量测试通过，结果为`174/174`、退出码`0`；仓库根 `git diff --check` 通过。独立 Pi `0.84.1` 宿主源码核对确认：`--no-session` 每次进程创建新的 in-memory SessionManager，`/reload` 重建 Extension Runtime但复用同一个 SessionManager，Slash Command命中后在 Input、认证、Model与 Agent Run之前直接返回；当前 Probe实现也通过当前 Branch、脱敏、零追加和新 Context复核。该证据仍不能替代真实 TUI中的`0 -> 1 -> reload仍1 -> 2`与全新进程`0 -> 1`对照，5.7继续保持未完成。
  - 5.7 `--no-session` Probe 稳定文档与独立复审 Green（2026-08-17）：Extension README、学习索引和`06-extensions.md`已同步显式 fixture、当前 Branch严格重放、固定有界 TUI摘要、零追加及 Print/JSON通知为 no-op 的合同；稳定正文没有写入尚未发生的真实 A/B运行计数。独立复审确认主工厂未接入 Probe、每次 Command使用新 Context、失败只返回固定不可用文案；`git diff --check`、Markdown围栏成对和本地链接`23/23`均通过。该证据仍不替代真实 TUI模式接线、同进程 Reload累计或全新进程归零，5.7继续保持未完成。
  - 5.7 `--no-session` 真实 A 组启动基线已验证（2026-08-18）：用户在新的私有临时根中以 Pi `0.84.1`、`--no-session`、禁用内置 Tool和自动资源，并仅显式加载`pi-study-guard`与`state-probe`启动 TUI；启动页只列这两个 Extension，输入框为空且没有启动错误。该证据只证明隔离进程与观测口已加载，不证明初始状态为零、生命周期无 Agent/Tool主链或 Reload后的状态延续。下一步只执行一次`/pi-study-state-probe`，预期固定摘要为`sessionFile=none ownedEntries=0 blockedCount=0 lastDecision=none`。
  - 5.7 `--no-session` 真实 A 组初始零状态已验证（2026-08-18）：用户在上述进程首次执行只读`/pi-study-state-probe`，TUI逐字段显示`sessionFile=none ownedEntries=0 blockedCount=0 lastDecision=none`。同一固定追踪仍严格只有`factory -> session_start reason=startup`，没有`input`、Agent、Turn、Tool或Shell事件；因此该 Command只读观测当前空 Branch，没有启动 Model/Provider主链，也没有追加自有状态。该证据只证明进程 A 的初始 in-memory Session为空；下一步执行一次固定未知用户 Shell请求，预期门禁拒绝并提交第一份内存快照。
  - 5.7 `--no-session` 真实 A 组第一次 deny 已验证（2026-08-18）：用户只执行一次固定未知用户 Shell请求`!printf '%s\n' PI_STUDY_STATE_DENY`，TUI得到固定`PI_STUDY_SHELL_BLOCKED`与`exit 126`；追踪严格新增`user_bash -> shell_gate entry=user_bash rule=other decision=deny reason=policy_denied excludeFromContext=false`，没有 Agent、Model或Tool主链。该组合证明请求进入用户 Shell门禁并在执行前被显式拒绝；它尚未直接证明状态快照已写入当前 in-memory Branch，下一步必须用只读 Probe核对`ownedEntries=1 blockedCount=1`。
  - 5.7 `--no-session` 真实 A 组第一份内存快照已验证（2026-08-18）：第一次 deny 后，用户再次执行只读`/pi-study-state-probe`，TUI逐字段显示`sessionFile=none ownedEntries=1 blockedCount=1 lastDecision=user_bash/other/deny/policy_denied`；固定追踪仍保持到第一次`user_bash -> shell_gate ... policy_denied`，没有因 Probe新增 Agent、Tool或状态决策事件。该结果直接证明门禁把完整脱敏快照追加到当前 in-memory Branch，且 Probe本身零追加；它尚未证明新 Extension Runtime能在 Reload后重放该快照。下一步只执行一次`/reload`。
  - 5.7 `--no-session` 真实 A 组 Reload Runtime替换已验证（2026-08-18）：用户只执行一次`/reload`；同一固定追踪严格在旧实例第一次 deny之后追加`session_shutdown reason=reload`，随后新实例的局部序号重新从`0001 factory -> 0002 session_start reason=reload`开始，没有新增 Agent、Tool或Shell事件。该结果直接证明旧 Extension Runtime已关闭并创建了新 Runtime，而不是旧闭包继续工作；Reload动作本身不证明新 Store已从仍在内存 SessionManager中的 Branch快照恢复。下一步必须用 Reload后的新 Command Context执行只读 Probe，预期仍为`ownedEntries=1 blockedCount=1`。
  - 5.7 `--no-session` 真实 A 组 Reload 后状态重放已验证（2026-08-18）：用户在新 Runtime 中只执行一次`/pi-study-state-probe`，TUI逐字段显示`sessionFile=none ownedEntries=1 blockedCount=1 lastDecision=user_bash/other/deny/policy_denied`。结合此前旧实例`session_shutdown reason=reload -> 新 factory/session_start reason=reload`，该结果直接证明新 Extension Store通过新的 Command Context从同一个 in-memory SessionManager的当前 Branch重放了第一份快照，而不是沿用旧闭包内存或从默认值开始；只读 Probe自身没有追加状态。下一步只执行第二次相同固定 deny，先验证门禁结果，再单独用 Probe检查是否从`1`累计到`2`。
  - 5.7 `--no-session` 真实 A 组 Reload 后继续累计已验证（2026-08-18）：用户在新 Runtime 中第二次执行相同固定 deny，TUI仍得到`PI_STUDY_SHELL_BLOCKED`与`exit 126`；随后只读 Probe逐字段显示`sessionFile=none ownedEntries=2 blockedCount=2 lastDecision=user_bash/other/deny/policy_denied`。该结果直接证明新 Store不是从默认值重新计数，而是从 Reload前重放得到的`1`继续提交到`2`；`sessionFile=none`同时证明当前 SessionManager没有 Session文件路径。真实 A 组核心因果链`0 -> 1 -> reload后仍为1 -> 2`已经闭合，但进程仍需正常`/quit`并核对最终 shutdown与无 JSONL，之后才进入全新进程 B对照。
  - 5.7 `--no-session` 真实 A 组正常退出与零 JSONL已验证（2026-08-18）：用户执行一次`/quit`后，只读核验确认追踪以新 Runtime的`session_shutdown reason=quit`结束；完整白名单链为`startup -> deny -> shutdown reason=reload -> 新 factory/start reason=reload -> deny -> shutdown reason=quit`，没有 Input、Agent、Turn、Tool或Model事件。实验根内 Session JSONL计数严格为`0`，文件仅有受控设置与`run-a`生命周期追踪，且没有仍引用该实验根的 Pi进程。该结果证明进程 A已正常结束且当前实验没有 Session文件可供新进程恢复；它尚未直接证明新进程 B会从零开始，下一步必须用相同配置启动 B并先做初始 Probe。
  - 5.7 `--no-session` 真实 B 组全新进程启动基线已验证（2026-08-18）：用户用同一私有实验根、相同隔离配置和独立`run-b`追踪启动新的 Pi `0.84.1`进程；启动页只列`pi-study-guard`与`state-probe`，输入框为空且没有启动错误。只读追踪严格为`factory -> session_start reason=startup`，尚无 Shell、Input、Agent、Turn或Tool事件。该证据只证明新的 Extension Runtime已启动且尚未执行实验动作，不证明状态已经归零；下一步只执行一次`/pi-study-state-probe`，预期为`sessionFile=none ownedEntries=0 blockedCount=0 lastDecision=none`。
  - 5.7 `--no-session` 真实 B 组初始零状态已验证（2026-08-18）：用户在全新进程 B中首次执行只读`/pi-study-state-probe`，TUI逐字段显示`sessionFile=none ownedEntries=0 blockedCount=0 lastDecision=none`；同一`run-b`追踪仍严格只有`factory -> session_start reason=startup`，没有因 Probe新增 Shell、Input、Agent、Turn或Tool事件。与进程 A退出前的`ownedEntries=2 blockedCount=2`形成单变量对照，该结果直接证明当前主 Extension在 Pi `0.84.1`的`--no-session`模式下不会跨全新进程恢复旧 in-memory Branch；它不证明内存被安全擦除或系统其他位置绝无写入。下一步只执行一次相同的固定未知用户 Shell请求，验证 B 组从`0`提交到`1`。
  - 5.7 `--no-session` 真实 B 组第一次 deny 已验证（2026-08-18）：用户只执行一次固定未知用户 Shell请求`!printf '%s\n' PI_STUDY_STATE_DENY`，TUI得到固定`PI_STUDY_SHELL_BLOCKED`与`exit 126`；`run-b`追踪严格从启动基线新增`user_bash -> shell_gate entry=user_bash rule=other decision=deny reason=policy_denied excludeFromContext=false`，没有 Input、Agent、Turn或Tool事件。该组合直接证明全新进程 B中的门禁仍在执行前显式拒绝请求；它尚未直接证明第一份状态快照已追加到 B 的 in-memory Branch，下一步必须用只读 Probe核对`ownedEntries=1 blockedCount=1`。
  - 5.7 `--no-session` 真实 B 组独立`0 -> 1`反例已验证（2026-08-18）：第一次 deny 后，用户只执行一次只读`/pi-study-state-probe`，TUI逐字段显示`sessionFile=none ownedEntries=1 blockedCount=1 lastDecision=user_bash/other/deny/policy_denied`；`run-b`追踪仍严格停在`factory -> session_start reason=startup -> user_bash -> shell_gate ... policy_denied`，Probe没有新增 Shell、Input、Agent、Turn或Tool事件。与进程 A退出前的`2/2`对照，该结果直接证明全新进程 B没有继承旧 in-memory Branch，而是在自己的内存 Branch从`0`提交到`1`；它不证明旧进程内存被安全擦除、系统全局零写或其他 Extension没有持久化渠道。下一步只执行`/quit`并复核正常 shutdown、零 JSONL和无残留进程。
  - 5.7 `--no-session` 真实 B 组正常退出与零 JSONL已验证（2026-08-18）：用户执行一次`/quit`后，只读核验确认`run-b`严格在`factory -> session_start reason=startup -> user_bash -> shell_gate ... policy_denied`之后新增且仅新增`session_shutdown reason=quit`；实验根内 JSONL计数仍为`0`，没有引用该实验根的 Pi进程。实验根仍是当前用户持有的`0700`普通目录，文件仅为受控`settings.json`与 A/B两份生命周期追踪。该结果闭合了全新进程 B的退出边界；不证明安全擦除或系统全局零写。下一步统一可恢复清理5.7私有实验目录，再执行最终回归与验收。
  - 5.7 私有实验目录可恢复清理已验证（2026-08-18）：在两组 Pi进程均正常退出且证据完成复核后，用户将持久 Session实验根与`--no-session`实验根按两个精确字面路径整体移入废纸篓；只读复核确认两个源路径均不存在，两个废纸篓目标均存在、不是符号链接，并保持当前用户持有的`0700`普通目录。该操作可从废纸篓恢复，但不等于安全擦除；下一步只执行最终自动回归、文档检查和验收。
  - 5.7 最终自动回归、文档检查与模块验收已通过（2026-08-18）：扩展包严格 TypeScript 检查与全量`174/174`测试通过，仓库根`git diff --check`通过；Extension README、学习索引、`06-extensions.md`和本计划的 Markdown围栏分别为`4/0/30/0`且均成对，本地相对链接由当前 Marked parser检查`23/23`存在。结合严格快照与失败锁存、当前 Branch重放、真实持久 Session的 Reload/跨进程/Tree/Fork/Resume、`--no-session`同进程 Reload延续与新进程归零、脱敏观测和可恢复清理，5.7既定验收闭合。结论不扩大为事务式持久化、崩溃恢复、安全擦除、系统全局零写、任意并发/版本、其他 Extension渠道或 OS沙箱；5.7完成，下一步进入5.8最小Widget。
  - 核心内容：区分内存状态、`appendEntry` Custom Entry、进入 Model Context 的消息和 Tool Result `details`；状态按当前 Branch 重放，在 Session 启动、Tree 导航和 Session 替换后重建。
  - 贯穿实验：保存门禁模式、累计阻止数和最近一次脱敏决策；验证 `/reload`、退出后恢复、Tree 分支切换、Fork、Resume，以及 `--no-session` 不持久化的反例。
  - 验收与边界：用脱敏 Session 结构投影和界面状态证明当前活动分支恢复正确；Custom Entry 不进入 Model Context 还必须由 Pi Core 转换源码或受控 `buildSessionContext` 投影证明，不能询问 Model代替。Session Entry 不是全局配置、数据库、Git 版本或项目文件恢复机制。
- [x] 5.8 实现最小 Widget，展示 Extension 当前状态。
  - 5.8 实现进行中（2026-08-18）：已确认开始编码；先补 Widget Controller、生命周期清除/恢复和状态提交后原位更新的 Red/Green 测试，Widget 不参与 Shell 分类、审批或状态提交。下一步完成自动回归后再运行受控 TUI/Print 验收。
  - 5.8 系统地图开始（2026-08-18）：已核对本机 Pi `0.84.1` 的公开 UI 合同：`ctx.ui.setWidget(key, content, options)` 使用稳定 `key` 设置或原位替换同一 Widget，`content=undefined` 清除；内容可用字符串数组或 Component factory，位置为 `aboveEditor`（默认）或 `belowEditor`。本轮先用 Shell Gate 状态牌场景区分 Widget、通知、Command 和 Tool Result，并明确 Widget 只展示 Store 已发布的脱敏状态，不参与分类、审批、状态提交或 Model Context。尚未冻结代码结构、编写实现或运行 Widget 动态实验，5.8 保持未完成。
  - 5.8 Widget 作用与职责已理解（2026-08-18）：用户已理解 Widget 是 Pi 提供给 Extension 的通用 UI 展示能力，Shell Gate 只是当前选用的业务例子；State Store 是账本、Shell Gate 是作出放行/拒绝决策的保安、Widget 是持续展示 `mode/blockedCount/lastDecision` 的电子状态牌。其他 Extension 也可以用 Widget 展示自己的进度、连接或检查状态；同一稳定 `key` 用于原位更新，区别于一次性 `notify`、主动查询的 Slash Command 和进入 Agent/Model 链的 Tool Result。Widget 缺失不等于门禁失效，Widget 显示某个计数也不能单独证明 Shell 未执行或快照已落盘。下一步讲清启动、决策提交、Reload/Tree/Session替换和Shutdown时的设置、更新与清除时序，仍不写代码。
  - 5.8 稳定正文已补齐通用 Widget 解释（2026-08-18）：`docs/learning/06-extensions.md` 新增 Widget 的通用用途、Shell Gate 示例、State Store/Shell Gate/Widget 职责表、稳定 key、脱敏字段、Theme 语义色、生命周期时序、TUI-only 模式和证据边界；`docs/learning/README.md` 同步扩展 06 章索引。5.8 仍只完成系统地图与文档整理，未实现或验收 Widget。
  - 5.8 最小实现与自动回归 Green（2026-08-18）：新增 `shell-widget.ts` Controller，主工厂在 `session_start/session_tree` 恢复后显示同一稳定 key，Shell Gate 仅在状态提交成功后原位更新，`session_shutdown` 幂等清除；无 UI/非 TUI 跳过，渲染异常不改变门禁结果。严格 TypeScript 检查与全量 `181/181` 测试通过，包含 Controller、生命周期、提交后更新顺序和主工厂接线；真实 TUI/Print、窄终端和用户手工验收尚未完成，5.8 保持未勾选。
  - 5.8 真实 TUI 初步验收（2026-08-18）：用户截图显示项目 Extension `pi-study-guard` 已加载；固定 SAFE 用户 Shell 输出正常，未知命令返回 `PI_STUDY_SHELL_BLOCKED` 与 `exit 126`；执行 `/reload` 后同一 TUI 显示 `Shell Gate | enforce`、`blocked=1`、`last=user_bash/other/deny/policy_denied`。该证据证明真实生命周期/用户 Shell/Widget 恢复链已跑通，并看到单一状态牌；尚未证明拒绝后的允许恢复、Shutdown 清除、Print 无 UI、窄终端可读性或无重复渲染的完整矩阵。
  - 5.8 真实 TUI 拒绝后允许恢复已验证（2026-08-18）：用户在同一 `/reload` 后 TUI 再执行固定 SAFE 命令，真实 Shell 输出 `PI_STUDY_SHELL_SAFE`；Widget 仍为同一单一状态牌，`blocked` 保持 `1`，`last` 原位更新为 `user_bash/safe/allow/safe`。该证据闭合允许/拒绝/恢复和稳定 key 的可见链；正常退出、全新进程初始状态、Print 无 UI与窄终端仍待验证。
  - 5.8 真实 TUI 正常退出已验证（2026-08-18）：用户执行 `/quit` 后返回普通终端提示符，未报告挂起或残留；该证据证明本次 TUI 进程正常结束，不单独证明 Widget 清除调用已被终端绘制可见，下一步用全新进程初始状态做 A/B 对照。
  - 5.8 全新进程初始 Widget 已验证（2026-08-18）：用户以相同隔离参数启动新的 `--no-session` Pi 进程，在未执行任何命令时截图显示单一状态牌 `Shell Gate | enforce`、`blocked=0 | last=none`。与上一进程退出前的 `blocked=1` 对照，证明新进程没有继承旧 in-memory Branch；该结果不证明安全擦除或全局零写。窄终端、当前进程退出和 Print 无 UI仍待验证。
  - 5.8 窄终端可读性由用户操作确认（2026-08-18）：用户保持同一 TUI 缩窄终端后确认 Widget 文本可读、未遮挡 Editor/Footer、无重复状态牌且无报错；本步没有保留新的截图级直接证据，不扩大为任意终端宽度、字体或 TUI 模式均兼容。当前进程退出和 Print 无 UI仍待验证。
  - 5.8 Print 无 UI 动态验收已通过（2026-08-18）：用户正常退出 TUI 后，以 `--approve --print --no-session --no-context-files --no-skills --no-prompt-templates --no-themes --no-tools --model openai/gpt-5.6-terra` 运行固定单次请求，完整非敏感输出仅为 `PRINT_WIDGET_OK`并返回 Shell；没有 `Shell Gate` Widget 文本、UI/`setWidget`错误或挂起。该证据只覆盖 Print/text 模式，不外推 JSON/RPC。
  - 5.8 最终差异审查清理 Red -> Green（2026-08-18）：确定性 Fake 先模拟宿主 `setWidget` 可能部分挂载后抛错，聚焦测试真实得到 `3/4`、失败项显示 Shutdown 清理调用数期望 `2` 而实际 `1`；最小修复把 cleanup 标记移到 `setWidget` 调用前，使失败渲染仍保留一次 best-effort 清理机会，重复 `clear()` 仍幂等，随后同一聚焦测试 `4/4` 通过。该测试证明 Controller 的调用合同，不证明真实宿主一定存在“部分挂载后抛错”行为；下一步重跑全量回归和文档检查。
  - 5.8 最终自动回归、文档检查与模块验收已通过（2026-08-18）：扩展包严格 TypeScript 检查与全量 `182/182` 测试通过，仓库根 `git diff --check` 通过；Extension README、`06-extensions.md` 和本计划的 Markdown 围栏分别为 `4/34/0` 且均成对，当前 Marked parser 检查的本地相对链接 `14/14` 存在。结合稳定 key、提交后更新、异常隔离与清理 Red -> Green，真实 TUI 的初始/拒绝/允许/Reload/全新进程对照、正常退出、用户确认的窄终端可读性，以及真实 Print 仅输出 `PRINT_WIDGET_OK` 并正常退出，5.8 既定验收闭合。结论不扩大为 JSON/RPC、任意终端/主题、OS 沙箱、状态持久化证明或 Shell 未执行证明；5.8 完成，下一步进入 5.9 自动测试与手工验收记录收口。
  - 核心内容：只覆盖 `setWidget`、稳定标识、更新、清除、`hasUI/mode` 门禁和 Theme 语义色；完整 Component、键盘、Overlay 与 TUI 内部机制留在阶段 7。
  - 贯穿实现：Widget 显示门禁模式、阻止计数和最近决策；状态变化后原位更新，Reload/Shutdown 时清除，无 UI 模式跳过 UI。
  - 验收与边界：TUI 中初始、允许、拒绝、恢复四种状态有可观察对照，无重复 Widget，Print 不报错或挂起，窄终端仍可读。Widget 只呈现状态，不负责门禁，也不证明状态已持久化。
- [x] 5.9 为 Extension 补充自动测试、错误路径测试和手工验收记录。
  - 5.9 证据地图盘点进行中（2026-08-18）：已从唯一计划断点恢复，先按纯函数单测、Fake API/Runner、隔离 Pi CLI、真实 TUI/Provider 四层归档现有 `pi-study-guard` 证据，再逐项对照 Schema、路径逃逸、截断、取消、Tool 抛错、模式分支、双 Shell 入口、默认拒绝、状态重放、Widget 更新与幂等清理。当前只做只读盘点，不把已有 `182/182` 自动 Green 直接当成 5.9 完成，也不在缺口冻结前新增代码或测试。
  - 5.9 当前自动基线与合同盘点已验证（2026-08-18）：当前共有 `16` 个测试文件、`129` 个顶层用例并展开为 `182` 个实际测试结果；重新运行 `npm run check` 后严格 TypeScript 与 `182/182` 全部通过，仓库根 `git diff --check` 通过。用例实现已覆盖计划列出的 Schema、路径逃逸/文件对象、截断、取消、Tool/Handler/Executor异常、TUI/RPC/Print/JSON分支、Model/User双 Shell入口与默认拒绝、Branch状态重放、Widget原位更新和异常后幂等清理；这些仍只是各测试所处层级的证据，不自动证明真实 Pi Loader、CLI外壳或TUI。
  - 5.9 四层证据缺口已冻结（2026-08-18）：第一层已有纯函数、真实 Markdown parser和真实临时文件单测；第二层已有 Fake ExtensionAPI、真实 `ExtensionRunner`与`SessionManager.inMemory`；第四层已有5.2-5.8的真实TUI/Provider/Print人工实验。第三层尚无任何自动测试启动真实`pi`可执行文件，当前`npm test`里的子进程只启动Node并手工构造Fake API/Context；真实CLI证据只能依赖计划中的历史人工命令，无法由测试套件重复。另一个文档缺口是现有TUI/Print验收分散在各计划项，尚未形成按能力/模式/成功/拒绝/异常/证据边界统一的手工矩阵。下一步先设计一个不请求Provider、不读取真实凭据的隔离Pi CLI集成对照，再整理手工矩阵；写测试代码前需用户明确确认。
  - 5.9 隔离 CLI 集成测试实现进行中（2026-08-18）：用户已确认实现。首轮只读探测使用清空后的最小环境，但仍以相对命令 `node` 启动，因隔离 `PATH` 中没有 Node 而以 `env: node: No such file or directory`、退出码 `127` 结束；该结果只证明启动器装配错误，不是 Pi Loader 或 Extension Red。下一步改用当前测试进程的绝对 Node 可执行路径，在同一隔离环境验证不加载 Extension 时 Flag 为 `0`、唯一增加 `-e index.ts` 后为 `1`。
  - 5.9 隔离 CLI 集成层已验证（2026-08-18）：新增 `test/pi-cli-loading.test.ts`，通过当前测试进程的绝对 Node 路径启动锁定依赖 `@earendil-works/pi-coding-agent@0.84.1` 的真实 `dist/cli.js`；两组共用全新临时 `HOME`、`PI_CODING_AGENT_DIR`、空工作目录和最小环境，唯一变量是加载组增加 `-e <index.ts>`。两次 `--help` 均退出 `0`，对照组 `--pi-study-guard` 出现 `0` 次、加载组出现 `1` 次；聚焦测试 `1/1` 与严格 TypeScript 检查通过。Pi 会在隔离配置根创建权限为 `0600` 的临时存储文件，测试不读取内容并在 `finally` 递归清理整个临时根；本证据证明真实 CLI Loader 导入 factory 并完成 Flag 注册，不证明 Handler/Tool/TUI/Provider 正确，也不把最小环境或临时目录称为 OS 沙箱。下一步整理统一手工验收矩阵并跑全量回归。
  - 5.9 实现与开发者自检已通过、用户学习验收待验证（2026-08-18）：`docs/learning/06-extensions.md` 已按纯逻辑/真实文件、Fake API/真实 Runner 与 in-memory SessionManager、隔离真实 Pi CLI、真实 TUI/Print/Provider 四层整理证据，并按生命周期、取消、错误传播、Tool、Command、Shell Gate、Branch 状态和 Widget 形成统一手工索引；Extension README同步登记真实 CLI 测试职责。当前共有 `17` 个测试文件，教练执行的全量 `npm run check` 中严格 TypeScript 与 `183/183` 测试通过，仓库根 `git diff --check` 通过，围栏和本地链接检查通过，CLI 临时根无残留；这些只算实现与开发者自检证据。此前错误地把自动 Green 和文档完成直接记作 5.9 学习完成，用户指出本人未参与后已撤销勾选；下一步由用户亲自运行隔离 CLI 聚焦测试，并说明 `0 -> 1` 直接证明真实 Loader/工厂/Flag 注册，但不证明 Handler、Tool、TUI、Provider、所有输入或 OS 权限安全。通过前不进入5.10。
  - 5.9 用户聚焦实验已验证、因果边界待确认（2026-08-18）：用户在扩展目录亲自运行 `node --test test/pi-cli-loading.test.ts`，截图显示目标用例通过、`tests 1`、`pass 1`、`fail 0`、无取消或跳过，并正常返回 Shell。该输出没有直接打印两组计数，但用例 Green 直接表示测试中的对照组 Flag `0` 与显式加载组 Flag `1` 两条断言均成立；本步补上用户亲自执行证据，尚需用户确认它只证明真实 Pi Loader 导入工厂并完成 Flag 注册，不证明 Handler、Tool、TUI、Provider、所有输入或 OS 权限安全。确认前保持5.9未勾选。
  - 5.9 用户参与验收收口（2026-08-18）：用户明确不需要追加问答式复述并要求继续学习。结合用户此前主动追问实验用途与内部逻辑证据、亲自运行真实 Pi CLI 聚焦测试取得`1/1` Green，以及已完成的四层证据地图、八类手工矩阵和教练侧`183/183`回归，5.9验收闭合；只记录教学覆盖、用户实际操作和自然确认，不声称用户已脱离提示独立复述。5.9完成，进入5.10系统地图。
  - 核心内容：建立纯函数单测、Fake API 注册测试、隔离 Pi CLI 集成和真实 TUI 手工验收四层证据；高风险门禁使用稳定 Red -> Green -> 回归流程。
  - 贯穿验证：覆盖 Schema、路径逃逸、截断、取消、Tool 抛错、模式分支、双 Shell 入口、默认拒绝、状态重放、Widget 更新和幂等清理；执行类型检查、完整测试、`git diff --check` 和实际差异审查。
  - 验收与边界：记录实际命令、关键 FAIL/PASS 和手工矩阵。Fake Handler 不能证明 Pi Core 已真实派发，Mock/Fake 不能冒充真实 Provider 链，单次手工成功不能证明所有输入和并发均安全。
- [x] 5.10 审查资源释放、热重载、Session 切换、并发和模式兼容性风险。
  - 5.10 系统地图历史检查点（2026-08-18）：5.9 完成后，先只读盘点主工厂创建的对象、各生命周期 Handler 的创建/恢复/清理顺序、Reload 后新旧 Runtime 边界、SessionManager/State Store/Widget 引用、同轮并发共享状态和 TUI/Print/JSON/RPC 分支；在风险地图与真实实验合同冻结前未修改代码，也未把 5.7/5.8 的单次 Reload 与退出证据当成 5.10 完整验收。
  - 5.10 资源所有权盘点已完成（2026-08-18）：主工厂每个Runtime创建一份Shell State Store和Widget Controller并由Handler闭包持有，不启动常驻子进程、Socket或Watcher；生命周期追踪每次写入都在`finally`关闭文件描述符，只读Tool每次执行结束关闭Markdown句柄，取消探针在完成/取消/异常路径清理Timer与Abort listener。长期内存仅为Store快照和Widget保存的当前UI Context；Shutdown把Store锁定为`shutdown`并清空快照，Widget清理已幂等，但Controller会保留旧Context引用直到旧Runtime被释放或同Runtime下一次bind替换。本结论来自当前源码与既有自动测试，不证明真实进程不存在其他宿主资源或内存泄漏。
  - 5.10 Core Reload与实例边界已核对（2026-08-18）：Pi`0.84.1`源码顺序为旧Runner收到`session_shutdown(reason=reload)`、随后旧Runtime失效并退订EventBus、Settings/资源Reload、创建并绑定新Runner、最后新Runner收到`session_start(reason=reload)`；失效Runtime的公开操作受stale Context门禁。TUI在streaming或compaction期间拒绝`/reload`，但RPC路径是否在所有活动状态下具备同等前置门禁尚未取得直接证据。当前Extension不主动调用`ctx.reload/newSession/fork/switchSession`，只在`session_start/session_tree`绑定宿主传入的新Context；这些源码事实解释预期顺序，不替代真实连续Reload与Session切换实验。
  - 5.10 并发与模式缺口已冻结（2026-08-18）：Shell策略分类大多同步，但MARKER确认会`await`，多个请求可能按完成顺序而非开始顺序提交同一Store；`commitDecision()`本身同步读取最新快照、追加并发布，当前不依赖兄弟Tool Result，理论上不应丢计数，但尚无并发直接测试。等待确认期间若Shutdown，旧Store应因`shutdown`拒绝提交并令请求fail-closed，尚无聚焦证据。最终实验需覆盖连续两次Reload无重复Handler/Widget、New/Resume/Fork/Tree使用新Context且状态不串线、两个交错Shell决策累计正确、等待期间Shutdown拒绝、正常退出无残留，以及TUI和Print最终兼容；JSON/RPC若不做真实客户端实验必须明确保留未验证边界。
  - 5.10 Branch恢复衔接已讲解并自然确认（2026-08-18）：`session_start`不是复活旧JavaScript内存，而是新Runtime通过当前活动Branch从根到叶重放本Extension自己的Custom Entry，用最后一条合法快照恢复Store；无条目回到默认状态，非法条目进入不可用并默认拒绝。它不恢复Git、工作区文件、其他Branch或自动补回Model上下文。用户确认理解后继续，不追加问答式复述，也不声称已脱离提示独立复述。
  - 5.10 第一组确定性实验合同已冻结、待代码授权（2026-08-18）：场景一在同一真实Shell State Store上先挂起需要确认的Model MARKER请求，再让用户未知Shell请求先完成拒绝，最后放行MARKER；唯一变量是确认Promise的释放时机，预期两次提交按完成顺序追加、阻止计数保持`1`且最终最近决策为MARKER允许，证明当前Store同步提交使用最新快照而不丢计数。场景二在MARKER等待确认时调用同一Store的Shutdown，再释放确认；预期提交失败、请求默认拒绝、Executor为`0`、不追加快照也不发布Widget并记录`state_error`，证明旧请求不能越过已关闭Store。两组固定为Fake Context、无真实Shell、Provider、凭据或Session写入；只证明Store/Gate确定性交互，不证明Pi宿主真实并发调度、完整Reload或任意资源均无泄漏。用户明确确认前不新增测试代码。
  - 5.10 第一组确定性测试实现进行中（2026-08-18）：用户已明确确认实现。仅在现有Shell Gate聚焦测试中复用生产Shell State Store，增加受控确认Promise以固定交错顺序，并覆盖等待期间Shutdown；不修改运行时策略、不启动真实Shell或Provider。下一步先取得聚焦测试结果，再决定是否需要最小修复。
  - 5.10 第一组确定性聚焦测试已通过（2026-08-18）：`test/shell-state-gate.test.ts`新增两条生产Store/Gate组合用例；受控确认Promise先挂起Model MARKER，再让用户未知命令提交拒绝，最后释放确认，实际快照按完成顺序为`blocked=1/user_bash deny`、`blocked=1/tool_call allow`，最终计数未丢且发布两次。另一用例在确认等待期间调用Store Shutdown，释放确认后Model请求仍被阻止、Executor计数`0`、Custom Entry与发布均为`0`并记录`state_error`。聚焦测试`16/16`和严格TypeScript检查通过，生产代码无需修复；证据只覆盖确定性进程内组合，不替代真实Pi并发、Reload或资源泄漏验证。下一步运行全量回归与差异检查。
  - 5.10 第一组自动证据收口（2026-08-18）：全量`npm run check`通过严格TypeScript与`185/185`测试，仓库根`git diff --check`通过；最终差异确认本轮只在现有Shell Gate测试中加入生产Store导入、受控确认Promise和两条用例，未修改生产策略或依赖，其他Widget/状态改动属于此前未提交学习成果并保持不变。该组实现完成但5.10总项不勾选；连续Reload、Session切换、正常退出及TUI/Print最终矩阵仍需真实Pi证据。下一步冻结真实Pi连续Reload与Session切换实验卡。
  - 5.10 真实 Pi 连续 Reload 历史初始合同（2026-08-18）：固定同一 Pi TUI 进程、项目自动发现 Extension、私有脱敏生命周期追踪、`--no-session`、关闭上下文/Skill/Prompt/Theme 与 Model Tool 且不发送普通 Prompt；用户截图实际运行版本为 Pi `0.84.2`，因此结论按该版本记录。最初计划以完成的 Reload 轮次为唯一推进变量：初始 `blocked=0`，执行三次固定未知用户 Shell 使计数依次到 `1/2/3`，中间连续执行两次 `/reload`，最后正常 `/quit`。初始通过标准是全程只有一个 Widget、每个拒绝只加 `1`、Reload 后保留前值，退出后追踪中恰有三次 factory、`startup/reload/reload` 三次 start、`reload/reload/quit` 三次 shutdown，并且三次用户 Shell 各只有一个门禁决定。该初始合同原本只能证明本次真实 TUI 中的旧实例关闭、新实例重建、当前内存 Branch 恢复和未观察到重复派发，不能证明任意内存/文件描述符均无泄漏、崩溃恢复、磁盘 Session 持久化或 JSON/RPC 兼容；最终 Green 实际缩减为两次未知用户 Shell，第三次没有执行，见后续生命周期子实验记录。
  - 5.10 第一次未知用户Shell拒绝已验证（2026-08-19）：用户在真实Pi`0.84.2` TUI中输入固定未知命令，界面显示替代结果`PI_STUDY_SHELL_BLOCKED`与`exit 126`，Widget从`blocked=0 | last=none`更新为单一状态牌`blocked=1 | last=user_bash/other/deny/policy_denied`。该证据证明本次用户Shell入口走Extension门禁拒绝分支并更新状态；不单独证明OS层绝无执行、追踪文件中恰有一条记录或其他入口行为。下一步第一次`/reload`后只观察状态恢复，不重复执行Shell。
  - 5.10 第一次真实Pi Reload已验证（2026-08-19）：用户在同一Pi`0.84.2` TUI中执行`/reload`，界面显示`Reloaded keybindings, extensions, skills, prompts, themes, and context files`；Reload后仍显示单一Widget，状态保持`blocked=1 | last=user_bash/other/deny/policy_denied`。该证据证明本次Reload后的新Runtime恢复了当前状态牌，且未观察到重复Widget；不单独证明旧Handler无重复注册、生命周期追踪计数完整或所有旧资源均已释放。下一步第二次固定未知用户Shell。
  - 5.10 第二次未知用户Shell与累计行为已验证（2026-08-19）：用户在第一次Reload后的同一Pi`0.84.2` TUI再次输入固定未知命令，得到第二次`PI_STUDY_SHELL_BLOCKED`与`exit 126`；Widget从`blocked=1`只增加到`blocked=2`，仍为单一状态牌，最近决策保持`user_bash/other/deny/policy_denied`。该证据证明在本次Reload后当前用户Shell只产生一次门禁拒绝并按最新快照累计，没有观察到重复Handler导致的`blocked=3`；仍需第二次Reload和退出后的追踪日志核对。
  - 5.10 第二次真实Pi Reload已验证（2026-08-19）：用户在同一Pi`0.84.2` TUI执行第二次`/reload`，再次看到资源重载提示；Reload后仍为单一Widget，状态保持`blocked=2 | last=user_bash/other/deny/policy_denied`。结合第一次拒绝后的`0 -> 1`、第二次拒绝后的`1 -> 2`，当前界面证据支持连续两次Reload后状态延续且未观察到重复Widget/门禁；尚需正常退出与追踪日志核对生命周期事件。
  - 5.10 真实Pi正常退出已由用户确认（2026-08-19）：用户回复已完成退出操作，当前未取得终端提示符截图或追踪内容，因此只记录为用户操作报告，不把它扩大为限定时间内无残留或生命周期清理已直接证明。下一步读取私有脱敏追踪固定事件投影。
  - 5.10 追踪读取首次尝试失败（2026-08-19）：用户运行脱敏`awk`计数时，Shell 报告无法打开`/tmp/pi-study-5-10-reload.ML120K/pi-study-lifecycle.log`并退出；该结果只证明当前命令找不到该路径，不能判断Pi是否写过追踪、目录是否已被清理或路径变量是否仍指向同一实验。下一步只读检查变量、目录和文件存在性，必要时重新做一次带持久路径变量的最小生命周期实验。
  - 5.10 追踪路径原因已定位并记录Red（2026-08-19）：用户只读检查确认`PI_STUDY_510_DIR`目录存在但为空；结合当前`lifecycle-trace.ts`的安全实现，追踪路径必须位于`realpath(os.tmpdir())`之下，而本轮命令手写`/tmp/...`，在macOS上通常解析到另一棵目录，导致工厂阶段追踪初始化被拒绝并降级为无追踪记录。该Red是实验装配错误，不否定已取得的真实TUI状态/Reload证据；下一步改用`mktemp -d`默认系统临时目录重做生命周期计数。
  - 5.10 修正追踪路径后的启动基线已验证（2026-08-19）：用户以`mktemp -d`默认系统临时目录重新启动Pi`0.84.2`，截图显示目录位于系统临时树，Extension已加载，单一Widget初始为`blocked=0 | last=none`。该证据证明修正后的启动条件和UI基线成立；追踪文件实际事件内容仍需在后续序列和退出后计数中验证。下一步第一次未知用户Shell。
  - 5.10 修正追踪路径后的第一次未知Shell已验证（2026-08-19）：用户在新的真实Pi`0.84.2`进程输入固定未知命令，得到`PI_STUDY_SHELL_BLOCKED`与`exit 126`，Widget从`blocked=0`更新为`blocked=1 | last=user_bash/other/deny/policy_denied`。该证据证明修正后的运行条件仍命中预期门禁分支；是否写入生命周期追踪仍待退出后计数核对。下一步第一次`/reload`。
  - 5.10 修正追踪路径后的第一次Reload已验证（2026-08-19）：用户在新的真实Pi`0.84.2`进程执行第一次`/reload`，看到资源重载提示；Reload后仍只有一个Widget，状态恢复为`blocked=1 | last=user_bash/other/deny/policy_denied`。该证据证明修正条件下新Runtime恢复当前内存Branch状态；追踪事件计数仍待退出后核对。下一步第二次未知用户Shell。
  - 5.10 修正追踪路径后的第二次未知Shell已验证（2026-08-19）：用户在第一次Reload后的同一Pi`0.84.2` TUI再次输入固定未知命令，得到第二次`PI_STUDY_SHELL_BLOCKED`与`exit 126`；Widget从`blocked=1`只增加到`blocked=2`，未出现`blocked=3`或重复Widget。该证据证明修正条件下第二次请求仍按一次门禁决定累计；追踪事件计数仍待退出后核对。下一步第二次`/reload`。
  - 5.10 修正追踪路径后的第二次Reload已验证（2026-08-19）：用户在同一Pi`0.84.2` TUI执行第二次`/reload`，再次看到资源重载提示；Reload后仍只有一个Widget，状态保持`blocked=2 | last=user_bash/other/deny/policy_denied`。修正条件下TUI序列闭合为`0 -> 1 -> reload -> 2 -> reload -> 2`，尚需正常退出和新追踪文件计数核对。
  - 5.10 连续 Reload 生命周期子实验已通过（2026-08-19）：修正为 `mktemp -d` 系统临时目录后，用户完成真实 Pi `0.84.2` 启动、两次未知用户 Shell 拒绝、连续两次 `/reload` 和 `/quit`；实际执行序列从历史初始合同的三次请求缩减为 `0 -> 1 -> reload -> 2 -> reload -> 2`，第二次 Reload 后没有再执行第三次 Shell，始终只有一个 Widget。退出后用户运行脱敏 `awk` 得到 `factory=3 startup=1 reload_start=2 reload_shutdown=2 quit_shutdown=1 denied=2`。该联合证据证明本次版本的旧 Runtime 按 Reload 关闭、新工厂和新 `session_start` 各执行一次，当前状态在两次 Reload 后延续，已执行的两次用户拒绝均未观察到重复派发；它没有动态证明“第二次 Reload 后的下一次拒绝仍只派发一次”，也不证明任意资源均无泄漏、崩溃恢复、磁盘 Session 持久化、其他客户端模式或所有 Session 切换操作。此前 `/tmp` 路径实验作为装配 Red 保留，不与本次 Green 混淆。
  - 5.10 New Session隔离实验卡已冻结并通过（2026-08-19）：本组固定Pi`0.84.2`、新的系统临时Session目录、同一Extension、无Model Tool和一份脱敏生命周期追踪；唯一变量是执行一次`/new`。用户实测序列为启动`blocked=0`、第一次拒绝`blocked=1`、`/new`显示`New session started`并重置为`blocked=0 | last=none`、第二次拒绝再次为`blocked=1`、最后正常退出；脱敏追踪为`factory=2 startup=1 new_start=1 new_shutdown=1 quit_shutdown=1 denied=2`。该证据证明本次真实Pi New的旧Runtime关闭、新Runtime建立、新Context绑定和内存/Session隔离；不证明Fork、Resume、Tree、跨进程恢复或任意资源无泄漏。下一步设计`/tree`同Runtime分支切换实验。
  - 5.10 `/tree` 同Runtime分支切换实验卡待执行（2026-08-19）：固定同一Pi`0.84.2`进程、同一Extension、持久Session目录、无Model Tool和一份脱敏追踪；唯一变量是在已有状态节点后执行一次`/tree`切换到另一条已准备的Branch。预期不出现新的`factory`或`session_start`，只出现`session_tree`并让当前Widget按目标Branch重放；旧Branch状态与目标Branch状态必须形成可观察差异，切换后不得串入旧计数，且退出只出现一次`session_shutdown reason=quit`。该实验只证明当前版本的同Runtime Branch切换与Extension状态重放，不证明磁盘事务、Fork/Resume或任意Tree UI选择器行为；开始前先建立两条无敏感内容的分支状态材料。
  - 5.10 `/tree` 首次目标Branch重放已验证（2026-08-19）：用户在同一真实Pi`0.84.2`进程先连续执行两条未知用户Shell，Widget到达`blocked=2 | last=user_bash/other/deny/policy_denied`；随后打开`/tree`，选择器真实显示`PI_STUDY_TREE_DENY_A`与`A2`两个`[bash]`节点，从`(2/2)`上移选中第一条`(1/2)`，并在`Summarize branch?`中保持`No summary`。返回主界面后出现`Navigated to selected point`，单一Widget恢复为`blocked=1`且最近决策仍对应第一条deny。该`2 -> 1`直接证明本次`session_tree`按选中Branch重放Extension状态，没有沿用切换前的内存`2`；当前还未用追踪排除新factory/start，也未建立状态不同的兄弟叶节点。下一步在当前共同点执行一次固定SAFE用户Shell，形成`blocked=1 + safe/allow`的新路线B，再与旧路线A2双向切换对照。
  - 5.10 `/tree` 可区分兄弟路线B已建立（2026-08-19）：用户在首次Tree恢复出的`blocked=1 + deny`共同点执行固定`PI_STUDY_SHELL_SAFE`用户Shell，界面实际输出同名安全标记、无`exit 126`，单一Widget计数保持`blocked=1`且最近决策更新为`user_bash/safe/allow/safe`。因此旧路线A2保留`blocked=2 + deny`，当前路线B为`blocked=1 + safe`，两者同时具备计数和最近决策差异；下一步只打开`/tree`检查真实分叉与当前高亮位置，再定位旧路线A2，不先提交。
  - 5.10 `/tree` 真实兄弟分叉已显示（2026-08-19）：用户从当前SAFE路线只打开`/tree`，选择器显示共同节点`PI_STUDY_TREE_DENY_A`，其下并列保留当前`PI_STUDY_SHELL_SAFE`与旧`PI_STUDY_TREE_DENY_A2`两个子节点；当前高亮在SAFE节点`(2/3)`，A2位于下一行`(3/3)`。该截图直接证明新路线B没有覆盖或删除旧路线A2，但尚未提交切回A2，也未观察恢复后的Widget。下一步只按一次`↓`高亮A2并确认`(3/3)`，暂不回车。
  - 5.10 `/tree` 从SAFE路线反向恢复A2已验证（2026-08-19）：用户在真实分叉中从当前SAFE节点`(2/3)`下移选中A2`(3/3)`，保持`No summary`后返回主界面；界面显示`Navigated to selected point`，单一Widget从切换前B路线的`blocked=1 + user_bash/safe/allow/safe`恢复为A2路线的`blocked=2 + user_bash/other/deny/policy_denied`。该结果直接证明A2按自己的Branch快照恢复，没有沿用B路线内存；下一步再从A2切回SAFE节点，验证B也不受A2污染，随后退出并核对脱敏生命周期追踪。
  - 5.10 `/tree` 双向Branch隔离TUI证据已闭合（2026-08-19）：用户从已恢复的A2路线再次打开Tree，选择器把当前A2显示为`(2/3)`、SAFE兄弟节点显示为`(3/3)`；选中SAFE并保持`No summary`后，界面再次显示`Navigated to selected point`，单一Widget从A2的`blocked=2 + deny`准确恢复为SAFE路线自己的`blocked=1 + user_bash/safe/allow/safe`。结合此前A2到共同点的`2 -> 1`、SAFE到A2的`1/safe -> 2/deny`和本次A2到SAFE的`2/deny -> 1/safe`，真实TUI已证明两条兄弟Branch双向切换时各自重放且未观察到Widget重复或状态串线。用户随后报告已执行正常退出；当前追踪器没有登记`session_tree`事件，因此三次Tree只能由上述TUI证据证明，退出后日志只核对全程一个factory/start、一次quit shutdown和两deny一safe，不能声称日志直接证明Tree次数。
  - 5.10 `/tree` 同Runtime子实验最终通过（2026-08-19）：退出后用户亲自运行本轮专用脱敏`awk`，实际得到`factory=1 startup=1 quit_shutdown=1 denied=2 safe=1`，与冻结预期完全一致。联合真实Tree分叉、三次`Navigated to selected point`和Widget序列`2/deny -> 1/deny -> 1/safe -> 2/deny -> 1/safe`，本组直接证明Pi`0.84.2`同一Runtime内当前Branch重放、兄弟路线双向隔离、单一Widget及无重复Shell派发；日志没有`session_tree`观察器，故不声称日志直接计数Tree，也不外推Fork、Resume、跨进程、磁盘事务或任意资源泄漏。下一步冻结Fork/Resume新Context组合实验。
  - 5.10 Fork/Resume新Context组合实验卡已冻结（2026-08-19）：新建私有系统临时根、持久Session目录和单一脱敏追踪，固定Pi`0.84.2`、同一Guard、禁用内置Tool与自动资源。源Session先由固定未知用户Shell建立`blocked=1 + deny`，随后两轮固定普通Prompt分别建立Assistant baseline与文本唯一anchor；这两轮会真实调用Provider但不调用Tool，只为满足`/fork`仅枚举普通User Message的选择边界，不算状态证据。选择anchor执行Fork后，预期旧Runtime记录`shutdown reason=fork`，新factory与`start reason=fork`各一次，单一Widget继承`1 + deny`；清空回填anchor后执行SAFE，使Fork路线成为`1 + safe`。再用anchor唯一定位仍包含该消息的源Session执行`/resume`，预期旧Fork Runtime记录`shutdown reason=resume`，新factory与`start reason=resume`各一次，源Widget恢复`1 + deny`而不沿用Fork的`1 + safe`。最终quit追踪预期`factory=3 startup=1 fork_start=1 resume_start=1 fork_shutdown=1 resume_shutdown=1 quit_shutdown=1 denied=1 safe=1`。该组证明当前版本Fork/Resume的Runtime替换、新Context绑定、继承/隔离与单Widget，不证明任意选择器、Provider、跨进程或资源绝无泄漏；每一步按TUI实际结果推进。
  - 5.10 Fork/Resume组合实验启动基线已验证（2026-08-19）：用户以新的系统临时私有根启动真实Pi`0.84.2`，显式Session目录与追踪目录均为`0700`，禁用上下文、Skill、Prompt Template、Theme和内置Tool；启动页显示项目Guard已加载，单一Widget为`blocked=0 | last=none`，未见启动错误。该证据只证明隔离装配和零状态UI基线，不证明追踪内容、Session写入或后续Fork/Resume行为。下一步执行一次固定未知用户Shell，建立源Session的`1 + deny`。
  - 5.10 Fork/Resume源Session状态基线已验证（2026-08-19）：用户在上述真实Pi TUI执行固定未知Shell`PI_STUDY_510_FR_DENY`，得到`PI_STUDY_SHELL_BLOCKED`与`exit 126`，单一Widget从零更新为`blocked=1 | last=user_bash/other/deny/policy_denied`。该证据证明源Session已建立一份可恢复deny状态；尚未证明追踪计数、状态落入哪份Session文件或Fork继承。下一步发送第一条固定普通Prompt并等待固定Assistant baseline，只建立Fork的普通消息边界，Widget应继续保持`1 + deny`。
  - 5.10 Fork普通Assistant baseline已验证（2026-08-19）：用户发送`只回复 PI_STUDY_510_FR_BASELINE`，真实Assistant精确回复`PI_STUDY_510_FR_BASELINE`；界面没有Tool调用，单一Widget继续保持`blocked=1 + user_bash/other/deny/policy_denied`。这证明该普通Model轮次没有改变Shell State，并为Fork后的继承路径提供Assistant历史；它不证明Provider通用稳定性，也不是Fork状态恢复证据。下一步发送文本唯一的普通anchor Prompt并等待固定ACK，Widget仍应不变。
  - 5.10 Fork普通User anchor已验证（2026-08-19）：用户发送`只回复 PI_STUDY_510_FR_ANCHOR_ACK`，真实Assistant精确回复`PI_STUDY_510_FR_ANCHOR_ACK`；界面仍无Tool调用，单一Widget继续保持`blocked=1 + user_bash/other/deny/policy_denied`。因此两轮普通消息只建立Fork选择边界，没有改变Shell State；下一步只打开`/fork`，确认选择器包含baseline与文本唯一anchor并定位anchor，暂不提交。
  - 5.10 Fork选择器与anchor定位已验证（2026-08-19）：用户只打开`/fork`，真实选择器仅显示两条普通User Message，baseline为`Message 1 of 2`、唯一anchor为`Message 2 of 2`，先前BashExecution未进入候选；当前高亮已位于anchor且尚未提交。下一步只按一次回车创建Fork；返回后不再次提交可能回填到编辑器的anchor文本，先观察`Forked to new session`、Widget继承值和输入框状态。
  - 5.10 Fork复制边界与Widget继承表象已验证（2026-08-19）：用户提交唯一anchor后，真实TUI显示`Forked to new session`；新Session可见历史截止在baseline回复，所选anchor及其ACK未复制，同时anchor原文被回填到输入框但尚未发送。单一Widget保持`blocked=1 + user_bash/other/deny/policy_denied`，说明Fork后的Guard已呈现继承状态而非零状态；但仅凭UI尚不能独立证明旧Runtime关闭和新factory/start，需最终追踪确认。下一步只用`Ctrl+C`清空回填文本，不提交它，再执行固定SAFE验证Fork Store从继承的`1`继续。
  - 5.10 Fork继承状态与独立更新检查点已通过（2026-08-19）：用户报告已用`Ctrl+C`清空回填anchor且未发送，随后执行固定`PI_STUDY_SHELL_SAFE`；真实TUI输出安全标记、无`exit 126`，单一Widget保持`blocked=1`并把最近决策更新为`user_bash/safe/allow/safe`。若Fork Store从默认零开始，SAFE后应为`0 + safe`；实际`1 + safe`证明它从源Branch的`1 + deny`继承后继续，并已形成与源Session可区分的状态。用户主动询问是否通过，已明确答复该Fork检查点通过，但整组仍需Resume回源和最终生命周期追踪。下一步打开`/resume`，用唯一anchor文本定位仍包含该消息的源Session，先不提交。
  - 5.10 Resume选择器两份Session边界已观察（2026-08-19）：用户只打开`/resume`，真实Threaded选择器显示两份同属当前目录的Session，第二份缩进为Fork子节点；两项预览都只显示baseline，无法仅凭位置、相对时间或预览文本可靠识别源Session。当前尚未提交Resume。下一步在过滤框输入带双引号的精确短语`"PI_STUDY_510_FR_ANCHOR_ACK"`，利用anchor只存在于源Session这一事实把候选缩到唯一一项，再观察而不回车。
  - 5.10 Resume源Session唯一定位已验证（2026-08-19）：用户在选择器过滤框输入精确短语`"PI_STUDY_510_FR_ANCHOR_ACK"`后，真实候选从源与Fork两份缩为唯一一项并自动高亮；Fork创建时已证明anchor未复制，因此该唯一命中可可靠定位源Session，不依赖默认顺序、时间或相同baseline预览。当前尚未提交切换。下一步按一次回车执行Resume，返回后先观察源历史是否重新包含anchor/ACK，以及单一Widget是否从Fork的`1 + safe`恢复为源的`1 + deny`。
  - 5.10 Resume源Context与Branch状态恢复检查点已通过（2026-08-19）：用户提交唯一候选后，真实TUI显示`Resumed session`，历史重新包含源Session的anchor及其ACK；单一Widget从Fork路线的`blocked=1 + user_bash/safe/allow/safe`恢复为源路线的`blocked=1 + user_bash/other/deny/policy_denied`。计数相同但最近决策不同，使本次对照能够直接排除沿用Fork最近内存值，并证明Resume后的Guard按源Branch恢复。当前UI已闭合Fork继承、Fork独立更新和Resume源恢复；还需正常退出和脱敏追踪确认三次factory、fork/resume Runtime shutdown/start与单次Shell决策计数。
  - 5.10 Fork/Resume新Context组合子实验最终通过（2026-08-19）：正常退出后，用户亲自运行专用脱敏`awk`，实际得到`factory=3 startup=1 fork_start=1 resume_start=1 fork_shutdown=1 resume_shutdown=1 quit_shutdown=1 denied=1 safe=1`，与冻结预期逐项一致。联合Fork复制边界、源`1/deny`继承到Fork、Fork独立更新为`1/safe`、精确Resume回源恢复`1/deny`和全程单一Widget，本组直接证明Pi`0.84.2`中Fork/Resume各自关闭旧Runtime、创建新factory/start、绑定目标Context并按目标Branch恢复，且未观察到重复Shell派发。证据不外推任意选择器内容、Provider稳定性、跨进程默认Session、崩溃恢复或所有资源绝无泄漏。下一步执行最终Print模式兼容性子实验，再做全量自动回归与差异检查。
  - 5.10 Print模式兼容性子实验最终通过（2026-08-19）：用户在新的系统临时私有根中，以`--print --no-session`、禁用上下文、Skill、Prompt Template、Theme和内置Tool，并显式选择`openai/gpt-5.6-terra`运行真实Pi；终端标准输出只有精确固定回复`PI_STUDY_510_PRINT_OK`，未出现Shell Gate Widget或其他TUI内容。随后用户亲自统计专用追踪，实际得到`factory=1 startup=1 quit_shutdown=1 user_bash=0 shell_gate=0`，逐项命中冻结预期，证明本次Print路径只创建一个Runtime、正常关闭且没有误触发Shell入口。该证据不证明任意Provider稳定性、无文件写入、JSON/RPC模式或所有资源绝无泄漏。下一步执行全量自动回归、文档结构和差异检查。
  - 5.10 最终阶段门禁通过（2026-08-19）：扩展包`npm run check`中的严格TypeScript检查与全量`185/185`自动测试通过，聚焦用例包含交错Shell决定不丢计数和确认等待期间Shutdown后仍fail-closed；仓库`git diff --check`、三份变更文档围栏、`11/11`本地链接、增量敏感信息扫描和后台Pi进程检查均通过，实际差异未发现任务外文件。稳定结论已同步`docs/learning/06-extensions.md`。自动层锁定Pi`0.84.1`开发依赖，真实Reload/New/Tree/Fork/Resume/TUI/Print矩阵使用本机CLI`0.84.2`；证据不覆盖JSON/RPC动态呈现、崩溃退出、跨进程锁、任意版本或所有资源绝无泄漏。用户亲自完成全部真实实验和结果核验；按连续教学约定记录实际操作与自然确认，不声称脱离提示独立复述。5.10与阶段5完成，下一步进入6.1系统地图。
  - 核心内容：工厂阶段不启动长期资源，资源延后到 Session 或实际入口；`session_shutdown` 必须幂等；Reload 后旧实例失效，Session 替换后不得复用旧 Context/SessionManager；同轮 Tool 可能并发，不能依赖兄弟 Tool Result。
  - 贯穿实验：连续两次 `/reload`，再执行 New、Resume、Fork、Tree 和正常退出；用实例标识与脱敏计数检查重复 Handler/Widget、状态串线和未释放资源；TUI 与 Print 运行最终兼容矩阵。
  - 验收与边界：真实序列证明 Reload/Session 切换后的重建与清理，退出在限定时间内完成，门禁始终默认拒绝，并由用户完成完整场景复述。观察不到泄漏不等于任意资源均无泄漏；结论限定当前 Pi 版本。

验收产物：`pi-study-guard` 源码、锁定的开发依赖、自动测试、脱敏生命周期矩阵、TUI/Print 手工验收记录，以及 `docs/learning/06-extensions.md` 中的稳定结论。

阶段门禁：类型检查与完整测试通过；真实 TUI 和至少一种无 UI 模式均验证；Model `bash` 与用户 `!`/`!!` 两条入口均覆盖；危险、取消、超时、无 UI 和策略异常默认拒绝；状态经 Reload、恢复和分支切换正确；无凭据、原始 Session、后台残留或意外 Git 改动。

### 阶段 6：Packages、Models 与 Providers

目标：掌握 Pi 能力的安全复用、安装与分发，并理解模型目录、认证、协议适配和自定义流式 Provider 的接入边界。

贯穿产物：把阶段 4 的 Prompt/Skill/Theme 与阶段 5 的 `pi-study-guard` 封装为本地 `pi-study-workbench` Package；再用当前版本文档、必要源码、完整使用场景、决策表和流程图理解内置 Provider、Custom Model、认证来源与 Custom Provider 的职责边界。

统一实验边界：使用课程自建材料、隔离 `PI_CODING_AGENT_DIR`、临时目录和本机回环地址；默认不安装真实第三方 Package、不访问外部模型服务、不读取现有认证文件、不产生模型费用。真实安装、登录、Provider 调用、发布或推送必须另行确认。

实用概念路线（2026-08-22）：学习者明确要求阶段 6 停止低收益的重复动态实验，后续只学习实际会用于选型、配置和排错的概念，并用完整场景、准确规则和流程图复习。6.4 的安装、两级作用域、筛选、范围更新和两级移除均已取得学习者直接证据；当前只保留一次隔离环境清理，不再用“重装”重复验收同一安装链。6.5-6.8 改为概念教学、当前版本文档/必要源码静态证据、场景决策表和流程图，不再要求认证解析实验、localhost Custom Model 请求或完整 Mock Provider。每个理论模块必须沉淀到 `docs/learning/07-packages-models-providers.md`，包含版本范围、证据边界和至少一张可复习流程图；理论验收只记录教学覆盖与学习者自然确认，不冒充动态 Provider、真实认证、计费或生产证据。按当前断点估计阶段 6 剩余约 1.5-2.5 小时，可拆成两次短课。

- [x] 6.1 掌握 npm、Git、本地路径三种 Package 来源及其固定版本策略。
  - 6.1 启动检查点（2026-08-19）：已从唯一计划断点恢复，核对本机 Pi CLI 与 npm 包均为 `0.84.2`，并完整读取该版本随包 `docs/packages.md`；该时点只进入系统地图，尚未安装、编写或运行任何 Package。
  - 系统地图检查点（2026-08-20）：已用同一 Package 从 A 演进到 B 的完整场景讲清 Source、Package、Resource、解析、加载、更新和固定的关系；用户已自然确认理解 Pi Package 是资源的组织与分发单元，以及 Git 分支是可移动指针、Git Commit 是固定快照。稳定术语、通用加载流程图和本地 fixture 可观察链已写入 `docs/learning/07-packages-models-providers.md`；该检查点未执行三类来源动态 A/B，因此不能作为实验验收证据。
  - 本地路径实验实现历史检查点（2026-08-20）：用户已明确确认准备实验；当时冻结同一绝对路径、隔离配置、无 Session/Model/外网，仅将 Package 标记从 A 原地切到 B 的单变量合同，并开始新增课程自有无依赖 fixture 和确定性测试。自动验证只作为实现证据，不能替代用户亲自运行真实 Pi；后续 Green 见下一条记录。
  - 本地路径实验实现 Green、用户实验待执行（2026-08-20）：`labs/6.1-package-sources/` 已加入无依赖 Package、受限 A/B 切换器、实验卡和真实 Pi CLI 测试；自动测试通过 CLI `-e` 传入同一临时绝对路径，分别启动新 Pi 进程取得 A-only -> B-only，`1/1` 通过，三份 JavaScript `node --check`、仓库 `git diff --check` 通过，仓库 fixture 仍为 A。该证据只覆盖 CLI `-e` 本地绝对路径的实验实现，不动态覆盖用户 Settings、项目 Settings、相对路径或同进程热重载，也不替代用户亲自观察。下一步用户运行隔离 A 基线。
  - 本地路径用户 A 基线已通过（2026-08-20）：用户在仓库根亲自使用临时 `PI_CODING_AGENT_DIR`、`PI_OFFLINE=1`、CLI `-e` 本地绝对路径、无 Session/Context/Skill/Prompt/Theme/Tool 和 `--help` 启动新 Pi 进程；经 `rg` 筛选的真实输出只出现 `--pi-study-package-a PI_STUDY_PACKAGE_A`，未出现 B，且命令已正常返回 Shell。该证据证明本次本地 Source 已导入 A 版 Extension、执行工厂并注册 Flag；不证明 Flag 被调用、Handler/Model/Tool 执行、Settings/Git/npm 行为、同进程热重载或权限隔离。下一步在同一终端和同一 Source 路径原地切换到 B。
  - 本地路径用户 A/B 对照与恢复已通过（2026-08-20）：用户在同一终端保留相同隔离 `PI_CODING_AGENT_DIR` 和同一 Package 绝对路径，只用课程切换器把 `source-marker.js` 从 A 原地改为 B；新的 Pi `--help` 进程真实输出 `local_marker=B`，随后只显示 `--pi-study-package-b PI_STUDY_PACKAGE_B`，未出现 A，并正常返回 Shell。之后切换器输出 `local_marker=A`，源文件只含 `PI_STUDY_PACKAGE_A`，限定该 fixture 的 `git diff --exit-code` 通过并输出 `FIXTURE_RESTORED_A_GIT_CLEAN`。A-only -> B-only 单变量对照证明本次 CLI `-e` 本地绝对路径链在新进程中读取当前磁盘内容，没有继续以旧 A 作为加载来源；恢复证据证明仓库 fixture 没有残留实验修改。该结论仍不覆盖 Settings、相对路径、同进程热重载、Git/npm、缓存删除、安全隔离或正式分发复现。下一步进入本机 Git 来源实验设计。
  - Git 来源实验实现进行中（2026-08-20）：用户已明确确认准备实验材料。合同冻结为系统临时目录中的课程自建无依赖 Git Package、只监听 `127.0.0.1` 的临时 Git 服务、隔离 `PI_CODING_AGENT_DIR`、无 Session/Model/外网；先让 `main` 指向 A 并观察 A，再新增 B Commit、推动同一 `main` 后观察 B，最后以完整 Commit A 观察仍为 A。下一步先保留缺少实验运行器的稳定 Red，再实现最小 Green；自动测试不能替代用户实操。
  - Git 来源实验稳定 Red（2026-08-20）：新增 `test/git-source.test.mjs`，锁定回环地址、不同 A/B Commit、`main` 的 A -> B 和完整 Commit A 仍为 A；首次聚焦测试实际 `0/1`、退出码 `1`，唯一失败为 `scripts/run-git-source-lab.mjs` 不存在的 `MODULE_NOT_FOUND`。该 Red 只证明实验职责尚未实现，不证明测试合同已完整或真实 Git 行为。下一步实现最小运行器并复跑同一测试。
  - Git 来源首轮 Green 尝试在清理阶段失败（2026-08-20）：最小运行器已建立临时 Git 仓库、回环 Git daemon 和三次真实 Pi 观察；两份脚本 `node --check` 通过，但聚焦测试仍为 `0/1`，子进程因 daemon 退出事件竞态触发 `unsettled top-level await`，以状态 `13` 结束。该失败不能证明 Git A/B 合同通过；下一步修正有界退出等待后复跑原测试，不放宽业务断言。
  - Git 来源第二轮 Green 尝试暴露观察通道问题（2026-08-20）：daemon 有界退出修正后不再出现状态 `13`，聚焦测试仍为 `0/1`；真实 Pi 成功退出，但运行器只检查 `stdout`，得到 `seenA=false seenB=false`。现有本地路径实验以 `stdout + stderr` 作为帮助输出证据，因此下一步统一两组观察口径后复跑，业务断言保持不变。
  - Git Source 解析差异已定位（2026-08-20）：统一输出通道后 Pi 仍只显示内置帮助；对 Pi `0.84.2` 的 `parseGitUrl()` 做只读直接调用确认，裸 `git://127.0.0.1/...@main` 返回 `null`，显式 `git:git://127.0.0.1/...@main` 才解析为 `type=git`、`host=127.0.0.1`、`ref=main`、`pinned=true`。这与随包文档所称无前缀 `git://` 可接受存在版本差异。下一步仅改用当前实现可解析的显式 Source，再观察 ref 缓存行为；不把该差异外推到其他版本。
  - Git temporary ref 缓存行为已形成实现 Red（2026-08-20）：改用可解析的 `git:git://` 后，真实观察为 `branchBeforeMove=A`、同一隔离根中的 `branchAfterMove=A`、`commitAAfterBranchMove=A`，聚焦测试仍为 `0/1`。这与 `parseGitUrl(...@main).pinned=true` 及 temporary scope 对已存在 pinned checkout 不自动 refresh 的源码分支一致，证明原先假定“同一临时缓存中的 `@main` 会自动到 B”不成立。实验合同收紧为同时记录同缓存仍 A、新隔离缓存重新解析 `main` 得 B、新隔离缓存解析完整 Commit A 得 A；这不是放宽断言，而是分离 Git ref 可移动性与 Pi 临时缓存更新语义。
  - Git 来源实验实现 Green、用户实验待执行（2026-08-20）：`scripts/run-git-source-lab.mjs` 已使用系统临时仓库、只监听 `127.0.0.1` 的 Git daemon、三个隔离配置根和四次真实 Pi `-e --help` 观察；聚焦测试 `1/1`、连同本地路径实验的扩展回归 `2/2`、两份新增脚本及既有切换器的 `node --check`、仓库 `git diff --check` 均通过，人类可读入口也实际输出四项预期和 `result=PASS`。结果严格为 `branchBeforeMove=A`、`cachedBranchAfterMove=A`、`freshBranchAfterMove=B`、`commitAAfterBranchMove=A`，A/B Commit 为不同完整 SHA；每轮结束后均未发现遗留实验临时目录或后台 Git daemon。该 Green 只证明当前实验实现和 Pi `0.84.2` 链路可运行，不替代用户亲自执行，不证明 Settings 安装更新、Tag、外部 Git 服务、认证、任意版本或供应链复现。下一步用户运行该课程实验并观察摘要。
  - Git 来源用户实验已通过、边界理解确认中（2026-08-20）：用户亲自在仓库根运行课程 Git 来源运行器，终端显示只使用 `loopback-only`、`127.0.0.1` 和 `main`，生成 A/B 两个不同的完整 40 位 Commit SHA，并依次取得 `branch_before_move=A`、`cached_branch_after_move=A`、`fresh_branch_after_move=B`、`commit_a_after_branch_move=A`、`result=PASS`，命令正常返回 Shell。该直接证据证明本次 Pi `0.84.2` CLI `-e` 实验中：新缓存重新解析可移动 `main` 时由 A 变为 B，同一 pinned temporary checkout 没有自动刷新，完整 Commit A 在分支前进后仍解析为 A。它不证明 Settings 安装/更新、Tag、外部 Git 服务、认证、缓存删除、其他版本或完整供应链复现；用户正在确认上述缓存与 ref 边界，6.1 仍不勾选。
  - Git 来源缓存与 ref 边界理解已确认（2026-08-20）：在完整讲解“远端 `main` 当前指针”与“Pi 本地已检出的 temporary checkout”是两个状态后，用户要求继续学习，按连续教学约定记为自然确认；不声称脱离提示复述。Git 用户实验与边界讲解已闭合，下一步进入 npm 来源。
  - npm 精确版本/范围系统地图与实验卡已准备、待开发确认（2026-08-20）：已核对本机 Pi CLI `0.84.2` 随包 `docs/packages.md`、`dist/core/package-manager.js` 和同包 SemVer 实现。当前实现只把合法单一版本如 `1.0.0` 标为 `pinned` 并在 Package 更新中跳过；`^1.0.0` 解析为 `>=1.0.0 <2.0.0-0`、允许 `1.0.0/1.1.0` 但拒绝 `2.0.0`，更新时选择范围内最高可用版本；无版本 Source 则按 npm 当前默认目标重新解析。冻结实验为本机回环 registry 先提供无依赖 A=`1.0.0`，再新增 B=`1.1.0`，使用两套隔离 Pi 用户配置分别安装精确版本与范围版本，发布 B 后执行 Package 更新，预期精确版本仍 A、范围版本变 B。实验不访问真实 npm、不读取认证、不请求 Model、不安装第三方 Package；实现与用户动态证据均尚未取得，确认前不编写代码，6.1 保持进行中。
  - npm 来源实验实现进行中（2026-08-20）：用户已明确确认按上述实验卡编写代码。实现范围冻结为系统临时目录、课程自建无依赖/无 lifecycle script 的 A/B tarball、只监听 `127.0.0.1` 的最小 registry、两套隔离 `HOME`/`PI_CODING_AGENT_DIR` 和真实 Pi `install`/`update --extensions`/`--help`；npm 子进程只继承最小环境并强制使用回环 registry、禁用 audit/fund/update notifier/脚本。先保留缺少运行器的稳定 Red，再实现最小 Green；自动证据不替代用户实操。
  - npm 来源实验稳定 Red（2026-08-20）：新增 `test/npm-source.test.mjs`，锁定回环 registry、A=`1.0.0`、B=`1.1.0`、精确 Source 更新后仍 A 和范围 Source 显式更新后 A -> B；测试脚本 `node --check` 通过，首次聚焦测试实际 `0/1`、退出码 `1`，唯一失败为 `scripts/run-npm-source-lab.mjs` 不存在的 `MODULE_NOT_FOUND`。该 Red 只证明运行器职责尚未实现，不证明 registry、Pi install/update 或版本选择行为；下一步实现最小运行器并原样复跑。
  - npm 来源实验聚焦 Green、扩展验证进行中（2026-08-20）：新增 `scripts/run-npm-source-lab.mjs`，在系统临时目录生成无依赖/无 lifecycle script 的 `1.0.0` A 与 `1.1.0` B tarball，启动只监听 `127.0.0.1` 的最小 registry，并以两套隔离 Pi 用户配置运行真实 `install`、`update --extensions` 和 `--help`；运行器与测试 `node --check`、仓库 `git diff --check` 通过，原始聚焦测试在未修改业务断言下由 `0/1` 转为 `1/1`。该 Green 只证明实现链初步跑通，不替代用户实操；下一步验证人类可读输出、三组来源回归、清理和文档。
  - npm 来源实验实现 Green、用户实验待执行（2026-08-20）：人类可读入口两次稳定输出回环地址、实际 Settings Source、精确/范围四项观察和 `result=PASS`；最终结果为精确 `A@1.0.0 -> A@1.0.0`、范围 `A@1.0.0 -> B@1.1.0`，两者均由 Pi 帮助标记与安装目录版本交叉确认，隔离 `settings.json` 回读证明 Source 保持为精确 `1.0.0` 与范围 `^1.0.0`。npm 聚焦测试 `1/1`、本地路径/Git/npm 串行扩展回归 `3/3`、六份 JavaScript `node --check`、fixture JSON 解析、仓库 `git diff --check`、12 份 Markdown 的 H1/围栏与 49 个相对链接、增量敏感模式检查均通过；npm `11.6.2` 实际确认四个安全配置值为 `true/false/false/false`，每轮结束后无实验临时目录或相关进程残留。该自动 Green 只证明课程实现和本机 Pi `0.84.2`/npm `11.6.2` 用户级 install/update 链可运行，不替代用户亲自执行，不证明公共 Registry、认证、Tag/无版本 Source、项目 Settings、第三方依赖、生命周期脚本、其他版本或供应链安全。下一步用户运行课程 npm 实验并观察摘要，6.1 仍不勾选。
  - npm 来源用户实验已通过、综合策略收口进行中（2026-08-20）：用户亲自在仓库根运行课程 npm 来源运行器，终端显示只使用 `loopback-only` 和 `127.0.0.1`，A=`1.0.0`、B=`1.1.0`，实际 Settings Source 分别为精确版本 `1.0.0` 与范围 `^1.0.0`；四项结果为精确版本 `A@1.0.0 -> A@1.0.0`、范围版本 `A@1.0.0 -> B@1.1.0`，并输出 `registry_requests=7`、`result=PASS`，命令正常返回 Shell。结合已验证的运行器合同，这直接证明本次 Pi `0.84.2`/npm `11.6.2` 用户级 install/update 链中，精确版本在显式更新后保持不变，版本范围在 `update --extensions` 后重新选择范围内的 `1.1.0`；实验后的只读清理检查未发现相关临时目录或后台进程。该用户证据不证明公共 Registry、认证、Tag/无版本 Source、项目 Settings、第三方依赖、生命周期脚本、其他版本或供应链安全。6.1 仍不勾选，下一步完成三类 Source 的综合策略与证据边界收口。
  - 文档整理已验证（2026-08-20）：仅整理计划、学习索引、06/07 正文和 6.1 实验 README；`git diff --check`、Markdown 围栏/标题/表格/相对链接与页内锚点检查、33/33 Mermaid 本地渲染、动态状态与关键证据 `rg` 检查、Git 状态及四个既有实验文件 SHA-256 复核均通过，阻塞：无。该记录只验收整理本身，不承担 6.1 后续动态实验状态；当前状态以相邻的新检查点及顶部、底部断点为准。
  - 综合策略与 6.1 验收已完成（2026-08-20）：用户已亲自完成本地路径 A-only -> B-only、Git `main`/temporary 缓存/完整 Commit 和 npm 精确版本/范围三组真实 Pi 对照，并在“本地路径用于调试、分支或范围允许变化、完整 Commit 或精确版本提供明确基线，但都不等于完整供应链复现”的选型讲解后要求继续学习，按连续教学约定记为自然确认，不声称脱离提示复述。Git 用户实验记录 Commit A=`e52c747ec45033cd0f742e6f25a5e8498291574a`、Commit B=`f89a615fea0016e40b605d884217782e1413d3ab`；npm 记录精确 `1.0.0` 与范围 `^1.0.0` 的 A/B 结果；本地 fixture A/B 文件 SHA-256 分别为 `efda597299f2f4cf83751fd57322e946d2337d2ff7a61bec31478593bed89589` 与 `a319bfc85d161358f0ef8ce3d6a58712485743e15c3c20b811fd705e04e742b9`，B 哈希由恢复后的 A 通过课程切换器同一内存规则只读计算，仓库 fixture 仍为 A 且无差异。选型矩阵、流程图、最小交付记录和证据边界已写入 07 正文；6.1 验收通过。
  - 核心内容：比较三类 Source 的身份判定、全局/项目位置、临时 `-e` 和更新语义；npm 固定精确版本，Git 比较分支、Tag 与 Commit，本地路径记录 Git Commit、文件清单和 SHA-256。
  - 贯穿实验：用同一课程自建包构造 local、localhost npm fixture 和本机 Git fixture，准备 A/B 两版，对比未固定与固定来源在更新后的实际解析结果，全程禁止外网。
  - 验收与边界：记录 Source、解析位置、版本/Commit、内容哈希和 A/B 结果，并能为个人调试、团队复现和正式分发选择策略。Tag 可被重指向，本地路径内容可原地变化，文档规则不能替代运行证据。
- [x] 6.2 学会审查 Package 的资源清单、依赖、安装脚本和系统权限风险。
  - 进行中（2026-08-20）：6.1 已验收，开始建立 Package 安全审查系统地图；当前只读区分 Manifest/发布清单、依赖、lifecycle scripts、Extension 初始化和外部能力，尚未创建或执行风险样例。
  - 安装与加载边界澄清中（2026-08-20）：用户明确表示尚未理解“禁用 Extension 不会撤销安装脚本副作用”；当前退回完整场景讲解，区分 Package 安装、Extension 加载、资源禁用、Package 卸载和副作用恢复五个动作，不记录理解验收。下一步待用户自然确认后再进入实验卡设计。
  - 安装、加载与禁用边界理解已确认（2026-08-20）：用户已理解已加载的 Extension 在单纯修改禁用配置后仍暂存于当前进程，并在纠正为“执行 `/reload` 或重新启动都会触发下一次资源解析”后要求沉淀文档；稳定时间线、动作对照、shutdown 清理边界和 Pi `0.84.2` 静态证据已写入 07 正文。该确认不证明任意 Extension 能正确清理副作用，6.2 仍进行中；下一步设计安全 A/B 实验卡。
  - 6.2 安全 A/B/C/D 实验卡修订版已冻结、待开发确认（2026-08-20）：课程自建 npm Package 无依赖，只包含一个受限 `postinstall` 和一个 Extension 工厂，两者只向运行器创建且通过前缀校验的系统临时 marker 目录写固定内容；使用回环 Registry、隔离 `HOME`/`PI_CODING_AGENT_DIR`/npm cache、无 Session/Model/外网。静态审查先核对 tarball 文件清单、Manifest、四类依赖、lifecycle scripts、Extension 入口、文件/进程/网络能力。动态矩阵为 A 默认安装产生 install marker、启动后产生 load marker；B 以 npm `ignore-scripts` 安装不产生 install marker，但启动后仍产生 load marker；C 在 B 的实际安装根目录定向 `npm rebuild` 并关闭 `ignore-scripts`，预期补出 install marker，rebuild 本身不增加 load marker；D 将隔离 Package 配置的 `extensions` 过滤为 `[]` 后启动新 Pi 进程，load 计数不再增加而既有 install marker 保留。该实验不覆盖同进程外部改配置后执行 `/reload`、第三方依赖、卸载、缓存删除、真实网络或通用安全结论；用户明确确认前不编写代码。
  - 安装脚本与 Extension 开关边界澄清中（2026-08-20）：用户明确表示尚未理解“限制安装脚本不等于限制 Extension”；当前停止实验确认，回到 `npm install` 的 lifecycle script 开关与 Pi 启动/重载阶段的 Extension 资源过滤两条独立控制链，不记录理解验收。下一步待用户理解后再确认实验卡。
  - 安装脚本与 Extension 开关边界理解已确认（2026-08-20）：用户已理解 npm `ignore-scripts` 只跳过安装阶段的 lifecycle scripts，不删除 Package 文件或改变 Pi 的资源加载配置，并要求继续学习；按连续教学约定记为自然确认，不声称脱离提示复述。稳定结论和“缺少构建产物导致偶发加载失败不等于可靠禁用”的边界已写入 07 正文；下一步进入依赖与锁文件静态审查地图，实验代码仍未开始。
  - lifecycle scripts 补执行边界理解已确认（2026-08-20）：用户已理解 `ignore-scripts` 跳过的是执行而非文件安装，并要求将后续放行路径落盘后继续学习。07 正文已补充 npm `11.6.2` 定向 `rebuild`、实际安装根目录、单独 reload 和不可回滚边界；当前只有官方/本机静态文档证据，尚未执行用户 rebuild 实验，不证明具体脚本安全或可用。
  - 依赖与锁文件系统地图理解已确认（2026-08-20）：用户在完整场景、分类表和决策图讲解后要求继续学习，按连续教学约定记为自然确认，不声称脱离提示复述。稳定结论已写入 07 正文：`dependencies` 负责运行依赖，Pi 核心能力由 `peerDependencies` 声明，其他随包 Pi Package 同时使用 `dependencies` 与 `bundledDependencies`，开发工具归 `devDependencies`；锁文件只记录一次已解析依赖图，不证明发布内容或供应链安全。
  - 安装前静态审查清单理解已确认（2026-08-20）：用户在 Source、发布清单、Manifest、依赖图、lifecycle scripts、Extension 入口和系统能力的六步讲解后要求继续，按连续教学约定记为自然确认，不声称脱离提示复述。稳定清单和“静态可见不等于安全或运行证明”的边界已写入 07 正文；尚未执行动态实验。
  - 实验开发门禁（2026-08-20）：因学习者进一步询问跳过脚本后的放行方式，原实验卡增加 C 组定向 rebuild，并将 Extension 禁用顺延为 D 组；其余固定条件与证据边界不变。当前等待用户明确说“确认”，此前不创建实验代码、不执行 Package 安装。
  - 实验开发已确认、实现进行中（2026-08-20）：用户已明确回复“确认”，授权按冻结的 A/B/C/D 合同创建课程实验代码。当前先复用 6.1 的回环 Registry、隔离配置和临时目录约束，新增稳定 Red 测试；自动测试只作为实现证据，不替代学习者后续亲自运行。
  - 实验实现稳定 Red（2026-08-20）：新增 `labs/6.2-package-security/test/package-security.test.mjs`，锁定 tarball 三文件清单、空四类依赖、单一 `postinstall`/Extension 入口及 A/B/C/D marker 合同；测试脚本 `node --check` 通过，首次聚焦测试实际 `0/1`、退出码 `1`，唯一失败为 `scripts/run-package-security-lab.mjs` 不存在的 `MODULE_NOT_FOUND`。该 Red 只证明运行器职责尚未实现，没有触发 npm、Pi 或 Package 安装；下一步实现最小 fixture 与隔离运行器并原样复跑。
  - 首次 Green 尝试安全门禁 Red（2026-08-20）：fixture、回环 Registry 和运行器已实现并通过四个 JavaScript `node --check`、fixture JSON 解析与 `git diff --check`；聚焦测试在 A 组安装期被 `PI_STUDY_62_MARKER_DIR` 前缀校验拒绝，实际 `0/1`、退出码 `1`。根因是运行器把子进程 `TMPDIR` 改为 scope 内目录，使 fixture 中的 `os.tmpdir()` 与创建实验根时的系统临时目录不一致；安装脚本没有写 marker，B/C/D 未执行。下一步统一系统临时根而不放宽 marker 路径约束，再原样复跑。
  - 实验实现聚焦 Green、扩展验证进行中（2026-08-20）：保持测试断言不变，将 npm/Pi 子进程 `TMPDIR` 恢复为创建实验根时的同一系统临时目录，未放宽 fixture 的固定前缀、绝对路径和目录类型校验；聚焦测试由 `0/1` 转为 `1/1`。A 默认安装与加载、B `ignore-scripts` 后仍加载、C 定向 rebuild 补执行且不触发 Extension、D `extensions: []` 后新进程不再增加加载计数均命中冻结合同。该 Green 只证明实现链初步跑通，不替代用户实操；下一步补 README、人类可读入口、扩展回归与清理检查。
  - 实验实现 Green、用户实验待执行（2026-08-20）：新增 6 文件课程材料，README、无依赖 Package、受限 `postinstall`/Extension、回环 Registry 隔离运行器和合同测试均已就绪。人类可读入口实际输出 A=`install:true, load:0->1`、B=`install:false, load:0->1`、C=`install:true, load:1->1`、D=`install:true, load:1->1`、`registry_requests=4`、`result=PASS`；聚焦复测 `1/1`、6.1+6.2 串行真实 CLI 回归 `4/4`、十个 JavaScript `node --check`、两份 fixture JSON、四份 Markdown H1/围栏与相对链接、`git diff --check`、增量敏感模式和文件范围检查均通过，临时目录与相关进程无残留。该 Green 只证明课程实现和本机 Pi `0.84.2`/npm `11.6.2` 受控链可运行，不替代用户实操，不证明第三方 Package、传递依赖、真实网络、卸载、缓存删除、脚本安全或 OS 隔离；6.2 保持进行中。
  - 用户 A/B/C/D 实验与 6.2 验收已通过（2026-08-20）：用户亲自在仓库根运行课程运行器，终端显示 `network=loopback-only`、`host=127.0.0.1`、精确 Package `1.0.0` 和 tarball 三文件清单；A=`install:true/load:0->1`、B=`install:false/load:0->1`、C=`install:true/load:1->1`、D=`install:true/load:1->1`、`registry_requests=4`、`result=PASS`，命令正常返回 Shell。运行后只读检查课程临时目录残留为 0。结合已确认的静态六步审查、依赖分类、安装/加载/禁用/rebuild 边界和自动 Green，直接证明本机 Pi `0.84.2`/npm `11.6.2` 课程 fixture 的四条受控路径；不证明第三方 Package、传递依赖、真实网络、卸载、缓存删除、任意脚本安全、幂等、可回滚或 OS 隔离。6.2 验收通过。
  - 核心内容：审查显式 `pi` Manifest 与约定目录、发布清单、直接/传递依赖、`dependencies`/`peerDependencies`/`bundledDependencies`、锁文件、lifecycle scripts、Extension 初始化及外部命令、网络和文件访问。
  - 贯穿实验：为课程自建风险样例加入只向临时目录写 marker 的 lifecycle script 和 Extension factory；先完成静态审查，再分别观察安装期、加载期和资源禁用路径，禁止真实恶意代码。
  - 验收与边界：形成安全审查表、归档清单和 marker A/B 证据，能在执行前指出安装期与加载期代码。安装脚本限制不等于 Extension 受限，资源禁用不等于卸载，Project Trust 和 Tool 白名单都不是进程沙箱。
- [x] 6.3 将阶段 4-5 的资源封装成一个本地 Pi Package。
  - 进行中（2026-08-20）：已从 6.2 验收断点进入系统地图，并按本机 Pi `0.84.2` 随包文档与现有资源结构冻结第一版职责边界：仓库根作为 `pi-study-workbench` Package 根，显式 Manifest 指向 `.pi/extensions/pi-study-guard/index.ts`、`.agents/skills/java-readonly-analysis/`、`.pi/prompts/java-review.md` 和 `.pi/themes/pi-study-lab.json`，不复制第二套源码；`pi` Manifest、npm `files` 与依赖字段分别负责资源发现、归档内容和模块提供者。稳定流程图、发布排除项、Peer/开发依赖及隔离等价验证边界已写入 07 正文。当前只有静态设计证据，尚未取得用户理解确认，也未创建根 `package.json`、移动文件、打包或加载。
  - 系统地图理解已确认（2026-08-20）：用户在单一源码、三张清单和源码到归档完整链路讲解后明确回复“确认”，按连续教学约定记为自然确认，不声称脱离提示复述。该确认只覆盖封装职责，不证明 Manifest、tarball 或资源发现已经实现。
  - 源码/归档等价实验卡已冻结、待开发确认（2026-08-20）：A 指向仓库根，B 指向同一真实 tarball 的解包根；固定 Pi/Node/npm、资源内容、观察脚本、隔离配置和无 Session/Model/外网条件，逐边核对 Extension Flag 与 Command、Skill、Prompt、Theme、资源诊断和退出状态。静态门禁先核对 Manifest、Peer/开发依赖、`npm pack --dry-run`、离线 `npm publish --dry-run`、真实 tarball 白名单及 SHA-256；任一缺失、重复、诊断或越界文件即失败。实验不发布 Registry、不推送 Git、不安装第三方 Package，也不替代阶段 4-5 原验收或提前覆盖 6.4。用户再次明确确认前不创建代码。
  - 实验开发已确认、稳定 Red 准备中（2026-08-20）：用户再次明确回复“确认”，授权按冻结合同创建根 Package Manifest 和 6.3 课程实验材料。当前先新增只调用预期运行器的合同测试，保留运行器不存在的稳定 Red；自动测试只作为实现证据，不替代用户后续亲自运行。
  - 实验实现稳定 Red（2026-08-20）：新增 `labs/6.3-local-package/test/package-artifact.test.mjs`，锁定离线、Package 身份、publish dry-run、源码/归档四类资源、诊断、tarball SHA-256 和清理摘要；测试脚本 `node --check` 与仓库 `git diff --check` 通过，首次聚焦测试实际 `0/1`、退出码 `1`，唯一失败为预期运行器不存在的 `MODULE_NOT_FOUND`。该 Red 没有运行 npm、Pi、打包或发布；下一步实现根 Manifest、发布白名单和隔离运行器并原样复跑。
  - 首次 Green 尝试观察器 Red（2026-08-20）：根 `package.json`、Package README、6.3 README 与隔离运行器已实现并通过两个 JavaScript `node --check`、根 JSON 解析和 `git diff --check`；原测试仍为 `0/1`，失败发生在 A 组资源加载后的路径归属观察器，因错误使用 Prompt 的 `path` 与 Theme 的 `filePath` 字段而收到 `undefined`。打包与资源加载已进入观察阶段，但不能据此记为 Green；下一步按 Pi `0.84.2` 公开类型改用 Prompt `filePath`、Theme `sourcePath`，不改变业务断言。
  - 实验实现 Green、用户实验待执行（2026-08-20）：按公开类型修正观察字段后，原聚焦测试由 `0/1` 转为 `1/1`。人类可读入口实际输出 Pi `0.84.2`、Node `v25.2.1`、npm `11.6.2`、Package `0.1.0`/`UNLICENSED`、`publish_dry_run=true`、tarball `19` 文件及 SHA-256 `ea474d92ef11af09ecce4fcac3de2bafb540776e4edee6b7c03b5b5e6efb281c`；源码根与解包根的 Extension Flag/Command、Skill、Prompt、Theme 均为 `1`，诊断和临时残留为 0，`result=PASS`。6.1-6.3 串行真实链回归 `5/5`，根 Package 类型检查与阶段 5 自动测试 `185/185`，全部课程 JavaScript `node --check`、三份 Package JSON、Markdown 围栏/相对链接、计划断点、`git diff --check`、敏感模式、临时目录和相关进程检查通过。该 Green 只证明实现与本机受控链，不替代用户亲自运行，不证明真实发布、安装/更新/卸载、其他环境或安全隔离；6.3 保持进行中。
  - 用户运行器输出已通过、6.3 当时误记验收（2026-08-20）：学习者亲自在仓库根启动 `node labs/6.3-local-package/scripts/run-package-artifact-lab.mjs`，取得 `network=offline`、Pi `0.84.2`、Node `v25.2.1`、npm `11.6.2`、`pi-study-workbench@0.1.0`、`license=UNLICENSED`、`publish_dry_run=true`、`tarball_files=19` 及与自动实验一致的 tarball SHA-256；源码根与同一 tarball 解包根的五项公开信号均为 `1`，且 `diagnostics=0`、`temp_residuals=0`、`result=PASS`。这些结果证明学习者启动的受控运行器完成了打包与等价检查，但打包、清单检查、解包和两组加载均由运行器自动执行，不能据此认定学习者亲自掌握了制品因果链；当时记录的完整学习验收现已撤回。证据也不覆盖真实 Registry 发布、外部安装、更新、卸载、其他环境兼容性或安全隔离。
  - 6.3 学习验收撤回、重新进行中（2026-08-21）：学习者明确指出 6.3 与 6.4 存在相同问题，自己只运行总控脚本并查看最终摘要，不知道具体学到了什么。保留自动测试、真实 tarball、SHA-256、源码/归档资源等价和清理结果作为实现证据，但它们不能替代学习者亲自执行 `npm pack`、检查归档清单、解包制品、分别加载源码根与解包根并理解各步因果。6.3 checkbox 已撤回；6.4 已有证据不删除，但在 6.3 重新验收前暂停推进。
  - 6.3 手工教学合同已确认（2026-08-21）：学习者明确要求目标是教会本人，而不是由代理静默完成。后续自动化只可准备隔离环境、回环依赖和可逆清理，不代替核心学习动作；每轮先讲完整场景和本步目的，再由学习者亲自执行一个原生 npm/Pi 动作，观察原始结果并说明它证明什么、不能证明什么。只有学习者亲手走通 Manifest -> 发布清单 -> tarball -> 解包根 -> Pi 资源发现因果链后，6.3 才能重新验收。
  - Manifest 首次手工观察已完成、理解确认中（2026-08-21）：学习者亲自在仓库根只读输出当前 `package.json` 的身份、`files` 和 `pi` 字段，实际看到 `pi-study-workbench@0.1.0`、`files` 的 17 个显式路径，以及 `pi` 下 Extension、Skill、Prompt、Theme 各一个入口。该证据只证明学习者已查看真实 Manifest，不证明发布清单、tarball 内容或 Pi 加载结果；下一步由学习者运行一次禁用 lifecycle scripts 的 `npm pack --dry-run --json`，亲自核对 npm 实际选择的文件。
  - 发布清单首次手工预览已完成（2026-08-21）：学习者亲自运行 `npm pack --dry-run --ignore-scripts --offline --json`，完整输出显示 `.npmignore` 缺失时回退使用 `.gitignore` 的 warning、`pi-study-workbench@0.1.0`、预期 tarball 文件名、压缩/解包大小、SHA-1/SHA-512 校验字段和最终文件清单；Manifest 的 17 个显式路径加上 npm 自动纳入的 `README.md` 与 `package.json`，实际 `entryCount=19`，命令正常返回 Shell。仓库根只读检查未发现生成的 `.tgz`，符合 dry-run 只预览不落制品的边界；该证据尚不证明真实 tarball、解包内容或 Pi 加载。下一步由学习者在独立临时目录真实生成 tarball，并核对文件与校验值。
  - 真实 tarball 首次手工生成已完成（2026-08-21）：学习者亲自在独立临时目录运行无 dry-run 的 `npm pack --ignore-scripts --offline --pack-destination`，npm 输出 `pi-study-workbench-0.1.0.tgz`、Package `0.1.0`、压缩 `18.9 kB`、解包 `64.5 kB`、19 个文件，以及与 dry-run 相同的完整 SHA-1；随后取得 `pack_exit=0` 和临时目录 `/tmp/pi-study-6.3-pack.kayGFi`。教练侧只读存在性检查确认 tarball 位于该目录，未写入仓库。该证据证明本机真实制品已生成，不证明学习者已核对物理文件 SHA-256、归档内部路径、解包结果或 Pi 加载；下一步由学习者直接检查该 tarball 的文件信息和 SHA-256。
  - 物理制品首次核对为环境变量 Red、待重试（2026-08-21）：学习者进入仓库后用当前 Shell 中未赋值的 `PI_STUDY_63_PACK_DIR` 拼接 tarball 路径，实际展开为错误的根路径 `/pi-study-workbench-0.1.0.tgz`，因此 `ls` 与 `shasum` 均报告文件不存在。教练侧只读检查确认真实 tarball 仍位于 `/tmp/pi-study-6.3-pack.kayGFi/`；该失败不是打包失败，也没有形成 SHA-256 证据。下一步在当前 Shell 重新赋值为已验证的完整临时路径后，原样重试文件信息和 SHA-256 检查。
  - 物理制品文件与 SHA-256 手工核对已完成（2026-08-21）：学习者在当前 Shell 重新绑定完整 tarball 路径后，`ls -lh` 直接确认 `/tmp/pi-study-6.3-pack.kayGFi/pi-study-workbench-0.1.0.tgz` 为约 `18K` 的普通文件；`shasum -a 256` 取得 `ea474d92ef11af09ecce4fcac3de2bafb540776e4edee6b7c03b5b5e6efb281c`，与此前受控自动实验记录的同一制品 SHA-256 一致。该 Green 证明物理文件存在且字节身份可复核，不单独证明归档内 19 个路径正确、解包成功或 Pi 能加载；下一步由学习者使用系统 tar 只读列出真实归档目录。
  - 真实归档目录首次手工核对已完成（2026-08-21）：学习者亲自执行 `tar -tzf`，真实输出的 19 个条目全部位于标准 `package/` 根下，包含 `package.json`、`README.md`、Extension 入口及其 12 个内部文件、Skill 正文与 reference、Prompt 和 Theme，未出现 `labs`、测试、Session、凭据、缓存、`node_modules` 或其他仓库文件；`tar_list_exit=0`，独立计数为 `19`。该证据证明压缩归档目录可读取且与发布清单一致，不证明文件已成功解包到磁盘或 Pi 能从解包根发现资源；下一步由学习者解包到新的独立临时目录并核对实际文件树。
  - 真实制品首次手工解包已完成（2026-08-21）：学习者亲自创建独立临时目录并执行 `tar -xzf ... -C`，取得 `extract_exit=0` 和解包目录 `/tmp/pi-study-6.3-extract.cQAUqK`；随后用 `find` 直接观察 `package/` 下落盘的 Extension、Skill/reference、Prompt、Theme、`package.json` 与 `README.md`，普通文件计数为 `19`。教练侧只读交叉检查确认该解包根仍存在且文件数为 19。该证据证明真实制品已成功解包为独立 Package 根，不证明这些文件与源码根逐字节一致或 Pi 已读取 Manifest 并发现资源；下一步固定源码根 A 与解包根 B，先比较 Package 身份，再分别执行真实 Pi 加载观察。
  - 源码根与解包根 Manifest 手工对照已完成（2026-08-21）：学习者在当前 Shell 明确绑定源码根 A=`<repo-root>` 与解包根 B=`/tmp/pi-study-6.3-extract.cQAUqK/package`；两份 `package.json` 的 SHA-256 均为 `17b573cb5c4f5eb3102c07d29be1bf6e549fc903c56a45a32dba6f55fbe53a90`，`cmp` 无差异且 `manifest_cmp_exit=0`。这证明 A/B 使用字节相同的 Package 身份与 Pi 资源入口声明，不证明入口依赖文件均等价或任一资源已经被真实 Pi 发现；下一步先用两个独立隔离配置分别执行 Pi `--help`，观察同一 Extension Flag。
  - 源码根 A 的真实 Pi Extension 观察已完成（2026-08-21）：学习者亲自创建隔离 `PI_CODING_AGENT_DIR=/tmp/pi-study-6.3-source-agent.cugYIL`，在离线、无 Session/Context/Skill/Prompt/Theme/Tool 条件下用 `pi -e "$PI_STUDY_63_SOURCE_ROOT" --help` 加载源码根；真实输出包含 `--pi-study-guard` 及其描述，`source_pi_exit=0`、`source_rg_exit=0`。这直接证明 A 组本次导入 Extension、执行工厂并注册 Flag，不证明 Flag 被调用、Command/Handler/Tool 执行、其他三类资源发现或解包根 B 等价；下一步使用全新隔离配置对 B 执行相同观察。
  - 解包根 B 的真实 Pi Extension 观察与 A/B 对照已完成（2026-08-21）：学习者亲自创建另一隔离 `PI_CODING_AGENT_DIR=/tmp/pi-study-6.3-archive-agent.esY689`，在相同关闭条件下用 `pi -e "$PI_STUDY_63_ARCHIVE_ROOT" --help` 加载解包根；真实输出同样包含 `--pi-study-guard` 及相同描述，`archive_pi_exit=0`、`archive_rg_exit=0`。与 A 组联合后，只能得出源码根和真实 tarball 解包根均可导入同一 Extension 入口、执行工厂并注册同一 Flag；仍不证明 Flag/Command/Handler/Tool 被调用或 Skill、Prompt、Theme 已发现。下一步从空工作目录分别启动隔离 Pi TUI，先观察 A 组三类非 Extension 资源。
  - 源码根 A 的真实 Pi TUI 资源观察已完成（2026-08-21）：学习者从空临时工作目录 `/private/tmp/pi-study-6.3-source-tui-work.xBiLUF` 启动离线、无 Session/Context/Tool 的隔离 Pi `0.84.2`，并通过完整启动资源区亲眼看到课程专有 Skill `java-readonly-analysis`、Prompt `/java-review`、Extension `pi-study-guard` 和 Theme `pi-study-lab`；同屏的其他 Skill 不归因于本 Package。离线 `fd` 下载跳过和无 Model 提示不否定本地资源发现，空输入框未形成 Provider 请求；随后按 `Ctrl+D` 正常返回 Shell。该证据完成 A 组四类资源发现观察，不证明这些资源的全部行为，也不替代解包根 B；下一步从另一空工作目录对 B 执行相同 TUI 观察。
  - 解包根 B 的真实 Pi TUI 资源观察与 A/B 发现对照已完成（2026-08-21）：学习者从另一空临时工作目录 `/private/tmp/pi-study-6.3-archive-tui-work.O4sE84` 启动离线、无 Session/Context/Tool 的隔离 Pi `0.84.2`；完整启动资源区同样显示课程专有 Skill `java-readonly-analysis`、Prompt `/java-review`、Extension `pi-study-guard` 和 Theme `pi-study-lab`，并出现相同 Shell Gate Widget，随后按 `Ctrl+D` 返回 Shell。同屏其他 Skill 和离线/无 Model warning 不纳入本 Package 对照。结合 A 组，只能得出源码根与真实 tarball 解包根均发现同名四类课程资源且 Extension TUI 初始化可见；不重新证明各资源全部行为、Provider、发布或安全性。下一步由学习者复述完整制品因果链，并清理本轮精确临时目录。
  - 6.3 详细制品因果流程图已落盘（2026-08-21）：07 正文已用一张四段流程图串联单一源码与 Manifest 职责、发布清单预览与真实 tarball、源码根 A/解包根 B 隔离对照及结论边界，并明确 `pi` 已声明但制品漏带、制品带走但 `pi` 未声明两类失败；图中 `17 -> 19` 限定为本 Package 实测。该 Mermaid 已真实渲染通过，正文围栏、相对链接和 `git diff --check` 通过；文档落盘不替代学习者理解确认与临时目录清理，6.3 保持进行中。
  - 6.3 制品因果边界已自然确认、临时目录清理待执行（2026-08-21）：学习者在详细流程图及 `files`、`pi`、内部 import/reference 三层关系讲解后明确要求继续，记为连续教学中的自然理解确认，不声称独立复述。清理前只读检查确认本轮 8 个精确临时目录仍位于 `/private/tmp`，相关 Pi 进程为 0，仓库状态未扩散；下一步由学习者亲自删除这 8 个精确目录并核对残留为 0，完成前 6.3 不勾选。
  - 6.3 手工学习重新验收完成（2026-08-21）：学习者报告精确清理完成后，教练只读核对 8 个目标路径均不存在，`/private/tmp` 下 `pi-study-6.3-*` 目录为 0，相关 Pi 进程为 0，仓库状态未扩散且 `git diff --check` 通过。结合学习者亲手完成的 Manifest、dry-run、真实 tarball、SHA-256、归档清单、解包、A/B Manifest、Extension Flag 与四类 TUI 资源发现，以及自然确认的 `files`/`pi`/内部引用边界，6.3 重新满足验收并勾选完成；结论仍限于本机 Pi `0.84.2` 的受控本地制品链，不证明真实 Registry 发布、外部安装、完整资源行为、Provider、其他环境或安全隔离。
  - 核心内容：建立单一 Package 根目录，用显式 Manifest 声明 Prompt、Skill、Theme 和 Extension；明确运行依赖、Pi Peer Dependencies、语义化版本、许可证、入口、资源命名和发布文件清单，避免形成两套可漂移源码。
  - 贯穿实验：执行 `npm pack --dry-run` 与 `npm publish --dry-run`，再生成本地归档并检查实际内容；从干净隔离目录分别加载源目录和归档解包后的目录，逐类验证资源发现和阶段 5 关键行为回归。
  - 验收与边界：Manifest、发布前检查、归档清单和 SHA-256 固定，制品不含 Session、凭据、缓存或无关实验文件，四类资源均从 Package 来源被发现。本项不向外部 Registry 发布、不推送 Git；真实发布必须另行确认，打包成功也不替代 Extension 的原验收，等价性验证前不得删除原资源。
- [x] 6.4 验证 Package 的安装、禁用、筛选、更新和卸载。
  - 进行中（2026-08-20）：已从 6.3 验收断点进入系统地图，并只读核对本机 Pi `0.84.2` 随包 `docs/packages.md`、`package-manager-cli.js` 和 `package-manager.js`。当前先区分 Settings 中的 Source、Pi 受管理安装内容和新进程资源发现三层状态，再映射 `install`、`list`、`config`、`update`、`remove`、用户/项目作用域及 Project Trust；尚未执行任何 6.4 管理命令、创建实验代码或取得动态证据，6.4 不勾选。
  - 三层状态与完整移除边界理解已确认（2026-08-20）：用户在 Settings、受管理安装内容和当前 Extension runtime 的完整场景讲解后明确表示理解；稳定正文已补充“配置不存在”的精确定义、npm/Git 与本地 Source 的清理差异、`/reload`/重启及副作用恢复边界和流程图。该确认属于连续教学中的自然确认，不是 `install/list/config/update/remove` 动态证据；6.4 保持进行中，下一步讲资源筛选语义。
  - 资源筛选系统地图理解已确认（2026-08-20）：用户在 Manifest 候选集合、字段省略、空数组、glob、`!` 排除、`+` 精确包含、`-` 精确排除和 `autoload: false` 项目 delta 的完整讲解后明确表示理解；稳定规则与优先级流程图已写入 07 正文。该确认不证明 `pi config`、作用域合并或新进程资源发现已经动态执行。
  - 隔离管理实验卡已冻结、待开发确认（2026-08-20）：使用 6.3 `pi-study-workbench` 归档的临时 A/B 版本、只监听 `127.0.0.1` 的回环 Registry、隔离 `PI_CODING_AGENT_DIR` 和临时项目；依次验证用户级安装与 `list`、项目级 `config` 单资源禁用及普通/delta 筛选、新进程资源集合、范围版本 A -> B 更新、项目/用户两级移除、磁盘路径核对和重装恢复。实验不读取真实用户 Settings 或认证，不访问外网、不请求 Model、不发布 Registry、不删除仓库源码，也不把自动实现证据当作用户实操；用户明确确认前不创建代码或执行管理动作。
  - 实验开发已确认、稳定 Red 准备中（2026-08-21）：用户已明确回复“确认”，授权按冻结合同创建 6.4 课程实验代码。当前先新增只调用预期运行器的合同测试，锁定隔离、作用域、筛选、A/B 更新、两级移除、三层状态和重装摘要，再取得运行器不存在的稳定 Red；该授权不包含真实外网、真实用户配置、发布、提交或代替用户完成验收。
  - 实验实现稳定 Red（2026-08-21）：新增 `labs/6.4-package-management/test/package-management.test.mjs`，锁定 6.3 制品 A/B、用户/项目 Settings、`pi list`、两级安装路径、普通筛选优先级、项目 delta、范围更新、两级移除、重装和临时残留摘要；测试脚本 `node --check` 与仓库 `git diff --check` 通过，首次聚焦测试实际 `0/1`、退出码 `1`，唯一失败为预期运行器不存在的 `MODULE_NOT_FOUND`。该 Red 未运行 npm、Pi 或 Package 管理命令；下一步实现最小隔离运行器并原样复跑。
  - 实验实现聚焦 Green、说明与扩展验证进行中（2026-08-21）：新增最小隔离运行器后，原合同测试未修改断言即由 `0/1` 转为 `1/1`，实际用时约 22 秒；两份 JavaScript `node --check` 与仓库 `git diff --check` 通过。运行器使用当前 6.3 tarball 作为 A、只在临时解包副本中生成 B、回环 Registry、隔离用户目录和临时项目，真实执行 `install/list/update/remove`，并通过结构化项目 Settings 覆盖普通/delta 筛选；当前仍需核对人类可读摘要、补 README 和扩展回归，自动 Green 不替代用户亲自操作 `pi config` TUI。
  - 人类可读入口与真实 Config TUI 入口 Green、扩展验证进行中（2026-08-21）：默认入口实际取得用户安装 `A@0.1.0`、普通筛选优先级 `0 -> 1 -> 0`、项目空 delta 保持 Extension、项目禁用 delta 只关闭 Extension、范围更新 `0.1.0 -> 0.1.1`、项目/用户两级移除、B 重装、`temp_residuals=0` 和 `result=PASS`；新增 README 与 `--config-tui` 独立入口。代理在独立伪终端中对真实 `pi config -l --approve` 输入筛选、按一次 Space 并 Esc，TUI 显示继承 `[x]` 变为项目卸载 `[-]`，退出后核对 `autoload=false`、`-.pi/extensions/pi-study-guard/index.ts`、Extension=`0`、其他三类资源=`3`、临时残留=`0` 和 `result=PASS`。修改后的聚焦测试复测 `1/1`，两份脚本 `node --check` 与 `git diff --check` 通过。该伪终端操作仍是实现验证，不是学习者本人实操；下一步完成跨阶段回归、根检查和文档验证。
  - 实验实现 Green、用户实验待执行（2026-08-21）：6.1-6.4 六组真实 CLI 合同先在并发隔离调度中通过 `6/6`，再以 `--test-concurrency=1` 严格串行通过 `6/6`，串行总计约 54 秒；根 Package `npm run check` 通过严格 TypeScript 与阶段 5 全量 `185/185`。全部 6.1-6.4 JavaScript `node --check`、四份 Package JSON、8 份本次涉及 Markdown 的 H1/层级/围栏/链接、17 份 Markdown 的围栏/相对链接、07 正文 Mermaid `8/8` 真实渲染、`git diff --check`、26 文件高置信敏感模式、暂存区、临时目录和相关后台进程检查均通过。全目录严格标题检查只报告未改动的 03 文档既有 `H1 -> H3` 历史结构，不影响本次目标文件验证，也未越界整理。该 Green 证明课程实现和本机 Pi `0.84.2`/npm `11.6.2` 的隔离链路可运行，不替代学习者亲自执行，不证明真实 Registry、认证、共享缓存、Package 外副作用、当前其他 Pi 进程立即卸载、其他版本或通用安全性；6.4 保持进行中。
  - 默认管理矩阵用户实验已通过、Config TUI 待执行（2026-08-21）：学习者亲自在仓库根运行 6.4 默认运行器，终端显示 `network=loopback-only`、`host=127.0.0.1`、Pi `0.84.2`、A=`0.1.0`、B=`0.1.1`、范围 Source `npm:pi-study-workbench@^0.1.0` 和两版各 `19` 个 tarball 文件；实际取得用户安装 `A@0.1.0`、普通空筛选只禁用 Extension、筛选优先级 `0 -> 1 -> 0`、空项目 delta 保持 Extension、禁用 delta 只关闭 Extension、更新 `0.1.0 -> 0.1.1`、项目移除后项目列表/路径为 0 但用户级 Extension 仍为 1、用户移除后列表/路径/资源均为 0、重装 B 后四类资源恢复、`registry_requests=9`、`temp_residuals=0` 和 `result=PASS`，命令正常返回 Shell。该证据证明本次用户启动的隔离管理矩阵命中 Settings、受管理路径与新一轮资源发现的预期；筛选阶段仍由运行器写入结构化隔离 Settings，不证明学习者已亲自操作 `pi config` TUI，也不外推真实 Registry、认证、共享缓存、Package 外副作用或其他版本。6.4 保持进行中，下一步执行独立 Config TUI 实验。
  - Config TUI 首次用户实验为操作 Red、待重试（2026-08-21）：学习者亲自启动隔离 `--config-tui` 入口并成功进入 `Project Local Resources`，输入 `pi-study-guard` 后筛选到目标 Extension；退出前截图仍显示 `[x] pi-study-guard/index.ts inherited global`，没有出现按空格键后应有的 `[-] ... project unload`，运行器随后明确报错 `pi config did not create the expected project package delta` 并返回 Shell。该证据证明真实 TUI 入口与筛选已命中，也证明本轮没有写入项目 delta，不能作为禁用 Green；失败后只读检查未发现 6.4 临时目录或相关进程残留。下一步重新运行同一命令，确认目标行实际变为 `[-] project unload` 后再按 `Esc`。
  - Config TUI 用户重试已通过、6.4 当时误记验收（2026-08-21）：学习者重新运行同一隔离入口，在真实 `Project Local Resources` 中筛选 `pi-study-guard`，按空格键将目标行从继承状态切换为 `[-] pi-study-guard/index.ts project unload`，再按 `Esc` 关闭；运行器实际回读 `project_autoload=false`、`project_extensions=-.pi/extensions/pi-study-guard/index.ts`，新一轮资源解析为 Extension=`0`、其他三类资源=`3`，并输出回环地址、Pi `0.84.2`、Package `0.1.0`、`registry_requests=2`、`temp_residuals=0`、`result=PASS` 后正常返回 Shell。当时把这组 TUI 操作与自动矩阵合并视为完整学习验收，后续已根据学习者反馈撤回；这些结果只证明实现与操作链路，不证明学习者亲自掌握了自动执行的安装、更新、移除和重装。证据也不覆盖真实 Registry、认证、共享缓存、Package 外副作用、其他正在运行的 Pi 会立即卸载 Extension、其他版本或通用安全性。
  - 6.4 学习验收撤回、重新进行中（2026-08-21）：学习者明确指出默认运行器自动完成了安装、筛选、更新、两级移除和重装，自己只启动脚本并在 Config TUI 中执行一次资源切换，因此不知道本项具体学到了什么。该反馈证明此前把“用户启动自动矩阵 + 单次 TUI 操作”扩大为完整学习验收是错误的；保留自动 `6/6`、默认矩阵输出和真实 TUI Green 作为实现/操作证据，但它们不能替代学习者亲自执行原生命令、观察每步状态变化和理解因果链。6.4 checkbox 已撤回，6.5 不开始；下一步重新讲解完整手工场景，并在再次确认后准备一次只暴露一条原生 Pi 管理命令的交互式隔离实验。
  - 6.4 在 6.3 重新验收后恢复推进（2026-08-21）：6.3 手工制品链与清理已经闭合，6.4 从暂停恢复为进行中。下一步先用同一 `pi-study-workbench` 场景重新串联 Source 配置、受管理安装内容和新进程资源发现三层状态，再由学习者在隔离环境逐条执行 `install`、`list`、`config`、`update`、`remove` 和重装；已有总控运行器结果不替代这些手工步骤，6.4 不勾选。
  - 6.4 逐步教学入口缺口已确认、待开发确认（2026-08-21）：只读检查现有运行器仅支持默认一键矩阵、`--json` 和自动安装后的 `--config-tui`，没有“只准备回环 Registry、隔离配置和临时项目，随后逐条等待学习者执行原生 Pi 命令”的模式；继续使用现有入口会再次由运行器代做核心动作。下一步先完成三层状态完整场景教学，再由学习者明确确认是否新增最小逐步入口；确认前不修改实验代码、不执行管理动作，6.4 保持进行中。
  - 6.4 逐步教学入口开发已确认、稳定 Red 准备中（2026-08-21）：学习者明确回复“确认”，授权新增最小 guided 模式。合同限定它只准备 A/B tarball、`127.0.0.1` 回环 Registry、隔离 HOME/`PI_CODING_AGENT_DIR`/npm cache/临时项目和可审查环境文件，等待 `publish-b` 或 `quit`；启动基线必须没有 Package Settings、受管理安装内容或 Registry 安装请求，退出必须清理临时根。该授权不允许代理代跑 `install/list/config/update/remove`，这些核心动作仍由学习者逐条执行；当前先补合同测试取得 Red。
  - 6.4 guided 合同稳定 Red（2026-08-21）：新增聚焦测试后，测试文件 `node --check` 通过；只运行 guided 用例实际 `0/1`，约 10 秒后按测试门限以 `SIGTERM` 结束，stdout/stderr 均为空，失败点为没有出现 `guided_status=READY`。这证明当前运行器尚无准备模式、`--guided` 会落入原一键矩阵路径；Red 期间没有取得任何 Package 管理输出，下一步实现只准备环境并等待 `publish-b`/`quit` 的最小分支，原合同断言不变。
  - 6.4 guided 最小实现聚焦 Green、扩展验证进行中（2026-08-21）：运行器新增 `--guided` 分支，只生成 A/B 归档、启动 `127.0.0.1` 回环 Registry、创建隔离 HOME/`PI_CODING_AGENT_DIR`/npm cache/临时项目和 `0600` 环境文件；基线断言 Settings 与两级受管理安装目录均不存在、Registry 请求为 0，并只等待 `publish-b` 或 `quit`。两份脚本 `node --check` 通过，guided 聚焦测试由 Red 转为 `1/1`，约 2 秒完成，退出后临时根和环境文件均不存在。该 Green 只证明教学入口的实现合同，不是学习者的 Package 管理证据；下一步补说明并跑原管理矩阵回归，随后由学习者亲手启动 guided 模式。
  - 6.4 guided 入口实现验证完成、用户实验待开始（2026-08-21）：README 已把逐步手工教学与自动矩阵、独立 Config TUI 回归分开，并明确 `READY`/`management_commands=0` 只代表环境准备且没有代做管理动作。完整 6.4 测试实际 `2/2` Green：原 Settings/安装/筛选/更新/移除/重装矩阵约 24 秒通过，guided 准备、`publish-b` 与退出清理约 2 秒通过；其中 `publish-b` 后 Registry 请求仍为 0。该结果属于实现回归，不计入学习验收；6.4 保持进行中，下一步由学习者启动 guided 环境并先核对三层零状态基线。
  - 6.4 guided 入口交付前静态与清理检查通过（2026-08-21）：运行器和测试再次通过 `node --check`，6.4 README 的 H1/标题层级、围栏和指向 07 正文的相对链接通过，仓库 `git diff --check` 通过；`/private/tmp` 下同名前缀目录为 0，排除查询命令自身后相关后台进程为 0，现有脏工作区未发生任务外扩散。顶部断点、6.4 条目、底部断点和验收表均保持 6.4 进行中，下一步一致指向学习者启动 guided 环境。
  - 6.4 guided 用户零状态基线已验证（2026-08-21）：学习者亲自在终端 A 启动 guided 入口，实际输出回环网络 `127.0.0.1`、Pi `0.84.2`、课程 Package A=`0.1.0`/B=`0.1.1`、范围 Source `^0.1.0`，四项用户/项目 Settings 与受管理安装目录基线均为 0，Registry 请求和运行器执行的管理命令均为 0，Registry 为 A，并停在 `guided_status=READY` 等待输入。该证据只证明隔离环境已准备且运行器没有代做管理动作，不证明 Package 已安装、可发现、可更新或可移除；6.4 保持进行中。下一步保持终端 A 运行，在终端 B 加载本轮环境文件。
  - 6.4 guided 终端 B 隔离环境已验证（2026-08-21）：学习者在独立终端加载本轮 `guided-env.sh`，实际回传的 `project_dir`、`PI_CODING_AGENT_DIR` 和 `PI_STUDY_64_SOURCE` 均指向同一 guided 临时根及范围 Source `npm:pi-study-workbench@^0.1.0`。这证明终端 B 已进入本轮隔离项目并使用隔离 Agent 配置根，不证明 Package 已安装或资源已加载；下一步亲手运行只读 `pi list --approve`，验证安装前列表为空。
  - 6.4 guided 安装前 `pi list` 用户证据已验证（2026-08-21）：学习者在隔离终端 B 亲手运行 `pi list --approve`，真实输出 `No packages installed.`。该结果直接证明此时 Pi 的用户级和项目级有效登记视图中都没有 Package；`list` 本身不替代 Settings 文件检查，也不证明任意磁盘缓存均不存在或资源未加载。下一步亲手执行不带 `-l` 的用户级安装，Registry 当前只提供 A=`0.1.0`。
  - 6.4 guided 第一次用户级安装已执行、三层核对进行中（2026-08-21）：学习者亲手运行 `pi install "$PI_STUDY_64_SOURCE"`，真实输出安装范围 Source、npm `added 1 package` 和 `Installed npm:pi-study-workbench@^0.1.0`，命令正常返回 Shell。该结果证明安装与持久化调用成功，不带 `-l` 的目标应为隔离用户级作用域；绿色行仍显示 Source 范围而非磁盘实际版本，且尚未分别核对 Settings/list、安装目录 Manifest 和新进程资源发现。下一步先用 `pi list --approve` 验证用户级记录和受管理路径。
  - 6.4 guided 用户级 Source 与受管理路径已验证（2026-08-21）：安装后学习者再次亲手运行 `pi list --approve`，真实输出只包含 `User packages` 下的 `npm:pi-study-workbench@^0.1.0`，没有 `Project packages`，并报告本轮隔离 Agent 根下的 `npm/node_modules/pi-study-workbench` 路径。该证据闭合用户级 Settings/list 记录及 Pi 解析出的受管理路径，不单独证明路径中实际版本、文件完整性或新进程资源发现；下一步只读检查安装目录 Manifest 的 name/version。
  - 6.4 guided 用户级受管理实际版本已验证（2026-08-21）：学习者对 `$PI_STUDY_64_USER_INSTALL` 亲手运行只读 `npm pkg get name version --prefix ...`，真实输出 `pi-study-workbench`、`0.1.0`。这证明 Pi 报告的隔离受管理目录实际存在且当前安装的是 A 快照，范围 Source `^0.1.0` 与磁盘具体版本 `0.1.0` 已分开核对；它不证明完整文件清单或资源发现。下一步启动新的无 Model Pi 帮助进程，检查 Extension Flag。
  - 6.4 guided 用户级 Extension 新进程发现已验证（2026-08-21）：学习者亲手启动 `PI_OFFLINE=1`、无 Session/Context/Skill/Prompt/Theme/Tool 的新 Pi `--help` 进程并精确筛选课程 Flag，真实输出唯一 `--pi-study-guard` 及其课程描述。该证据证明本轮新进程从用户级 Package 发现并导入 Extension、完成 Flag 注册；由于命令显式禁用了其他三类资源，不能据此证明 Skill、Prompt 和 Theme 发现。下一步打开不禁用四类 Package 资源的真实 TUI 启动页完成联合观察。
  - 6.4 guided 用户级 A 四类资源 TUI 发现已验证、待正常退出（2026-08-21）：学习者亲手启动离线、无 Session/Context/Tool 的真实 Pi `0.84.2` TUI，启动页同时显示 Skill=`java-readonly-analysis`、Prompt=`/java-review`、Extension=`pi-study-workbench@0.1.0:.../pi-study-guard`、Theme=`pi-study-lab`，并出现课程 `Shell Gate` Widget。该证据证明新进程从用户级 A Package 发现四类资源且 Extension 工厂执行到 UI 注册；离线 `fd` 与无 Model 警告符合隔离条件，不影响资源结论。当前进程仍在 TUI，下一步从空输入框正常退出；本证据不外推资源全部行为、安全性或其他已运行进程热更新。
  - 6.4 guided 用户级 A 新进程已正常退出（2026-08-21）：学习者报告已从上述无 Session Pi TUI 正常退出并返回终端 B 的 Shell。结合用户亲手取得的安装命令、`pi list` 用户级 Source 与受管理路径、安装目录 Manifest `0.1.0`、新进程 Extension Flag 和四类资源启动页，本轮用户级安装已完成配置记录、磁盘副本与新进程资源发现三层核对；退出证据来自学习者回传，不是独立进程检查。该结果不证明项目级作用域、更新、筛选、卸载或重装；下一步只增加 `-l`，执行同一 Source 的项目级安装对照。
  - 6.4 guided 项目级安装已执行、三层核对进行中（2026-08-21）：学习者在同一隔离终端 B 亲手运行 `pi install -l --approve "$PI_STUDY_64_SOURCE"`，真实输出同一范围 Source、npm `added 1 package` 和 `Installed npm:pi-study-workbench@^0.1.0`，命令正常返回 Shell。与用户级安装相比唯一操作变量是 `-l`，该结果证明项目级安装与持久化调用成功；绿色 Source 仍是版本范围，不能单独证明项目 Settings 已正确记录、项目受管理目录实际存在、磁盘版本为 A，或两级记录是否同时保留。下一步先用只读 `pi list --approve` 核对两级 Source 和两个受管理路径。
  - 6.4 guided 用户级与项目级 Source/list 已并存（2026-08-21）：学习者亲手运行 `pi list --approve`，真实输出同时包含 `User packages` 与 `Project packages`；两者都保留范围 Source `npm:pi-study-workbench@^0.1.0`，分别解析到隔离 Agent 根的 `agent/npm/node_modules/pi-study-workbench` 和项目根的 `.pi/npm/node_modules/pi-study-workbench`。macOS 输出中的 `/var/...` 与 `/private/var/...` 是同一临时根的路径表示差异，两个安装副本由后半段的作用域目录区分。该证据证明两级 Settings/list 记录与受管理路径并存，不单独证明项目副本 Manifest 的实际版本或资源优先级；下一步只读核对项目安装目录的 name/version。
  - 6.4 guided 项目级受管理实际版本已验证（2026-08-21）：学习者对 `$PI_STUDY_64_PROJECT_INSTALL` 亲手运行只读 `npm pkg get name version --prefix ...`，真实输出 `pi-study-workbench`、`0.1.0`。这证明项目级独立受管理目录实际存在，目录中的 Manifest 身份正确且当前安装的是 A 快照；它与 `pi list` 中保留的范围 Source `^0.1.0` 是不同层次。该结果不证明将来更新仍停留在 A，也不证明项目项相对用户项的资源优先级或四类资源行为；下一步先讲清三层含义，再进入筛选对照。
  - 6.4 Source/路径/实际版本三层含义已自然确认（2026-08-21）：学习者确认理解范围 Source 是允许选择规则、受管理路径是安装位置、安装目录 Manifest 的 `0.1.0` 是本次实际选择结果；该确认结合上述亲手读取证据，不外推将来更新版本、资源加载或安全性。下一步进入项目普通 Package 的资源筛选实验，先只打开项目级 Config TUI 观察而不修改。
  - 6.4 guided 项目级 Config TUI 修改前基线已观察（2026-08-21）：学习者亲手打开真实 `pi config -l --approve`，截图显示顶部为 `Project Local Resources`，同名 Source 标记为 `(project)`，Extension、Skill、Prompt、Theme 四类项目资源均为非灰色 `[x]`；没有执行 `Space` 切换。该证据证明 Config TUI 当前把已安装的普通项目 Package 作为项目资源控制对象并显示四类启用基线，不证明 Settings 已发生修改或新 Pi 进程的最终资源发现；下一步只输入课程 Extension 名称缩小列表。
  - 6.4 普通项目 Package 优先级实验顺序已纠正（2026-08-21）：学习者指出仅观察 Config TUI 中的 `(project)` 与 `[x]` 不能证明用户级和项目级资源优先级。正确 A/B 应固定两级同名 Package 与用户级启用状态，先由新进程取得项目项也启用时的 Extension 基线，再只把项目 Extension 改为排除，由另一新进程观察是否不回退到用户级资源；当前界面尚未修改，先按 `Esc` 退出。
  - 6.4 guided 未修改的项目级 Config TUI 已正常退出（2026-08-21）：学习者报告已按 `Esc` 返回终端 B；此前未执行 `Space`，因此本步只关闭观察界面，没有用户证据表明资源状态发生变化。下一步由新 Pi 帮助进程取得两级 Extension 均启用时的 A 组 Flag 基线。
  - 6.4 guided 普通项目优先级 A 组基线已验证（2026-08-21）：学习者亲手启动离线、无 Session/Context/Skill/Prompt/Theme/Tool 的新 Pi `--help` 进程并精确筛选课程 Flag，真实输出 `--pi-study-guard` 及其描述。该结果证明用户级与项目级同名 Package 都保持 Extension 启用时，新进程可以发现并导入该 Extension；A 组单独不能确定由哪一级提供，也不能证明项目优先级。下一步只修改项目级 Extension 为排除，再由同条件新进程执行 B 组。
  - 6.4 guided 普通项目优先级 B 组目标已精确定位（2026-08-21）：学习者重新打开 `Project Local Resources`，在搜索框输入 `pi-study-guard` 后，截图只保留 `(project)` Package 下的 `Extensions > [x] pi-study-guard/index.ts`，选择箭头位于该 Extension；尚未切换状态。该证据仅证明即将修改的对象与作用域正确，下一步按一次 `Space` 制造唯一变量“项目级 Extension 排除”。
  - 6.4 guided 普通项目优先级 B 组项目排除已在 TUI 写入（2026-08-21）：学习者对精确选中的项目 Extension 按一次 `Space`，截图显示 `[-] pi-study-guard/index.ts project unload`，Package 仍标记 `(project)`。该证据证明 Config TUI 已把目标切换为项目级排除状态；它没有修改用户级 Package，也不表示项目 Package 已卸载，但尚未通过结构化 Settings 回读和新 Pi 进程证明规则已持久化并取得优先效果。下一步按 `Esc` 正常关闭后回读项目 Settings。
  - 6.4 guided B 组项目级 Config 已正常关闭（2026-08-21）：学习者截图显示 `Esc` 后已返回终端 B，关闭前目标保持 `[-] ... project unload`，没有第二次切换。该用户证据证明本次 Config 交互已结束，不单独证明磁盘 Settings 的精确结构或新进程效果；下一步只读解析项目 `.pi/settings.json` 的 `packages` 字段。
  - 6.4 guided B 组项目负向 Extension 规则已落盘（2026-08-21）：学习者用 Node 结构化读取当前项目 `.pi/settings.json` 的 `packages` 字段，真实输出仍保留 `source: npm:pi-study-workbench@^0.1.0`，并写入唯一 `extensions: ["-.pi/extensions/pi-study-guard/index.ts"]`。这证明项目普通 Package 没有卸载，Settings 已持久化对目标 Extension 的精确排除；它不单独证明新进程不会回退到用户级 Extension。下一步按 A 组同条件运行 B 组帮助进程并核对双退出码。
  - 6.4 guided 普通项目 Package 的 Extension 优先级 A/B 已验证（2026-08-21）：学习者在 A 组两级 Extension 均启用时由新 Pi 进程取得 `--pi-study-guard`；B 组只增加项目 Settings 的精确负向 Extension 规则，使用同一离线、无 Session/Context/Skill/Prompt/Theme/Tool 的新 Pi `--help` 进程后没有 Flag 输出，并取得 `pi_exit=0`、`rg_exit=1`。结合用户级与项目级同名 Source/安装均已验证且 Config 仅操作项目作用域，该 A/B 直接证明本机 Pi `0.84.2` 在本次普通同名 npm Package 场景中采用项目级 Extension 筛选且不回退加载用户级同名 Extension；不外推其他资源类型、`autoload:false` delta、其他版本、已运行进程热卸载或通用安全性。下一步用 `pi list` 再确认两级 Package 仍存在。
  - 6.4 guided B 组两级 Package 仍安装且项目项标记过滤（2026-08-21）：B 组 Flag 消失后，学习者亲手运行 `pi list --approve`，真实输出仍同时包含用户级与项目级 `npm:pi-study-workbench@^0.1.0` 及各自独立的受管理路径，项目行标记 `(filtered)`。该证据排除了“Extension 因 Package 被卸载而消失”的解释，并证明 `filtered` 表示 Package 仍在但资源集合受 Settings 约束；它不单独证明 Skill、Prompt 和 Theme 的新进程发现结果。下一步打开无 Model TUI，观察精确 Extension 排除是否保留其他三类资源。
  - 6.4 guided 精确项目 Extension 排除保留其他三类资源（2026-08-21）：学习者亲手启动离线、无 Session/Context/Tool 的新 Pi `0.84.2` TUI；启动页仍显示 Skill=`java-readonly-analysis`、Prompt=`/java-review`、Theme=`pi-study-lab`，但没有 `[Extensions]` 区块、没有 `pi-study-guard`，也没有其注册的 `Shell Gate` Widget。结合项目 Settings 的唯一负向 Extension 规则和两级 Package 仍安装证据，这证明本次新进程只从资源集合排除了目标 Extension，未连带禁用其他三类 Package 资源；不证明这些保留资源的全部行为、安全性、已运行进程热卸载或其他筛选语法。下一步从空输入框正常退出。
  - 6.4 guided 精确项目 Extension 排除观察进程已正常退出（2026-08-21）：学习者报告已从上述无 Session Pi TUI 按正常路径退出并返回终端 B；该证据只完成本轮观察进程收尾，不改变或删除用户级/项目级 Package、项目负向筛选记录和受管理安装目录。下一步进入普通项目 Package 的筛选语义对照，先验证 `extensions: []`。
  - 6.4 guided 普通项目项的 Extension 空数组已落盘（2026-08-21）：学习者在终端 B 先用环境变量与当前路径的相等检查锁定本轮隔离项目，再结构化修改项目 `.pi/settings.json`；真实输出保留唯一 Source `npm:pi-study-workbench@^0.1.0`，并把 `extensions` 写为 `[]`。这证明普通项目项已采用“该类型资源全部禁用”的空数组配置，不单独证明两级受管理安装目录仍存在、新 Pi 资源发现已经应用配置，或多 Extension Package 的逐项结果。下一步先运行 `pi list --approve` 排除卸载解释。
  - 6.4 guided Extension 空数组下两级 Package 仍安装（2026-08-21）：学习者亲手运行 `pi list --approve`，真实输出仍同时包含用户级与项目级 `npm:pi-study-workbench@^0.1.0` 及各自独立的受管理路径，项目项标记 `(filtered)`。该证据排除了“空数组通过卸载 Package 使 Extension 消失”的解释，并证明过滤配置与受管理安装内容是两层状态；它不单独证明新 Pi 进程已应用空数组或其他三类资源仍被发现。下一步启动新的无 Model TUI 观察资源集合。
  - 6.4 guided 普通项目项的 Extension 空数组运行时已验证（2026-08-21）：学习者亲手启动新的离线、无 Session/Context/Tool Pi `0.84.2` TUI；启动页保留 Skill=`java-readonly-analysis`、Prompt=`/java-review` 和 Theme=`pi-study-lab`，但没有 `[Extensions]` 区块、没有 `pi-study-guard`，也没有 `Shell Gate` Widget。结合已回读的 `extensions: []` 与 `pi list` 中仍存在的两级 Package/路径，这直接证明本次普通项目项空数组让该类型的当前全部候选 Extension 不参与新进程加载，同时不连带禁用另外三类资源；当前 Package 只有一个 Extension，因此不外推多 Extension Package 的逐项结果，也不证明卸载、磁盘删除或安全性。下一步正常退出观察进程后进入 glob 对照。
  - 6.4 guided Extension 空数组观察进程已正常退出（2026-08-21）：学习者报告已从上述无 Session Pi TUI 正常退出并返回终端 B；该证据只完成空数组运行时观察的收尾，不改变两级 Package、受管理安装内容或项目空数组配置。下一步进入普通 glob、普通排除、精确加回与精确排除对照。
  - 6.4 后续学习路线已按实用价值精简（2026-08-21）：学习者确认复杂筛选与低频机制以理论和流程图为主，同时要求理论内容完整落盘供后续复习。普通 glob、`!pattern`、`+path`、`-path` 与 `autoload:false` delta 不再逐组重复 TUI 动态实验，改用 07 正文的完整场景、规则表、流程图、当前版本静态证据和既有隔离自动证据收口；更新、两级移除、重装和最终清理仍由学习者亲手执行。该调整不是把未做实验标为完成，6.4 保持进行中。
  - 6.4 复杂筛选理论材料已补强、理解确认与最终渲染待完成（2026-08-21）：07 正文新增“优先使用最简单规则”的实用选型、三候选资源的 glob -> 普通排除 -> 精确加回 -> 最终精确排除示例、筛选决策图，以及普通项目项与 `autoload:false` delta 的空数组分流图；同时明确本课程单 Extension 动态结果不能外推多 Extension Package。`git diff --check`、20 个 Markdown 围栏、10 个 Mermaid 块、15 个标题和相对链接结构检查通过；本机与工作区依赖均没有可用 Mermaid CLI，离线 npm 缓存也未命中，因此新增两图尚未取得本地实际渲染证据，失败命令生成的精确 npm 调试日志已清理。下一步在对话中完成理论讲解与可视确认；自然确认后先移除项目级 A，验证普通项目项消失后回退到用户级 A，再进入发布 B 与用户级更新，最终文档验收时补实际渲染检查。
  - 6.4 项目级移除操作 Red、隔离环境待重建（2026-08-22）：学习者在仓库根运行 `pi remove -l --approve "$PI_STUDY_64_SOURCE"`，Pi 返回 `Missing remove source.` 和用法提示。只读核对确认原 guided 临时根与 `guided-env.sh` 均已不存在，因此当前 Shell 中该变量没有有效 Source，命令在执行移除前即被参数校验拒绝；没有证据表明用户级或项目级 Package、Settings 或受管理目录被删除。此前 guided 环境中的安装、筛选和优先级证据作为历史用户实验保留，但不能假定旧临时状态仍存在。下一步重新启动 guided 环境，只重建继续移除所需的最小状态；不得从真实仓库作用域硬编码 Source 继续操作。
  - 6.4 新 guided 隔离环境已恢复 READY（2026-08-22）：学习者在仓库根重新启动课程 `--guided` 运行器，真实输出 `network=loopback-only`、`host=127.0.0.1`、Pi `0.84.2`、A=`0.1.0`、B=`0.1.1`、范围 Source、全新的项目/Agent/Settings/受管理目录和 `guided_status=READY`；四项 baseline 与 `registry_requests`、`management_commands` 均为 `0`。随后只读核对确认新临时根、`guided-env.sh` 和 guided 进程仍存在。该证据只证明新的隔离实验底座已就绪，不证明 Package 已安装或旧状态已恢复；下一步在终端 B 加载新环境文件并核对三项关键路径。
  - 6.4 新 guided 终端 B 环境已核对（2026-08-22）：学习者在终端 B 加载新 `guided-env.sh`，真实输出的当前项目目录、`PI_CODING_AGENT_DIR` 与 `PI_STUDY_64_SOURCE` 分别指向同一新 guided 根的 `project`、`agent` 和 `npm:pi-study-workbench@^0.1.0`。该证据证明后续管理命令将落在新的隔离作用域，不证明任何 Package 已安装；下一步先重建用户级 A。此处及后续重建动作只恢复实验状态，不重复计入学习验收。
  - 6.4 新 guided 用户级 A 已重建（2026-08-22）：学习者在已核对的终端 B 运行不带 `-l` 的 `pi install`，真实输出范围 Source、npm `added 1 package` 和 `Installed npm:pi-study-workbench@^0.1.0`，命令正常返回 Shell。该结果证明本轮隔离用户级安装调用成功；作为状态重建不重复记入学习验收，也不以绿色 Source 行替代实际版本或资源发现证据。下一步只增加 `-l` 重建项目级 A。
  - 6.4 新 guided 项目级 A 已重建（2026-08-22）：学习者在同一终端 B 运行只增加 `-l` 的 `pi install`，真实输出范围 Source、npm `added 1 package` 和 `Installed npm:pi-study-workbench@^0.1.0`，命令正常返回 Shell。该结果证明本轮隔离项目级安装调用成功；作为状态重建不重复记入学习验收，且尚未恢复项目 Extension 空数组或核对两级 `pi list`。下一步结构化恢复项目项的 `extensions: []`。
  - 6.4 恢复项目空数组首轮脚本 Red、原因已定位（2026-08-22）：学习者运行带路径门禁的结构化修改命令，门禁通过后脚本报 `未找到隔离项目 Package` 并在写入前退出。只读核对显示新用户级与项目级 Settings 的 `packages` 均为字符串数组 `["npm:pi-study-workbench@^0.1.0"]`，两个受管理安装目录都存在且 guided 进程仍在；原脚本只按 `item.source` 查找对象条目，因此无法匹配字符串条目。该 Red 没有修改 Settings，也不否定两级安装；下一步用同时识别字符串与对象形式的结构化命令，把匹配的项目字符串条目转换为 `{ source, extensions: [] }`。
  - 6.4 项目 Extension 空数组已在新环境恢复（2026-08-22）：学习者运行兼容字符串/对象条目的结构化命令，路径门禁与 Source 匹配均通过，真实输出显示项目 `packages` 已从 Source 字符串简写转换为 `{ "source": "npm:pi-study-workbench@^0.1.0", "extensions": [] }`，命令正常返回 Shell。该直接证据证明项目 Settings 写入成功；它不单独证明 `pi list` 已标记 `(filtered)`、新进程已应用空数组、Package 安装目录仍存在或用户级回退行为。字符串简写、对象形式及下一次解析边界已补入 07 正文并配流程图。下一步运行 `pi list --approve` 核对两级 Package 状态。
  - 6.4 新环境两级 Package/list 状态已核对（2026-08-22）：学习者亲手运行 `pi list --approve`，真实输出同时列出用户级与项目级范围 Source 及两个独立受管理路径，项目项标记 `(filtered)`。这与项目 Settings 的 `extensions: []` 组合证明两级 Package 仍登记且安装路径可解析，项目 Package 具有资源筛选配置；`pi list` 本身不会展示具体筛选字段，也不能证明新 Pi 进程最终加载了哪些资源。学习者指出单看列表无法看出 Extension 结果，证据边界判断正确。下一步用最小新进程 Flag 观察取得项目移除前的“不加载”基线。
  - 6.4 普通项目项优先的机制与设计边界已自然确认（2026-08-22）：针对“为什么项目级优先”，已结合 Pi `0.84.2` 随包文档和 `package-manager.js` 说明当前实现先按 npm 包名、标准化 Git 仓库或本地绝对路径判定身份，再让普通同身份项目项在资源解析前替代用户项；因此两级配置/安装记录可并存，但同一逻辑 Package 不会双重加载。项目作用域更具体、便于团队一致性并避免重复注册属于合理设计推断，不冒充官方动机或安全保证；`autoload: false` 是需要用户结果加减语义时的显式例外。稳定说明和完整流程图已补入 07 正文，学习者已明确确认理解；不外推为运行时移除证据，6.4 仍进行中。
  - 6.4 新环境项目移除前运行时基线已通过（2026-08-22）：学习者在同一隔离项目与 Agent 目录中亲手启动新的离线、无 Session/Context/Skill/Prompt/Theme/Tool 的 Pi `--help` 进程，并只检索课程 Extension 注册的 `--pi-study-guard` Flag；实际没有匹配行，随后取得 `pi_exit=0`、`rg_exit=1`。这直接证明 Pi 进程正常结束且当前项目普通 Package 的 `extensions: []` 已使该 Extension 不参与本次新进程加载；结合 Settings 与 list 证据，形成项目移除前基线。它不证明 Package 记录或受管理目录已经删除，也不证明已运行进程热卸载、其他资源类型或通用安全性。下一步只移除项目级 Package，再验证用户级 A 是否成为有效来源。
  - 6.4 新环境项目级 `remove` 动作已成功（2026-08-22）：学习者在终端 B 先通过非空 Source 与当前项目 Settings 路径门禁，再亲手运行带 `-l` 的 `pi remove`；Pi 真实输出范围 Source、npm `removed 1 package` 和 `Removed npm:pi-study-workbench@^0.1.0`，随后正常返回当前隔离项目 Shell。该证据证明项目级移除调用已被 Pi 与 npm 正常执行，不等于已经独立证明项目 Settings 条目、项目受管理目录和新进程资源发现的最终状态，也不影响用户级 A 的结论需后续核对。下一步先运行 `pi list --approve` 检查两级登记结果。
  - 6.4 项目级移除后的 list 状态已核对（2026-08-22）：学习者亲手运行 `pi list --approve`，真实输出只保留用户级 `npm:pi-study-workbench@^0.1.0` 及其隔离 Agent 受管理路径，不再出现 `Project packages` 区块、项目路径或 `(filtered)` 标记。该证据证明 Pi 的当前登记视图中用户级 A 仍存在且项目级项已消失，排除了“无 `-l` 误删用户项”的解释；它不单独证明项目 `.pi/settings.json` 的精确内容、项目受管理目录是否已删除或新进程是否已回退加载用户级 Extension。下一步结构化读取项目 Settings。
  - 6.4 项目级移除后的 Settings 状态已核对（2026-08-22）：学习者用 Node 结构化读取本轮隔离项目的 `.pi/settings.json`，真实输出 `packages: []`、`sourcePresent: false`。该证据证明项目配置层已经不再记录目标范围 Source，与 list 中项目项消失相互印证；它不证明项目受管理安装目录已经物理删除，也不证明用户级目录或新进程资源回退。下一步分别检查项目与用户受管理目录的存在性。
  - 6.4 项目级移除后的受管理目录状态已核对（2026-08-22）：学习者对本轮环境文件提供的两个精确安装路径执行存在性检查，真实输出 `project_install_exists=false`、`user_install_exists=true`；随后从用户受管理目录读取 Manifest，得到 `pi-study-workbench@0.1.0`。该证据证明项目受管理 Package 副本已从其精确路径消失，而用户级 A 的物理副本仍存在；它不证明共享 npm cache 已清空、安装目录之外的副作用已恢复，或新 Pi 进程已经回退加载用户级资源。下一步运行移除后的最小 Flag 观察。
  - 6.4 项目级移除后的用户级 A 运行时回退已验证（2026-08-22）：学习者在同一项目、Agent、Pi `0.84.2`、离线和最小 `--help` 参数下启动全新进程，真实输出重新出现 `--pi-study-guard`，并取得 `pi_exit=0`、`rg_exit=0`；与移除前项目 `extensions: []` 生效时的 `pi_exit=0`、`rg_exit=1` 形成单变量对照。结合项目 list/Settings/受管理目录均消失且用户级 A `0.1.0` 仍存在的证据，这直接证明本次普通同身份 npm Package 场景在项目项移除后回退加载用户级 Extension。该结论不外推其他资源类型、其他版本、共享缓存清理、已运行进程热切换或通用安全性。项目级移除闭环完成，下一步让回环 Registry 提供 B `0.1.1`，进入范围更新。
  - 6.4 回环 Registry 已增加 B、用户更新尚未执行（2026-08-22）：学习者切到保持运行的终端 A，只输入 guided 控制命令 `publish-b`，运行器真实输出 `guided_registry=B` 并继续等待。该动作只使隔离 `127.0.0.1` Registry 提供课程 B `0.1.1`，没有调用 `pi update`，也不自动改写 Settings 或用户受管理安装目录；当前用户安装版本仍需在终端 B 重新核对。下一步对照范围 Source、已安装版本和 Registry 可用版本。
  - 6.4 用户级范围更新前基线已核对（2026-08-22）：学习者回到终端 B，结构化读取用户 Settings 与受管理安装 Manifest，并查询当前隔离 Registry；真实输出 `settingsPackages: ["npm:pi-study-workbench@^0.1.0"]`、`installedVersion: "0.1.0"`，Registry 版本为 `0.1.0`、`0.1.1`。这直接区分了“Source 允许 B”“Registry 已提供 B”和“磁盘仍安装 A”三种状态，证明 `publish-b` 本身没有自动更新。该证据不证明 `pi update` 一定成功，也不外推外部 Registry、并发发布或其他 semver 范围。下一步执行用户级范围更新。
  - 6.4 用户级范围更新动作已成功（2026-08-22）：学习者在终端 B 通过当前隔离项目路径门禁后亲手运行 `pi update --extensions --approve`，Pi 真实输出正在更新 `npm:pi-study-workbench@^0.1.0`、npm `changed 1 package` 和 `Updated packages`，随后正常返回 Shell。该证据证明 Package 更新命令已执行且 npm 报告内容发生变化；它不单独证明用户 Settings 的 Source 未被改写、磁盘实际版本已是 `0.1.1`、新进程加载了 B，或外部 Registry 更新行为。下一步结构化读取 Settings 与安装 Manifest。
  - 6.4 用户级范围更新后的 Settings/Manifest 已核对（2026-08-22）：学习者在同一隔离终端 B 结构化读取用户 Settings 与精确受管理安装目录的 `package.json`；真实输出 `settingsPackages: ["npm:pi-study-workbench@^0.1.0"]`、`installedName: "pi-study-workbench"`、`installedVersion: "0.1.1"`。结合更新前同一 Source 下的 `0.1.0` 基线和 `pi update --extensions` 动作收据，这直接证明本次范围 Source 保持不变，而用户级受管理副本已重新解析并落盘为范围内的 B `0.1.1`；它不单独证明新 Pi 进程已经加载 B 的资源，也不外推外部 Registry、其他范围或传递依赖。下一步启动新的隔离 Pi 进程，核对更新后的资源发现与初始化。
  - 6.4 用户级范围更新后的新进程资源发现已验证（2026-08-22）：学习者亲手启动新的隔离 Pi `0.84.2` TUI，启动页真实显示 Skill=`java-readonly-analysis`、Prompt=`/java-review`、Extension=`pi-study-workbench@^0.1.0::.pi/extensions/pi-study-guard`、Theme=`pi-study-lab`，并出现 `Shell Gate | enforce` Widget。该界面中的 `@^0.1.0` 是 Settings Source 范围，不是实际安装版本；结合前一步精确受管理 Manifest 的 `installedVersion: "0.1.1"`、项目级 Package 已移除以及全新进程证据，形成“配置规则仍为范围 -> 磁盘已更新到 B -> 新进程发现四类资源并初始化 Extension”的三层闭环。截图单独不能证明具体版本为 `0.1.1`，也不证明已经运行进程热更新、真实 Provider、外部 Registry、资源全部业务行为或通用安全性；离线 `fd` 与无 Model 警告符合实验设置。
  - 6.4 更新后观察进程已退出（2026-08-22）：学习者明确报告当前 Pi 观察进程“已退出”。这关闭了本次 TUI 观察动作，允许继续操作同一隔离环境；本轮没有单独采集进程列表或退出码，因此该记录不声称 `exit=0`，也不证明退出会撤销已经加载的 Extension、删除 Package、清理缓存或恢复安装副作用。下一步移除用户级 Package，并分别核对配置、磁盘和新进程运行时。
  - 6.4 用户级移除动作已成功（2026-08-22）：学习者在终端 B 通过 Source 非空、用户 Settings 非空、当前项目路径和隔离 `PI_CODING_AGENT_DIR` 四项门禁后，亲手执行不带 `-l` 的 `pi remove`；Pi 真实输出 `Removing npm:pi-study-workbench@^0.1.0`、npm `removed 1 package` 和 `Removed npm:pi-study-workbench@^0.1.0`，随后返回隔离项目 Shell。这里显示的 `@^0.1.0` 是登记使用的范围 Source，不表示移除前磁盘仍是 A；前一步 Manifest 已直接证明当时安装的是 B `0.1.1`。本条只证明用户级移除命令已执行且 npm 报告移除一个受管理 Package，不能单独证明用户 Settings 已无 Source、用户受管理目录已消失、新 Pi 进程不再发现资源、共享缓存已删除或 Package 外副作用已恢复。下一步运行 `pi list --approve` 核对两级登记视图。
  - 6.4 用户级移除后的 list 状态已核对（2026-08-22）：学习者亲手运行 `pi list --approve`，真实输出 `No packages installed.`，不再出现 `User packages`、`Project packages`、受管理路径或 `(filtered)` 标记。结合此前项目级项已移除，可证明 Pi 当前有效登记视图中用户级与项目级 Package 均已消失；它不单独证明用户 Settings JSON 已无目标 Source、用户受管理安装目录已物理删除、新 Pi 进程不再发现资源、共享 npm cache 已清空或 Package 外副作用已恢复。下一步结构化读取隔离用户 Settings。
  - 6.4 用户级移除后的 Settings 状态已核对（2026-08-22）：学习者在终端 B 先通过 Source、用户 Settings 和隔离 `PI_CODING_AGENT_DIR` 三项门禁，再以结构化投影读取用户 Settings；真实输出 `packageCount=0`、`sourcePresent=false`。这直接证明隔离用户 Settings 的 `packages` 集合为空，目标 Source 已从配置层删除；结合上一条 `No packages installed.`，配置文件与 Pi 有效登记视图相互印证。它不证明用户受管理安装目录已物理删除、新 Pi 进程不再发现 Package 资源、共享 npm cache 已清空或 Package 外副作用已恢复。下一步检查精确用户受管理安装目录。
  - 6.4 用户级移除后的受管理目录状态已核对（2026-08-22）：学习者在终端 B 先通过用户安装路径非空及其等于 `$PI_CODING_AGENT_DIR/npm/node_modules/pi-study-workbench` 的精确门禁，再只读检查目录存在性；真实输出 `user_install_exists=false`。这直接证明本轮隔离用户级 Package 的精确受管理安装目录已不存在；结合用户 Settings 空列表与 `pi list` 无 Package，配置层和磁盘层均已闭环。它不证明共享 npm cache 已清空、Package 外副作用已恢复，也不能替代新 Pi 进程的资源发现验证。下一步启动新的隔离 Pi 进程核对四类 Package 资源均不再发现。
  - 6.4 用户级移除后的新进程资源发现已验证（2026-08-22）：学习者在同一隔离 Agent 和项目中亲手启动新的 Pi `0.84.2` TUI；启动页不再出现 Package 提供的 `[Skills]`、`[Prompts]`、`[Extensions]`、`[Themes]` 区块，也没有 `java-readonly-analysis`、`/java-review`、`pi-study-guard` 或 `pi-study-lab`。结合用户 Settings 空集合、`pi list` 无 Package 和精确受管理目录不存在，已完成“配置 -> 磁盘 -> 新进程发现”三层闭环。截图中的离线 `fd` 与无 Model 警告符合实验设置，与 Package 移除结论无关；该证据不证明共享 npm cache 已清空、Package 外副作用已恢复、其他 Pi 版本行为或通用卸载安全性。6.4 核心学习已完成，最终清理证据见后续检查点。
  - 6.4 用户级移除后的观察进程已退出（2026-08-22）：学习者截图显示上述“无四类 Package 资源”的 Pi TUI 已关闭并返回隔离项目 Shell。该证据关闭本次新进程观察动作；本轮没有单独采集进程退出码或进程列表，因此不声称 `exit=0`，也不证明系统中不存在其他 Pi 进程。该时点的下一步为回到终端 A 输入 `quit`；完成结果见下一检查点。
  - 6.4 guided 最终清理与验收完成（2026-08-22）：学习者在保持运行的终端 A 输入 `quit`，运行器真实输出 `guided_stop_reason=quit`、`final_registry_requests=8`、`guided_registry=B`、`guided_status=STOPPED`、`temp_residuals=0`。这直接证明 guided 运行器按约定停止，且其追踪的隔离临时根清理为零；`final_registry_requests=8` 与 `guided_registry=B` 仅记录课程回环 Registry 的最终请求数和版本集合状态，不代表访问外部 Registry。结合此前安装、两级作用域、筛选、范围更新、两级移除及配置、磁盘、新进程三层证据，6.4 验收完成。该结论仍不证明共享 npm cache 已清空、Package 外副作用已恢复、其他 Pi 版本行为或通用卸载安全性。
  - 6.4 最终文档渲染闭环（2026-08-22）：使用本机 Mermaid CLI `11.12.0` 对 07 正文当前全部 `11` 个 Mermaid 块逐图执行真实渲染，取得 `11/11` 个非空 SVG；这关闭了此前复杂筛选两图只有结构检查、没有实际渲染证据的缺口。该结果只证明当前流程图可由本机 Mermaid 渲染器生成，不证明图中每项行为均有动态实验覆盖；事实依据仍分别受静态、自动和学习者手工证据边界约束。
  - 核心内容：掌握 `install/list/config/update/remove`、全局/项目作用域、临时 `-e`、资源级启停，以及省略、空数组、glob、排除和精确包含/排除的筛选语义。
  - 贯穿实验：只在隔离目录管理 6.3 自建 Package；安装、单资源禁用、空数组、A/B 范围更新以及项目/用户两级移除均已亲手完成，复杂筛选与项目 delta 采用理论/静态/既有自动证据；分别观察 Settings、安装目录和新进程资源发现，最终已清理隔离临时环境，没有重复重装。
  - 验收与边界：复杂筛选和 delta 需在 07 正文形成完整规则、示例和流程图并取得自然确认；更新和移除保留结构化配置变化、实际资源清单与新进程证据，隔离运行器清理后不得残留临时根。“列表不存在”“资源未加载”“缓存已删除”是三个不同结论；本课程未验证共享缓存删除或 Package 外副作用恢复。
- [x] 6.5 区分内置 Provider、Custom Model 和 Custom Provider。
  - 进行中（2026-08-22）：学习者已明确确认开始学习；已从唯一计划恢复到 6.5，并确认本机 Pi CLI 与随包 npm 版本均为 `0.84.2`。当前版本随包 `providers.md`、`models.md`、`custom-provider.md` 及 Provider 组合、Model Runtime、Extension 公共类型的必要源码已只读核对：内置目录、`models.json` 声明层和 Extension 注册层最终进入同一 Provider/Model 组合与请求路由，但能力与实现门槛不同。下一步完成三个场景的系统地图讲解；尚未取得稳定正文、流程图或学习者理解验收，不勾选完成。
  - 理解缺口（2026-08-22）：学习者反馈首次“场景一二三”讲解干涩，缺少能代入的完整大白话例子和流程图；该轮不记为理解验收。下一步从真实开发需求的起点、变化、Pi 处理过程和终点重新讲解，并逐图映射准确术语与边界。
  - 完成与验收（2026-08-22）：已用同一份 Java 代码审查任务完整重讲三个场景：目录内换 Model 直接使用内置 Provider，兼容受支持 API 的新地址用 `models.json` 声明 Custom Model，现有机制无法表达的认证、动态目录或协议逻辑才使用 Custom Provider Extension；学习者在完整大白话讲解和流程图后以“继续”自然确认进入下一项。07 正文已形成三个起点到终点的场景、决策表和四张新增 Mermaid 图；本机 Mermaid CLI `11.12.0` 对当前全部 `15/15` 图真实渲染成功，本地链接和 `git diff --check` 通过。独立只读验收为 `PASS WITH NOTES`；唯一备注指出企业登录、Token 刷新和动态目录不是固定启动顺序，正文已改为并行入口及请求时过期判断并再次完成 `15/15` 渲染。结论只覆盖 Pi `0.84.2` 静态文档/源码与学习选型，不证明真实 Provider、认证、请求、计费或生产可用性。
  - 核心内容：内置 Provider 提供既有协议适配和认证约定；Custom Model 通过 `models.json` 复用受支持 API；Custom Provider 通过 Extension 注册，用于自定义认证、动态 Model 目录或非标准流协议。
  - 贯穿教学：从“换模型”“兼容 API 换地址”“非标准协议/企业 OAuth”三个真实场景建立对照，结合当前版本文档和必要源码定位配置入口、协议适配者与认证来源；不为目录展示单独搭建三套动态环境。
  - 验收与边界：07 正文形成“需求 -> 内置 Provider / Custom Model / Custom Provider”决策表和流程图，学习者自然确认能按场景选择入口。Custom Model 不是训练模型；配置或出现在列表中不证明请求成功；本项不冒充真实 Provider、认证、计费或生产可用性证据，SDK/RPC 留在阶段 7。
- [x] 6.6 理解认证解析顺序、环境变量、认证文件、命令取值和 OAuth 的边界。
  - 进行中（2026-08-22）：已开启“公司 AI 大门查工作证”贯穿场景，并只读核对 Pi `0.84.2` 随包认证文档及实际请求源码。发现需明确教学的版本差异：`providers.md` 写为环境变量先于 `models.json` Custom Provider Key；当前 `provider-composer` 实际在没有存储凭据时先解析配置的 `apiKey`，仅在未配置时才调用内置 Provider 的环境/ambient 认证。下一步把文档口径、源码口径、失败不回退、取值语法、Header 和 OAuth 刷新分别画成稳定流程；尚未取得学习者理解验收，不勾选完成。全程不读取现有认证文件内容、不执行 `!command`、不打开登录流程、不交换 Token、不调用 Provider。
  - 草稿独立审查与纠偏（2026-08-22）：首轮 6.6 正文独立只读审查为 `FAIL`，阻塞原因为把 `models.json` 的命令边界泛化到 `auth.json`：认证存储命令可能在模型可用性刷新读取凭据时执行并按进程缓存；存储 API Key 解析为空时，带内置认证器的 Provider 还可能继续查 ambient 环境。已据源码把来源图补为成功/停止分支，把命令图拆成认证存储与 Provider 声明两条生命周期，并补充显式请求 `Authorization` Header 覆盖自动生成值。下一步完成重新渲染和独立复验后再向学习者展示；6.6 仍为进行中。
  - 草稿复验通过、待学习确认（2026-08-22）：纠偏后的独立只读复验为 `PASS WITH NOTES`，确认认证来源及 ambient 例外、两类 `!command`、Header 和 OAuth 主链准确；两处非阻塞措辞已进一步收紧为“到期检查、必要刷新”和“锁内复查后是否仍即将到期”，并在最终主审中同步交换后者的 `是/否` 分支，保证“仍临期才刷新”。07 当前全部 Mermaid `18/18` 真实渲染成功，本地链接、Markdown 围栏和 `git diff --check` 通过。稳定正文和流程图已具备，但尚未取得本轮讲解后的学习者自然理解确认，因此 6.6 保持进行中。
  - 完成与验收（2026-08-22）：学习者阅读“公司 AI 大门查工作证”稳定讲义后以“继续”自然确认进入下一项。当前结论能区分运行时临时 Key、认证存储、Extension/`models.json` Provider Key、内置 ambient 认证，两类环境变量和两类 `!command` 生命周期，以及 API Key、Header 与 OAuth 登录/临期刷新边界；保留 Pi `0.84.2` 文档与请求源码最后两层顺序不一致这一版本事实。结合纠偏后的独立 `PASS WITH NOTES`、主审分支修正和全部 Mermaid `18/18` 实渲染，6.6 验收完成。证据不外推真实凭据、命令执行、OAuth 登录、Provider 接受或模型响应。
  - 核心内容：通过当前文档与源码确认 CLI、认证存储、环境变量和 Provider 配置的实际解析顺序；区分 `$ENV`、字面值、`!command`、自定义 Header、API Key 与 OAuth 登录/刷新/取值链。
  - 贯穿教学：使用一个“同一 Provider 同时存在环境变量、认证存储和命令取值候选”的脱敏场景，沿当前版本文档与必要源码画出解析顺序、失败分支和 OAuth 刷新边界；只使用占位符，不创建认证文件、不运行取值命令、不打开浏览器、不交换 Token。
  - 验收与边界：07 正文形成认证来源决策表和解析流程图，学习者自然确认能区分来源、优先级与失败边界。不得读取现有 `auth.json`；`!command` 是本地代码执行入口；静态证据不证明真实凭据有效、OAuth 登录成功、协议兼容或服务授权。
- [x] 6.7 理解如何使用受支持 API 配置 Custom Model，以及配置到请求的完整链路。
  - 进行中（2026-08-22）：学习者已明确继续，6.7 从一个脱敏的 OpenAI-compatible 公司网关场景开始。下一步只读核对 Pi `0.84.2` 随包 `models.md`、配置 Schema、Provider 组合与请求路由源码，形成字段职责表、配置到请求流程图和错误定位顺序；不创建或读取用户 `models.json`，不读取凭据、不启动 Mock、不发送网络请求、不调用 Provider。
  - 稳定草稿待验收（2026-08-22）：07 已补“公司网关说明书”贯穿场景、Provider/Model 字段职责、默认值与能力声明边界、配置发现图、协议请求图和错误定位图；并记录 Model 级 `baseUrl`/`headers`、Custom Model 与 `modelOverrides` 顺序、Header 独立合并、`/model` 先旧快照后后台刷新、CLI 全目录/临时 Model fallback、`maxTokens` 上下文夹紧等当前源码细节。全文 Mermaid `21/21` 实渲染、Markdown 围栏和 `git diff --check` 已通过，独立只读验收进行中；尚未取得验收结论和学习者自然理解确认，不勾选完成。
  - 独立验收通过、待学习确认（2026-08-22）：独立只读验收为 `PASS`，字段职责与默认值、配置加载和组合回退、文档/源码覆盖差异、容量与 CLI 边界、请求与流事件、错误定位及证据范围均无阻塞问题；唯一事件措辞备注已修正为“文本/推理/Tool Call 更新，最终消息携带 Usage 和终态”。全文 Mermaid `21/21` 实渲染、Markdown 围栏和 `git diff --check` 已通过。稳定正文已具备，但尚未取得本轮讲解后的学习者自然理解确认，因此 6.7 保持进行中。
  - 两类 Provider 流程图更新、待验证（2026-08-23）：应学习者要求，07 场景二、三的旧简图已分别替换为“声明式 Provider 配置”和“Custom Provider Extension”完整流程；Custom Provider 图明确区分复用内置 API 适配器与实现非标准流处理，不把自定义认证或动态目录等同于必然自写协议。下一步执行全文 Mermaid 实渲染、链接和差异检查；验证前 6.7 保持进行中。
  - 两图首轮语义复核未通过（2026-08-23）：格式检查与全文 Mermaid `21/21` 实渲染已通过，但独立复核指出声明式图把可选认证/Header 画成必填，Custom Provider 图又让非标准流处理绕过用户选择和请求阶段。已将前者改为“按需”，并把后者拆成启动期注册 `streamSimple` 与请求期调用两个节点；下一步重新实渲染并独立复验，6.7 继续保持进行中。
  - 两类 Provider 流程图最终验证（2026-08-23）：声明式图已明确认证/Header 为按需项；Custom Provider 图已把特殊认证、动态目录和自定义 `streamSimple` 拆为独立能力，并明确受支持协议复用内置适配器、非标准协议才在请求期调用自定义流处理。全文 Mermaid `21/21` 再次实渲染，链接、围栏和 `git diff --check` 通过；独立复核最终为 `PASS`，确认没有固定登录/刷新时序误导。该文档增强不替代学习者自然理解确认，6.7 保持进行中。
  - 完成与验收（2026-08-23）：学习者在稳定讲义后连续追问 Custom Model/Provider 分工、声明式 Provider 含义，并要求把声明式与 Custom Provider 两条完整流程落入正文；随后主动询问下一步学习内容，构成自然理解确认。结合字段/默认值、配置发现、请求适配、错误分层的独立 `PASS`，两图纠偏后的最终独立 `PASS`，以及全文 Mermaid `21/21` 实渲染、链接、围栏和差异检查，6.7 验收完成。结论仍不证明用户配置可用、真实认证、端点连通、协议兼容、图片/推理能力、费用或生产稳定性。
  - 核心内容：学习 Provider/Model 字段、API 类型、Model ID、输入类型、Context Window、最大输出、费用元数据、Thinking 映射和兼容开关；明确序列化和流解析仍由 Pi 内置协议适配器负责。
  - 贯穿教学：以一个假设的 localhost OpenAI-compatible 服务为完整场景，阅读脱敏 `models.json` 示例并追踪“配置发现 -> 选择 `provider/model-id` -> 内置适配器组装请求 -> 流事件 -> 成功/错误终态”；不启动 Mock、不发请求。
  - 验收与边界：07 正文形成字段职责表、配置到请求的流程图和常见错误定位顺序，学习者自然确认 Custom Model 复用的是受支持协议适配器。静态示例不证明配置可解析、请求成功、真实认证、计费、限流、图片、质量或商业可用性。
- [x] 6.8 阅读 Custom Provider 公共接口，理解生命周期、实现门槛与测试边界。
  - 进行中（2026-08-23）：学习者已明确开始。当前范围固定为 Pi `0.84.2` 静态导读：核对 `registerProvider()`/注销、静态与动态 Model 目录、API Key/OAuth、复用内置适配器与 `streamSimple`、统一流事件、Usage/Cost、Tool Call、停止原因、Context Overflow、AbortSignal 和错误终态，再形成接口职责表、生命周期图、实现门槛与测试清单。全程不实现 Mock、不读取凭据、不执行认证命令、不联网或调用 Provider。
  - 稳定草稿待验证（2026-08-23）：07 已补“公司私有 AI 总机”场景、两种注册形态、注册/注销生命周期、动态目录、自定义流合同、实现门槛和测试清单，并记录显式注销不删除凭据、`/reload` 不自动清空 Provider 注册 Map、`onPayload`/`onResponse` 硬要求等当前源码边界。下一步执行全文 Mermaid 实渲染、链接、围栏和差异检查，再进行独立只读语义验收；6.8 保持进行中。
  - 格式验证通过、待独立验收（2026-08-23）：07 当前全部 Mermaid `24/24` 实渲染成功，本地链接、Markdown 围栏和 `git diff --check` 通过。下一步独立复核完整/兼容注册形态、注销与 Reload 残留、动态目录失败保留、自定义流事件/取消终态、Usage/Cost、Tool Call 和四类 Context 恢复判断；未取得语义验收和学习者确认前不勾选。
  - 首轮独立语义验收未通过（2026-08-23）：独立复核指出目录图把认证门槛错误放在无网缓存恢复之前，并把 `lazyStream` 的异常兜底范围写得过宽。已改为“先 `allowNetwork=false` 恢复 stored 目录、再解析联网 Credential、最后单独做 auth/filter”，并区分 Setup/迭代器错误与脱离 EventStream 的异步生产者错误；下一步重新实渲染和独立复验，6.8 保持进行中。
  - 独立验收通过、待学习确认（2026-08-23）：修订后的独立只读验收为 `PASS`，确认两种注册形态、初始队列/即时生效、Provider 合成、注销/Reload/陈旧 Model 边界、离线目录恢复与联网刷新、认证过滤、自定义流钩子/取消/唯一终态、Usage/Cost、Tool Call、四类 Context 恢复信号、实现门槛与测试清单均与 Pi `0.84.2` 静态源码一致。全文 Mermaid `24/24` 实渲染，链接、围栏和差异检查通过；尚未取得学习者自然理解确认，6.8 不勾选完成。
  - 理解缺口（2026-08-23）：学习者明确反馈当前 6.8 整块看不懂；此前把注册、目录、认证、自定义流、取消和注销同时展开，信息跨度过大，该轮不记为理解验收。下一步先只讲一个“Pi 打给公司私有 AI，由翻译员接线”的完整故事，将 Custom Provider 收敛为“列出可用 Model、把请求翻成公司格式、把回复翻回 Pi 格式”三项职责；待这条主线理解后，再逐层加入注册/注销和动态目录，最后才讲流终态与恢复细节。
  - 第一层理解确认、文档待验证（2026-08-23）：学习者确认“在非标准协议的典型场景里，Custom Provider 是可执行翻译员”可以理解，并要求写入正文后继续；该确认只覆盖主线比喻，不表示每个 Custom Provider 都必须自写流。07 已把故事移到 6.8 第一屏，并补“为什么必须先注册”的报到/路由/撤下流程；下一步实渲染并复核准确分叉。6.8 仍不勾选完成。
  - 新手桥接首轮复核未通过（2026-08-23）：新增两图虽通过全文 Mermaid `26/26`、链接、围栏和差异检查，但独立复核指出“翻译员”比喻把双向协议转换泛化为所有 Custom Provider。已改为三种可能职责：特殊认证、动态目录、非标准协议翻译；并在两图中明确受支持协议复用内置 Adapter，只有非标准协议才调用 `streamSimple`。下一步重新渲染并复验。
  - 新手桥接最终验证（2026-08-23）：修订后全文 Mermaid `26/26` 实渲染、链接、围栏和差异检查通过，独立复核最终为 `PASS`。正文已明确：协议、登录和目录均可声明时用 `models.json`；Custom Provider 可只定制认证或目录；非标准协议才自写 `streamSimple`；注册、用户选择、请求路由和注销是不同阶段。下一步继续讲 `registerProvider` 报到与路由这一层，6.8 保持进行中。
  - 第二层理解确认、目录桥接待验证（2026-08-23）：学习者明确确认 `registerProvider` 报到、用户选择后路由、标准协议走内置 Adapter、非标准协议走 `streamSimple` 的流程可以理解，并要求继续。07 新手入口已追加 Model 目录层：静态名单、启动发现一次、运行中刷新、失败保留旧目录，以及认证后按员工 Credential 过滤；下一步实渲染新增图并独立复核，再讲解该层。6.8 保持进行中。
  - 目录桥接最终验证、待学习确认（2026-08-23）：新增目录图已通过全文 Mermaid `27/27` 实渲染、链接/围栏/差异检查和独立 `PASS`。流程准确区分静态名单、async factory 启动发现一次、运行期先离线恢复再按凭据联网刷新、失败保留旧目录、认证门槛，以及可选 `filterModels`；尚未取得学习者对该层的自然确认，6.8 保持进行中。
  - 第三层理解确认、认证权限说明待验证（2026-08-23）：学习者在目录层追问“当前用户的认证和权限”，经员工证/门禁场景解释后明确确认理解并要求写入文档。07 已补当前身份由 Provider Credential 表示、个人 OAuth/团队 Key/ambient 角色差异、后台授权与可选 `filterModels` 两种路径，以及“配置存在 -> 凭据解析 -> 身份接受 -> Model 授权”四层证据；下一步完成静态检查和独立复核，再进入自定义流层。6.8 保持进行中。
  - 认证权限说明最终验证（2026-08-23）：新增说明通过链接/差异检查和独立 `PASS`，确认当前身份不是 OS 用户、不同 Provider 可代表不同身份、目录后台裁剪与可选 `filterModels` 均可承载授权、ambient 认证下传入过滤器的存储 Credential 可能为空，并准确拆分四层证据。该层已取得学习者自然确认，下一步进入 `streamSimple` 流事件主线；6.8 保持进行中。
  - 第四层开始、流式桥接待验证（2026-08-23）：学习者明确继续。07 新手入口已追加“检查 OrderService”单一场景，追踪公司私有文本与 `read` Tool 参数片段如何翻成 start、text/toolcall 增量、唯一 done(toolUse)，再由 Agent 校验执行 Tool、写入 ToolResult 并进入下一轮；本层暂不展开错误、取消、Usage 或 Overflow。下一步实渲染新增图并独立复核，6.8 保持进行中。
  - 流式桥接最终验证、待学习确认（2026-08-23）：新增段落通过全文 Mermaid `28/28` 实渲染、链接/差异检查和独立 `PASS`。事件顺序准确覆盖 start、文本与 Tool Call 的 start/delta/end、唯一 done(toolUse)、Agent 后续 Tool 校验执行、ToolResult 和下一轮；也明确 length 截断的 Tool Call 不执行。尚未取得学习者对该层的自然确认，6.8 保持进行中。
  - 第四层理解确认、错误取消桥接待验证（2026-08-23）：学习者确认流式 Tool Call 主线可以理解，并准确区分 Custom Provider/`streamSimple` 是 Adapter、AssistantMessageEvent 是转换过程、AssistantMessage 是 Pi 公共统一 DTO，随后要求写入文档并继续。07 已补三者职责表，以及正常 done、故障 error、用户取消 aborted 的唯一终态流程；下一步实渲染新增图并独立复核。6.8 保持进行中。
  - 错误取消桥接最终验证、待学习确认（2026-08-23）：新增 DTO/Adapter 对照与终态流程通过全文 Mermaid `29/29` 实渲染、链接/差异检查和独立 `PASS`。正文准确覆盖 done 的 `stop|length|toolUse|deferred`、error 的 `error|aborted`、reason/stopReason 一致、AbortSignal 停止底层 I/O、异步生产者自行 catch/error/end、Setup 在 start 前失败以及 result 到 message_end；尚未取得学习者对本层自然确认，6.8 保持进行中。
  - 第五层开始、Usage/Cost 桥接待验证（2026-08-23）：学习者明确理解错误/取消终态并继续。07 新手入口已追加“用量小票”场景：区分 input/output/cacheRead/cacheWrite、reasoning 为 output 子集、totalTokens，沿上游用量 -> AssistantMessage.usage -> Model 费率 -> calculateCost -> 本地估算追踪，并明确零费率不代表免费、估算不等于真实账单。下一步实渲染新增图并独立复核，6.8 保持进行中。
  - Usage/Cost 首轮独立复核未通过（2026-08-23）：主线和 `$0.0037` 示例正确，但字段表遗漏 `cacheWrite1h`，分层费率与一小时缓存写入算法也不够精确。已补其为 cacheWrite 子集且不重复计入 totalTokens，并写明总输入严格 `>` 阈值、取最高匹配层，普通/一小时缓存写入分别按 cacheWrite 费率和两倍 input 费率计算；下一步重新复验，6.8 保持进行中。
  - Usage/Cost 桥接最终验证、待学习确认（2026-08-23）：新增小票说明通过全文 Mermaid `30/30` 实渲染、链接/差异检查和独立 `PASS`。字段、两个子集、`$0.0037` 示例、calculateCost、严格阈值与最高匹配层、一小时缓存写入、零费率和真实账单边界均准确；尚未取得学习者自然确认，6.8 保持进行中。
  - 第五层理解确认（2026-08-23）：学习者在用量小票、`$0.0037` 示例、reasoning/cacheWrite1h 子集和“Cost 不等于账单”讲解后明确表示可以并要求继续，构成 Usage/Cost 层自然确认；该确认不扩大为真实 Provider 用量或账单准确性证据。
  - 第六层开始、Overflow 桥接待验证（2026-08-23）：学习者确认 Usage/Cost 小票主线并继续。07 新手入口已追加“一万张纸办公桌”场景，区分当前同一 Provider/Model 下 error 文案、成功 stop 超窗、length 零输出填满 99%、length 提前截断四类信号，以及成功 stop 只压缩、失败/截断移除 Live Message 后最多重试一次、aborted 不恢复和限流不误判。下一步实渲染并独立复核，6.8 保持进行中。
  - Overflow 首轮独立复核未通过（2026-08-23）：主流程正确，但错误文案信号漏写 NON_OVERFLOW_PATTERNS 的优先否决，99% 信号也未明确包含 cacheRead。已补“命中 Overflow 且不命中 rate limit 等已知非 Overflow 模式”，并写成 `input + cacheRead >= contextWindow * 0.99`；下一步重新复验，6.8 保持进行中。
  - Overflow 桥接最终验证、待学习确认（2026-08-23）：新增办公桌说明通过全文 Mermaid `31/31` 实渲染、链接/差异检查和独立 `PASS`。四类信号、NON_OVERFLOW 否决、同 Provider/Model 门槛、aborted 跳过、成功 stop 只压缩、错误/length 移除 Live Message 后最多重试一次、范围化规范错误和容量/Usage 影响均准确；尚未取得学习者自然确认，6.8 保持进行中。
  - 第六层理解确认（2026-08-24）：学习者连续追问自动压缩时机、固定 reserve 与请求时 maxTokens 夹紧、失败压缩重试边界，并在完整决策图重排后确认可以、主动询问下一步，构成 Overflow 层自然确认。刚重排的精简总图目前只在对话中，07 已有等价详细图；6.8 仍需最后的注销/Reload/当前 Model 边界和测试门槛。
  - 最后一层开始、清理生命周期桥接待验证（2026-08-24）：学习者同意继续并要求先用完整大白话场景。07 已追加“周五把 corp-ai Extension v1 升级到 v2”主线，区分 unregisterProvider、/logout、/reload、当前 Model 重选和新进程五份状态，覆盖旧 Runner 显式注销、Credential 保留/永久下线、陈旧 Model 和分层验收。下一步实渲染新增图并独立复核，再讲解该层和最终测试门槛；6.8 保持进行中。
  - 清理生命周期首轮独立复核未通过（2026-08-24）：升级主线正确，但永久下线只写 `/logout` 与远端撤销，漏掉 `models.json`、Extension/Settings、环境变量/云角色等可让新进程再次注册或认证的来源；ProviderConfig 重复注册也需明确顶层浅合并。已补当前 Provider 再注销、本地 Credential、声明/ambient 来源和远端 Token 的分层清理，并写明非 `undefined` 顶层字段覆盖、未提供字段保留、已提供的嵌套对象/数组整体替换而不递归合并；下一步重新复验。
  - 清理生命周期最终验证、待学习确认（2026-08-24）：新增升级/永久下线场景通过全文 Mermaid `32/32` 实渲染、链接/差异检查和独立 `PASS`。正文准确区分 unregister、logout、reload、Model 重选、新进程，覆盖显式注销、凭据保留、声明/ambient/远端来源清理、同 ID 刷新/缺 ID 陈旧和 ProviderConfig 顶层浅合并；尚未取得学习者自然确认和最终测试门槛回顾，6.8 保持进行中。
  - Logout 入口纠正（2026-08-24）：进一步核对真实 TUI 源码确认 Pi `0.84.2` 只接受单独的 `/logout`，随后通过选择器选择存储凭据；此前正文中的 `/logout corp-ai` 已纠正。该动作只删除 `/login` 保存的 Credential，不改变环境变量或 `models.json` 配置 Key，因此仍不能单独证明 Provider 不可用或永久下线。
  - 第七层理解确认（2026-08-24）：学习者在周五升级场景后继续追问保留/清理的选择标准与具体顺序，理解同身份升级保留 Credential、换账号只 logout/login、暂时停用撤路由但可留 Credential、永久下线分层移除注册/声明/ambient/本地/远端来源，并主动询问下一步。该确认关闭生命周期教学层，不替代真实清理实验。
  - 最终测试门槛引导开始（2026-08-24）：学习者明确要求由教练带着逐步学习，再彻底结束阶段 6；不接受只给清单后自行消化。后续固定“团队声称 corp-ai v2 已可交付”场景，按静态接口、Fake/Mock、本地真实 Pi、新进程和真实 Provider 证据逐卡判断，每卡只确认其最大证明范围；全部理解、正文收口和独立复核前不完成 6.8 或阶段 6。
  - 最终门槛理解缺口（2026-08-24）：学习者反馈第一张“静态证据”验收卡仍看不懂，说明从证据等级术语切入依然过于抽象；该轮不记为理解验收。下一步只做“看文件”和“启动 Pi”两个动作对照：看到源码中的 `registerProvider` 只能证明字写在文件里，只有实际启动并在 `/model` 看到 Provider 才证明加载结果；暂不引入 Fake、真实 Provider或更多等级。
  - 第一张验收卡理解确认（2026-08-24）：改用“排班表写 Alice 上班 vs Alice 真实到公司”及“文件里写了 vs 启动 Pi 后 `/model` 真实出现”场景后，学习者明确确认理解。当前只闭合静态源码与运行观察分界：源码存在不证明执行，真实目录观察才证明本次加载/注册；不扩大为请求或 Provider 可用性。
  - 第二张验收卡理解确认（2026-08-24）：通过“同事 Bob 假装公司 AI 与翻译员彩排”场景，学习者确认 Fake/Mock 能证明 `streamSimple` 等逻辑在受控输入下真实执行并产生预期事件，但不能证明 Pi Loader、真实 SSO、网络或公司 Provider。下一张进入本地真实 Pi 运行观察。
  - 第三张验收卡理解确认（2026-08-24）：通过“翻译员真实刷门禁进入公司并坐到总机工位”场景，学习者确认本地真实 Pi 中 Extension 启动、无注册诊断、`/model` 出现和同进程注销恢复能证明 Loader/factory/ModelRuntime 本次集成，但未发请求时不能证明 `streamSimple`、真实认证、网络或公司服务。下一张进入全新进程证据。
  - 第四张验收卡理解缺口（2026-08-24）：学习者反馈“全新 Pi 进程”仍不理解；此前把进程、Runtime、磁盘来源和远端状态同时展开过多，该轮不记为理解验收。下一步只用“办公室白板内存”场景解释：删除员工档案不等于擦掉当前白板，`/reload` 仍在同一办公室复用白板，彻底退出 Pi 才销毁白板；新进程只证明旧白板已消失，不证明磁盘档案或远端账号已删除。
  - 四张验收卡文档化待验证（2026-08-24）：学习者询问先更新还是继续，决定先沉淀再学习。07 最小测试清单前已追加排班表、Bob 彩排、本地真实 Pi、全新进程四卡对照和证据阶梯图，并保留第五张真实 Provider 预告；第四张仍为理解待确认，文档存在不替代学习验收。下一步实渲染、静态检查和独立复核后，继续白板场景。
  - 四张验收卡文档最终验证（2026-08-24）：07 当前全文 Mermaid `33/33` 实渲染，`14/14` 本地链接目标、Markdown 围栏和 `git diff --check` 通过；独立只读复核为 `PASS`，确认四层证据逐层增加但不能互相替代，`/model` 出现未外推为认证或请求成功，新进程也未外推为 Credential、环境、ambient 身份或远端 Token 已清理。第四张仍待学习者自然确认；下一步只确认“旧内存白板已销毁”与“磁盘/远端材料仍可能存在”的区别。
  - 第四张卡延伸讲解进行中（2026-08-24）：学习者继续追问“完全清理怎样做”，当前缺口从新进程内存边界推进到永久下线的实际顺序。下一步只用员工离职场景串联撤当前注册、移除磁盘声明、清理本地与 ambient 认证、远端撤销、全新进程复验五步；本轮追问暂不替代学习者最终确认，也不执行任何真实凭据、认证或 Provider 操作。
  - 第四张验收卡理解确认（2026-08-24）：经“当前进程白板 -> 永久下线五步链”讲解后，学习者明确确认理解。该确认覆盖：新进程只能排除旧内存，完整本机下线还需撤注册、声明来源、本地与 ambient 认证，远端授权必须另行撤销并取得独立证据；不扩大为任何真实清理已执行。下一步进入第五张真实 Provider 证据卡，只讲一次受控真实调用最多能证明什么，不读取凭据、不发请求或产生费用。
  - 第五张验收卡理解确认、阶段收口待验证（2026-08-24）：学习者明确确认理解“单次真实 Provider 成功最多证明该时间、账号、Model 和输入下的认证、网络、协议与响应链成功，不能外推账单、稳定性、安全或生产可用性”。下一步把第五张卡补入 07，重新完成全文 Mermaid、链接、围栏、差异检查及独立语义复核；这些证据通过前不勾选 6.8 或阶段 6，全程不读取凭据、不发真实请求或产生费用。
  - 完成与验收（2026-08-24）：学习者已逐层确认 Custom Provider 主线、注册与路由、目录与身份权限、流事件与统一消息、错误/取消、Usage/Cost、Overflow、注销/Reload/永久下线，以及五张证据卡。07 已形成稳定正文和五卡竖向证据图，全文 Mermaid `33/33` 实渲染、`14/14` 本地链接目标、Markdown 围栏和 `git diff --check` 通过；独立最终复核为 `PASS`，无阻塞或剩余备注。全部结论限定于 Pi `0.84.2` 静态课程与既有受控证据；没有读取凭据、执行认证命令、联网、发送真实请求、调用 Provider 或产生 Provider 费用。6.8 与阶段 6 完成，里程碑 B 达成。
  - 核心内容：理解 Provider 注册与注销、注册时机、Model 目录、认证解析、流式事件、消息与 Usage/Cost、Tool Call、停止原因、Context Overflow、AbortSignal 和错误终态，并判断何时不应自定义 Provider。
  - 贯穿教学：复用阶段 5 的 Extension 心智模型，阅读当前版本公共接口和必要定点源码，沿“注册 -> 选择 Model -> 请求 -> 流事件 -> 成功/错误/取消终态 -> 注销恢复”追踪一条静态调用链；不实现完整 Mock Provider，不提前完成阶段 7/8。
  - 验收与边界：07 正文形成公共接口职责表、生命周期流程图、实现门槛与测试清单，学习者自然确认 Custom Model 足够与必须 Custom Provider 的分界。静态证据和流程图不能证明真实 API、OAuth、Token 计量、跨 Provider 兼容或某个实现正确。

验收产物：可安装、可筛选、可更新、可卸载的本地 `pi-study-workbench` Package；Package 安全审查表；内置 Provider、Custom Model、Custom Provider 对比报告与决策流程图；Custom Model 配置、请求与错误定位导读；Custom Provider 公共接口、生命周期和测试边界导读。

阶段门禁：6.1-6.8 均有当前版本依据和用户理解验收；高频 Package 链路取得受控动态实验，6.5-6.8 取得当前版本静态证据、稳定正文、完整场景和可复习流程图。Package 管理在隔离配置中验证安装、筛选、范围更新、两级移除和最终清理，发布前演练和可发布制品检查通过；Custom Model 与 Custom Provider 只做概念、配置和公共接口导读，不声称运行时可用；不读取真实凭据，不运行认证命令，不调用真实 Provider，不安装第三方 Package，不执行外部发布。

里程碑 B：完成阶段 4-6 后，达到“Pi 定制开发者”。

### 阶段 7：SDK、RPC、JSON 与 TUI

目标：将 Pi 作为组件接入外部程序，并正确处理协议、事件和生命周期。

- [x] 7.1 使用 SDK 创建 Agent Session，加载资源、选择模型并订阅事件。
  - 进行中（2026-08-24）：学习者已确认开始阶段 7，并要求所有新 TypeScript 概念先用 Java 例子对照；尚未编写 SDK 代码、运行实验或调用真实 Model。
  - 理解缺口（2026-08-24）：首轮“Java 代码审查平台”只是虚构例子，却引入了未解释的平台对象，不记为系统地图理解验收。下一步改用“你写的普通程序把一个任务交给 Pi”这一最小场景，先只解释用户、外部程序和 Pi，再逐步引入 SDK 与 Agent Session。
  - 第一层理解确认（2026-08-24）：学习者已将“程序引入 Pi SDK 后调用 Pi 能力”映射为 Spring 项目引用 Maven 依赖。该类比只覆盖依赖引入与 API 调用入口；Pi SDK 实际是 npm/TypeScript 依赖，不能由 Java 项目直接作为 Maven 依赖导入，且依赖存在不证明 Session、Model、认证或请求链可用。下一步解释依赖加载后为什么还要创建有状态的 `AgentSession`。
  - 事件订阅理解缺口（2026-08-24）：学习者尚不理解 `subscribe()` 的用途，暂不继续堆叠 Session 组成对象。下一步只用 Java Listener 对照解释“`prompt()` 等待整次 Run，`subscribe()` 实时接收中间事件”，再区分文字增量、Tool 状态和生命周期通知。
  - 事件订阅用途理解确认与正文验证（2026-08-24）：学习者已理解 `subscribe()` 是当前进程内的 Java Listener/Observer 对照，用于实时接收文字、Tool 与生命周期事件；它不启动任务，`prompt()` 才负责提交并等待整次 Run。稳定说明已写入 `docs/learning/08-sdk-rpc-json-tui.md` 并加入学习索引；Markdown 围栏、本地链接、`git diff --check` 和新增 Mermaid `1/1` 实渲染通过。该证据不扩大为具体事件顺序、SDK 运行或真实 Model 调用。
  - `AgentSession` 理解确认（2026-08-24）：学习者在“npm 依赖只是工具箱，`AgentSession` 是独立、有记忆、可持续交互的工作上下文”讲解后确认继续。该确认覆盖同一 Session 的多轮上下文和不同 Session 的状态隔离，不表示已会创建、配置、持久化或运行 Session。
  - `ResourceLoader` 理解缺口（2026-08-24）：学习者主动询问 `ResourceLoader` 是什么。下一步只解释它按 `cwd`/`agentDir` 发现并装配 Pi 资源、不会自动读取普通源码，以及默认加载器与自定义加载器的边界；暂不展开 Model 选择。
  - `ResourceLoader` 理解确认与正文验证（2026-08-24）：学习者已理解 `ResourceLoader` 是为 `AgentSession` 发现并装配 Pi 专用资源的扫描器，不会自动读取整个源码目录，也不负责 Model、Session 持久化或 Prompt 执行。`AgentSession` 与 `ResourceLoader` 的稳定说明已补入 `docs/learning/08-sdk-rpc-json-tui.md`；Markdown 围栏、本地链接、`git diff --check` 和全文 Mermaid `2/2` 实渲染通过。该证据不扩大为 SDK 运行、资源实际发现或真实 Model 调用。
  - `ModelRuntime` 理解确认与完整图验证（2026-08-25）：学习者已理解 `ModelRuntime` 是 Model 目录、认证入口和请求路由器，Model 只是描述对象，并主动要求形成完整流程图。08 正文已补选模顺序、证据边界和“资源 -> 选模 -> Session -> 订阅 -> Agent Loop -> 收尾”总图；当前版本语义复核、Markdown 围栏、本地链接、`git diff --check` 和全文 Mermaid `3/3` 实渲染通过，新增总图经图片检查无孤立节点、文字重叠或不可读分支。该结果只证明 Pi `0.84.2` 静态课程图与当前文档一致，不证明 SDK、认证、Tool 或真实 Model 已运行；7.1 仍未完成。
  - 7.1 系统地图理解确认（2026-08-25）：学习者在对话中查看内嵌完整流程图后明确确认“非常清晰，理解了”。该确认闭合 SDK 依赖、资源准备、选模、Session 组装、事件订阅、Agent Loop、Tool 回环与收尾的第一遍系统地图；尚未覆盖精确事件顺序、TypeScript SDK 代码、受控实验或真实 Model，因此 7.1 保持进行中。
  - 事件顺序层进行中（2026-08-25）：已按 Pi `0.84.2` 随包 SDK 文档、`pi-agent-core` 事件类型与 Agent Loop、`AgentSession` 包装层只读核对 Core/Session 两层事件。下一步用一次“Model 请求 `read` -> Tool Result -> Model 最终回答”的完整场景讲清 Agent、Turn、Message、Tool 事件及 `agent_end -> Session 后处理 -> agent_settled`；取得学习确认前不写入稳定正文或标记完成。
  - Message 生命周期理解缺口（2026-08-25）：学习者明确反馈用户 `message_start/end` 与 Assistant `message_start/update/end` 这一段看不懂，上一轮完整事件图不记为事件顺序理解验收。下一步只解释两条消息：用户输入提交前已完整，所以没有增量；Assistant 由 Provider 流式生成，同一个 Message 会经历零到多次 `message_update` 后定稿。确认前不展开 Tool Result、Turn 或 Session 终态。
  - Assistant 标记理解缺口（2026-08-25）：学习者追问“Assistant 消息 A1”是什么。`A1`/`U1` 只是本轮课程自造的“第 1 条 Assistant/User 消息”编号，不是 Pi 类型、字段、Message ID 或事件名；该表达已撤回。下一步只用正式术语解释 `AssistantMessage` 是 Model 生成、由 Pi 统一表示的一条 Assistant 角色消息，不等于 Agent、Session 或整个回答过程。
  - `AssistantMessage` 理解确认与正文验证（2026-08-25）：学习者已理解它是 Model 一方生成的一条正式消息，既可能包含 Tool Call，也可能包含最终文字，不等于 Agent、Session 或整个 Run。稳定定义已补入 08 正文并移除课程自造编号；Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果不扩大为 Message 生命周期或运行证据，下一步继续解释 `message_start/update/end`。
  - Message 生命周期理解确认与正文验证（2026-08-25）：学习者已理解完整 `UserMessage` 只需 `message_start/end`，流式 `AssistantMessage` 在两者之间会有零到多次 `message_update`，且这些事件描述同一条 Message。稳定说明已补入 08 正文；Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果不扩大为 Tool、Turn 或 Session 终态理解，下一步进入 Tool 执行事件与 `ToolResultMessage`。
  - Tool 事件层进行中（2026-08-25）：已只读核对 Pi `0.84.2` Agent Loop 的 Tool 查找、Schema 校验、前门禁、Executor、更新、后处理、`tool_execution_end` 与 `ToolResultMessage` 顺序。下一步用单个 `read` 场景解释 start/update/end 和结果消息；特别保留 `tool_execution_start` 早于校验与门禁、不能证明 Executor 已运行的边界。学习者确认前不写入稳定正文。
  - `tool_execution_update` 理解缺口（2026-08-25）：学习者尚不理解该事件。下一步改用当前源码中的 `read`/`bash` 对照：`read` 不调用更新回调，通常没有 update；`bash` 在长命令输出变化时主动上报当前阶段性快照，Pi 才转发 `tool_execution_update`。该事件证明 Executor 已调用更新回调，但不证明最终成功，也不进入 Model Context；确认前不继续 ToolResult 层。
  - Tool 事件理解确认与正文验证（2026-08-25）：学习者已理解 `tool_execution_update` 是 Executor 主动上报的可选阶段性结果，并确认继续学习；结合前一轮讲解，本层稳定范围包括 start 早于校验/门禁、update 零到多次、end 形成最终结果，以及 `ToolResultMessage` 才进入下一轮 Model Context。08 正文已补 Tool 事件表、`read`/`bash` 对照和证据边界；Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果不扩大为 Turn 或 Session 终态理解，下一步进入 `turn_start/end`。
  - Turn 层进行中（2026-08-25）：下一步以同一 Agent Run 内的单个 `read` 场景说明：第一 Turn 包含 Model 生成 Tool Call 及 Tool 批次，`turn_end` 后第二 Turn 才把 `ToolResultMessage` 交给 Model 并形成最终回答。当前只讲一次 Model 响应及其 Tool 批次的边界，学习者确认前不写入稳定正文或进入 `agent_end/agent_settled`。
  - Turn 理解确认与正文验证（2026-08-25）：学习者已理解一次 Turn 是一次 Model 响应及其全部 Tool 批次，并要求更新文档后继续。08 正文已补“第一 Turn 生成 Tool Result、第二 Turn 消费结果并形成最终回答”的单 `read` 流程、Java 循环对照及边界；语义复核、Markdown 围栏、本地链接、`git diff --check` 和全文 Mermaid `4/4` 实渲染通过，新增 Turn 图经图片检查无重叠或不可读节点。该结果不扩大为 Agent/Session 终态理解。
  - Agent 终态层进行中（2026-08-25）：已按 Pi `0.84.2` Core/Session 源码确认 `agent_end` 后 `AgentSession` 仍检查自动重试、Compaction/Overflow 恢复及队列 continuation，可能再次产生 `agent_start -> ... -> agent_end`；全部后处理不再续跑后才发 `agent_settled`。下一步用正常结束与自动续跑两条路径讲清该边界，学习者确认前不写入稳定正文。
  - 事件总图重讲进行中（2026-08-25）：学习者反馈 Message、Tool、Turn 与 Agent/Session 终态概念分散，要求合并为完整流程图。当前图限定为单 `read` 正常主线加 Session 可选自动续跑，按“第一 Turn -> 第二 Turn -> Session 后处理”分组；先在对话内渲染并取得理解确认，再决定是否替换或补充 08 正文，避免未验收总图进入稳定文档。
  - 事件总图草稿验证、待学习确认（2026-08-25）：首轮本机 Mermaid 因 subgraph 内冗余 `direction TD` 解析失败，移除后可渲染；第一次图片检查又发现自动续跑回线经过 `prompt()` 完成节点，虽语义无直接连边但视觉易误读。第二版将循环折叠为“重复 `agent_end` 与后处理，直到无需续跑”，重新渲染成功并经图片检查无交叉歧义、重叠或孤立节点。当前只在对话展示，学习者确认前不写入 08 正文或验收 Agent 终态层。
  - 事件总图学习确认与正文验证（2026-08-25）：学习者查看第二版内嵌图后确认可以，并主动要求更新文档。08 正文已新增 `事件完整流程`，保留 Message、Tool、两 Turn、`agent_end -> Session 后处理 -> agent_settled -> prompt()` 主线，并修正原“事件顺序后续展开”的失效说明；语义复核、Markdown 围栏、本地链接、页内锚点、`git diff --check` 和全文 Mermaid `5/5` 实渲染通过，总图经图片检查无交叉歧义、重叠或孤立节点。该结果只证明 Pi `0.84.2` 静态课程图准确，不证明 SDK 或真实 Model 已运行。
  - Agent 终态理解确认（2026-08-25）：学习者通过事件总图自然确认 `agent_end` 只关闭当前 Core Agent Loop，Session 仍可能因重试、Compaction/Overflow 恢复或队列消息自动续跑；不再续跑后才发 `agent_settled`，随后 `prompt()` 完成。该确认不表示任务业务成功、回答正确或已经掌握 SDK 实现，7.1 继续保持进行中。
  - 真实 Model 与费用授权（2026-08-25）：学习者明确允许 7.1 Demo 直接调用当前已配置的真实 Model，并表示无需考虑学习调用费用。该授权免除本课程后续学习调用的逐次费用确认，但不允许读取、输出或记录凭据内容，也不扩大到生产变更、外部发布、Git 提交/推送或第三方 Package 安装；单次真实响应最多证明该时间、账号、Model 和输入下的认证、网络、协议与响应链成功。创建或修改 TypeScript Demo 仍需学习者按仓库铁律单独确认“开始写代码”。
  - 代码授权与实现开始（2026-08-25）：学习者已明确“开始写代码”。实现范围固定为新建 `labs/7.1-sdk/`：使用 Pi SDK `0.84.2`、内存 Session、单一 `read` Tool、禁用项目 Extension 执行但保留受控资源发现，记录脱敏事件并读取专用 fixture；不修改既有 Extension/Package，不读取凭据内容。先完成类型检查和确定性事件投影测试，全部通过后才调用已授权的真实 Model。
  - Demo 实现与自动验证完成（2026-08-25）：已新增独立 `labs/7.1-sdk/` package、严格 TypeScript 配置、真实 SDK 入口、脱敏事件投影/契约检查、3 个单元测试、固定 marker fixture 和实验 README；依赖锁定 `@earendil-works/pi-coding-agent@0.84.2`。`npm install` 成功且审计为 `0 vulnerabilities`，`npm run check` 通过 TypeScript 与 `3/3` 测试。该 Green 只证明实现可编译、投影不扩散消息正文和契约检查函数有效，尚未证明资源实际发现、认证、Provider、Tool Loop 或真实事件顺序；下一步执行一次已授权真实 smoke。
  - 真实 SDK/Model smoke 通过、待最终文档与学习验收（2026-08-25）：仅执行一次 `npm run demo`，进程正常退出且未重试。`DefaultResourceLoader` 真实发现项目 `AGENTS.md`，Context/Skill/Prompt/Theme 数量为 `1/6/1/1`、诊断 `0`、项目 Extension 保持禁用；显式选择 `openai/gpt-5.6-sol`、`openai-responses`、Thinking `off`。事件序列共 `42` 条：单次 `read` 在 22/23 发出 start/end 且 `isError=false`，第一 `turn_end` 为 26、第二 `turn_start/end` 为 27/40，`agent_end(willRetry=false)` 为 41，`agent_settled` 为 42；最终文字严格为 `SDK_REAL_MODEL_MARKER_7101`，消息数 `4`、契约偏差为空。该证据只证明该时间、账号、Model 和固定输入下的资源发现、认证、网络、协议、单次 Tool Loop、事件订阅与响应链成功，不证明费用、质量、长期稳定、其他资源/Tool 或生产可用性。
  - 7.1 工程最终复核通过、待学习者代码验收（2026-08-25）：08 正文和学习索引已加入 7.1 Lab、自动/真实证据与边界；Lab 严格 TypeScript 和 `3/3` 测试再次通过，既有全仓严格 TypeScript 和 `185/185` 测试通过，lockfile `3` 精确锁定 SDK `0.84.2`，npm audit 为 `0 vulnerabilities`，Markdown 围栏/本地链接/页内锚点、全文 Mermaid `5/5` 实渲染、tracked/untracked whitespace、凭据样式扫描和 `git diff --check` 均通过。最终差异检查首轮发现新文件尾空白，清理后复验通过；锁文件首轮验证命令因 Lab cwd 下误用仓库相对路径而失败，改用 `package-lock.json` 后通过。未重复调用真实 Model。当前工程与一次 smoke 为 `PASS`，但学习者尚未逐段理解 TypeScript 实现和契约测试，因此 7.1 不勾选完成。
  - Demo 代码教学进行中（2026-08-25）：学习者明确要求教练带着学习已实现 Demo。当前教学顺序固定为“文件职责地图 -> `sdk-demo.ts` 入口与 TypeScript 基础 -> ResourceLoader/ModelRuntime/AgentSession 组装 -> subscribe/prompt/finally -> 脱敏事件投影与契约测试 -> 真实 42 事件证据”，每层使用 Java 对照并在理解确认后回写；当前先讲文件地图和入口 1-21 行，不因工程 Green 提前验收 7.1。
  - Demo 代码学习深度确认（2026-08-25）：学习者表示“大概了解怎么写即可”。后续不要求从空白手写、记忆全部 TypeScript 语法或逐行复述；验收深度调整为能读懂 SDK 主调用链、说明 ResourceLoader/ModelRuntime/AgentSession/subscribe/prompt/finally 的职责、修改 Model/Tool/fixture 等关键参数，并亲自运行一次 Demo 判断资源、Tool、Turn、终态和 marker 是否通过。事件投影内部实现只需理解脱敏目的和契约检查作用。
  - 学习者真实运行验收通过（2026-08-25）：学习者亲自在终端运行 `npm run demo` 并回传完整脱敏输出，进程正常结束。结果再次为项目 `AGENTS.md` 已发现、资源数量 `1/6/1/1`、诊断 `0`，显式 Model/协议为 `openai/gpt-5.6-sol`/`openai-responses`，单次 `read` 在 22/23 start/end 且 `isError=false`，两个 Turn 在 2-26 与 27-40，`agent_end(willRetry=false)` 为 41、`agent_settled` 为 42，最终 marker 正确、Message 数 `4`、事件数 `42`、契约偏差为空。聊天中的反斜杠只是 Markdown 对下划线的转义，不属于终端原始事件名。该证据证明学习者能启动 Demo、识别主链通过条件并取得一次独立真实复现，不扩大为质量、费用、长期稳定或生产证据。
  - 7.1 完成与验收（2026-08-25）：系统地图、Java/TypeScript 对照、ResourceLoader/ModelRuntime/AgentSession、Message/Tool/Turn/Agent-Session 事件、完整流程图、最小 SDK Demo、严格类型与契约测试、工程真实 smoke、稳定正文和学习者亲自复现均已闭环。最终工程门禁为 Lab `3/3`、既有全仓 `185/185`、SDK `0.84.2`、npm audit `0 vulnerabilities`、全文 Mermaid `5/5` 及链接/锚点/差异/凭据扫描通过。学习者不要求从空白默写 TypeScript，但已达到会读主链、会改关键参数、会运行判断的本项深度；7.1 勾选完成。证据仍不证明持久化 Session、重试/取消/队列/Compaction 动态行为、其他 Tool/Model、账单、质量、长期稳定或生产可用性，后续分别进入 7.2 及更高阶段。
- [x] 7.2 限制可用工具，处理取消、重试、队列、Compaction 和 Session 持久化。
  - 进行中（2026-08-25）：7.1 已验收后进入 7.2 系统地图。当前按 Pi `0.84.2` SDK/Settings 文档核对六个控制面：`tools/noTools/excludeTools` 约束 Model 可见 Tool，`abort()` 协作取消当前运行，Agent 与 Provider retry 位于不同层，`steer/followUp` 决定流式期间新消息何时注入，Compaction 处理 Model Context 容量，`SessionManager` 处理会话树与磁盘恢复。下一步用同一个长任务场景串联用途和边界；尚未新增代码、运行实验或取得学习确认。
  - 系统地图理解确认与正文验证（2026-08-25）：学习者已理解长任务中的 Tool 限制、取消、Agent/Provider 两层重试、Steering/Follow-up、Compaction 和 Session 持久化分别保护不同层，并主动要求更新文档。08 正文已新增 7.2 六控制面流程图、Java 对照和关键边界；语义复核、Markdown 围栏、本地链接、`git diff --check` 和全文 Mermaid `6/6` 实渲染通过。图片复核发现“程序重启后恢复 Session”应为不会自动运行的终点，改为终点形状后复验通过。该结果只证明 Pi `0.84.2` 静态系统地图，不扩大为任一机制的动态证据；下一步进入 Tool 限制层。
  - Tool 限制层进行中（2026-08-25）：Pi `0.84.2` 源码确认：未传 `tools`/`noTools` 时使用 Settings `defaultTools` 或内置默认 `read,bash,edit,write`，并默认加入 Extension/Custom Tool；`noTools="builtin"` 清空默认内置集合但保留 Extension/Custom Tool，`noTools="all"` 将允许集合固定为空；显式 `tools` 覆盖 `noTools` 的默认抑制并形成全类型 allowlist；`excludeTools` 最后过滤内置、Extension 和 Custom Tool，未知名称忽略。下一步用集合运算和真实场景讲清组合结果及非沙箱边界；尚无动态实验或学习确认。
  - Tool 限制理解确认与正文验证（2026-08-26）：学习者已理解 `tools` 显式 allowlist、`excludeTools` 最终 denylist、`noTools` 默认抑制模式及非沙箱边界，并确认继续。08 正文已补 Tool 集合计算、组合表、动态激活 Registry 边界和实际集合检查入口；语义复核、Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果只证明 Pi `0.84.2` 静态组合语义，尚无 Tool 组合动态实验；下一步进入取消层。
  - 取消层进行中（2026-08-26）：Pi `0.84.2` 源码确认 `session.abort()` 先取消等待中的 Agent Retry，再触发当前 Agent Run 的 AbortController，并等待 Session 回到 idle；Provider stream、Tool Executor、Extension Handler 和 Custom Tool 需协作响应 `AbortSignal`。内置 `read` 监听信号并拒绝，`bash` 尝试终止进程树；`abort()` 不取消独立 Compaction/Branch Summary，分别需 `abortCompaction()`/`abortBranchSummary()`。下一步用长 `bash` 场景讲清信号传播、事件终态、非立即保证和非回滚边界；尚无动态实验或学习确认。
  - 取消理解确认与正文验证（2026-08-26）：学习者已理解 `session.abort()` 触发当前 Agent Run 的协作取消并等待 idle、底层需响应 `AbortSignal`、取消不回滚已发生副作用，以及 Compaction/Branch Summary 使用独立取消入口，并确认继续。08 正文已补取消调用链、组件职责、事件/等待边界和非回滚清单；语义复核、Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果只证明 Pi `0.84.2` 静态取消边界，尚无动态取消实验；下一步进入重试层。
  - 重试层进行中（2026-08-26）：Pi `0.84.2` 源码/Settings 确认 Provider Retry 在单次 `ModelRuntime.streamSimple()` 请求内部，默认 `maxRetries=0`；Agent Retry 在 Pi 收到过载、限流、服务端等可重试 Assistant 错误后运行，默认最多 3 次并按 2s/4s/8s 指数退避，产生 `auto_retry_start/end`，Context Overflow 则交给 Compaction。两层同时开启时单个持续失败的 Model 调用可能放大实际 Provider 请求次数；下一步用 Java HTTP Client/Service Retry 对照讲清事件可见性、次数和取消边界，尚无动态实验或学习确认。
  - 重试理解确认与正文验证（2026-08-27）：学习者已理解 Provider Retry 位于一次 Model 调用内部、Agent Retry 位于 Pi 观察到最终失败之后，以及两层叠加可能放大请求次数，并确认继续。08 正文已补两层配置/事件/退避对照、`(provider+1)*(agent+1)` 条件化上限、错误分类和取消边界；语义复核、Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果只证明 Pi `0.84.2` 静态重试语义，尚无动态重试实验；下一步进入消息队列层。
  - 消息队列层进行中（2026-08-27）：Pi `0.84.2` 源码确认 Steering 在当前 Assistant Turn 的全部 Tool Call 完成后、下一次 Model 请求前排空；Follow-up 只在 Tool 与 Steering 都耗尽、Agent 原本准备结束时排空。两类队列默认 `one-at-a-time`，可配置 `all`；入队/交付会发 `queue_update`。流式期间再次调用 `prompt()` 必须指定 `streamingBehavior`，直接 `steer/followUp` 会展开 Skill/Prompt Template 但拒绝 Extension Command。下一步用“改方向”和“追加任务”场景讲清两类投递时机、非中断边界和队列模式；尚无动态实验或学习确认。
  - 消息队列理解确认与正文验证（2026-08-27）：学习者已理解 Steering 在当前 Tool 批次后改变下一步方向、Follow-up 在 Agent 原本准备结束时追加任务，并确认继续。08 正文已补两类入口/投递点对照、`one-at-a-time/all`、`queue_update`、清空/读取 API 及 Extension Command 例外；语义复核、Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果只证明 Pi `0.84.2` 静态队列语义，尚无动态队列实验；下一步进入 Compaction。
  - Compaction 层进行中（2026-08-27）：Pi `0.84.2` 源码/Settings 确认 Compaction 默认开启，`reserveTokens=16384`、`keepRecentTokens=20000`，threshold 条件为 `contextTokens > contextWindow - reserveTokens`。manual 先取消当前 Agent Run 且不续跑被打断 Turn；threshold 只压缩已完成上下文；overflow 对成功响应只压缩，对错误/可恢复 length 会移除活跃失败消息、压缩并最多 continuation 一次。三类分别产生 `compaction_start/end(reason=manual|threshold|overflow)`，摘要与近期消息重建有效 Model Context，原始 Session JSONL 历史保留。下一步用办公桌/档案库对照讲清触发、重建、失败与非删除边界；尚无动态实验或学习确认。
  - Compaction 理解确认与正文验证（2026-08-27）：学习者已理解 Compaction 用摘要与近期消息重建有效 Model Context、不删除 Session JSONL，manual/threshold/overflow 的续跑边界不同，并确认继续。08 正文已补默认容量参数、三类触发、事件/恢复/取消、摘要风险和 Event Store/Snapshot 对照；语义复核、Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果只证明 Pi `0.84.2` 静态 Compaction 语义，尚无动态 Compaction 实验；下一步进入 Session 持久化。
  - Session 持久化层进行中（2026-08-27）：Pi `0.84.2` 源码/SDK 文档确认 `SessionManager.inMemory()` 不落盘，`create(cwd)` 新建持久化 Session，`continueRecent(cwd)` 恢复最近或新建，`open(path)` 打开指定 JSONL；未显式传入时 `createAgentSession()` 默认使用持久化 `create(cwd)`。JSONL Header 与 Entry 形成 `id/parentId` 树，当前 leaf 决定有效 Branch；普通 Message 在 `message_end` 后追加，Compaction/Branch Summary 等由专用入口写入。下一步讲清恢复、崩溃窗口、Session 替换后重新订阅、敏感信息和非 Git/非永久记忆边界；尚无动态持久化实验或学习确认。
  - 7.2 学习价值与深度缺口（2026-08-28）：学习者表示对六个机制“大概理解”，并追问实际用途及是否需要全部记住。当前不要求背默认值、API 签名或事件顺序；验收深度固定为遇到权限、停止、失败、新指令、Context 容量、进程恢复六类问题时能选择 Tool 限制、abort、两层 retry、steer/followUp、Compaction、SessionManager，并说出各自最关键的非沙箱/非回滚/请求放大/投递时机/非删除/非 Git 边界。下一步用决策表收口用途，再判断 Session 持久化理解是否闭合。
  - 学习价值与深度确认（2026-08-28）：学习者已理解学习这些控制面的价值是遇到真实问题时选对机制，而非背默认值、API 签名和事件字段，并确认继续。后续验收保持“问题 -> 机制 -> 关键风险”深度，不要求从空白默写配置。
  - Session 持久化理解确认与正文验证（2026-08-28）：学习者已大致理解内存/JSONL Session、`message_end` 落盘、树路径恢复、崩溃窗口、替换 Session 后重新订阅，以及非事务/非 Git/非永久记忆边界。08 正文已补入口对照、JSONL 树、恢复和敏感数据说明；语义复核、Markdown 围栏、本地链接和 `git diff --check` 通过，没有新增 Mermaid。该结果只证明 Pi `0.84.2` 静态持久化语义，尚无动态持久化实验。
  - 7.2 静态主线完成、动态实验待实施（2026-08-28）：Tool 限制、协作取消、Provider/Agent Retry、Steering/Follow-up、Compaction、Session 持久化的系统地图、Java 对照、用途选择和关键风险均已取得学习确认并写入稳定正文。当前没有动态证明 Tool 组合、AbortSignal 传播、重试次数、队列投递点、摘要重建或 JSONL 恢复，因此 7.2 保持未勾选；下一步先冻结独立实验矩阵和文件范围，再由学习者单独授权新增代码。
  - 7.2 实验矩阵提案、待代码授权（2026-08-28）：拟新建独立 `labs/7.2-sdk-controls/`，锁定 Pi SDK `0.84.2`，使用一个确定性 Custom/Fake Provider、协作延迟 Tool 和临时目录，不调用真实 Provider。矩阵覆盖：实际激活 Tool 集合；abort 后信号/错误终态且无晚完成；Provider 与 Agent retry 次数/事件；Steering/Follow-up 的投递顺序和两种排空模式；固定 Compaction Entry 对有效 Context 的摘要+近期消息重建；持久化 create/open/continueRecent 与 `message_end`/Branch 恢复。每项保留正反分支和证据上限；尚未获得本阶段新增代码授权，不创建文件或运行实验。
  - 真实 Model 优先矩阵修订、待代码授权（2026-08-28）：学习者明确要求 7.2 动态实验使用真实 Model，费用无需考虑。矩阵调整为：Tool 限制、协作取消、Steering/Follow-up、手动 Compaction 摘要、持久化恢复与恢复后新 Prompt 均使用当前 `openai/gpt-5.6-sol`；Provider/Agent Retry 的精确失败次数仍以本地故障注入制造，因为无法要求真实 Provider 确定地前两次返回 503/限流，且另设真实 Model 正常成功链，不把注入失败证据冒充真实 Provider 故障。最终验收必须同时标明真实 Model、故障注入和纯状态检查三类证据。费用授权沿用，凭据内容仍禁止读取/输出；本阶段尚未获得新增代码授权，不创建文件或运行调用。
  - 7.2 代码授权与实现开始（2026-08-28）：学习者明确“开始写 7.2 代码”，并要求完成后继续由教练带着学习。实现范围固定为新建独立 `labs/7.2-sdk-controls/`，锁定 SDK `0.84.2`；`npm run check` 用回环故障注入、协作 Tool 和临时目录验证确定性分支，`npm run real` 用当前 `openai/gpt-5.6-sol` 验证 Tool 限制、取消、Steering/Follow-up、真实 Compaction 摘要、JSONL 恢复和恢复后 Prompt。自动门禁通过前不调用真实 Model，异常真实结果不盲目重试；日志不输出凭据、Prompt/Tool 参数或完整 Session 内容。工程完成后仍按 Java 对照讲解并由学习者亲自运行，未取得学习验收前不勾选 7.2。
  - 7.2 实现与自动矩阵通过、待真实主线（2026-08-28）：已新增独立 package/tsconfig、共享脚本 Provider、Tool 集合测试、取消/队列时序测试、Provider/Agent Retry 测试、Compaction-aware JSONL 恢复测试、真实控制面入口和实验 README，依赖锁定 Pi SDK/pi-ai `0.84.2`。`npm install` 成功且审计 `0 vulnerabilities`；首轮类型检查因 `Model` 缺 API 泛型失败，补为 `Model<Api>` 后通过；测试首轮 2/6，四项因 Custom Provider 缺顶层 `api` 失败，补后仍因自定义 Model 缺 `baseUrl` 失败，加入不访问网络的本地占位地址后严格 TypeScript 和 6/6 全量通过。Provider Retry 确认本地 Fetch 前两次 503、第三次成功；Agent Retry、取消无晚完成、Steering 先于 Follow-up、Tool 集合和 Compaction/Session 恢复均 Green。该证据只证明本地故障注入/状态矩阵与夹具实现，尚未调用真实 Provider；下一步仅执行一次已授权真实主线。
  - 7.2 真实 Model 主线通过、待最终文档与学习验收（2026-08-28）：仅执行一次 `npm run real`，同一进程顺序完成多个受控步骤且未重跑。显式 Model/协议为 `openai/gpt-5.6-sol`/`openai-responses`；真实取消结果为 `signalObserved=true`、`lateCompletion=false`、`settled=true`。真实队列/Compaction/持久化结果全部为 true：Steering 先于 Follow-up、队列归零、真实 Summary 已生成、恢复 Context 含 Compaction Summary、恢复后 Prompt 返回 `RESUME_OK`、Session 文件在关闭/重开间保持同一；最终 `passed=true`，专用 `mkdtemp` 目录在 finally 清理。该证据证明该时间、账号、Model 和受控输入下的真实 Tool Call、AbortSignal、队列投递、摘要调用、JSONL 重开和恢复后响应链成功；Provider/Agent 失败重试次数仍只由本地故障注入证明，不冒充真实 Provider 故障、费用、质量、长期稳定或生产证据。
  - 7.2 工程实现与文档收口通过、待学习者验收（2026-08-28）：稳定正文已加入三层动态证据和边界，学习索引与 Lab README 已入链。最终无真实 Model 回归为 7.2 严格 TypeScript/测试 `6/6`、7.1 `3/3`、根项目 `185/185`；依赖顶层版本均为 `0.84.2`，npm audit 有一次 TLS 中断失败记录，有界重试后为 `0 vulnerabilities`；4 个相关 Markdown 的 27 个本地链接与围栏通过，08 全文 Mermaid `6/6` 实渲染通过，高置信凭据模式无命中，7.2 实验/Mermaid 临时残留为 0。`git diff --check` 通过；完整未跟踪文件的 no-index 检查首轮错误地把正常差异退出码 1 当失败，修正判定后全部通过。该结果证明当前工程候选可进入教学，不代表学习者已经掌握或亲自复现，7.2 保持未勾选。
  - 7.2 Demo 代码教学进行中（2026-08-28）：已从计划断点进入文件职责与运行入口层，当前只讲 `package.json`/`tsconfig.json`、共享 `test-runtime.ts`、四组测试、真实入口 `real-controls.ts`、README/lockfile 的 Java 对照，以及 `npm run check` 确定性链与 `npm run real` 真实链的证据分工。学习者确认前不记为掌握；下一步进入第一组 `tool-controls.test.ts`，用 Java 集合与权限白名单对照实际 Tool 集合断言。
  - 7.2 文件职责与运行链理解确认（2026-08-28）：学习者已理解 `package.json`/`tsconfig.json`、共享测试夹具、四组测试、真实入口、README/lockfile 的职责，以及 `npm run check` 不调用真实 Provider、`npm run real` 才执行真实集成链。下一步阅读 Tool 集合测试，不要求记忆 TypeScript 配置字段。
  - Tool 集合代码教学进行中（2026-08-28）：当前按“注册表 -> `createAgentSession()` 配置 -> `getActiveToolNames()` -> 五组集合断言”解释 `tool-controls.test.ts`；重点是显式 allowlist、最终 denylist 和默认抑制模式的实际集合，不把本测试扩大为 Tool 执行、Model 选择或操作系统沙箱证据。学习者确认前不记为代码理解验收。
  - Tool 集合代码理解确认（2026-08-28）：学习者已理解默认内置加 Custom Tool、`noTools` 默认抑制、显式 `tools` allowlist、`excludeTools` 最终减法，以及 `createEmptyResourceLoader()`/内存 Session/`dispose()` 的测试隔离作用；也理解本测试只证明 Model 可见集合，不证明 Tool 执行或 OS 沙箱。
  - 取消代码教学进行中（2026-08-28）：当前只讲 `cancel-queue-controls.test.ts` 第一项，按“脚本 Model 发 Tool Call -> `deferred()` 确认 Executor 已开始 -> `session.abort()` 传播信号 -> Tool reject -> `tool_execution_end` -> `agent_settled`”解释，并用 `aborted=true`、`completed=false` 排除未启动取消和晚完成。学习者确认前不记为代码理解验收。
  - `abort()` 等待边界理解缺口（2026-08-28）：学习者追问“等待任务收尾”是否等整个任务执行完。Pi `0.84.2` 本地实现确认 `abort()` 顺序为 `abortRetry()`、`agent.abort()`、`waitForIdle()`；它等待当前 Run 的取消传播、Tool/事件终态和 `agent_settled` 后 Session 回到 idle，不让原计划剩余 Turn/Tool 正常跑完。若底层忽略 `AbortSignal`，则可能长期等不到 idle；已发生副作用也不回滚。该边界确认前不进入队列教学。
  - 取消代码理解确认（2026-08-28）：学习者已理解 `abort()` 不是等待原任务正常执行完，而是发送取消、等待正在运行的组件协作停止并完成事件收尾，直到 Session idle；也理解 Tool 忽略信号会拖住等待，且已发生副作用不会回滚。取消测试代码层验收闭合。
  - 队列代码教学进行中（2026-08-28）：当前按“Gate Tool 固定窗口 -> 同时入队 Steering/Follow-up -> 释放 Gate -> 第二次 Model Context 只含 Steering -> 第三次 Context 含 Follow-up -> 队列归零”解释 `cancel-queue-controls.test.ts` 第二项；重点证明投递点顺序，不把两类消息误解为立即中断 Tool。学习者确认前不记为代码理解验收。
  - 队列代码理解确认（2026-08-28）：学习者已理解 Gate Tool 用两个 `deferred()` 固定入队窗口，三次 Model 请求分别为 Tool Call、Steering continuation 和 Follow-up continuation；Context 断言与最终队列归零共同证明投递顺序，两类消息都不立即中断当前 Tool。
  - 两层 Retry 代码教学进行中（2026-08-28）：当前并排讲 `retry-controls.test.ts` 两项：Provider Retry 通过注入本地 `fetch` 令前两次 503、第三次 SSE 成功，只观察 `requestCount=3` 和单个最终 Message；Agent Retry 通过脚本 Stream 前两次最终错误、第三次成功，观察 `auto_retry_start=2`、`agent_start=3`、成功 `auto_retry_end` 与最终 `agent_settled`。学习者确认前不记为代码理解验收。
  - 两层 Retry 代码理解确认（2026-08-28）：学习者已理解 Provider Retry 类似 HTTP Client 内部重发，只返回一个最终 Message；Agent Retry 类似 Service/Workflow 在最终失败后发起新 Agent 尝试，产生可见事件。也理解 `maxRetries` 是额外次数、两层叠加可能乘法放大，以及本地注入 503 不等于真实 Provider 故障。
  - Compaction/Session 恢复代码教学进行中（2026-08-28）：当前按“临时持久化目录 -> 写 Model/Thinking 与新旧 Message Entry -> 固定 Compaction Summary/firstKeptEntryId -> `open()` 重建 Context -> `continueRecent()` 恢复同一文件与 leaf -> finally 清理”解释 `session-controls.test.ts`。固定 Summary 只验证选择/恢复算法，不冒充真实 Model 摘要生成；学习者确认前不记为代码理解验收。
  - Compaction/Session 恢复代码理解确认（2026-08-28）：学习者已理解 JSONL 保留完整 Entry 历史，固定 Compaction Entry 通过 Summary 与 `firstKeptEntryId` 重建有效 Model Context，`open()` 打开指定文件、`continueRecent()` 恢复同一文件与 leaf，并理解 Context 排除旧消息不等于磁盘历史删除。确定性恢复层验收闭合。
  - 真实主线代码教学进行中（2026-08-28）：当前先讲 `real-controls.ts` 顶层“选择 `openai/gpt-5.6-sol` -> 真实取消 -> 真实队列/手动 Compaction/JSONL 重开与恢复后 Prompt -> 最终 `passed=true`”编排，以及 `withTimeout`、`finally`、有界日志和断言失败即停止的职责。下一步逐段理解真实取消与组合场景；学习者确认前不记为代码理解验收。
  - 真实主线总编排理解确认（2026-08-28）：学习者已理解 `npm run real` 串行选择已配置 Model、运行两个真实场景，任一断言失败都会阻止最终 `passed=true`；也理解 `withTimeout` 只限制等待实验 Tool 起跑、Retry 关闭以暴露单次失败、`finally` 负责订阅/Session/临时目录清理。
  - 真实取消代码教学进行中（2026-08-28）：当前对照本地取消测试解释 `runCancellationScenario()`：真实 Model 负责产生唯一 Tool Call，受控本地 Tool 固定等待 `AbortSignal`，`withTimeout` 防止 Model 未调用 Tool 时无限等待，随后断言信号、无晚完成、唯一 start、错误 end 和最终 settled；本场景不冒充 Provider 流取消或危险副作用回滚证据。学习者确认前不进入组合场景。
  - 真实取消代码理解确认（2026-08-28）：学习者已理解真实 Provider/Model 负责发现并生成唯一 Tool Call，取消目标是受控本地 Tool，严格事件断言和有界日志构成额外真实链证据；也理解本场景不证明 Provider 生成流取消、任意第三方 Tool 协作或副作用回滚。
  - 真实队列代码教学进行中（2026-08-28）：当前只讲组合场景 184-250 行：持久化 Session 和真实 Model 调用 Gate Tool，Gate 起跑后同时 `steer/followUp`，释放后按 `message_start` 收集 Marker 交付顺序，并断言队列曾为 2 且最终归零。该层用真实事件/状态补充确定性 Context 测试，不以真实回答措辞作为顺序证据；学习者确认前不进入 Compaction/重开段。
  - 真实队列代码理解确认（2026-08-28）：学习者已理解真实 Gate Tool 固定 Agent 忙碌窗口，`message_start` Marker 顺序证明 Steering 先于 Follow-up，`queue_update` 曾为 2 与最终 `pendingMessageCount=0` 分别证明同时等待和全部消费；真实回答措辞不作为投递顺序证据。
  - 真实 Compaction/重开代码教学进行中（2026-08-28）：当前讲组合场景 252-317 行：人工追加长 Message 只构造可压缩输入，`activeSession.compact()` 使用真实 Model 生成 manual Summary 并验证 start/end，随后释放旧订阅/Session、`open(sessionFile)` 重建含 `compactionSummary` 的 Context、创建无 Tool/无 Retry/无自动压缩的新 Session、重新订阅并要求真实 Model 返回 `RESUME_OK`，最后核对同一文件和清理临时目录。学习者确认前不记为真实主线代码验收。
  - 7.2 代码学习深度调整（2026-08-28）：学习者明确反馈代码细节看着很晕，希望只作简单了解。后续停止逐行讲 TypeScript、`deferred()`、事件收集和 Context 重建内部实现；已讲到的真实 Compaction/重开段只记为浏览，不要求代码理解确认。7.2 最终深度调整为：能按“权限/停止/失败/新指令/Context/重启恢复”选择六类机制，知道 `npm run check` 是本地确定性 `6/6`、`npm run real` 是真实 Model 主线，并能从 `passed=true` 与关键布尔值判断运行结果；仍需学习者亲自运行后才勾选。
  - 学习者确定性矩阵运行通过（2026-08-28）：学习者在 `labs/7.2-sdk-controls` 亲自执行 `npm run check` 并提交终端截图。截图显示 `tsc --noEmit` 无错误，取消、队列、Provider Retry、Agent Retry、持久化恢复和 Tool 集合六项均为绿色，汇总为 `tests 6`、`pass 6`、`fail 0`、`cancelled 0`、`skipped 0`，终端正常返回提示符。该证据证明学习者能启动并识别本地确定性矩阵通过，不证明真实 Provider/Model 主线；下一步执行一次 `npm run real`。
  - 学习者真实 Model 主线运行通过（2026-08-28）：学习者在同一 Lab 亲自执行 `npm run real` 并提交终端截图。截图显示 `openai/gpt-5.6-sol`、`openai-responses`；`real_cancel` 为 `signalObserved=true`、`lateCompletion=false`、`settled=true`；`real_queue_compaction_persistence` 的 Steering 顺序、队列归零、Summary 生成、恢复 Context、恢复 marker 和同一 Session 文件六项均为 `true`；最终 `real_controls_result.passed=true`，终端正常返回提示符。该证据证明学习者能启动并识别一次真实主线通过，不证明真实 Provider 失败 Retry、费用、质量、长期稳定或生产可用性。
  - 7.2 完成与验收（2026-08-28）：Tool 限制、协作取消、Provider/Agent Retry、Steering/Follow-up、Compaction 和 Session 持久化已完成系统地图、Java 对照、受控实现、工程门禁和学习者复现。最终工程/学习者确定性矩阵均为 `6/6`，工程/学习者真实主线均为 `passed=true`；学习深度按学习者要求收口为能按问题选择机制、说明关键边界、运行两条命令并判断结果，不要求逐行掌握 TypeScript 测试。7.2 勾选完成；证据边界保持本地故障注入不冒充真实 Provider 故障、两次真实成功不证明长期稳定或生产可用性。
- [x] 7.3 理解 RPC 的 stdin/stdout JSONL framing、请求关联和异步事件。
  - 进行中（2026-08-28）：已按 Pi `0.84.2` 随包 `docs/rpc.md` 与 `rpc-types.d.ts` 核对系统地图。当前场景固定为 Java `ProcessBuilder` 启动一个长期运行的 `pi --mode rpc` 子进程，stdin 每行发送一个 Command，stdout 混合输出带同一可选 `id` 的 `response` 与通常不带请求 `id` 的异步 Agent Event；严格 framing 只以 LF `\n` 分隔。下一步讲清 Prompt accepted Response 与后续 Message/Tool/终态事件的独立含义，学习确认前不写稳定正文或进入 7.4 代码。
  - 完成与验收（2026-08-28）：学习者已理解 RPC 是 Java 外部程序管理的长期 Pi 子进程，Command 经 stdin、Response/Event 混合经 stdout；严格 JSONL 只以 LF 分帧，可选请求 `id` 只关联 Response，普通 Agent Event 通常无请求 `id`。学习者也已理解 `success=true` 只表示接收/排队/立即处理，接收后失败进入 Message/Event 流，Run 收尾看 `agent_settled`，业务成功另看最终 Message/Tool/错误。稳定正文已加入系统图、Java 对照与边界；本地链接/围栏、全文 Mermaid `7/7` 实渲染和 `git diff --check` 通过。该证据为 Pi `0.84.2` 静态协议与学习确认，不证明 Java 客户端或真实 RPC 进程；动态实现进入 7.4。
- [x] 7.4 编写一个最小 RPC 客户端并处理成功、拒绝、运行期失败和退出。
  - 代码授权与合同冻结（2026-08-28）：学习者明确“开始写代码”。新建独立 `labs/7.4-rpc-java/`，使用 JDK 8 风格 Java、Maven、Jackson 和 JUnit 5；Java `ProcessBuilder` 管理长期 Pi RPC 子进程，stdin 写 Command、stdout 严格按 LF 读取 JSONL，普通 Command Response 通过 `id` 关联，因普通 Agent Event 无请求 `id`，高层 `runPrompt()` 同时只允许一个活动 Run。Fake Java 子进程必须覆盖正常成功、接收前 `success=false`、accepted 后 Assistant/Event 失败、进程提前退出和关闭 stdin 后正常退出；代码遵循类/方法 Javadoc、`@author xiexu`、无 `System.out`、无凭据输出。自动测试通过前不调用真实 Model；之后只执行一次已授权 `openai/gpt-5.6-sol` smoke，不盲目重试。学习者理解并亲自运行前不勾选 7.4。
  - 首轮 Maven 入口失败（2026-08-28）：工具会话在 Lab 工作目录直接执行 `mvn test` 返回退出码 127、`command not found: mvn`，未进入编译或测试；本机已确认 Maven 位于 `/Users/sxie/maven/apache-maven-3.6.3/bin/mvn`。下一步使用该绝对路径继续建立代码 Red/Green，不能把 PATH 失败归因于实现。
  - Java 客户端确定性矩阵首轮 Green（2026-08-28）：已新增 Maven/Jackson/JUnit 5 Lab、严格 LF JSONL 读取器、长期 `ProcessBuilder` 客户端、ID Response Map、单活动 Prompt 状态机、分层结果、`@Slf4j` 真实 Demo 和 Fake Java RPC 子进程。绝对 Maven 路径首次进入编译即成功，JDK 21 以 `release 8` 编译主代码 8 个类与测试 3 个类；严格 LF/CRLF/U+2028、逆序 Response ID 关联、正常 accepted+settled、接收前拒绝、accepted 后 Assistant error、Response 前退出码 7 共 `6/6` 通过，`BUILD SUCCESS`。本次只新增 `slf4j-simple 2.0.17` 并由已配置 Maven 镜像获取；该 Green 证明 Fake 子进程与当前实现，不证明真实 Pi RPC、Provider 或 Model。下一步构建可执行 jar 后仅运行一次真实 smoke。
  - 真实 Java RPC smoke 通过、待最终文档与学习验收（2026-08-28）：`mvn package` 再次通过 `6/6` 并生成可执行 shaded jar；Shade 仅报告依赖 MANIFEST/LICENSE/module-info 重叠警告，构建成功。随后仅执行一次 `java -Dpi.command=/opt/homebrew/bin/pi -jar target/pi-study-rpc-java.jar`，未重试；`openai/gpt-5.6-sol` RPC 结果为 `status=SUCCESS`、`accepted=true`、`settled=true`、`markerSeen=true`、`eventCount=16`、`exitCode=0`、`passed=true`。该证据证明该时间、账号、Model 和固定 Prompt 下 Java 子进程、严格 JSONL、Response/事件状态机、最终文字和 stdin EOF 正常退出链成功；不证明接收前拒绝/接收后失败的真实 Pi 分支、并发 Prompt、费用、质量、长期稳定或生产可用性。下一步补稳定正文和最终回归，学习者理解并亲自运行前不勾选 7.4。
  - 重复打包制品污染 Red（2026-08-28）：真实 smoke 后不清理 `target` 执行离线 `mvn package`，测试仍为 `6/6`，但 Shade 将上次已替换为 fat jar 的主 artifact 再次合并，报告当前 jar 与 Jackson/SLF4J 存在 787/223/75/57/9 个重复类或资源。该结果不否定客户端运行证据，但说明“替换主 artifact”无法保证重复打包干净。下一步改为保留普通 jar、以 `all` classifier 附加 fat jar，并通过 clean package 与第二次 package 对照验证不再自我重打包。
  - 重复打包修复 Green（2026-08-28）：Shade 改为保留普通 `pi-study-rpc-java.jar`，另按 Maven 坐标附加可执行 `pi-study-rpc-java-0.0.0-all.jar`；`mvn clean package` 与紧接着不清理 target 的 `mvn -o package` 均通过 `6/6`。第二次打包不再出现当前 jar 与 Jackson/SLF4J 的 787/223/75/57/9 个重复类，自身重打包问题闭合；剩余 MANIFEST/LICENSE/module-info 是不同依赖间的已知合并警告，不影响当前 classpath smoke。
  - 7.4 工程最终复核通过、待学习验收（2026-08-28）：7.4 离线 Maven 打包 `6/6`，根项目 `185/185`、7.1 `3/3`、7.2 `6/6` 全部通过；README/08/索引共 29 个本地链接与围栏通过，08 Mermaid `7/7` 实渲染，tracked/untracked 空白、高置信凭据、`System.out/System.err`、临时目录和残留 `pi --mode rpc` 进程检查通过，`target/` 正确忽略。稳定正文已加入组件、四态结果、Fake/真实证据和边界。当前代码 8 个主类、3 个测试类均有类/方法 Javadoc 和 `@author xiexu`；工程候选可进入教学，但学习者尚未理解和亲自运行，7.4 保持未勾选。
  - 7.4 Java 主链教学进行中（2026-08-28）：按学习者“简单了解代码”偏好，只讲 `RpcDemo` 组装命令、`PiRpcClient.start()` 启动子进程、`runPrompt()` 发送 Prompt 并分离 Response/Event、`PromptRunResult` 四态、`close()` 关闭 stdin 并等待退出；`pendingResponses`、线程池和 `StrictJsonlReader` 内部算法不要求掌握。下一步取得主链理解确认，再解释 6 个测试分别保护什么并由学习者亲自运行。
  - 7.4 Java 主链理解确认（2026-08-28）：学习者已理解 `RpcDemo -> PiRpcClient.start -> runPrompt -> PromptRunResult -> close` 的 Facade 主链，知道 Response 先确认 accepted、Event 推进到 settled、普通 Command 可按 ID 关联而高层同时只跑一个 Prompt，以及 SUCCESS/REJECTED/RUNTIME_FAILURE/PROCESS_EXITED 四态；不要求记忆线程池、Map 或 JSONL Reader 内部实现。
  - Fake 测试理解缺口（2026-08-28）：学习者追问“Fake 测试是什么”。当前需先说明 `FakeRpcServerMain` 是由测试真正启动的 Java 子进程，客户端仍执行真实 `ProcessBuilder`、stdin/stdout、严格 JSONL、ID Map、Event Tracker 和退出清理；仅把不可控的 Pi/Provider/Model 替换为能稳定返回成功、拒绝、运行错误、逆序 Response 和退出码 7 的协议替身。它类似进程级 MockWebServer，不是伪造断言，也不证明真实 Pi 失败分支；确认前不记 6 项测试理解验收。
  - 真实 Pi 学习策略确认（2026-08-28）：学习者明确要求使用真实 Pi 学习，费用无需考虑。7.4 后续教学与学习者验收改为真实 Pi/真实 Model 主线，Fake 仅保留为自动化回归的确定性失败夹具，不要求学习其实现，也不冒充真实动态证据。工程已完成一次真实 smoke（16 条 Event、`passed=true`）；下一步由学习者亲自运行 Maven 构建和真实 `-all.jar`。费用授权继续有效，凭据内容仍禁止读取/输出，失败真实请求不盲目重试。
  - 学习者构建通过、jar 文件名指引 Red（2026-08-28）：学习者亲自执行 Maven package，截图显示 `BUILD SUCCESS`，Shade 只报告依赖 MANIFEST/LICENSE/module-info 合并警告；随后按 README 的错误路径 `target/pi-study-rpc-java-all.jar` 启动，Java 返回 `Unable to access jarfile`。实际制品为 `target/pi-study-rpc-java-0.0.0-all.jar`，因此本次未启动 Pi、未调用 Model、未产生费用，不能记为真实 smoke 失败或成功。README 与计划已改为实际文件名；下一步只执行修正后的真实命令，不重跑 Maven。
  - 学习者真实 Pi RPC 运行通过（2026-08-28）：学习者使用修正后的 `target/pi-study-rpc-java-0.0.0-all.jar` 亲自启动 Java 客户端并提交截图。结果为 `status=SUCCESS`、`accepted=true`、`settled=true`、`markerSeen=true`、`eventCount=16`、`exitCode=0`、`passed=true`，终端正常返回提示符。该证据证明学习者能启动并判断一次真实 Java -> Pi RPC -> Provider/Model -> Event -> EOF 退出链通过，不证明 Fake 覆盖的真实失败分支、并发 Prompt、费用、质量、长期稳定或生产可用性。
  - 7.4 完成与验收（2026-08-28）：学习者已按简单 Java 深度理解 `RpcDemo -> PiRpcClient -> runPrompt -> PromptRunResult -> close` Facade 主链和四态结果，Fake 只作为自动化失败夹具、不要求掌握实现；工程 Fake 矩阵 `6/6`、可重复打包、工程/学习者真实 Pi RPC 两次均为 16 条 Event 与 `passed=true`，构建指引 Red 已修正并复现成功。7.4 勾选完成；证据边界保持 Fake 不冒充真实失败、两次真实成功不证明长期稳定或生产可用性。
- [x] 7.5 使用 JSON Event Stream 完成一次结构化单次任务并解析关键事件。
  - 系统地图进行中（2026-08-28）：已按 Pi `0.84.2` 随包 `docs/json.md`、`print-mode.js` 与 `json-event.d.ts` 核对。JSON 模式是 finite single-shot：`pi --mode json "prompt"` 启动后先输出 Session Header，再输出 Agent/Turn/Message/Tool/Session Event，Prompt 完成后释放 Runtime 并退出；没有 RPC Command/Response、请求 `id` 或长期双向命令循环。`message_update` 只有 delta/usage，不含累计 Message，实时 UI需按 `contentIndex` 组装；`message_end.message` 才是最终权威消息。源码还确认 JSON 模式不会像 text 模式那样因最终 Assistant `stopReason=error|aborted` 自动把 exitCode 改为 1，因此退出码 0 只证明进程控制流正常结束，业务成功仍需解析最终 Message，并结合 `agent_settled`。下一步完成 RPC/JSON 对照理解确认，再决定真实单次实验；未获代码授权不创建新实现。
  - 系统地图理解确认与正文验证（2026-08-28）：学习者已理解 JSON Event Stream 是 Prompt 随启动一次性提交、Header/Event 单向输出、任务后进程退出的 single-shot 模式，并能与长期双向 RPC 区分；也理解 `message_update` 是 delta、`message_end.message` 是权威结果、`agent_settled` 表示不再续跑，以及退出码 0 不能单独证明 Assistant 成功。08 正文已加入 RPC/JSON 对照、时序图和退出码边界；本地链接/围栏、全文 Mermaid `8/8` 实渲染和 `git diff --check` 通过。下一步用真实 `openai/gpt-5.6-sol` 完成单次 JSON 任务并通过 jq 解析关键记录，动态证据前保持 7.5 未勾选。
  - 真实单次实验待学习者执行（2026-08-28）：固定只调用一次 `pi --mode json --no-session --no-approve --provider openai --model gpt-5.6-sol`，Prompt 要求返回 `JSON_STREAM_REAL_OK` 且不调用 Tool；stdout 写入 `mktemp` 专用 JSONL，jq 只汇总 `headerSeen`、最终 Assistant `stopReason`、`markerSeen`、`settledSeen` 和 `eventCount`，Shell 单独记录 `piExitCode`，随后删除临时文件。通过标准为前述布尔值全 true、stopReason 仅 `stop`、exitCode 0；该证据只覆盖本次真实单任务，不证明错误退出码、Tool Event、长期稳定或生产可用性，异常结果不盲目重跑。
  - 学习者真实 JSON single-shot 通过（2026-08-28）：学习者仅执行一次固定真实命令并提交脱敏 jq 汇总。结果为 `headerSeen=true`、Assistant `stopReason=["stop"]`、`markerSeen=true`、`settledSeen=true`、`recordCount=16`、`eventCount=15`、`piExitCode=0`，终端正常返回；`mktemp` JSONL 已按命令删除。该证据证明本次 `openai/gpt-5.6-sol` single-shot 的 Header、最终 Message、Session settled 和进程退出链成功，不证明错误分支、Tool Event、长期稳定或生产可用性。
  - 7.5 完成与验收（2026-08-28）：学习者已理解 JSON Event Stream 与 RPC 的生命周期/输入/输出/关联差异，知道 delta-only `message_update`、权威 `message_end.message`、`agent_settled` 和退出码的分层职责；稳定正文、Mermaid `8/8` 与真实单次 jq 解析均闭环，无新增 Java/TypeScript 代码。7.5 勾选完成。
- [x] 7.6 理解 TUI 的 Component、Container、Input、Overlay、键盘和渲染机制。
  - 进行中（2026-08-28）：恢复学习时本机 `pi --version` 已为 `0.84.3`，高于 7.1-7.5 的 `0.84.2` 基线。下一步先只读核对随包 TUI 文档、导出类型和版本变更，再从“一个终端任务界面如何接收按键并刷新画面”的完整场景建立 Java UI 对照；本轮不编写代码，未取得学习确认前不勾选完成。
  - `0.84.3` 版本门禁已通过（2026-08-28）：全局 `pi-coding-agent` 与 `pi-tui` 均为 `0.84.3`；其 942 行 `docs/tui.md` 与课程 Lab 锁定的 `0.84.2` 文件 SHA-256 完全相同，`TUI`、`Component`、`Input` 公共类型无差异。当前可见变化主要是部分默认 Keybinding、设置选择器和文字/Markdown 渲染修复，不改变本项六个核心对象的职责；该结果属于随包文档、类型与 Changelog 静态证据，不证明真实终端输入、焦点或差分刷新行为。下一步讲解完整系统地图并取得学习确认。
  - 系统地图理解缺口（2026-08-28）：学习者明确反馈 Java UI 类比使本节更难理解，上一轮 Component/Container/Input/Overlay 总图不计为理解验收。自 7.6 起默认停止 Java 类比，先用真实 Pi 画面一次只解释一个因果关系；当前只讲“普通输入 -> 弹出选择菜单 -> 关闭菜单”三步中按键接收者的变化，确认后才引入正式术语。
  - 按键接收者第一层理解确认（2026-08-28）：学习者已确认理解普通输入时按键交给底部输入区、选择菜单打开后改交给菜单、菜单关闭后再交回输入区。该确认只覆盖“同一时刻一个按键接收者及其切换”，不扩大为焦点术语、Input、Overlay、Container 或渲染机制理解；下一步只引入“焦点”并区分光标与高亮。
  - 焦点术语理解确认（2026-08-28）：学习者已理解焦点决定当前按键接收者，并能与文字插入位置的光标、菜单当前项的选中高亮区分。该确认不扩大为 Input、Overlay、Container 或渲染机制理解；下一步只解释基础 `Input` 的状态与按键职责，并明确它不是 Pi 底部完整主编辑器。
  - `Input` 理解确认与总图要求（2026-08-28）：学习者已理解基础 `Input` 只保存单行文字与光标、处理编辑按键并通知提交/取消，不负责搜索、菜单或 Model；也已区分基础 `Input` 与 Pi 底部更复杂的 Editor。学习者要求最终整合为流程图；待焦点、Input、Overlay、Component、Container、键盘和刷新全部逐层确认后，在 `docs/learning/08-sdk-rpc-json-tui.md` 补一张无 Java 类比的纵向总图，覆盖“按键分发”和“状态到终端刷新”两条连续主线，并完成 Mermaid 实际渲染检查。当前下一步只解释 Overlay。
  - `Overlay` 理解确认（2026-08-28）：学习者已理解 Overlay 把内容覆盖显示在原画面之上，交互式 Overlay 通常取得焦点并在关闭后归还焦点，同时能区分“显示在上面”与“接收按键”；非捕获 Overlay 可以只显示而不取得焦点。该确认不扩大为 Component、Container 或刷新机制理解；下一步只解释 Component。
  - `Component` 理解确认（2026-08-28）：学习者已理解 Component 是把自身当前状态描述成一行或多行终端画面的独立界面块，并能区分纯显示 Component 与取得焦点后可接收按键的 Component；也已串联 Input 是 Component、菜单 Component 可按 Overlay 方式显示。该确认不扩大为 Container、完整键盘路径或刷新机制理解；下一步只解释基础 Container。
  - `Container` 理解确认（2026-08-28）：学习者已理解基础 Container 按顺序保存多个 Component，并把各子 Component 生成的文字行纵向拼接成更大的画面；Container 可嵌套，但不自动向子 Component 广播或分发按键，按键接收者仍由焦点决定。该确认不扩大为完整键盘路径或刷新机制理解；下一步串联一次按键从终端到焦点 Component 的路径。
  - 完整键盘路径理解确认（2026-08-28）：学习者已理解按键经终端交给 Pi，Pi 查找焦点后只交给焦点 Component，由其按自身职责修改状态或触发动作；同一按键在 Input 与菜单中会产生不同结果。该确认只覆盖主调用方向，不扩大为终端转义序列、Kitty 协议、全局 Listener 顺序、按键释放事件或源码实现掌握。
  - 7.6 学习深度调整（2026-08-28）：学习者询问是否需要继续学习到当前细度。当前阶段收口为“过一遍主线、能解释对象职责和输入/刷新方向、需要实现时会查稳定文档”，不要求记忆 API、终端协议、Overlay 栈、IME 光标、缓存策略或差分算法，也不新增代码或动态实验；源码级输入分发与渲染明确延后到 8.5 `packages/tui`。剩余只讲一遍刷新主线，随后生成并实渲染总流程图、沉淀稳定正文。
  - 7.6 学习深度理解确认（2026-08-28）：学习者已确认理解并继续；本项后续不再逐层展开底层实现，只完成刷新主线、总流程图和稳定文档。该确认不等于刷新路径或整个 7.6 已验收。
  - 刷新主线理解确认与文档收口开始（2026-08-28）：学习者已大概理解“状态变化 -> 请求刷新 -> Component 生成文字行 -> Container 拼接 -> Overlay 叠加 -> 比较新旧画面 -> 更新终端”，并主动确认是否已进入文档。此前只有计划台账，稳定正文尚未包含 7.6；当前开始在 08 正文补精简职责表、无 Java 类比的纵向总图和证据边界。学习确认按“了解并会查文档”深度计，不扩大为差分算法或真实终端运行证据；文档门禁通过前 7.6 保持未勾选。
  - 文档门禁与独立复核通过（2026-08-28）：08 正文已加入职责表、键盘/Agent 事件到状态变化及刷新输出的纵向总图、学习深度和证据边界，学习索引同步区分 7.1-7.5 的 `0.84.2` 与 7.6 的 `0.84.3`。首轮本地链接检查器把 `#事件完整流程`、`#实验入口` 错当磁盘路径而失败，修正 anchor-only 解析后为 `31/31`；独立复核首轮发现“按键只交给焦点 Component”忽略全局输入可消费或改写的 P2，正文与图增加“未消费”边界后复核 `PASS`。最终 08 Mermaid `9/9` 实渲染，新增图图片检查无孤立节点、重叠或交叉歧义；围栏 `9/9`、标题层级、本地链接/锚点、敏感模式和 `git diff --check` 通过，临时渲染残留为 `0`。
  - 7.6 完成与验收（2026-08-28）：学习者已按“了解并会查文档”深度理解焦点、基础 Input 与主 Editor 的差异、Overlay、Component、Container、键盘主线和刷新主线；稳定正文与总流程图闭环，7.6 勾选完成。证据只覆盖 Pi/TUI `0.84.3` 随包文档、类型、本机静态实现、文档门禁和学习确认；没有新增真实 TUI 动态实验，不证明任意终端、IME、键盘协议、性能或差分算法，源码级细节留到 8.5。
- [ ] 7.7 构建一个外部小程序：提交任务、展示流式状态、限制工具并保存会话。
- [ ] 7.8 为外部集成补充协议测试、异常测试和资源清理验证。

验收产物：一个可运行、可测试的 SDK 或 RPC 集成程序；另一种接入方式需完成对比实验。

### 阶段 8：源码与综合项目

目标：从会用和扩展，进阶到能定位 Pi 内部实现并独立交付完整方案。

- [ ] 8.1 拉取并构建官方 `pi-mono`，运行无需模型凭据的测试。
- [ ] 8.2 阅读 `packages/ai`，说明模型、Provider 和流式事件抽象。
- [ ] 8.3 阅读 `packages/agent`，追踪 Agent Loop、消息和 Tool Call 生命周期。
- [ ] 8.4 阅读 `packages/coding-agent`，追踪 CLI、资源加载、Session、Compaction 和 Extension。
- [ ] 8.5 阅读 `packages/tui`，理解终端渲染和输入分发。
- [ ] 8.6 从用户输入开始，完成一次跨包调用链路追踪并标注关键源码位置。
- [ ] 8.7 使用调试能力定位一个刻意制造的问题，并提交排查报告。
- [ ] 8.8 完成 Pi 毕业综合项目并通过核心验收；展示网站在阶段 9 单独验收。

### 阶段 9：学习网站与面试作品集

目标：把已验收的学习材料转化为可公开、可追溯、能独立讲解的面试作品，而不是简单发布聊天记录。

- [ ] 9.1 冻结网站信息架构，覆盖学习路线、架构模型、真实实验、源码追踪、Extension、外部集成和综合项目。
- [ ] 9.2 审核全部内容，删除重复结论并脱敏凭据、Session、个人路径和终端输出。
- [ ] 9.3 使用 VitePress 接入现有 Markdown，配置导航、目录、代码高亮和全文检索。
- [ ] 9.4 补充 Agent Loop、Tool 生命周期、Session/Compaction 和 Extension 事件链流程图。
- [ ] 9.5 制作面试入口，展示个人学习路线、核心理解、关键取舍、失败实验和最终项目。
- [ ] 9.6 部署到公开地址，并从 GitHub 仓库建立双向入口。
- [ ] 9.7 完成桌面端、移动端、链接、构建和隐私检查。

验收产物：可公开访问的网站、可复现的 GitHub 仓库、5-10 分钟项目讲解路径和一组基于项目证据的面试问答。

## 6. 毕业综合项目

项目目标：在本仓库交付一套“安全可控的 Pi 开发工作台”。

必须包含：

- [ ] 项目级规则与最小权限配置。
- [ ] 一个结构化 Prompt Template。
- [ ] 一个只读分析 Skill。
- [ ] 一个包含自定义 Tool、Command 和危险操作审批的 Extension。
- [ ] 一个可安装的本地 Pi Package。
- [ ] 一个 SDK 或 RPC 外部客户端，支持流式事件、取消、错误处理和会话保存。
- [ ] 自动化测试、手工验收清单、安全说明和使用文档。
- [ ] 一份 Pi 核心调用链源码导读。

最终验收标准：

1. 新环境可按文档复现，不依赖未记录的全局状态。
2. 默认不执行危险操作，不泄露凭据，不隐式产生未确认费用。
3. 关键成功路径、拒绝路径和失败路径均有测试或可重复证据。
4. 用户能在不看答案的情况下解释并演示整个系统。
5. 所有阶段计划项均有验收记录，不以“文件已创建”代替学习完成。

## 7. 进度记录

### 当前断点

- 文档维护 A0（完成，2026-08-24）：已完成敏感标识脱敏、根 README 三入口、学习索引的阶段/实验映射、Provider/API Adapter 术语统一，以及手动、阈值、成功响应后压缩和失败/截断后一次重试四类 Compaction/Overflow 对照。全量验证覆盖 25 个 Markdown、138 个闭合围栏、85 个本地路径/锚点和 66 张 Mermaid，结果分别为链接/锚点通过、标题层级通过、`66/66` 实渲染通过且临时残留为 0；已跟踪 Markdown 敏感标识与明文凭据模式扫描为 0，旧 Provider/Adapter 混用检索为 0，`git diff --check` 通过。计划 checkbox 序列与 `HEAD` 完全一致，阶段 7 文档仍不存在，版本、权限、文件数量、A/B、失败、纠正与历史验收结论均保留。本批只修改 7 个授权文档，未拆分 06/07、未运行认证或 Provider、未调用真实 Model、未产生费用，也未执行暂存或提交。下一步仍按本断点进入 7.1；06/07 后续拆分须另行授权。
- 文档维护 C（完成，2026-08-24）：原 07 已收口为 46 行稳定 hub，并完成 Package、模型选型与认证、Custom Model、Custom Provider、版本证据五个子页；阶段 6 文档合计 1,227 行。四个直接迁移页按原 435/209/92/15 行稳定正文逐字包含；Custom Provider 按“完整场景 -> 对应精确合同”重排为 406 行，保留注册、目录、Credential、Stream、AssistantMessage、错误/取消、Usage/Cost、Overflow、Reload/注销及五层证据，删除 3 张被更精确图覆盖的入门图、2 段重复 Overflow 说明和失效过渡语。6.1-6.4 动态历史及其中 3 个内容哈希由本计划完整保留，稳定页旧动态副本、失效锚点和跨页精确重复段落均为 0；04、学习索引和四份 Lab 入链已更新，计划与教学 Skill 的网站内容源已明确包含主题子目录，Stage 5 顶层只读 Tool 合同未改变。全量门禁覆盖 30 个 Markdown、135 个闭合围栏、108 个本地路径/锚点和 63 张 Mermaid，围栏、标题层级、链接/锚点、职责/版本关键词、隐私模式、`63/63` 实渲染、临时残留与 `git diff --check` 均通过；计划 checkbox 序列与 `HEAD` 完全一致，阶段 7 文档仍不存在。当前工作区保留已验证 A0；本批未拆 06、未修改代码/Fixture/测试、未运行课程实验、认证或 Provider、未调用真实 Model、未产生费用，也未暂存或提交。下一步仍按当前断点进入 7.1；拆分 06 须另行授权。
- 文档维护 B（完成，2026-08-24）：顶层 `06-extensions.md` 已收口为 106 行兼容 hub，并完成 Context/Tool/Event、Shell Gate/Branch State/Widget、Runtime/加载、验证边界四个子页及学习索引入链；Extension README 已增加实现、依赖、生命周期、取消/错误、Tool/Command、Shell/State、证据边界标题。原 481/279/250/45 行稳定正文已迁移；生命周期总图拆为 Runtime 所有权和入口派发两图，State/Widget 图改为纵向。拆分前 `40/7`、`43/10` 和 SHA 已明确限定为历史文件快照，自动测试具体历史计数只留本计划。职责关键词、跨页精确重复、路径、版本和动态数字归属检查通过；Extension 源码/Fixture/测试无差异，顶层 `/study-inspect 06-extensions.md` 路径、固定补全和只读顶层 basename 合同不变。E 已补齐严格 TypeScript、`185/185` 自动测试和 64 张图实渲染，因此 B 验收闭合。当前 B 与 E 共 8 个预期文档路径保持未提交，未修改代码、Fixture 或测试，未运行认证/真实 Provider，未调用真实 Model或产生费用。
- 文档维护 E（完成，PASS，2026-08-24）：以本地提交 `ed71c93b28046a9c647f96a3919f9e2a4154d941` 为 A0+C 基线，对 B 的未提交差异完成全仓最终复核。根 `npm run check` 通过严格 TypeScript 与 `185/185` 自动测试；34 个 Markdown、136 个闭合围栏、113 个本地路径/锚点和 64 张 Mermaid 分别通过标题、围栏、链接/锚点与 `64/64` 实渲染，06 子页图分布为 Context 4、Gate/State/UI 3、Runtime 5，临时残留为 0。06/07 hub 与子页职责、Pi `0.84.1`/`0.84.2` 分界、历史数字限定、稳定正文动态历史归属、跨页精确重复和隐私扫描均通过；计划 checkbox 序列与 A0+C 提交完全一致，阶段 7 文档仍不存在，`git diff --check` 通过，工作区只包含 B/E 的 8 个预期文档路径。该 PASS 不证明真实 TUI、Provider、账单或生产行为；本轮未 push、未提交 B/E。下一步仍按当前断点进入 7.1；如需提交 B/E，必须另行授权。
- 状态：阶段 0、阶段 1、阶段 2、阶段 3、阶段 4、阶段 5、阶段 6 和阶段 7 的 7.1-7.6 已完成；下一项为 7.7。
- 已完成：阶段 0-6、7.1-7.6。
- 文档结构：学习笔记已按主题拆分，入口为 `docs/learning/README.md`；学习进度仍只在本文件维护。
- 教学方式：采用“系统地图 + 单一贯穿项目 + 三遍螺旋”的连续讲解模式；新主题先用有起点、过程和终点的完整大白话场景解释陌生对象，再映射术语并直接进入受控实验，不再逐知识点提问或要求反复复述；自 7.6 起默认不使用 Java 类比，除非学习者重新要求。用户可随时打断。用户运行本地 Pi 实验，我负责实验设计、证据分析、纠错和模块验收；无问答时不把教学覆盖扩大为独立背诵证据。
- 教学 Skill：`.agents/skills/pi-learning-coach/SKILL.md` 已创建并通过静态验证；由 Codex 使用它编排教学与读取唯一计划，Pi 只作为实验对象；不另建进度台账，也不自动提交。
- 计划强化：阶段 5-6 已扩展为两个贯穿项目、逐项受控实验、分层证据和独立阶段门禁；5.1-5.10 和 6.1-6.8 已完成。
- 网站策略：当前只积累网站可复用的 Markdown、流程图和脱敏证据；阶段 8 完成后进入阶段 9，不提前开发网站界面。
- 阻塞：无；6.4 guided 运行器已按 `quit` 停止，其追踪的临时残留为 `0`。
- 下一步：进入 7.7 外部小程序系统地图，先确定使用 SDK 还是 RPC、目标界面、流式状态、Tool 限制和 Session 保存边界；未取得单独代码授权前不编写实现。
- 新会话恢复：先读本文件，再从上述“下一步”继续；不得重新从安装或 0.2 开始，也不得提前进入网站开发。

### 阶段验收记录

| 日期 | 计划项 | 状态 | 验收证据 | 下一步 |
|---|---|---|---|---|
| 2026-08-28 | 7.6 TUI Component、焦点、Overlay、输入与刷新 | 已完成 | Pi/TUI `0.84.3` 静态合同、逐层学习确认、08 职责表与纵向总图闭环；独立 P2 修复后复核 `PASS`，Mermaid `9/9`、本地链接/锚点 `31/31`、围栏、标题、敏感模式、差异和临时残留检查通过；无新增真实 TUI 动态实验，源码细节延后到 8.5 | 进入 7.7 外部小程序系统地图，未授权前不写代码 |
| 2026-08-24 | 6.8 Custom Provider 公共接口、生命周期与测试边界 | 已完成 | Pi `0.84.2` 静态正文覆盖两种注册、目录、身份权限、流事件、Usage/Cost、Overflow、注销/Reload/永久下线和五层证据边界；学习者逐层自然确认。全文 Mermaid `33/33`、`14/14` 本地链接、围栏和差异检查通过，独立最终复核 `PASS`；未读取凭据、认证、联网、调用真实 Provider 或产生费用 | 阶段 6 完成，进入 7.1 SDK Agent Session 系统地图 |
| 2026-08-23 | 6.7 Custom Model 配置到请求完整链路 | 已完成 | 07 已形成公司网关说明书场景、字段职责表、配置发现/请求适配/错误定位三图，以及声明式 Provider/Custom Provider 两张纠偏流程；字段与请求链独立验收 `PASS`，两图最终独立验收 `PASS`，全文 Mermaid `21/21` 实渲染、链接、围栏和差异检查通过；学习者连续追问核心分界并主动询问下一步，构成自然理解确认。证据不外推真实配置、认证、连通、能力、费用或生产稳定性 | 进入 6.8 Custom Provider 公共接口与生命周期导读 |
| 2026-08-22 | 6.6 认证解析、取值命令、Header 与 OAuth | 已完成 | “公司 AI 大门查工作证”完整场景、认证来源图、两类 `!command` 图和 OAuth 图已写入 07；首轮独立 `FAIL` 的泛化问题已按源码纠正，复验 `PASS WITH NOTES` 后又由主审修正 OAuth 锁内判断箭头；当前全部 Mermaid `18/18` 实渲染、本地链接和差异检查通过；学习者以“继续”自然确认。证据不外推真实凭据、命令执行、登录、Provider 接受或模型响应 | 进入 6.7 Custom Model 配置到请求链路 |
| 2026-08-22 | 6.5 内置 Provider、Custom Model 与 Custom Provider 选型 | 已完成 | 同一 Java 代码审查任务的三个完整场景、决策表和四张新增 Mermaid 图已写入 07；学习者在重讲后以“继续”自然确认；当前全部 Mermaid `15/15` 实渲染、本地链接和差异检查通过；独立验收 `PASS WITH NOTES` 的唯一生命周期备注已修正并复验。证据不外推真实 Provider、认证、请求、计费或生产可用性 | 进入 6.6 认证解析顺序、取值与 OAuth 边界 |
| 2026-08-22 | 6.4 Package 安装、禁用、筛选、更新和卸载 | 已完成 | 普通同名 npm Package 的 Extension 优先级 A/B、精确排除和普通空数组已完成原三层闭环；项目级移除与用户回退已闭环；范围 Source 保持 `^0.1.0`，用户级受管理 Manifest 已由 `0.1.0` 更新到 `0.1.1`，新进程已发现四类资源并初始化 Extension；用户级 `remove` 后，`pi list` 显示 `No packages installed.`，隔离用户 Settings 显示 `packageCount=0`、`sourcePresent=false`，精确用户受管理目录显示 `user_install_exists=false`，最终全新 Pi 进程也不再显示四类 Package 资源且观察进程已退出返回 Shell；guided 最终输出 `guided_stop_reason=quit`、`guided_status=STOPPED`、`temp_residuals=0`；稳定解释和流程图已补入 07 | 进入概念路线 6.5，不执行真实 Provider、认证或计费实验 |
| 2026-08-21 | 6.3 本地 `pi-study-workbench` Package 与制品等价 | 已完成 | 学习者亲手完成 Manifest 17 项到 dry-run 19 项、真实 tgz 与 SHA-256、tar 清单与解包 19 项、A/B Manifest、Extension Flag 及 TUI 四类资源发现，自然确认三层清单边界；最终 8 个精确临时目录、同名前缀目录和相关 Pi 进程均为 0，仓库状态未扩散，`git diff --check` 通过 | 进入 6.4 Package 管理手工学习重验 |
| 2026-08-20 | 6.2 Package 资源、依赖、安装脚本与系统权限风险审查 | 已完成 | 六步静态清单与依赖/锁文件/安装加载边界已自然确认；用户真实 A/B/C/D 取得默认安装、`ignore-scripts`、定向 rebuild、`extensions: []` 四组预期及 `result=PASS`；自动 `4/4` 回归和清理检查通过，证据不外推第三方安全或 OS 隔离 | 进入 6.3 本地 Pi Package 封装系统地图 |
| 2026-08-20 | 6.1 npm、Git、本地路径三种 Package 来源及固定版本策略 | 已完成 | Pi `0.84.2` 下三组用户真实 A/B 均通过；记录本地 A/B SHA-256、Git A/B 完整 Commit、npm 精确/范围 Source 与安装版本；用户自然确认调试、复现、交付选型及供应链边界；稳定矩阵和流程图已写入 07 正文 | 进入 6.2 Package 安全审查系统地图 |
| 2026-08-19 | 5.10资源、Reload、Session切换、并发和模式兼容风险审查 | 已完成 | 连续Reload、New、Tree、Fork/Resume和Print真实矩阵通过；Print追踪为`factory=1 startup=1 quit_shutdown=1 user_bash=0 shell_gate=0`；并发/Shutdown聚焦合同、严格类型与`185/185`全量测试、差异、围栏、`11/11`链接、敏感信息和后台残留检查均通过；自动层为Pi`0.84.1`锁定依赖，动态层为CLI`0.84.2` | 进入6.1三种Package来源与固定版本策略 |
| 2026-08-18 | 5.9 Extension测试策略与手工验收收口 | 已完成 | 四层证据地图、八类手工矩阵、真实Pi CLI `0 -> 1`自动对照和教练侧`183/183`开发者自检已完成；用户亲自运行聚焦测试取得`1/1` Green，并明确不追加问答式复述、要求继续。记录实际操作与自然确认，不声称独立复述 | 进入5.10资源、Reload、Session切换、并发和模式兼容风险审查 |
| 2026-08-18 | 5.8 最小 Widget | 已完成 | 固定 key、TUI-only 模式、状态恢复后显示、提交成功后原位更新、失败不发布、异常隔离与幂等清理通过严格类型和 `182/182` 自动测试；清理异常路径保留 `3/4` Red 到 `4/4` Green；真实 Pi TUI 覆盖初始、拒绝、允许、Reload、全新 `--no-session` 进程和正常退出，用户现场确认窄终端可读；真实 Print 仅输出 `PRINT_WIDGET_OK` 并正常退出；差异、围栏和本地链接检查通过。证据不外推 JSON/RPC、任意终端/主题、OS 沙箱、状态持久化或 Shell 执行事实 | 进入 5.9 自动测试与手工验收记录收口 |
| 2026-08-18 | 5.7 Extension Entry Branch状态保存与恢复 | 已完成 | v1完整快照、严格恢复、append失败锁存、当前Branch重放与双入口fail-closed通过严格类型和174/174自动测试；真实Pi持久Session覆盖Reload、跨进程、Tree、Fork、Resume，`--no-session`真实A/B覆盖同进程Reload延续与新进程归零；脱敏Probe、Context排除、可恢复清理、围栏和本地链接23/23均通过。证据不外推事务式持久化、安全擦除、全局零写、任意并发/版本、其他Extension或OS沙箱 | 进入5.8最小Widget系统地图 |
| 2026-08-17 | 5.6 Model/User 双入口危险 Shell 审批门禁 | 已完成 | 精确三分类、Model `tool_call` 与用户 `user_bash` 两适配器、TUI-only确认、No/Esc/超时/无 UI/异常 fail-closed、替代 BashResult、追踪安全与主工厂顺序通过严格类型和 112/112 自动测试；错误交换顺序的 mutation 真实 0/1 Red，恢复后独立复跑聚焦 1/1、factory 12/12 和全量 112/112；真实 Pi TUI A-E 覆盖 Model SAFE/No、用户 `!` SAFE/Esc、`!!` Yes，真实 Print F 验证无本机 TUI默认拒绝与 marker 缺失；不外推 JSON/RPC、任意 Shell、组合 Extension或 OS 沙箱 | 进入 5.7 Extension Entry 状态保存与恢复 |
| 2026-08-15 | 5.5 Extension Slash Command `/study-inspect` | 已完成 | Tool/Command 共享服务、参数重校验、固定补全、TUI/RPC 与 Print/JSON 反馈适配、错误脱敏及主工厂接线通过严格类型和 80/80 自动测试；真实 Pi 0.84.1 TUI 验证补全候选、一次有界通知和 0 Agent/Model/Tool 主链，真实 Print 验证 stdout 0 字节、同摘要 stderr及同样的主链绕行；三路独立复审、稳定文档和可恢复临时目录清理均通过，JSON/RPC 动态行为与通用退出码不在证明范围 | 进入 5.6 危险 Shell 命令审批门禁 |
| 2026-08-14 | 5.4 自定义只读 Tool `pi_study_inspect` | 已完成 | 严格 Schema、真实路径/文件对象、Marked AST、显式取消、稳定错误、有限 `content/details` 与防御性 renderer 均有 53/53 自动测试；真实 Pi 0.84.1/Provider/Model 合法调用、compact/expanded TUI、前后 SHA-256、完整理解复述及合同/测试/安全三路独立复审通过；证据未扩大为 OS 沙箱、非法动态链或绝对无竞态 | 进入 5.5 Slash Command 系统地图 |
| 2026-08-13 | 5.3 Extension Context、模式、取消与错误处理 | 已完成 | TUI/Print Context 与反馈分支、Fake 协作取消及真实 `Esc` 信号链、observer/gate/executor 三组真实 Model/Provider/TUI 错误传播、隔离清理、稳定文档和独立复核均通过；用户最终复述准确区分生命周期观察、单次 Tool Call 门禁、Executor、fail-closed、错误 Tool Result及 Pi/Git/业务副作用恢复边界 | 进入 5.4 自定义只读 Tool |
| 2026-08-12 | 5.2 Extension 生命周期 | 已完成 | A-D 真实 Pi 事件链覆盖 Command、User Bash、Model 切换和普通 Prompt/内置 `read` Tool；用户最终复述通过工厂/Handler/Executor、两类快捷键、Turn、`agent_end`/`agent_settled` 与证据边界；稳定文档、3/3 Fake、类型检查、Markdown、6 张 Mermaid 和格式检查均通过 | 进入 5.3 Extension Context、模式、取消与错误处理 |
| 2026-08-12 | 5.1 Extension 开发环境与加载来源 | 已完成 | 四类加载来源与 Trust A/B、CLI 临时性、规范路径去重、自动发现规则、TypeScript/Fake Green-Red-Green、自动发现 `/reload` V1/V2、恢复与可恢复清理，以及工厂/Handler 证据边界复述均已通过 | 进入 5.2 Extension 生命周期 |
| 2026-08-11 | 阶段 5-6 学习计划深化 | 已确定 | 5.1-5.10、6.1-6.8 已补齐核心内容、贯穿实验、验收证据与边界；建议投入调整为 18-24 小时和 12-18 小时；所有 checkbox 保持未勾选 | 从 5.1 Extension 开发环境开始 |
| 2026-08-11 | 4.7 无重启资源调试 | 已完成 | Pi `0.84.1` watcher 与 `/reload` 源码、Theme 粉色 A 基线、磁盘橙色但界面仍粉色的 B 对照、显式重载后橙色的 C 证据、粉色恢复清理、稳定文档和格式检查均已通过 | 进入 5.1 Extension 开发环境 |
| 2026-08-11 | 4.6 最小自定义 Theme | 已完成 | Pi `0.84.1` Theme 源码与静态解析、项目发现、修正版 `accent` A/B/A2、保存后重启恢复、当前设置值及“Theme 呈现层与 Tool/权限层无决定关系”的组合理解验收均已通过 | 进入 4.7 无重启资源调试 |
| 2026-08-11 | 4.5 Skill 安全风险 | 已完成 | 指令注入 A/B/C、脚本执行 A/B/C、Pi `0.84.1` Tool/Package/Extension 源码边界、普通 Skill 与 Package 外部依赖、综合场景及针对性重试均已通过；稳定文档与格式检查通过 | 进入 4.6 Theme |
| 2026-08-09 | 4.4 只读 Java 代码分析 Skill | 已完成 | 标准 Skill 与参考清单、JDK 8 样例、静态加载与编译、正向自动触发、反向非触发、finding 证据复核和用户边界复述均已通过 | 进入 4.5 Skill 风险分析 |
| 2026-08-09 | 4.3 Skill 结构与渐进加载 | 已完成 | 目录/frontmatter、自动与手动加载、附属资源、脚本条件、4.3 只读实验及 4.4 正反实践均已验证；用户能区分发现与调用 | 由 4.4 实践闭合后进入 4.5 |
| 2026-08-09 | 4.2 结构化代码审查 Prompt Template | 已完成 | Pi `0.84.1` 静态加载与展开、真实 TUI 发现与参数提示、显式与默认参数、空对象保护、综合复述、稳定文档和格式检查均已通过 | 进入 4.3 Skill 结构与加载流程 |
| 2026-08-09 | 4.1 四类资源选型 | 已完成 | Pi `0.84.1` 文档基线、Java 贯穿场景、四项选型判断、Extension UI/门禁重试、稳定文档和格式检查均已通过 | 进入 4.2 结构化代码审查 Prompt Template |
| 2026-08-08 | 3.7 长任务断点模板 | 已完成 | 七字段模板、五步恢复、断点过期处理、Session A/B 独立恢复、`VERIFY_FAIL -> VERIFY_OK`、先验证后回写、稳定文档和可恢复清理均已通过 | 进入 4.1 资源适用场景 |
| 2026-08-08 | 3.6 Branch Summary | 已完成 | Tree 目标点与摘要选择器、公共原文与旧路线摘要联合恢复、两条路线共存、11 行 JSONL 脱敏结构、源码字段边界、稳定文档、理解重试和可恢复清理均已通过 | 进入 3.7 长任务断点模板 |
| 2026-08-08 | 3.5 手动 Compaction | 已完成 | `18,438` Token 手动压缩、`6.8% -> 2.3%` Footer 对照、摘要区与近期原文区行为验证、JSONL 脱敏投影、稳定文档和可恢复清理均已通过 | 进入 3.6 分支摘要 |
| 2026-08-08 | 3.4 Token 与上下文窗口 | 已完成 | Footer A/B、参数关系讲解、用户场景复述、主题文档和敏感信息边界均已验证 | 进入 3.5 手动 Compaction |
| 2026-08-08 | 3.3 Session JSONL 结构 | 已完成 | 0.84.0 类型核对、受控 Session、Tool/Model/自定义条目脱敏投影、用户开卷识别、稳定文档和可恢复清理均已验证 | 进入 3.4 Token 与上下文窗口 |
| 2026-08-07 | 3.2 Tree、Fork 与 Clone | 已完成 | Tree/Fork/Clone 的隔离实验、理解验收、稳定文档、格式检查及可恢复清理均有直接证据 | 进入 3.3 JSONL Session 结构 |
| 2026-08-07 | 3.1 Session 生命周期与边界 | 已完成 | 自动保存、名称/身份、恢复、导出、分享、删除、`--no-session`、稳定文档、格式检查和可恢复清理均已验证 | 进入 3.2 Tree、Fork 与 Clone |
| 2026-08-07 | 2.7 配置优先级故障排查 | 已完成 | 全局 `nano`、项目 `vi` 的 Vim 故障已复现；两轮定位、最小修复、PICO 反向验证、组合复述、排查文档、格式检查和可恢复清理均通过 | 进入 3.1 Session 生命周期与边界 |
| 2026-08-07 | 2.6 Keybindings 和外部编辑器 | 已完成 | 默认与自定义按键、`/reload` 前后对照、`externalEditor -> VISUAL -> EDITOR -> nano` 四级选择、草稿返回、隔离清理、综合场景和主题文档均已验收 | 进入 2.7 配置优先级故障排查 |
| 2026-08-06 | 2.5 Project Trust | 已完成 | 交互式与非交互实验、CLI 单次覆盖、父目录保存决定优先级和综合场景均已验证；Trust 记录精确查询输出 `NO_SAVED_DECISION`，实验目录原路径检查输出 `LAB_DIR_REMOVED`；文字说明与完整流程图已沉淀 | 进入 2.6 Keybindings 和外部编辑器 |
| 2026-08-06 | 2.4 Model 轮换 | 已完成 | 无匹配候选排除、`sol/terra/luna` 范围和 `⌃P` 正向轮换有直接界面证据；`⇧⌃P` 由用户操作确认，未保留直接界面输出；最终 `defaultModel=sol`、`enabledModels=null` | 进入 2.5 Project Trust |
| 2026-08-06 | 2.4 Shell 命令前缀 | 已完成 | 临时项目设置唯一 `shellCommandPrefix` marker；用户 `!` 输出 `USER_PREFIX=PREFIX_ACTIVE`，Model 绿色 `bash` Tool Result 输出 `MODEL_PREFIX=PREFIX_ACTIVE` | 验证 Model 轮换策略 |
| 2026-08-06 | 2.4 Shell 显式路径 | 已完成 | 临时项目只设置 `shellPath=/bin/zsh`；Session `2.4-shell-zsh` 中同一条用户 Shell 命令实际输出 `PI_SHELL=/bin/zsh`，与默认 `/bin/bash` 构成单变量对照 | 验证 `shellCommandPrefix` 对用户 Shell 与 Model `bash` Tool 的作用 |
| 2026-08-06 | 2.4 Shell 默认解释器 | 已完成 | Session `2.4-shell-default` 中通过用户 `!` 入口执行 `printf 'PI_SHELL=%s\n' "$0"`，实际输出 `PI_SHELL=/bin/bash`；同时验证 `--no-tools` 不会禁用用户 Shell 入口 | 临时设置 `shellPath=/bin/zsh` 做单变量对照，再验证 `shellCommandPrefix` |
| 2026-08-06 | 2.4 Images 默认恢复 | 已完成 | 只读核验全局 `terminal.showImages=true`、`images.blockImages=false`；项目 `.pi/settings.json` 未设置这两个字段，因此没有项目同名覆盖 | 学习 Shell 配置，随后验证 Model 轮换策略 |
| 2026-08-06 | 2.4 Images A/B 实验 | 已完成 | A：`Show images=false` 时无终端图片预览，`Block images=false` 时 Model 正确识别图片文字；B：`Show images=true` 时 TUI 显示预览，`Block images=true` 时 Model 返回 `IMAGE_BLOCKED`；实现会过滤图片内容，但实验未检查最终出站载荷 | 恢复默认 `Show images=true`、`Block images=false`，随后学习 Shell 配置 |
| 2026-08-06 | 2.4 Network 最小实验 | 已完成 | 用户真实终端输出：一次 `POST /v1/chat/completions`、`Request timed out.`、Provider 共收到 1 次请求、外层看门狗未介入、Pi 子进程用时 1367 ms，最终为 `实验结果：PASS` | 进入 Images 配置；不把单次请求超时扩大为整个 Run 总时限 |
| 2026-08-05 | 2.4 运行配置与策略 | 已完成 | Model/Thinking、Retry、Network、Images、Shell 解释器与命令前缀、Model 轮换策略均已验收 | 进入 2.5 Project Trust |
| 2026-08-05 | 2.3 最小项目规则 | 已完成 | 根目录 `AGENTS.md` 静态检查通过；正向对照在 `--no-approve --no-tools --no-session` 下输出 `PI_STUDY_AGENTS_V1`，反向只增加 `--no-context-files` 后输出 `CONTEXT_NOT_LOADED`；用户已通过规则与硬权限边界复述 | 学习 2.4 运行配置与策略 |
| 2026-08-05 | 2.2 项目规则与上下文文件 | 已完成 | 四部分系统地图、最小实验和源码链路已通过；用户最终准确说明四类内容通道、`--no-context-files`、显式 CLI `@文件`、Project Trust 正向职责及其非沙箱边界 | 学习 2.3 最小项目规则 |
| 2026-08-05 | 2.1 配置合并与优先级 | 已完成 | 项目 `.pi/settings.json` 为 `high`、全局为 `max`；无 CLI 参数时 Footer 为 `high`，显式 `--thinking low` 时为 `low`，退出后不带 `--thinking` 的新运行恢复 `high`；用户最终准确复述“显式 CLI > 项目配置 > 全局配置” | 学习 2.2 四类上下文文件 |
| 2026-07-29 | 计划制定 | 已完成 | 官方文档范围核对、本机只读环境快照 | 等待用户开始阶段 0 |
| 2026-07-31 | 教学策略与作品集阶段 | 已确定 | 三遍螺旋、本地实验协作方式、网站内容来源和阶段 9 验收已写入本计划 | 继续 0.3 安全边界实验 |
| 2026-07-31 | 0.3 Project Trust 与安全边界 | 已完成 | 用户实测三条独立路径，并准确复述资源加载、Model Tool 可见性、当前用户权限与真正沙箱的边界 | 学习 1.1 交互式界面与输入 |
| 2026-07-31 | Pi `0.83.0` 版本更新 | 已完成 | `pi --version` 与重新启动的 TUI 均显示 `0.83.0`；已核对本机 Changelog 与 1.1 快捷键文档 | 继续 1.1 编辑器实验 |
| 2026-07-31 | 1.1 交互式界面与输入 | 已完成 | TUI 四区、编辑器操作、外部编辑器、交互式与 CLI `@文件`、macOS 剪贴板图片和两类 `read` 链路均已实测；用户完成区域映射复述 | 学习 1.2 内置 Tool 行为与风险 |
| 2026-07-31 | 1.2 内置 Tool 行为与风险 | 已完成 | `write`、分段 `read`、精确 `edit` 和无副作用 `bash` 均完成独立实验；用户通过最小 Tool 选择、Schema、安全边界和 Git 恢复综合验收 | 学习 1.3 模型与上下文状态 |
| 2026-07-31 | 1.3 Model、Thinking 与上下文状态 | 已完成 | 用户实测 Thinking Level、Model 选择器、Scoped Models、`⌃P` 切换与 `⌃L` 恢复，并准确区分 Footer 的 Session 累计用量、当前上下文和自动 Compaction | 学习 1.4 Shell、取消与重试 |
| 2026-08-01 | 1.4 Shell、取消与重试 | 已完成 | 用户实测 `!`/`!!` 上下文差异、Shell 取消、本地假 `503` 的 `2/4/8` 秒退避和等待取消，并准确复述 Provider、Tool、Context Overflow 与取消边界 | 学习 1.5 Steering 与 Follow-up |
| 2026-08-03 | 1.5 Steering 与 Follow-up | 已完成 | 用户实测两类运行中消息队列，并准确复述 Steering 不打断已开始 Tool、Follow-up 完成前不能进入 `agent_settled` | 学习 1.6 非交互模式 |
| 2026-08-04 | 1.6 Interactive、Print 与 JSON | 已完成 | 用户完成相同任务的三模式对照实验，确认 JSONL 事件链，并结合源码准确复述输出消费者、退出行为和权限边界 | 学习 1.7 完整任务闭环 |
| 2026-08-05 | 1.7 完整任务闭环 | 已完成 | Session `1.7-workflow` 已完成 `read -> edit -> bash 测试 -> bash diff -> 总结`；`TEST_OK`、三行结构和单行目标 diff 均已核对；用户准确复述 Tool Result、测试、diff 与总结的职责 | 进入 2.1 配置合并与优先级 |
| 2026-07-29 | 0.1 本机环境体检 | 已完成 | `docs/learning/00-environment-report.md`；四项理解题全部通过 | 进入 0.4 数据/计费边界确认 |
| 2026-07-31 | 0.2 Pi 架构心智模型 | 已完成 | 0.2.1 至 0.2.8 全部通过；用户已完成真实只读请求并复述完整 Agent Loop | 学习 0.3 安全边界 |
| 2026-07-30 | 0.2.2 模型上下文三盒 | 已完成 | 用户独立完成三盒归属、Tool Result 去向、Tool 暴露范围及 Model/Pi 职责验收 | 学习 0.2.3 Provider、Model、认证与 API 协议 |
| 2026-07-30 | 0.2.3 Provider 与 API 协议 | 已完成 | 用户准确判断：服务入口与 API Key 改变时 Provider 配置改变，Responses 格式未变时协议不变 | 学习 0.2.4 Agent Loop |
| 2026-07-30 | 0.2.4 Agent Loop | 已完成 | 用户准确区分 Run、Turn、同轮多 Tool Call、错误 Tool Result 与终止条件，并纠正 `agent_settled` 等同成功的误解 | 学习 0.2.5 Tool 系统 |
| 2026-07-31 | 0.2.5 Tool 系统 | 已完成 | 用户准确串联 Tool 生命周期，说明 Executor 与当前用户权限，并区分 Model Tool 只读、用户 Shell、Extension 和沙箱边界 | 学习 0.2.7 Session 与记忆边界 |
| 2026-07-31 | 0.2.7 Session 与记忆边界 | 已完成 | 用户准确区分 Session 存档、当前上下文、模型永久记忆、工作区状态和 Git 历史，并说明各自的恢复边界 | 执行 0.2.8 端到端只读请求追踪 |
| 2026-07-31 | 0.2.8 端到端只读请求追踪 | 已完成 | Session `0.2.8-read-trace` 的用户消息、Tool Call、完整 Tool Result 和最终文本均已展示；用户准确复述完整链路及 `agent_end`、`agent_settled` 的产生方 | 学习 0.3 安全边界 |
| 2026-07-29 | 0.4 认证与费用方案 | 已完成 | 用户确认模型接入作为既定前提，学习调用费用不设上限；模型响应及凭据权限已验证 | 进入 0.2 架构心智模型 |
| 2026-07-29 | 0.5 Pi CLI 安装 | 已完成 | `pi 0.82.1`、`fd 10.4.2`；模型响应、Session、退出状态、凭据权限及 Git 范围均已验证 | 进入 0.1 理解验收与 0.4 边界确认 |

## 8. 官方资料索引

- 总览：`https://pi.dev/docs/latest`
- 自定义模型：`https://pi.dev/docs/latest/models`
- 快速开始：`https://pi.dev/docs/latest/quickstart`
- 安全：`https://pi.dev/docs/latest/security`
- 设置：`https://pi.dev/docs/latest/settings`
- 会话：`https://pi.dev/docs/latest/sessions`
- 上下文压缩：`https://pi.dev/docs/latest/compaction`
- Skills：`https://pi.dev/docs/latest/skills`
- Extensions：`https://pi.dev/docs/latest/extensions`
- Packages：`https://pi.dev/docs/latest/packages`
- SDK：`https://pi.dev/docs/latest/sdk`
- RPC：`https://pi.dev/docs/latest/rpc`
- JSON Event Stream：`https://pi.dev/docs/latest/json`
- TUI：`https://pi.dev/docs/latest/tui`
- 源码开发：`https://pi.dev/docs/latest/development`
- 官方源码：`https://github.com/earendil-works/pi-mono`

## 9. 版本漂移规则

- 每个大阶段开始前检查 Pi 当前版本和官方文档变更。
- 若版本变化影响命令、配置或 API，先更新本计划的版本基线与相关验收项，再继续学习。
- 不因版本更新自动重做已经掌握的概念；只补充受影响的差异实验。
